# 호스팅 운영 가이드 — Phase 3 종합 (2026-07-24)

- 문서 목적: Phase 3(호스팅 능력)의 전체 흐름 — 빌드 요청 → k8s 배포/라우팅 → 관리 → e2e — 를 한 곳에 정리.
- 대상 독자: 운영자, 개발자, AI 에이전트
- 상태: stable (Phase 3 완료, TASK-166~170)
- 최종 수정일: 2026-07-24
- 관련 문서: [k8s 배포/webhook](./k8s-deploy-webhook-2026-07-24.md), [sub-path 규약](./hosting-sub-path-2026-07-24.md), [Phase 3 컨셉](../PHASE-3-CONCEPT.md), [설계](../PHASE-3-DESIGN.md)

## 1. 한눈에

빌드된 이미지가 k8s 에 **지속 호스팅**되고 `http://<HOSTING_BASE_HOST>/<context-path>/` 로 접근·관리된다(평문 HTTP — HTTPS 미도입).

```
POST /builds (contextPath?, runtimePort?, stripPrefix?)
  → 서버가 context-path 할당(정규화/유일성) + build 에 저장
  → runner claim(contextPath/runtimePort/stripPrefix 수령)
  → 이미지 빌드 → 컨테이너 테스트 → k8s 배포(Deployment+Service+Ingress+APP_BASE_PATH)
  → 서버가 배포 성공 보고로 HostedService upsert(url 조립)
운영: GET/POST/DELETE /admin/hosted-services (또는 UI /admin/hosting)
```

## 2. 설정 (env)

| 컴포넌트 | env | 의미 |
|---|---|---|
| runner | `RUNNER_K8S_MODE=k8s` | k8s 배포 활성(§[k8s 배포 문서](./k8s-deploy-webhook-2026-07-24.md)) |
| runner | `RUNNER_K8S_CLUSTER` | kubeconfig context |
| build-server | `HOSTING_BASE_HOST` | 호스팅 URL host(설정해야 HostedService upsert — opt-in) |
| build-server | `HOSTING_KUBE_CONTEXT` | 관리(scale/delete) kubectl context |
| build-server | `HOSTING_KUBECTL_BIN` | kubectl 실행 파일(기본 kubectl) |
| build-server | `HOSTING_STATUS_SYNC_INTERVAL_MS` | 호스팅 status 캐시 주기 sync 간격 ms(기본 30000, `0`=비활성). `HOSTING_BASE_HOST` 설정 시에만 기동(TASK-174) |

전제: 클러스터에 **ingress-nginx**(또는 호환 Ingress 컨트롤러). runner/build-server 실행 환경에 `kubectl` + kubeconfig.

## 3. 빌드 요청 필드

| 필드 | 기본 | 의미 |
|---|---|---|
| `contextPath` | appName 정규화 | URL prefix(전역 유일, 예약어 회피) |
| `runtimePort` | 8080 | 앱 컨테이너 listen 포트 |
| `stripPrefix` | true | Ingress prefix strip 여부([sub-path 문서](./hosting-sub-path-2026-07-24.md)) |

앱은 `APP_BASE_PATH=/<context-path>/` env 를 읽어 emit URL 에 prefix 를 붙여야 한다(§sub-path).

### context path 점유 규칙

context path 는 호스팅 registry뿐 아니라 build 시작 시점에도 점유 검사한다. PostgreSQL은
context별 transaction advisory lock 안에서 이미 호스팅 중인 서비스와 다른 앱의 진행 중
build를 함께 확인하므로 동시 요청이 같은 path를 선점하지 못한다. 실패/완료 build의 path는
다시 사용할 수 있고, 같은 앱의 재배포는 허용된다. 충돌 시 API는 `CONTEXT_PATH_TAKEN`을
반환한다.

## 4. 관리 (admin)

| 동작 | API | k8s |
|---|---|---|
| 목록 | `GET /admin/hosted-services` | registry(SSOT) |
| 상세 | `GET /admin/hosted-services/:appName` | registry |
| 중지 | `POST /admin/hosted-services/:appName/stop` | `kubectl scale --replicas=0` → STOPPED |
| 재개 | `POST /admin/hosted-services/:appName/start` | `kubectl scale --replicas=1` → RUNNING |
| 제거 | `DELETE /admin/hosted-services/:appName` | `kubectl delete deployment,service,ingress` + registry 제거(context-path 반환) |

UI: `/admin/hosting`(Hosting 탭). 관리 kubectl 은 **build-server 가 직접 shell-out**(runner=배포 생성, build-server=수명 관리).

## 5. e2e

```bash
# kind + kubectl + docker 환경에서
bash apps/runner/scripts/e2e-hosting.sh
```

- Phase A(라우팅): kind + ingress-nginx + `examples/hosted-base-path-app` → 실제 `host/<cp>/` 접속 + `APP_BASE_PATH` 자산 로드 실측.
- Phase B(관리): 실 K8sAdmin 으로 stop(scale 0) + remove(delete) 실측.

2026-07-24 로컬 검증(kind v0.24.0 + kubectl v1.31.4 + ingress-nginx): **ALL PASS**.

## 6. status 캐시 (TASK-174 / v0.7.0)

desired lifecycle `status`(RUNNING/STOPPED/…, 관리·배포가 갱신)와 **분리된** live read cache. build-server 가 주기적으로(`HOSTING_STATUS_SYNC_INTERVAL_MS`, 기본 30s) REMOVED 를 제외한 전 HostedService 의 실측 available replica 수를 `kubectl get deployment ... availableReplicas` 로 읽어 registry 의 `availableReplicas` + `lastSyncedAt` 에 캐시한다.

- **분리 이유**: sync 가 관리 명령(stop/start)의 desired status 를 덮어쓰지 않는다. `status=RUNNING` 인데 `availableReplicas=0` 이면 UI 가 **degraded**(파드 미기동)로 파생 표시.
- **opt-in**: `HOSTING_BASE_HOST` 설정 시에만 스케줄러 기동(미설정 배포는 kubectl 호출 0). `HOSTING_STATUS_SYNC_INTERVAL_MS=0` 으로 비활성.
- **격리**: 개별 서비스 kubectl 조회 실패는 해당 캐시를 stale 로 두고 나머지 sync 계속(다음 tick 재시도). 재배포(upsert) 시 캐시는 null 로 리셋 후 재수집.
- UI(`/admin/hosting`): Replicas 컬럼 + degraded 배지. `lastSyncedAt` 은 셀 tooltip.

## 7. follow-up

- kind 기반 호스팅 e2e 의 nightly CI 편입(현재 수동).
- k8s adapter 확장(Helm/ArgoCD, per-build namespace 정리) / subdomain 스킴(sub-path 대안) / `<base>` 주입 옵션.
- webhook 확장(Slack·재시도).
