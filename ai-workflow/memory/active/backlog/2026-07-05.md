<!-- standard-ai-workflow-kit: v0.11.21-beta -->

# 2026-07-05 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress (close after wrap-up)
- 최종 수정일: 2026-07-05 (rev 1: 신규 일일 백로그. §1 PR #21 squash merge sync + §2 self-review follow-up. rev 2: §3 시뮬레이션 — 5 시나리오 + 2 실제 버그 발견 + fix)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-04.md)

## 1. PR #21 squash merge main 합류 sync

- 상태: completed
- 모드: Documentation
- 우선순위: high
- 담당: Codex
- 영향 문서: ai-workflow/memory/active/state.json, session_handoff.md, work_backlog.md, backlog/2026-07-05.md (신규)

### 1.1 계획 (Plan)

- 작업 목표: TASK-067 (Runner container run + healthcheck) 의 1차 PR #21 (`574932d`) squash merge main 합류를 workflow 메타 3종에 회수. `state.json` `current_focus` 를 신규 TASK-068 (real deploy adapter) 진입으로 전환. 신규 일일 백로그 2026-07-05.md 신설.
- 완료 기준: state.json / session_handoff.md / work_backlog.md rev counter 가 실제 main HEAD `574932d` 와 정합. `latest_backlog_path: backlog/2026-07-05.md`, 회귀 영향 없음.

### 1.2 실행 (Act)

- 진행 현황: PR #21 squash merge 완료 (워크플로우 본문 보강 + self-review fix 3건 봉인). main HEAD `574932d` clean. workflow 메타 4종 동시 갱신.
- 수정 사항:
  - `ai-workflow/memory/active/state.json` — `generated_at` `2026-07-05T13:08:00Z` 로 갱신, `purpose_digest_rev` 80→81 + 신규 purpose_digest (TASK-067 PR #21 squash merge + 9 신규 테스트 + self-review fix 3건 봉인), `current_baseline` 에 PR #21 squash merge 합류 항목 추가 (TASK-067, `574932d`), `current_axis` 를 TASK-068 (real deploy adapter) 진입으로 갱신, `current_focus` 를 "PR #21 squash merge 합류 → TASK-068 진입" 으로 갱신, `recent_done_items` head 에 PR #21 squash merge entry prepend, `latest_backlog_path` → `backlog/2026-07-05.md`.
  - `ai-workflow/memory/active/session_handoff.md` — `Updated` 라인 `2026-07-05` 로 갱신 + `Latest wrap-up note (2026-07-05)` 섹션 추가 (TASK-067 PR #21 squash merge 내역 + self-review fix 3건 + TASK-068 진입 plan).
  - `ai-workflow/memory/active/work_backlog.md` — header `최종 수정일` `2026-07-05` 로 갱신 + 끝에 `rev 53→54` sync entry 추가 (PR #21 squash merge 회수 + TASK-068 다음).
  - `ai-workflow/memory/active/backlog/2026-07-05.md` — 본 문서 (rev 1) 신설.

### 1.3 검증 (Validate)

- 테스트 결과: `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` OK. JSON 정합성 확인. rev counter (state 81 / handoff 60 / work_backlog 54) 모두 main HEAD `574932d` 와 정합.
- 미실행 사유: 본 작업은 메타 sync (문서) 이므로 코드 회귀 영향 없음 — TS / Go / Python 테스트 재실행 skip.

### 1.4 결과 (Result)

- 작업 결과: workflow 메타 3종이 main HEAD `574932d` 와 정합. 신규 일일 백로그 파일 생성. TASK-067 PR #21 squash merge 가 명시적으로 회수됨. 다음 작업 TASK-068 (real deploy adapter) 진입 상태.
- 다음 세션 시작 포인트: TASK-068 scope 정의 (deployment target 결정 — docker registry / compose / k8s / GitHub Pages 등) + 1차 PR 작성.
- 남은 리스크 및 후속 작업:
  1. TASK-068 의 deploy target 결정 사용자 확인 필요. options memo 후 결정.
  2. 2026-07-04.md 파일 자체의 header rev 가 TASK-066/067의 1차 PR 작성 entry 까지만 명시 (실제론 PR merge entry 가 추가됨). 차기 sync 에서 정합.

## 2. TASK-067 self-review follow-up — pickFreePort / strconv stdlib / inspect comment

- 상태: completed
- 모드: Refactor (post-merge amend via second commit on PR branch)
- 우선순위: medium
- 담당: Codex
- 영향 파일: apps/runner/internal/docker/{client.go, client_test.go}

### 2.1 계획 (Plan)

- 작업 목표: PR #21 self-review 에서 발견한 3개 보완점 중 2개는 fix, 1개는 honest comment 으로 마감. 모두 PR #21 branch 에 두 번째 commit (`d025b18`) 으로 묶어 자동 업데이트.
- 완료 기준:
  1. `pickFreePort` 가 dead code 가 되지 않도록 `TestPickFreePortReturnsEphemeral` 직접 호출 테스트 추가
  2. `TestProbePortAndHealth` 의 hand-rolled `fmtSscan` + `strconvErr` 제거, stdlib `strconv.Atoi` 로 교체
  3. client.go 의 cli mode + inspect-fail path 주석 정직화 ("WaitForHealth port-open probe 로 재시도 가능" 거짓 → `pickFreePort` 기반 host-port pre-allocation 권장으로 정정)

### 2.2 실행 (Do)

- `apps/runner/internal/docker/client_test.go`:
  - `import` 블록에 `strconv` 추가
  - `var port int` / `fmtSscan(portStr, &port)` → `port, err := strconv.Atoi(portStr)` 1줄 단순화
  - `fmtSscan` (10줄) + `strconvErr` type + `Error()` method (총 ~18줄) 전체 삭제
  - `TestPickFreePortReturnsEphemeral` 신규 — `pickFreePort()` 가 1..65535 사이 ephemeral port 를 반환하는지 직접 검증
- `apps/runner/internal/docker/client.go`:
  - RunContainer + auto-assign + inspect-fail path 의 주석 갱신 — "caller 가 WaitForHealth 의 port-open probe 로 재시도 가능" (false) → "`BuildService` 가 `HostPort=0` 으로 빌드할 때는 `pickFreePort` 로 OS ephemeral port 를 미리 잡아 host 포트로 명시적으로 넘기는 편이 안전하다. 후속 TASK 의 port-collision retry 정책과 함께 도입 예정."

### 2.3 검증 (Validate)

- 테스트 결과: `cd apps/runner && go test ./... -count=1` — 7 packages 모두 PASS, **64 case** (직전 63 + `TestPickFreePortReturnsEphemeral` 1 신규). `go vet ./...` clean. `go build ./...` clean.
- 회귀 baseline 유지: TS 4 packages clean, build-server 94/94 동일, Python skill_mcp 226/226 동일.

### 2.4 결과 (Result)

- 작업 결과: PR #21 에 두 번째 commit `d025b18` (test(runner): self-review 보강) 자동 push — PR 본체 squash merge 시 두 commit 모두 `574932d` 단일 history 로 합쳐짐. git log 정상.
- 다음 세션 시작 포인트: TASK-068 scope 결정.
- 남은 리스크 및 후속 작업:
  1. `StabilityWindow` 가 현재는 timeout soft cap 으로만 동작 (consecutive-3 으로 stability 자체는 보장되지만 시간 윈도우 개념은 미적용) — 주석이 정직하게 명시했으므로 1차 PR scope OK, 후속에서 env 노출.
  2. `StopContainer` 가 `docker rm -f` (graceful stop skip) — preview cleanup 용도로 적합.
  3. `pickFreePort` 의 실제 호출처는 후속 port-collision retry TASK 에서 도입 — 그때까지 `TestPickFreePortReturnsEphemeral` 가 dead code seal 역할.

## 3. 시스템 동작 시뮬레이션 — 5 시나리오 + 발견된 실제 버그 2건

- 상태: completed
- 모드: Integration (e2e simulation)
- 우선순위: high
- 담당: Codex
- 영향 파일: `apps/runner/internal/docker/client.go` (CRITICAL fix), `apps/runner/internal/docker/client_test.go` (regression test 신규), `apps/build-server/scripts/e2e-source-archive.sh` (whitespace fix 2곳), `packages/shared-contract/dist/**` (rebuild)

### 3.1 계획 (Plan)

- 작업 목표: 기존 unit-test / scenario-level 검증만 의존하던 회귀 baseline 에 더해, **실제 운영 조건** (real HTTP server boot + real docker daemon + real tar archive round-trip + real source archive e2e + real container run) 의 5가지 end-to-end 시나리오를 손으로 굴려서 결정적 실패를 잡아내기. Scenario 1 (drift check) / 2 (memory backend smoke) / 3 (source archive round-trip) / 4 (full happy path skeleton mode) / 5 (live container smoke cli mode).
- 완료 기준: 5 시나리오 모두 PASS 또는 발견된 버그를 즉시 fix 후 PASS. 회귀 baseline 유지.

### 3.2 실행 (Do)

- **Scenario 1 (drift check)** — `python3 -m apps.skill_mcp.skills.contract_drift_checker.cli`: 9 sync group 모두 `missing=0, extra=0`. TS↔Python↔Go canonical contract 3-way sync drift-free 확인.
- **Scenario 2 (memory backend smoke)** — `bash scripts/smoke.sh`: 1차 실행 시 `shared-contract/dist` 가 stale — `sourceArchiveUploadResponseSchema` export 누락으로 build-server 부팅 실패. 4 package (`packages/{shared-contract,shared-config,db}` + `apps/build-server`) `tsc` 재실행 후 정상. smoke 5 steps 모두 OK.
- **Scenario 3 (source archive round-trip)** — `bash apps/build-server/scripts/e2e-source-archive.sh`: 1차 실행 시 `wc -c <file` 의 leading whitespace 그대로 들어가서 `X-Source-Size-Bytes` header 비교 실패. 두 곳 (`SIZE=` / `DOWNLOAD_SIZE=`) 에 `tr -d ' '` 추가. 2차 실행 시 POST 201 → GET (sha+size+header 일치) → tampered 400 → 404 → DELETE parity 모두 PASS.
- **Scenario 4 (full happy path skeleton mode)** — `python3 /tmp/scenario4-happy-path.py`: Build Server memory backend 부팅 + tar.gz 소스 (FROM scratch Dockerfile) 업로드 + RUNNER_DOCKER_RUN_MODE=skeleton 으로 Runner 기동. 5초 안에 8 단계 phase (`QUEUE_CLAIMED → SOURCE_PREPARED → DOCKER_BUILD_STARTED → DOCKER_BUILD_COMPLETED → PREVIEW_READY → DEPLOYMENT_STARTED → DEPLOYMENT_COMPLETED → COMPLETED`) 다 emit + `lifecycle.status=COMPLETED` + `test.status=SUCCESS` + `deploy.status=SUCCESS` + `resultDelivery.status=SUCCESS`. previewUrl=`http://preview.local:38124/` (skeleton mock 정합).
- **Scenario 5 (live container smoke cli mode)** — `bash /tmp/scenario5-container-run.sh`: colima 기동 + busybox:1.36 Dockerfile (httpd -f -h . -p 8080 + wget healthcheck + WORKDIR /www) + source archive upload + Runner cli mode + docker build/run/healthcheck. **CRITICAL BUG 발견**: RunContainer 의 `docker inspect` format template `{{ (index (index .NetworkSettings.Ports "%d/tcp") 0) "HostPort" }}` 가 Go template parser 에서 trailing `"HostPort"` argument 허용 안 함 → inspect 빈 응답 → `status.HostPort=0` 유지 → WaitForHealth 가 `127.0.0.1:0` probe → 30s timeout → `ErrContainerHealthcheckTimeout` → FAILED. Fix: template 을 `{{(index (index .NetworkSettings.Ports "%d/tcp") 0).HostPort}}` 로 정정 (마지막 key 를 field access 로 표현). 추가로 `runDockerInspectCmd` 를 기존 `runContainerCmd`/`stopContainerCmd` 와 동일한 swappable field 로 승격 + `SetRunDockerInspectCmdForTest` seam 추가. Scenario 5 재실행 — previewUrl=`http://127.0.0.1:32776/` (실제 docker auto-assigned ephemeral port) + status=COMPLETED + test SUCCESS + deploy SUCCESS + resultDelivery SUCCESS.

### 3.3 검증 (Validate)

- **Scenario별 검증**:
  - Scenario 1: drift 0 (`missing=0, extra=0` ×9 group) + Go contract 9/9 PASS
  - Scenario 2: smoke 5 steps OK (compile 4 packages → boot → POST/GET/list/duplicate 409/admin)
  - Scenario 3: POST 201 → GET (sha+size+header 3-way) → tampered 400 (정확한 expected/actual JSON) → 404 (GET + DELETE) → DELETE parity
  - Scenario 4: phase 8/8 emit + lifecycle/test/deploy/resultDelivery 모두 SUCCESS
  - Scenario 5 (fix 후): previewUrl `127.0.0.1:32776/` (실제 ephemeral port) + status=COMPLETED + 모든 health flag true
- **회귀 baseline 재검증 (fix commit 적용 후)**:
  - TS 4 packages direct `tsc --noEmit` clean
  - Go: docker 12 PASS (직전 11 + 신규 TestRunContainerCliModeInspectTemplateParsesAndExtractsHostPort 1) + services 6 + hostclient 9 + source 6 + contract 9 + deploy 1 = 7 packages 모두 PASS
  - build-server 94/94 동일
  - Python skill_mcp 226/226 동일
- **회귀 가드 (regression guard)**:
  - `TestRunContainerCliModeInspectTemplateParsesAndExtractsHostPort` — `text/template` 으로 format parse 확인 + 이전 broken form (`{{(index (index .NetworkSettings.Ports "8080/tcp") 0) "HostPort"}}`) 회귀 시 명시적으로 검출
  - `TestRunContainerCliModeInvokesDockerWithExpectedArgs` 와 `TestPickFreePortReturnsEphemeral` 도 함께 회귀 가드

### 3.4 결과 (Result)

- 작업 결과: 5 시나리오 모두 PASS. 2건의 실제 버그 발견 + 즉시 fix + 회귀 가드 추가. fix commit `9d0f140` main HEAD.
- TASK-067 의 cli mode + auto-assign path 가 사실상 죽은 path 였음을 Scenario 5 가 결정적으로 발견 — 이는 TASK-067 1차 PR 의 self-review 가 단위 테스트만 의존했던 결과로, 본 시뮬레이션이 운영 수준의 진짜 회귀를 잡아낸 사례.
- 다음 세션 시작 포인트: TASK-068 (real deploy adapter) 진입. 후속 pickFreePort 기반 port-collision retry 정책은 본 fix 와 독립적으로 TASK-068 의 부수 작업으로 같이 처리 가능.
- 남은 리스크 및 후속 작업:
  1. `apps/runner/scripts/e2e-container-run.sh` 의 Dockerfile 이 busybox httpd + wget 으로 의존성 최소화 중이지만, `cmdWhitelist` 가 검증용 server baking 을 담당하면 user 시나리오에선 더 정교한 Dockerfile 이 필요할 수 있음. e2e script 자체는 dry-run + busybox-pull 양쪽 fallback 다 갖춤.
  2. `docker inspect --format '{{ (index ... 0).HostPort }}'` 가 docker daemon version 에 따라 동작이 안정적이지 않을 수 있음 (legacy Docker). 향후 `--format '{{json .NetworkSettings.Ports}}'` + jq 파싱으로 더 robust 하게 갈 수 있음 — 후속 TASK.
  3. Scenario 4 의 happy-path script (`/tmp/scenario4-happy-path.py`) / Scenario 5 의 preserved-logs script (`/tmp/scenario5-container-run.sh`) 는 일회성 시뮬레이션 도구였으므로 repo 에 보존하지 않음. 운영 검증 자동화하려면 각 scenario 를 `apps/{runner,build-server}/scripts/` 에 정식 shell 로 옮기는 게 좋음 (TASK-068 진입 시 함께).


## 4. TASK-069 Runner registry v1 + admin menu

- 상태: completed (main 합류 `beb49f0`)
- 모드: Feature (admin UI + backend registry)
- 우선순위: high
- 담당: Codex

### 4.1 계획

- 작업 목표: admin 메뉴에서 runner 들을 가시화 + 통제할 수 있는 surface. Runner 는 별도 등록 절차 없이 첫 `POST /builds/claim` 호출 시 `runnerId` payload 로 자기 자신을 ACTIVE 상태로 등록. admin 은 `GET /admin/runners` list + `PATCH /admin/runners/:id` 로 status=DISABLED/REACTIVATE 토글 + `DELETE` 로 영구 제거. DISABLED 로 토글한 runner 의 후속 claim 은 reason=RUNNER_DISABLED 로 거절. Build Service 는 `reportPhase` 의 terminal phase (COMPLETED/FAILED) 에서 runner registry 의 `buildsCompleted` counter 와 `currentBuildId` 를 자동 갱신.

### 4.2 실행

- `packages/shared-contract/src/build/runner-registry.ts` — `runnerStatusSchema` enum + `adminRunnerSchema` + list/patch/delete response 4 schema, `runnerStatuses = ["ACTIVE", "DISABLED"]` slice 가 TS↔Python↔Go 3-way sync anchor.
- `packages/shared-contract/src/build/response.ts` — `claimResponseSchema.reason` 에 `RUNNER_DISABLED` / `RUNNER_ID_REQUIRED` 두 enum 멤버 추가.
- `apps/skill_mcp/contract/canonical.py` — `RUNNER_STATUSES` frozenset 신규.
- `apps/skill_mcp/skills/contract_drift_checker/core.py` — `PYTHON_CANONICAL_MAP` + `GO_CANONICAL_MAP` 에 `runnerStatuses` 동기 그룹 등록, branch 추가.
- `apps/runner/internal/contract/runner_registry.go` — `RunnerStatusActive`/`RunnerStatusDisabled` const + `RunnerStatuses` slice + `runner_registry_test.go` 5 case 모두 PASS.
- Build Server `apps/build-server/migrations/0005_runner_registry.sql` + `packages/db/src/schema/runner.ts` + `bootstrap.ts` — `runner` table (TEXT PK, status, 2 timestamp, builds_claimed BIGINT, builds_completed INT, current_build_id, last_error) + 2 index (status_last_seen composite).
- `BuildRepository` interface + ClaimNextBuildResult kind 2종 신규 — `runner_disabled` / `runner_id_required`.
- `memory-build-repository.ts` — module-scope `runners` Map + 6 method 구현. snapshot/Idempotent preserves admin's status. fallback 처리 (anonymous caller 도 accept).
- `postgres-build-repository.ts` — Drizzle schema + 6 method. UPSERT with ON CONFLICT (preserves status), per-event conditional UPDATE.
- `BuildService` — `claimNextBuild(runnerId?)` 사전 gate (`getRunnerStatus === DISABLED` → RUNNER_DISABLED) + register/markSeen 자동 누적 + `reportPhase(buildId, phase, runnerId?)` 가 terminal phase 에서 build registry 갱신. `anonymous` fallback 으로 backward compat 유지.
- Routes — build-routes (claim + phase 둘 다 payload.runnerId 전달), admin-routes (3 endpoint + X-Admin-Id guard), openapi snapshot 5 schema + 4 path 등록.
- build-monitor — `AdminRunners.svelte` (테이블 + status pill + last-seen relative + Disable/Reactivate/Delete 액션 + status filter chips) + Header 링크 + App.svelte `/admin/runners` route + `lib/api.ts` 3 helper.

### 4.3 검증

- **build-server**: 117/117 PASS (TASK-069 23 신규 + 기존 94). 신규 `runner-registry.test.ts` 10 case + `admin-runners-routes.test.ts` 10 case + `openapi.test.ts` snapshot 갱신.
- **build-monitor**: 64/64 PASS (TASK-069 4 신규 + 기존 60). 신규 `AdminRunners.test.ts` 4 case. `svelte-check` 0 errors 0 warnings.
- **runner Go**: 65/65 PASS (직전 60 + 신규 `runner_registry_test.go` 5 case).
- **drift checker**: 13 sync group 모두 missing=0 extra=0. 신규 `skillRunnerStatuses`/`goRunnerStatuses` shared=2 with TS `runnerStatuses` 정합.
- **Python**: 226/226 PASS (변동 없음).
- **memory e2e**: smoke.sh + e2e-source-archive.sh 회귀 보존.
- **end-to-end live smoke** (`memory` 백엔드 + skeleton mode Runner):
  1. `/admin/runners` empty
  2. `/builds/claim` Runner → `NO_BUILD_AVAILABLE` (build 큐 없음) — 하지만 runner 는 자동 등록됨
  3. `/admin/runners` 1개 entry ACTIVE
  4. `PATCH` DISABLED → 다음 claim `RUNNER_DISABLED` reason
  5. `PATCH` ACTIVE → 다시 활성
  6. `DELETE` → empty
  7. 실제 build + Runner skeleton mode + REPORT PHASE COMPLETED → `buildsClaimed=1, buildsCompleted=1, currentBuildId=null` 정확히 반영

### 4.4 결과

- 작업 결과: TASK-069 main HEAD `beb49f0` 합류. admin menu 의 4번째 surface ("Admin · Runners") 가 build-monitor에 노출. Runner registry 가 visibility (list) + control (DISABLE) + lifecycle (auto-register/idempotent) 의 3 축을 모두 충족.
- 다음 세션 시작 포인트: TASK-068 (real deploy adapter) 진입 — M3 의 마지막 축. deploy target 결정 (docker registry push / compose / k8s / GitHub Pages) 후 skeleton adapter 를 real wire-up.
- 남은 리스크 및 후속 작업:
  1. `Runner.lastError` 는 Build Server 가 채워주는 데 반해, runner-side error 정보 (`s.docker.Client` 의 build error 등) 는 별도 hook 필요 — 후속 v2.
  2. multi-tenant token / capability metadata 는 v2 scope.
  3. heartbeat / liveness (e.g. "30s 안에 안 보이면 stale" UI 표시) 는 register-on-claim 으로 충분한 v1 — 필요 시 liveness column 추가.

