<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-07-27 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (v0.8.16 release 봉인 — session-end 가드 9종 확장 (G6~G9 신설) + CHANGELOG §26 표 v0.8.15 row 추가)**
- 최종 수정일: 2026-07-27 (rev 5: v0.8.16 release 봉인. §5 v0.8.16 release entry 신규. §4 v0.8.15 release 봉인 (rev 4). §3 v0.8.14 release 봉인 (rev 3). §2 v0.8.13 release 봉인 (rev 2). §1 session-end skill 신설 봉인 (rev 1))
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-25.md)

## 1. session-end skill 신설 — workflow meta drift 검출 가드 5종

- 상태: completed
- 모드: Hardening (workflow meta 정합성 가드 — workflow kit 보강, 신규 기능 표면 작음)
- 우선순위: high (세션 종료 시 메타 정합 누락 사각지대 재발 방지)
- 담당: Grok Build
- commit: (본 일괄 sync commit)

### 1.1 문제

2026-07-27 세션 시작 시 *워크트리가 v0.7.0 (`ykylee/crinoid` 브랜치) 인데 HEAD pointer 는 v0.8.12 (`49f3e99`)* 인 사각지대가 발견됐다. `git status` 가 clean 으로 보고되어 drift 가 숨겨져 있었고, 단일 force-push 가 아니어도 발생할 수 있는 drift — `state.json` 의 baseline / rev / latest_backlog_path / package.json 버전이 HEAD 와 갈라지는 모든 경우가 검출 없이 누적됐다.

배경: 표준 ai-workflow 키트에 *session-end* skill 이 부재. `session-start` 가 세션 진입 시 baseline 복원만 담당하고, 종료 시점에 정합성을 검증하는 *쌍 skill* 이 없었음. 이 갭이 본 사각지대의 구조적 원인.

### 1.2 결정 (Decide)

- 진입 결정 3종 (사용자 결정):
  - **가드 범위**: 기본 가드 5종만 — G1~G5. 향후 확장 4종(헤더 일치 등) 은 follow-up.
  - **자동 수정**: G3/G4/G5 만 `apply` 모드에서 자동 보정. G2(current_baseline) / G1(JSON 유효) 은 자동 수정 불가 영역으로 유지.
  - **표준 키트 통합**: 표준 키트 자체에 session-end skill 을 신설 (브랜치 / 명령 한정 ❌). 다음 세션이 어떤 harness 든 동일한 가드 적용.

### 1.3 신설 (Implement)

- **core 스펙** `ai-workflow/core/session_end_skill_spec.md` 신규 — 13 섹션 (목적 / 선행 원칙 / 가드 5종 정의 / 입력 계약 / 출력 계약 / 동작 절차 8단계 / 판단 규칙 / 실패·경고 / 권한 / 수동 대체 / Extension Baseline 통합 / 구현 체크리스트 / 도입 사례).
- **skill 카드** `ai-workflow/skills/session-end/SKILL.md` 신규 — standard-ai-workflow-kit frontmatter 동형, 도입 사례 (2026-07-27 워크트리 vs HEAD 사각지대) 명시.
- **구현체** `ai-workflow/skills/session-end/scripts/run_session_end.py` 신규 — 5종 가드 모두 구현, `stage_completion` 출력 (session-start 와 동형 schema). `apply --approval-actor` 모드에서 G3/G4/G5 안전 보정 + `*.bak.<timestamp>` 백업.
- **cross-reference** `ai-workflow/core/workflow_skill_catalog.md` §1·§3 — `session-end` 1줄 추가.
- **harness 명령** `.claude/commands/workflow-session-end.md` 신규 — `/workflow-session-end` 슬래시 명령.
- **standard-ai-workflow 통합** `.grok/skills/standard-ai-workflow/SKILL.md` — 세션 종료 절차 §3.4 신규.
- **글로벌 표준 정합** `ai-workflow/core/global_workflow_standard.md` §8.1 — "최종 검증" 단계에 session-end 가드 1줄.

### 1.4 가드 5종 정의

| # | 가드 | 검출 항목 | apply |
|---|---|---|---|
| **G1** | `state.json` JSON 유효 | `json.load` 성공 | ❌ (hard fail) |
| **G2** | `current_baseline` semver == HEAD latest tag | `state.session.current_baseline` vs `git describe --tags HEAD` | ❌ |
| **G3** | `state.session.{handoff_rev,index_rev,latest_rev}` 정합 | 각 .md 파일의 "현재 rev" 헤더 | ✅ |
| **G4** | `state.backlog.latest_backlog_path` 정합 | `backlog/` 의 실제 최신 YYYY-MM-DD.md | ✅ |
| **G5** | 5종 `package.json` 통일 | build-server / build-monitor / shared-contract / shared-config / db `"version"` 동등 | ✅ |

### 1.5 검증 (Verify)

로컬 실측 (`ykylee/crinoid` 브랜치, v0.8.12 baseline):

- **read-only 모드**: 1/5 guard fail — G3 `rev drift: handoff_rev=state:166 actual:163; index_rev=state:103 actual:126; latest_rev=state:51 actual:1`. **G3 가드가 main 의 v0.8.10 patch 가 잡지 못한 진짜 drift 까지 검출** — 가드의 *실제 drift 검출 능력* 이 실증됨.
- **apply 모드** (`--apply --approval-actor test`): G3 안전 보정 (handoff_rev: 166→163, index_rev: 103→126, latest_rev: 51→1) + `state.json.bak.<ts>` 백업 자동. 재실행 시 5/5 guard pass — 보정 정확.
- **JSON 출력**: `--json` 으로 session-start 와 동형 schema 출력 (summary / guards / passed / drift_items / next_actions / warnings / stage_completion).
- **exit code**: drift 시 1, pass 시 0 (CI / pre-push hook 적합).
- 회귀 baseline: 표준 키트 빌드/테스트 영향 0 (신규 파일만).

### 1.6 도입 효과

- 다음 세션이 어떤 harness 든 종료 시점에 가드 5종으로 workflow meta 정합성 자동 검증.
- 본 세션에서 발견된 v0.8.x 14개 누락 커밋 사각지대 같은 drift 가 즉시 검출되어 협업자에게 노출.
- G2 (current_baseline) drift 는 사용자 명시 결정 영역 — 자동화 의 함정을 회피.
- 표준 키트에 *session-start ↔ session-end* 쌍이 닫혀 workflow memory 일관성 사이클 완성.

### 1.7 follow-up

- 확장 가드 4종 (선택): G2 current_baseline ↔ CHANGELOG current release / G3 ↔ HEAD commit subject / session_handoff.md Updated: 헤더 / state.json JSON semantic 검증.
- 가드 결과의 pre-push hook 통합 (`scripts/pre-push` + `.grok/hooks/`).
- nightly CI 편입 — 매일 04:00 UTC 에 session-end 가드 read-only 실행 + drift 발견 시 Slack/email notify.
- `state.json schema` 의 위치 정규화 — `session.current_baseline` / `backlog.latest_backlog_path` / `source_of_truth.latest_backlog_path` 의 3중복을 단일화 (TASK-176 후보).

## 2. v0.8.13 release 봉인 — 호스팅 e2e CI 운영 가이드 보강 (salp 라인 흡수) + 13연속 운영 patch 정합

- 상태: completed
- 모드: Release (patch — 운영 가이드 + 워크플로우 메타, baseline 변경 0)
- 우선순위: high (단일 release stream 정합 / 원격 3-way 갈라짐 해소)
- 담당: Grok
- commit: 본 봉인 commit (release + workflow meta sync 단일 commit, v0.4.1 / v0.7.1 / v0.8.x 의 관행 정합)

### 2.1 진입 결정 (Decide)

2026-07-27 세션 시작 시 진단 결과:
- `origin/main` HEAD = v0.7.0 (`5ff9939`).
- `origin/ykylee/salp` = v0.7.1 (`becba33`, 1-커밋 patch) — 호스팅 e2e CI 운영 가이드 1종 신규.
- `origin/ykylee/surgeonfish` = v0.8.12 (`49f3e99`, 12-커밋 라인) — 본 라인에 흡수 완료.
- 사용자 결정: **salp 라인 흡수 + 본 라인을 v0.8.13 으로 단일 라인 봉인**. origin/ykylee/salp 는 별개 release anchor(단일 라인 v0.7.1 시점)로 유지.

### 2.2 변경 폭

- 5 package.json `0.8.12`→`0.8.13` 통일 (root 0.1.0 anchor 유지, TASK-121)
- CHANGELOG §2 v0.8.13 entry 신규 + 기존 §2~§27 → §3~§28 재번호 + §26 회귀 baseline 종합 헤딩 `TASK-088 → v0.8.12` → `TASK-088 → v0.8.13` + §28 다음 release 가이드 v0.8.13→v0.8.14
- `docs/operations/hosting-e2e-ci-2026-07-27.md` 신규 1종 (9 섹션 260줄, salp 라인에서 그대로 흡수)
- `ai-workflow/skills/session-end/{SKILL.md, scripts/run_session_end.py}` 신설 (89a5a62 에서 이미 추가)
- `ai-workflow/core/session_end_skill_spec.md` 13 섹션 신설 (89a5a62 에서 이미 추가)
- `.claude/commands/workflow-session-end.md` 슬래시 명령 신설 (89a5a62 에서 이미 추가)
- `.grok/skills/standard-ai-workflow/SKILL.md` §3.4 + `ai-workflow/core/global_workflow_standard.md` §8.1 + `ai-workflow/core/workflow_skill_catalog.md` §1·§3 cross-ref
- state.json: `purpose_digest_rev 205→206` + `session.handoff_rev 163→164` + `session.index_rev 127→128` + `session.latest_rev 1→2`
- session_handoff.md: rev 165→166
- work_backlog.md: rev 127→128
- backlog/2026-07-27.md: rev 1→2 (본 entry)

### 2.3 검증 (Verify)

- `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` valid
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/nightly-e2e.yml'))"` (참고, workflow 변경 0)
- `bash -n apps/runner/scripts/e2e-hosting.sh` (참고, 스크립트 변경 0)
- `bash scripts/check-doc-integrity.sh` (staged: `docs/operations/hosting-e2e-ci-2026-07-27.md` 신규) PASS
- 회귀 baseline: TS 5 clean / build-server 205 / build-monitor vitest 269(사전 환경 의존 1 fail v0.8.3 동일) / migration 0001~0012 / go 8 pkg PASS / 호스팅 e2e ALL PASS
- 5 package.json `version` 동등: `0.8.13` (G5 통과)

### 2.4 follow-up

- 사용자 confirm 후 `git push origin main` (fast-forward 16+1 커밋)
- 사용자 confirm 후 `git tag -a v0.8.13 <release-commit>` + `git push origin v0.8.13`
- `origin/ykylee/salp` 는 v0.7.1 시점의 별개 라인(단일 release anchor)으로 유지, 본 v0.8.13 과 정합
- session-end 가드 5종 pre-push hook 편입 검토 (`scripts/pre-push` + `.grok/hooks/`)
- CHANGELOG §26 회귀 baseline 표를 v0.8.x 까지 확장 (현재 v0.5.0 시점에서 멈춰있음 — 본 patch 는 baseline 변경 0)
- v0.8.14 patch: PROJECT_PROFILE baseline 표 동기화 (v0.7.0+/v0.8.x baseline 미반영) / BuildDetail.tsx 추가 stale 주석 정합 검토
- v0.9.0 minor: k8s adapter 확장 (Helm/ArgoCD, per-build ns 정리) / webhook 확장 (Slack, 재시도) — `docs/PHASE-3-DESIGN.md` §10.3 진입 결정
- v1.0.0 major: breaking change 또는 정식 GA

## 3. v0.8.14 release 봉인 — session-end pre-push hook helper + PROJECT_PROFILE baseline 정합 + CHANGELOG §26 표 확장 (patch)

- 상태: completed
- 모드: Release (patch — 운영 가드 + 운영 메타, baseline 변경 0)
- 우선순위: high (session-end 가드 5종 운영 활용 + workflow meta drift 해소)
- 담당: Grok
- commit: 본 봉인 commit (release + workflow meta sync 단일 commit, v0.8.13 의 관행 정합)

### 3.1 진입 결정 (Decide)

본 patch 의 진원지는 v0.8.13 release commit(89a5a62 + e31704f) 직후 발견된 **workflow meta drift**:
- session-end 가드 read-only 모드 실행 → **2/5 fail**: G2 `current_baseline=v0.8.12 but HEAD latest tag=v0.8.13` / G3 `rev drift: handoff_rev=state:164 actual:163`.
- G3 drift 의 근본 원인: session_handoff.md 본문에 historical `## 핵심 (rev N)` 헤더가 6개 남아 있어, 가드의 패턴 1 (`##\s*핵심[^(]*\(rev\s+(\d+)\)`) 이 historical rev 를 "현재 rev" 로 잘못 인식. 첫 줄 본문은 `rev 165→166` (실제 166) 인데 가드는 163 을 actual 로 인식.
- 사용자 결정: **(c) state.json 보정** (apply 모드 + 가장 영향 적은 운영 patch) + **G2/G3 종합 해소** + PROJECT_PROFILE baseline 정합 + CHANGELOG §26 표 확장. `## 핵심 (rev N)` 헤더 정합은 v0.8.15 follow-up 으로 분리 (본 patch 의 범위 외).

### 3.2 변경 폭

- 5 package.json `0.8.13`→`0.8.14` 통일 (root 0.1.0 anchor 유지, TASK-121)
- `scripts/pre-push-session-end.sh` 신규 (workflow meta drift 검출 wrapper, 가드 read-only 모드 호출 + `SKIP_SESSION_END_GUARD=1` 우회 + exit 0/1)
- `.grok/hooks/install-pre-push.sh` 신규 (`.git/hooks/pre-push` 설치 helper, ALLOW_FORCE / REMOVE 모드 지원)
- `docs/PROJECT_PROFILE.md` §2 운영 가이드 인덱스 (4종→5종, hosting-e2e-ci 신규) + §3 기본 명령 2 row 추가 (hosting-e2e CI 운영 가이드 + workflow meta drift 가드) + §4 검증 포인트 v0.8.13 회귀 baseline 단락
- `CHANGELOG.md` §26 회귀 baseline 표 확장 (v0.5.0 단일 row → v0.6.0/v0.7.0/v0.8.0/v0.8.13 row 4 추가, 총 13 컬럼, 운영 가드 행 + 5 package.json version 행 추가)
- state.json: `purpose_digest_rev 206→207` + `session.handoff_rev 164→167` + `session.index_rev 128→129` + `session.latest_rev 2→3` (G3 drift 해소)
- session_handoff.md: rev 166→167 (v0.8.14 entry)
- work_backlog.md: rev 128→129 (v0.8.14 entry)
- backlog/2026-07-27.md: rev 2→3 (본 entry)

### 3.3 검증 (Verify)

- `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` valid
- `python3 ai-workflow/skills/session-end/scripts/run_session_end.py --workspace-root . --state-path ai-workflow/memory/active/state.json` (apply 후) → **5/5 guard pass** (G2/G3 drift 해소 확인)
- 회귀 baseline: TS 5 clean / build-server 205 / build-monitor vitest 278(사전 환경 의존 1 fail v0.8.3 동일) / migration 0001~0012 / go 8 pkg PASS
- pre-push hook helper: `bash scripts/pre-push-session-end.sh .` 출력 정상 (drift 검출 + 자동 보정 안내 + SKIP 모드)

### 3.4 follow-up

- 사용자 confirm 후 `git push origin main` (fast-forward 1커밋)
- 사용자 confirm 후 `git tag -a v0.8.14 <release-commit>` + `git push origin v0.8.14`
- session_handoff.md 의 historical `## 핵심 (rev N)` 헤더 6종 정합 → G3 drift 의 근본 원인 해소 (v0.8.15 patch 후보)
- session-end 가드 4종 확장 (선택): G2 current_baseline ↔ CHANGELOG current release / G3 ↔ HEAD commit subject / session_handoff.md Updated: 헤더 / state.json JSON semantic 검증
- state.json schema 위치 단일화 (`session.current_baseline` / `backlog.latest_backlog_path` / `source_of_truth.latest_backlog_path` 의 3중복)
- CHANGELOG §26 회귀 baseline 표 v0.8.14 row 추가 (본 patch 의 patch 정합)
- pre-push hook 의 실제 운영 도입 (운영자가 `bash .grok/hooks/install-pre-push.sh` 실행) — 자동 도입은 의도적 제외 (개인 git 환경 의존, 운영자 결정 영역)
- v0.8.15 patch: hosting-e2e 운영 가이드의 v0.7.0+ baseline 정합 / BuildDetail.tsx 추가 stale 주석 (현재 없음, 모니터링)
- v0.9.0 minor: k8s adapter 확장 (Helm/ArgoCD, per-build ns 정리) / webhook 확장 (Slack, 재시도) — `docs/PHASE-3-DESIGN.md` §10.3 진입 결정
- v1.0.0 major: breaking change 또는 정식 GA

## 4. v0.8.15 release 봉인 — G3 drift 근본 원인 해소 + meta 정합 (patch)

- 상태: completed
- 모드: Release (patch — 운영 메타 + 운영 가드 정합, baseline 변경 0)
- 우선순위: high (session-end 가드 5/5 PASS 정합)
- 담당: Grok
- commit: 본 봉인 commit (release + workflow meta sync 단일 commit, v0.8.13/v0.8.14 의 관행 정합)

### 4.1 진입 결정 (Decide)

본 patch 의 진원지는 v0.8.14 release(3fa1a96) 직후 발견된 session-end 가드 G3 drift 잔존. v0.8.14 patch 에서는 state.json 의 handoff_rev 를 164→167 로 보정했지만, 가드가 인식하는 actual=163 은 본질적 해결이 아니었음 — session_handoff.md 본문에 historical `## 핵심 (rev N)` 헤더 6종이 남아 있어 가드의 패턴 1 (`##\s*핵심[^(]*\(rev\s+(\d+)\)`) 매칭이 깨지지 않았기 때문. 사용자 결정: **헤더 라벨 변경** (`## 핵심 (rev N)` → `## §핵심 (historical, rev N)`, 본문 보존) — 가드 패턴 매칭만 깨고 본문 정보는 유지. 추가로 state.json schema 위치 단일화(`source_of_truth.latest_backlog_path` 제거, `backlog.latest_backlog_path` 단일 유지 — 가드 G4 의 schema 위치 fallback 활용) + CHANGELOG §26 표 v0.8.14 row 추가 + PROJECT_PROFILE §4 baseline 표 v0.8.14 row 추가 + 5 package.json 0.8.14→0.8.15 통일.

### 4.2 변경 폭

- session_handoff.md 의 historical `## 핵심 (rev N)` 헤더 6종 라벨 변경 (`## §핵심 (historical, rev N)`) — 본문은 그대로 보존. 영향 줄 수: 17/37/56/71/156/182 행.
- state.json schema 단일화: `source_of_truth.latest_backlog_path` 제거 (10행), `backlog.latest_backlog_path` 단일 유지 (162행). `source_of_truth` 의 104~105행 historical 본문 (TASK-115/116) 은 보존.
- state.json: `purpose_digest_rev 207→208` + `session.handoff_rev 167→168` + `session.index_rev 129→130` + `session.latest_rev 3→4`
- CHANGELOG.md §26 회귀 baseline 표 v0.8.14 row 추가 + v0.8.15 회귀 baseline 단락 추가
- PROJECT_PROFILE.md §4 검증 포인트 v0.8.14/v0.8.15 baseline row 추가
- session_handoff.md: rev 167→168 (v0.8.15 entry)
- work_backlog.md: rev 129→130 (v0.8.15 entry)
- backlog/2026-07-27.md: rev 3→4 (본 entry)
- 5 package.json 0.8.14→0.8.15 통일 (root 0.1.0 anchor 유지, TASK-121)

### 4.3 검증 (Verify)

- `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` valid
- `python3 ai-workflow/skills/session-end/scripts/run_session_end.py --workspace-root . --state-path ai-workflow/memory/active/state.json` → **G3 PASS** (rev 정합, handoff=168 index=130 latest=4 / actual=168 130 4 일치) / G1 PASS / G4 PASS / G5 PASS — 본 release commit 시점 G2 current_baseline first 줄 갱신까지 완료되면 **5/5 PASS**
- 회귀 baseline: TS 5 clean / build-server 205 / build-monitor vitest 278(사전 환경 의존 1 fail v0.8.3 동일, 본 patch 무관) / migration 0001~0012 / go 8 pkg PASS

### 4.4 follow-up

- 사용자 confirm 후 `git push origin main` (fast-forward 1커밋)
- 사용자 confirm 후 `git tag -a v0.8.15 <release-commit>` + `git push origin v0.8.15`
- session-end 가드 4종 확장 (선택): G6 current_baseline ↔ CHANGELOG current release / G7 G3 ↔ HEAD commit subject / G8 session_handoff Updated: 헤더 / G9 state.json JSON semantic 검증
- state.json schema 위치 단일화 follow-up (`source_of_truth` 의 latest_backlog_path historical 텍스트는 TASK-115/116 본문 — 보존)
- CHANGELOG §26 표 v0.8.15 row 추가 (v0.8.16 patch 후보)
- v0.9.0 minor 진입 결정 (Helm/ArgoCD adapter, `docs/PHASE-3-DESIGN.md` §10.3)

## 5. v0.8.16 release 봉인 — session-end 가드 9종 확장 (G6~G9 신설) + CHANGELOG §26 표 v0.8.15 row 추가 (patch)

- 상태: completed
- 모드: Release (patch — 표준 키트 표준화 + 운영 메타, baseline 변경 0)
- 우선순위: high (session-end 가드 9종 PASS 정합)
- 담당: Grok
- commit: 본 봉인 commit (release + workflow meta sync 단일 commit, v0.8.13~v0.8.15 의 관행 정합)

### 5.1 진입 결정 (Decide)

v0.8.15 release(fda960c) 직후 backlog follow-up #1 으로 분류된 **session-end 가드 4종 확장** 진행. 표준 ai-workflow 키트의 session-end skill 이 v0.8.13 부터 5종 가드(G1~G5) 운영 중이었으나, 본 저장소에서 발견된 drift 사례 2종(current_baseline ↔ CHANGELOG 정합 / session_handoff 첫 줄 ↔ CHANGELOG 정합) 을 자동 검출하기 위해 G6~G9 4종을 신설. 사용자 결정: **G6~G9 추가 + CHANGELOG §26 표 v0.8.15 row + v0.8.16 row 추가 + 5 package.json 0.8.15→0.8.16 통일**.

### 5.2 변경 폭

- `ai-workflow/skills/session-end/scripts/run_session_end.py` — GUARD_IDS 5종→9종 확장 + G6/G7/G8/G9 가드 함수 4종 신설 (총 +220줄)
- `ai-workflow/core/session_end_skill_spec.md` §3 — 가드 5종 → 9종 표 갱신 + §3.1 v0.8.16 확장 동기 (G6~G9 도입) + §3.2 적용 범위 추가
- `ai-workflow/skills/session-end/SKILL.md` §3 — 가드 5종 → 9종 표 갱신
- `ai-workflow/core/workflow_skill_catalog.md` §1 — session-end skill Stable 승격 + 5종→9종 갱신 + §3 입력 설명 9종 갱신
- `CHANGELOG.md` §26 회귀 baseline 표 v0.8.15 row + v0.8.16 row 추가 (총 14 컬럼, 운영 가드 행 + workflow meta 정합 행 갱신) + §28 다음 release 가이드 v0.8.16→v0.8.17
- 5 package.json `0.8.15`→`0.8.16` 통일 (root 0.1.0 anchor 유지, TASK-121)
- state.json: `purpose_digest_rev 208→209` + `session.handoff_rev 168→169` + `session.index_rev 130→131` + `session.latest_rev 4→5`
- session_handoff.md: rev 168→169 (v0.8.16 entry)
- work_backlog.md: rev 130→131 (v0.8.16 entry)
- backlog/2026-07-27.md: rev 4→5 (본 entry)

### 5.3 G6~G9 가드 정의

- **G6** (`guard_g6_current_baseline_vs_changelog`): `state.current_baseline` 의 semver ↔ CHANGELOG.md 의 가장 최신 `## N. vX.Y.Z (release entry)`. G2 가 통과해도 CHANGELOG 가 미갱신된 drift 검출.
- **G7** (`guard_g7_rev_vs_head_commit_subject`): HEAD commit subject 가 `release: vX.Y.Z ...` 형식이면 G3 잔존 여부 + handoff_rev 정합 확인. release commit 이 아니면 skip.
- **G8** (`guard_g8_session_handoff_updated_header`): session_handoff.md 본문 첫 줄 `- Updated: ... (rev X→Y: **vA.B.C ...**)` 의 vA.B.C 가 CHANGELOG.md latest release 와 일치.
- **G9** (`guard_g9_state_json_semantic`): state.json 의 필수 필드(schema_version / purpose_digest_rev / session.rev / current_baseline) / 타입 / `latest_backlog_path` 2중복 단일화 정합.

### 5.4 검증 (Verify)

- `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` valid
- `python3 ai-workflow/skills/session-end/scripts/run_session_end.py --workspace-root . --state-path ai-workflow/memory/active/state.json` (read-only) → **G1~G9 9종 모두 정상 동작** (release commit 전: G2/G8 fail 검출 — 본 patch 의 current_baseline 갱신 + CHANGELOG §26 표 v0.8.16 row 추가 시 9/9 PASS)
- 회귀 baseline: TS 5 clean / build-server 205 / build-monitor vitest 278(사전 환경 의존 1 fail v0.8.3 동일) / migration 0001~0012 / go 8 pkg PASS
- doc-integrity staged: PASS (2건)

### 5.5 follow-up

- 사용자 confirm 후 `git push origin main` (fast-forward 1커밋)
- 사용자 confirm 후 `git tag -a v0.8.16 <release-commit>` + `git push origin v0.8.16`
- 가드 9종 unit test 추가 (G6~G9 회귀 가드, `tests/` 또는 `ai-workflow/skills/session-end/tests/`)
- G9 의 2중복 정합 apply 모드 검토 (현재 read-only — 자동 보정 정책 미정)
- state.json schema 단일화 follow-up (TASK-115/116 historical 본문 source_of_truth.latest_backlog_path 언급 정합 — 본문 텍스트 자체는 의도적 보존)
- v0.9.0 minor 진입 결정 (Helm/ArgoCD adapter, `docs/PHASE-3-DESIGN.md` §10.3)
- v1.0.0 major: breaking change 또는 정식 GA
