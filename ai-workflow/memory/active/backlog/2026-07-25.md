<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-07-25 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (TASK-175 봉인 — v0.8.0 1묶음: k8s adapter 운영 결함 3종 해소)**
- 최종 수정일: 2026-07-25 (rev 1: 신규 일일 백로그. §1 TASK-175 봉인)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-24.md)

## 1. TASK-175 k8s adapter 확장 — 운영 결함 3종 해소 (v0.8.0 후보 1묶음)

- 상태: completed
- 모드: Hardening (운영 결함 해소 — 신규 기능 표면 작음, 회귀 위험 표면 큼)
- 우선순위: medium (v0.7.0 직후 follow-up 으로 분류된 v0.8.0 후보 첫 묶음)
- 담당: Claude Code
- commit: `6260aa0`

### 1.1 문제

v0.7.0 봉인 직후 v0.8.0 후보로 분류된 k8s adapter 확장 묶음. v0.7.0 까지
남아 있던 운영 결함 3종을 옵트인/내부 보강으로 해소한다.

- **E1 Per-build namespace 미분리** — `kubectlDeployer.Deploy()` 가 단일
  공유 namespace(`RUNNER_K8S_NAMESPACE`) 만 지원. 빌드 간 ingress DNS
  충돌 가능 + audit 어려움.
- **E2 Ingress cleanup 누락** — `kubectlDeployer.Cleanup()` 이
  `delete deployment,service` 만. 동명 재빌드 시 stale Ingress 가 라우팅을
  잡아채는 잠복 결함.
- **E3 k8s 실패 시 docker registry 결과 손실** — `build_service.go:380`
  분기에서 k8s Deploy 실패 시 `TargetType=K8S` FAILED 만 보고하고 docker
  registry 결과(`targetRef`/`resultRef`)가 사라진다. registry push 는
  성공한 채로 k8s 배포만 실패한 케이스에서 결과 조회 불가.

### 1.2 결정 (Decide)

진입 결정 2종 (사용자 결정):

- **E1 옵트인(default=false)**: `RUNNER_K8S_NAMESPACE_PER_BUILD=true` 면
  buildID 별 namespace(`dib-<buildID>`) 로 격리. 기존 운영자 설정 호환.
- **E3 마지막 FAILED + cause**: docker registry 결과를 payload에 보존한
  채 `FAILED(K8S)` 단일 보고. errorMessage 에 "docker registry push
  survived: <ref>" 포함. registry 자동 cleanup 은 본 단계 범위 밖.

### 1.3 구현 (Implement)

#### 1.3.1 E1 — per-build namespace 옵트인

- `apps/runner/internal/config/config.go`: `K8sNamespacePerBuild bool` 추가,
  `parseBool()` 헬퍼(accepts `true/1/yes/on` / `false/0/no/off` / default).
- `apps/runner/internal/services/build_service.go`: `deployImage()` 의 k8s
  분기에서 `RUNNER_K8S_NAMESPACE_PER_BUILD=true` 면 ns =
  `dib-<buildID>`(DNS-1123 정제). 헬퍼 `dns1123Label()` (deploymentName
  규약과 동형, 53자 + 양끝 trim).
- 기본값(false)은 기존 `RUNNER_K8S_NAMESPACE`(기본 `dib-builds`) 공유.

#### 1.3.2 E2 — Ingress cleanup

- `apps/runner/internal/deploy/k8s_kubectl.go` `Cleanup()`: `delete
  deployment,service` → `delete deployment,service,ingress` 한 명령으로
  묶음. `--ignore-not-found` 유지(Ingress 없는 정상 케이스 안전).
- 같은 deploymentName(`dib-<buildID>`) 으로 Ingress 가 생성되므로 한
  명령으로 정리 가능.

#### 1.3.3 E3 — k8s 실패 시 docker registry 결과 보존

- `apps/runner/internal/services/build_service.go` `deployImage()`:
  - k8s 분기 시작에 `IN_PROGRESS(K8S, deliveryMode=POLLING)` 보고(best-effort).
  - k8s Deploy 실패 시 `payload.dockerRegistry = {targetRef, resultRef,
    survivedAt: now.UTC()}` 추가 + `FAILED(K8S, ErrorCodeDeploymentFailed)`
    보고. `ErrorMessage` 에 `k8s deploy failed: <원인> (docker registry
    push survived: <ref>)` 표기.
- docker registry 성공 결과를 인라인 보존(policy: 마지막 FAILED + cause).

### 1.4 테스트 (Test)

- `apps/runner/internal/config/config_test.go`: `TestLoadK8sNamespacePerBuild`
  11 케이스(빈/true/TRUE/1/yes/on/false/0/no/off/nonsense).
- `apps/runner/internal/deploy/k8s_kubectl_test.go`: `TestKubectlCleanup_*`
  강화 — `argsAnySubstr()` 헬퍼로 `deployment,service,ingress` 단언.
- `apps/runner/internal/services/build_service_test.go`:
  - `TestBuildService_K8sDeployer_PerBuildNamespace` 4 케이스(default-shared /
    explicit-false / per-build-true / per-build-true-ignores-shared).
  - `TestBuildService_K8sDeployer_CleanupIncludesIngress` (위임 경로).
  - `TestBuildService_K8sDeployer_FailurePreservesDockerRegistryPayload`
    (FAILED 보고 + dockerRegistry payload + IN_PROGRESS+FAILED 2회 보고
    단언).
- 신규 3건, 강화 1건.

### 1.5 검증 (Verify)

- `gofmt -w` 5파일(빌드 단계에서 자동 적용).
- `go build ./...` clean.
- `go vet ./...` clean.
- `go test ./...` 8/8 pkg PASS (config / contract / deploy / docker /
  hostclient / services / source / worker).
- `k8s e2e` (kind/kubectl 실배포 + ingress) — 환경 부재로 미실측.
  `k8s_e2e_test.go`(`//go:build k8se2e`)는 기존과 동일하게 build 제외.
  단위 + 통합 테스트로 변경 표면을 커버.

### 1.6 영향 (Impact)

- **외부 인터페이스**: env opt-in(`RUNNER_K8S_NAMESPACE_PER_BUILD`) +
  payload 필드(`dockerRegistry`) 만 추가. API/스키마 변경 0.
- **기존 동작**: 옵트인 false(default) + k8s 분기 미활성 + k8s Deploy
  성공 시 동작은 **완전히 불변**.
- **운영 문서**: `docs/operations/k8s-deploy-webhook-2026-07-24.md` §6
  신설(E1/E2/E3 각각 설정/동작/예시).
- **회귀 baseline**: TS 5 clean / build-server 205 / build-monitor 279 /
  go 8 pkg / skill_mcp 225 / migration 0001~0012 — 모두 불변.

### 1.7 남긴 것 (follow-up)

- **v0.8.0 릴리스 태깅 검토** — 사용자 결정 대기.
- **k8s e2e 실측** — kind/kubectl 환경 복귀 시 `e2e-k8s-deploy.sh` +
  신규 시나리오(per-build namespace + Ingress cleanup + 실패 시 registry
  보존) 추가.
- **잔여 v0.8.0 후보**: Helm/ArgoCD adapter(`mode="helm"` / `mode="argocd"`)
  / webhook 확장(Slack·재시도·서명) / 실 k8s e2e sync 캐시 실측 / kind e2e
  nightly CI 편입.

## scope_creep_warnings

- `[]` (PURPOSE.md 부재 → graceful skip, advisory only)
