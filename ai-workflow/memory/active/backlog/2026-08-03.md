<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-08-03 작업 백로그

## Helm adapter vertical slice

- 상태: done
- 범위: Helm release lifecycle 보강 + hosted chart Ingress + kind e2e 검증
- 구현:
  - `K8sCleanupOptions.HelmRelease`를 추가해 Deploy/Cleanup release 식별자 불일치 해소.
  - Helm `Apply`도 명시적 release 선택과 BuildID fallback을 사용.
  - `examples/helm-hosted-app`에 path/subdomain 입력을 반영하는 Ingress 추가.
  - Helm adapter가 `hosting.scheme`/`hosting.baseHost` values를 chart에 전달.
  - e2e가 Deployment, Service, Ingress, `helm status`, uninstall까지 검증.
- 검증:
  - `go test ./...` — runner 전체 PASS
  - `helm lint examples/helm-hosted-app` — PASS
  - `helm template dib-build examples/helm-hosted-app` — PASS
  - `bash apps/runner/scripts/e2e-helm-deploy.sh` — kind 실배포/cleanup ALL PASS
- 후속: ArgoCD adapter 및 Ingress controller 결합 hosted HTTP e2e는 별도 작업.
