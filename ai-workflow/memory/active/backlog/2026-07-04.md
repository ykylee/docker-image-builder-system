<!-- standard-ai-workflow-kit: v0.11.21-beta -->

# 2026-07-04 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress (close after wrap-up)
- 최종 수정일: 2026-07-04 (rev 1: 신규 일일 백로그. §1 메타 sync + §2 TASK-066 entry)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

## 1. 메타 sync — TASK-061/062 main 합류 회수 + 신규 active queue 전환

- 상태: in_progress
- 모드: Documentation
- 우선순위: high
- 담당: Codex
- 영향 문서: ai-workflow/memory/active/state.json, session_handoff.md, work_backlog.md, backlog/2026-07-04.md (신규)

### 1.1 계획 (Plan)
- 작업 목표: PR #17 (TASK-061, `a155d52`) + PR #18 (TASK-062, `3e360ec`) 이 main 에 합류 완료된 사실을 workflow 메타 3종에 회수. `state.json` `current_focus` 를 신규 TASK-066 으로 전환. 신규 일일 백로그 2026-07-04.md 신설.
- 완료 기준: state.json / session_handoff.md / work_backlog.md rev counter 가 실제 main HEAD `3e360ec` 와 정합. `in_progress_items: [TASK-066]`, `recent_done_items` head 에 TASK-062, `latest_backlog_path: backlog/2026-07-04.md`, 회귀 영향 없음.

### 1.2 실행 (Act)
- 진행 현황: 2026-07-04 workflow 메타 4종 동시 갱신
- 수정 사항:
  - `ai-workflow/memory/active/state.json` — `generated_at` `2026-07-04T00:30:00Z` 로 갱신, `purpose_digest_rev` 77→78 + 신규 purpose_digest (TASK-066 진입 + 회귀 baseline), `session.current_baseline/current_axis/current_focus` 3종 모두 TASK-066 으로 갱신, `session.in_progress_items` = `[TASK-066 ...]`, `session.recent_done_items` head 에 TASK-062 PR #18 squash merge 항목 prepend, `session.handoff_rev` 55→56, `session.index_rev` 48→50 (work_backlog 49→50 정합), `session.latest_rev` 48→1 (신규 daily file rev 1), `backlog.latest_backlog_path` → `backlog/2026-07-04.md`, `backlog.task_count` 34→35 (TASK-066 신규 1), `backlog.done_items` head 에 TASK-062 항목 prepend, `backlog.index_rev` "28"→"29", `backlog.latest_rev` "32"→"33", `next_documents` 에 `backlog/2026-07-04.md` 추가.
  - `ai-workflow/memory/active/session_handoff.md` — `Updated` 라인 `2026-07-04 (rev 55→56: 메타 sync ...)` 로 갱신. 본문 narrative 는 다음 세션 wrap-up note 에서 추가 갱신.
  - `ai-workflow/memory/active/work_backlog.md` — header `최종 수정일` `2026-07-04 (rev 49→50: 메타 sync ...)` 로 갱신, 끝에 `TASK-066` 1차 PR scope 정리 entry 신규 추가.
  - `ai-workflow/memory/active/backlog/2026-07-04.md` — 본 문서 (rev 1) 신설.

### 1.3 검증 (Validate)
- 테스트 결과: `python3 -c "import json; json.load(open('ai-workflow/memory/active/state.json'))"` OK. session keys (`current_baseline` / `current_axis` / `current_focus` / `in_progress_items` / `blocked_items` / `recent_done_items` / `environment_constraints` / `handoff_rev` / `handoff_path` / `index_rev` / `latest_rev`) 11종 정합. backlog keys (`latest_backlog_path` / `task_count` / `in_progress_items` / `blocked_items` / `done_items` / `index_rev` / `latest_rev`) 7종 정합.
- 미실행 사유: 본 작업은 메타 sync (문서) 이므로 코드 회귀 영향 없음 — TS / Go / Python 테스트 재실행 skip.

### 1.4 결과 (Result)
- 작업 결과: workflow 메타 3종이 main HEAD `3e360ec` 와 정합. 신규 일일 백로그 파일 생성. TASK-062 PR #18 squash merge 가 명시적으로 회수됨. TASK-066 이 `in_progress` 상태로 진입.
- 다음 세션 시작 포인트: TASK-066 1차 PR 작성 (Build Server source endpoint + Runner fetcher + real docker build).
- 남은 리스크 및 후속 작업:
  - 2026-07-03.md 파일 자체의 header rev 가 `rev 47→48` 까지만 명시되어 TASK-061/062 항목에 대한 rev 갱신 누락 (실제론 §53, §55 섹션이 추가됨). 후속 정리 가능.
  - session_handoff.md 본문 narrative (Current Implementation Note / Next Actions) 는 본 entry 가 wrap-up 될 때 후속 갱신.

## 2. TASK-066 Runner source archive fetch + real docker build (M3 follow-up)

- 상태: in_progress
- 모드: Implementation | Integration
- 우선순위: high
- 담당: Codex
- 영향 문서/파일: apps/build-server/src/repositories/{memory,postgres}-build-repository.ts, apps/build-server/src/routes/build-routes.ts, apps/build-server/src/app/openapi.ts, apps/build-server/tests/, apps/runner/internal/source/fetcher.go (신설), apps/runner/internal/docker/client.go, apps/runner/internal/services/build_service.go, apps/runner/internal/hostclient/build_control_client.go

### 2.1 계획 (Plan)
- 작업 목표: 현재 Runner 가 scratch Dockerfile + manifest 만 생성하는 noop 상태에서, Build Server 가 source archive 를 저장/조회하고 Runner 가 그 archive 를 fetch + extract + real `docker build` 까지 수행하는 첫 end-to-end 슬라이스를 만든다.
- 완료 기준 (1차 PR):
  1. Build Server 에 source archive 저장 (memory: in-memory buffer / postgres: `bytea` column or filesystem key) + `POST /builds/:buildId/source` (raw bytes + checksum verify) + `GET /builds/:buildId/source` (octet-stream + checksum verify) endpoint 신설.
  2. Runner `internal/source/fetcher.go` 신설 — Build Server `GET .../source` 호출, workspace `src/` 에 extract, sha256 verify, retry 1회.
  3. `docker.BuildImage` 가 extracted 소스의 `entrypointPath`/`dockerfilePath` 기준 진짜 `docker build` (RUNNER_DOCKER_BUILD_MODE=cli 일 때만 — 기본값 `skeleton` 은 manifest 만 작성하고 안전). scratch Dockerfile 자동 생성 코드 제거 (또는 BuildServer 가 Dockerfile 자체를 archive 에 포함한다는 모델로 전환).
  4. happy path e2e smoke: BuildRequest → source ingest → Runner claim → fetch → extract → checksum verify → `docker build` (cli mode) → tagged image.
- 회귀: TS build-server 52/52 + Go 6 packages + Python 226/226 + 신규 Go fetcher test (extract/checksum/retry) + 신규 build-server source endpoint test.

### 2.2 실행 (Do)
- (진행 예정) branch `codex/task-066-runner-source-archive-2026-07-04` (main `3e360ec` base) 생성.

### 2.3 검증 (Validate)
- (예정) TS 4 packages direct `tsc --noEmit` + `go build ./...` + `go test ./...` + Python `unittest discover`.

### 2.4 결과 (Result)
- (예정) 1차 PR self-review 후 squash merge → 후속 TASK-067 (container run + healthcheck) / TASK-068 (real deploy adapter).
