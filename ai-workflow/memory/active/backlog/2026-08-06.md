<!-- standard-ai-workflow-kit: v1.0.0-beta -->

# 2026-08-06 작업 백로그

- (rev 12→13: 본 파일 최신화 — Phase 2 1·2차 봉인 / v0.11.0 release staging 정합)

## Phase 2 — Runner 인증 (HMAC lease token) 1·2차 봉인

- 상태: done (1·2차 완료 / 3·4·5차 + v0.11.0 release commit 후속)
- 본 세션 결정:
  - 운영 가이드 `docs/operations/runner-lease-hmac-2026-08-06.md` 신규 8 섹션 (의도 / 결정 / 회귀 baseline / 운영 명령 Production·Self-dogfood·단위 / 사용 절차 / 운영 환경 배포 절차 / 사전 결함 + 보강 4건 / 운영 환경 baseline / 한계와 follow-up 5종).
  - PROJECT_PROFILE §3.14 신규 — Phase 2 운영 패턴. 결정 / 신규 회귀 가드 7 case / 핵심 변경 5 파일 / 운영 명령 / 운영 검증 절차 6 단계 / 사전 결함 + 보강 4건 / 운영 영향 / 회귀 baseline / follow-up 5종.
  - CHANGELOG.md §2 v0.11.0 entry 신규 + §28 회귀 baseline 표 v0.11.0 컬럼 추가 + §30 다음 release 가이드 v0.11.0→v0.12.0.
  - compose.dev.yaml env `RUNNER_LEASE_TTL_SECONDS` (default 300) 추가.
  - state.json `current_baseline` `v0.10.0`→`v0.11.0` 갱신 (G6 drift 해소) + `purpose_digest_rev 263→264` / `handoff_rev 175→176` / `index_rev 141→142` / `latest_rev 12→13`.
- 신규 회귀 가드 영향: build-server **285/285 PASS** (이전 278 + Phase 2 신규 7 case: login 4 + renew 3) / runner Go **8 packages PASS** (stub interface EnsureLease/RenewLease/LoginLease no-op 추가). TS 5 packages `--noEmit` clean. `git diff --check` clean. session-end 가드 9/9 PASS (release commit 후 G6 drift 해소 + G8/G9 정합).
- 운영 자산 무변경: build-monitor / SQL / schema / migration 변경 0. code 변경 0 (단, Phase 1 1·2차 봉인과 동일 wire format / secret 동일).
- follow-up (v0.11.0 release commit 후속):
  - git tag `v0.11.0` annotated tag 발매 — 사용자 결정 대기 (TASK-131 정합 정책).
  - 운영 환경 staging 검증 (release-checklist §3 Verify 의 Phase 2 6 단계).
  - 운영자 결정: v0.12.0 follow-up 후보 (메모리 revoke Set 멀티 replica 영속화 Redis/Postgres + long-running build lease 자동 갱신 worker + secret rotation 자동화 + Phase 2 3·4·5차 봉인 — Runner별 K8s RBAC / build sandbox 격리 / resource limits + node/namespace 분리 / DISABLED 토글 시 즉시 lease 회수 WebSocket/SSE 채널) 5종.
  - 운영자가 staging 에서 RUNNER_HMAC_SECRET 셋업 + `/auth/runner-login` round-trip 검증 후 production 적용 권장.

## Phase 1 — Identity + 테넌트 권한 5단계 (운영 가이드 + v0.10.0 메타) 봉인

- 상태: done (5단계 완료 / v0.10.0 release commit 후속)
- 본 세션 결정:
  - 운영 가이드 `docs/operations/identity-cookie-hmac-2026-08-06.md` 신규 8 섹션 (의도 / 결정 / 4단계 회귀 baseline / 운영 명령 Production·Self-dogfood·단위 / 사용 절차 cookie·Bearer·legacy / 운영 환경 배포 절차 4단계 / 사전 결함 + 보강 4건 / 운영 환경 baseline / 한계와 follow-up 5종 / 결정).
  - PROJECT_PROFILE §3.13 신규 — Phase 1 운영 패턴. 결정 / 신규 회귀 가드 32 case / 핵심 변경 5 파일 / 운영 명령 Production·Self-dogfood / 운영 검증 절차 6 단계 / 사전 결함 + 보강 4건 / 운영 영향 / 회귀 baseline / follow-up 5종.
  - CHANGELOG.md §2 v0.10.0 entry 신규 (Phase 1 Identity + 테넌트 권한 1차 봉인). 7 섹션 (1·2단계 / 3단계 / 4단계 / 운영 환경 baseline / 운영 명령 / 업그레이드 안내 / follow-up). §1 release history 의 v0.10.0 row + 현재 release 갱신. §28 회귀 baseline 표 v0.10.0 컬럼 추가 (16 컬럼). §30 다음 release 가이드 v0.10.0→v0.11.0.
  - 5 package.json `0.9.0`→`0.10.0` 통일 bump (G5 가드 정합).
  - release-checklist §3 Verify 보강 — Phase 1 Identity + 테넌트 권한 회귀 가드 (whoami 401 hint / login v2 cookie round-trip / admin builds 200 / admin 비통과 403 callerId echo / logout jti revoke + cookie 만료) 6 단계 + 운영자 staging 검증 권고.
  - compose.dev.yaml env 4종 추가 — `AUTH_HMAC_SECRET` (default 빈 문자열, 운영 필수), `AUTH_TOKEN_TTL_SECONDS` (default 28800), `AUTH_LEGACY_HEADERS` (default false), `BUILD_OWNER_POLICY_LEGACY_DEFAULT_SUBJECT` (default `<anonymous>`).
  - state.json `current_baseline` `v0.9.0`→`v0.10.0` 갱신 (G6 drift 해소).
  - session_handoff.md 첫 줄 `v0.10.0` 갱신 (G8 drift 해소).
- 신규 회귀 가드 영향: build-server **278/278 PASS** (변경 0 — 본 TASK 는 docs only + 운영 메타 + version bump 만). TS 5 packages `--noEmit` clean. `git diff --check` clean. session-end 가드 9/9 PASS (release commit 후).
- 운영 자산 무변경: build-monitor / runner / SQL / schema / migration 변경 0. code 변경 0.
- follow-up (v0.10.0 release commit 후속):
  - git tag `v0.10.0` annotated tag 발매 — 사용자 결정 대기 (TASK-131 정합 정책).
  - 운영 환경 staging 검증 (release-checklist §3 Verify 의 Phase 1 6 단계).
  - 운영자 결정: v0.11.0 follow-up 후보 (메모리 revoke Set 영속화 Redis/Postgres + SSO/OIDC/JWT 연동 + route-level preHandler 일원화 + legacy X-User-Id/X-Admin-Id sunset 정책 + 멀티 탭 logout broadcast channel) 5종.
  - 운영자가 staging 에서 `AUTH_HMAC_SECRET` 셋업 + cookie 인증 round-trip 검증 후 production 적용 권장.

## Phase 1 — Identity + 테넌트 권한 4단계 (admin-routes requireAdmin) 봉인

- 상태: done (4단계 완료 / 5단계 후속)
- 본 세션 결정:
  - `legacyHeadersEnabled` default OFF (3단계 build-routes 와 정합). legacy ON 환경에서만 X-Admin-Id 헤더 fallback.
  - helper `enforceAdminGuard(request, reply, isAdmin, { legacyHeadersEnabled })` 가 cookie/Bearer 인증 admin + legacy ON X-Admin-Id 헤더 allow-list 일치 → 통과, 그 외 reject. legacy ON + X-Admin-Id 가 allow-list 미통과면 403 + callerId echo. legacy OFF + X-Admin-Id 헤더만 → 401 + Authentication required + hint.
  - 모든 admin-scoped 라우트 (14개: `/admin/builds`, `/admin/hosted-services`, `/admin/hosting-capacity`, `/admin/hosted-services/:appName/{manifest,manifest/revisions,manifest PUT,database/*}`, `/admin/users`, `/admin/admins/{GET,POST,DELETE}`, `/admin/runners/{POST,GET,PATCH,DELETE}`, `/admin/hosted-services/:appName/{stop,start,DELETE}`) 의 inline 가드를 helper 호출 1줄로 일원화.
  - `requireAdmin` preHandler 등록 라인은 dead code 로 정리 — handler 내부의 helper 호출이 동일 envelope 을 재현. 향후 fastify route config 일원화 TASK 에서 route-level preHandler 활성화는 별도 봉인.
  - helper 가 owner 추적용 callerId 를 반환하지 않으므로, 별도 `resolveAdminCallerId` helper 를 추가해 `updateServiceManifest` 의 owner 인자에 전달.
- 신규 회귀 가드: `apps/build-server/tests/admin-routes-prehandler.test.ts` 6 case (legacy OFF cookie admin 200 / cookie user role 403 / legacy ON X-Admin-Id 200 / legacy OFF X-Admin-Id 401 hint / legacy ON X-Admin-Id 비-allow-list 403 callerId echo / 인증 부재 401 hint). 기존 `admin-routes.test.ts` + `admin-runners-routes.test.ts` 의 `buildAppWithService` 가 `AUTH_LEGACY_HEADERS=true` 를 default 로 설정 (legacy ON 환경). 기존 `auth-routes.test.ts` 의 legacy default 검증 테스트를 legacy OFF 401 envelope + legacy ON 200 통과 2 case 로 분리.
- 회귀: build-server **278/278 PASS** (이전 272 + 신규 6). TS 5 packages `--noEmit` clean. `git diff --check` clean. session-end 가드 9/9 PASS.
- 운영 자산 무변경: build-monitor / runner / SQL / schema / migration / 5 package.json (`0.9.0`) 변경 0.
- follow-up (5단계, 본 세션 보류):
  - 운영 가이드 `docs/operations/identity-cookie-hmac-2026-08-06.md` 신규 + PROJECT_PROFILE §3.13 + CHANGELOG v0.10.0 + compose/k8s manifest env (`AUTH_HMAC_SECRET`, `BUILD_OWNER_POLICY_LEGACY_DEFAULT_SUBJECT`) + release-checklist §3 Verify 에 whoami curl + v2 wire format 마이그레이션 안내 + admin-routes preHandler 일원화 결론 (route-level vs inline helper 의 trade-off 명시).
  - 향후: route-level preHandler 활성화 시 admin/* 14개 라우트 등록을 `withGuard({ preHandler })` helper 로 묶는 fastify route config 일원화 TASK — 본 TASK 에서는 inline helper 호출로 봉인.

## Phase 1 — Identity + 테넌트 권한 1차 봉인 (HMAC Bearer cookie + principal preHandler)

- 상태: done (1·2·3단계 완료, 4·5단계 후속)

## Phase 1 — Identity + 테넌트 권한 3단계 (build-routes owner policy) 봉인

- 상태: done (3단계 완료 / 4·5단계 후속)
- 본 세션 결정:
  - `legacyHeadersEnabled` default **OFF** (운영 baseline), `AUTH_LEGACY_HEADERS=true` 환경만 legacy X-User-Id 헤더 fallback 허용.
  - `legacyDefaultSubject` default `""` (X-User-Id 부재 시 401). legacy ON 환경의 createApp 가 default subject `<anonymous>` 를 자동 적용 (self-dogfood 호환).
  - admin role + admin allow-list 통과 시 본인 외 빌드도 200 (allow-list 검증).
  - POST /builds body `requestedBy` 가 principal.subject 와 다르면 403 (위조 차단).
  - `BuildSummary` 가 `requestedBy` 를 노출하지 않으므로 BuildRepository 에 `tryGetBuildOwner(buildId)` helper 1행 추가. BuildService.getBuildOwner thin wrapper.
  - build-routes 모든 build-scoped 엔드포인트(GET :id / :id/logs / :id/source GET/POST/DELETE/chunk, POST /builds, POST :id/phase, POST :id/container-test/start, POST :id/container-test/result, POST :id/deployment) 에 enforceOwnerPolicy 통합.
  - GET /builds listBuilds 의 user role 은 본인 빌드만. query.requestedBy 위조 시 본인 subject 로 덮어씀.
  - GET /services 도 인증 필수 + 본인 subject 의 서비스만.
  - enforceOwnerPolicy 는 build 부재와 unknown build 모두 404 (인증 정보 누설 방지).
- **wire format 잠복 결함 동시 봉인 (HmacIdentityProvider)**: 1·2단계의 `v1.<plain-utf8-payload>.<hex-sig>` 형식이 subject 가 `.` 를 포함할 때(예: `yky.lee`) `split(".")` 가 payload 를 더 잘라 sig mismatch 가 발생. `v2.<base64url(payload)>.<base64url(sig)>` 로 형식 변경. v1 토큰은 reject. subject 의 `:` 만 차단 유지 (`.` 는 base64url-safe).
- 신규 회귀 가드: `apps/build-server/tests/build-owner-policy.test.ts` 12 case (cookie 인증, owner mismatch 403, admin role allow-list 통과, legacy X-User-Id on/off, body requestedBy 위조 403, 부재 404, user role query 위조 무시, GET :id/logs/DELETE source owner 가드). `auth-routes.test.ts` 에 v2 round-trip + subject 의 `.` 허용 2 case 추가. `build-routes.test.ts` 5 fixture 옵션 갱신. `static-serve.test.ts` createApp env (legacy ON) 추가.
- 회귀: build-server **271/271 PASS** (이전 257 + build-owner 12 + auth-routes 2 신규). TS 5 packages `--noEmit` clean. `git diff --check` clean. session-end 가드 9/9 PASS.
- 운영 자산 무변경: build-monitor / runner / SQL / schema / migration / 5 package.json (`0.9.0`) 변경 0.
- follow-up (4·5단계, 본 세션 보류):
  - 4단계: admin-routes `requireAdmin` preHandler 통합 — admin-routes 가 현재 inline X-Admin-Id 가드 + `requireAdmin` preHandler 둘 다 가짐. preHandler 통합 + admin allow-list 자동 검증.
  - 5단계: 운영 가이드 `docs/operations/identity-cookie-hmac-2026-08-06.md` 신규 + PROJECT_PROFILE §3.13 + CHANGELOG v0.10.0 + compose/k8s manifest env (`AUTH_HMAC_SECRET`, `BUILD_OWNER_POLICY_LEGACY_DEFAULT_SUBJECT`) + release-checklist §3 Verify 에 whoami curl + v2 wire format 마이그레이션 안내.

- (rev 8→9: 본 파일 최신화)

## Phase 1 — Identity + 테넌트 권한 1차 봉인 (HMAC Bearer cookie + principal preHandler)

- 상태: in_progress (1·2단계 완료, 3~5단계 후속)
- 결정: AUTH_LEGACY_HEADERS **default false** (마이그레이션 경로) — 기존 self-dogfood 의 X-Admin-Id 호출은 그대로 통과.
- 본 세션 산출물 (1·2단계):
  - **공유 계약**: `packages/shared-contract/src/auth/index.ts` — `principalRoleSchema` / `principalSchema` / `authLoginRequestSchema` / `authLoginResponseSchema` / `whoAmIResponseSchema` zod schema + type export.
  - **서버 인증 인프라**: `apps/build-server/src/auth/identity-provider.ts` (interface) + `hmac-identity-provider.ts` (HMAC-SHA256 서명, 8h default TTL, 메모리 revoke Set) + `request-principal.ts` (cookie + Bearer 추출, `addHook("preHandler", ...)` 으로 `request.principal` 채움).
  - **인증 라우트**: `apps/build-server/src/routes/auth-routes.ts` — `POST /auth/login` (subject + role, admin role 시 allow-list 검증), `POST /auth/logout` (jti revoke), `GET /auth/whoami`.
  - **부트 wire**: `create-app.ts` 가 boot 시 `HmacIdentityProvider` instantiate + `registerPrincipalPreHandler` 호출. NODE_ENV !== production 일 때 dev default secret 허용.
  - **OpenAPI**: `getOpenApiDocument()` 에 `cookieAuth` (apiKey, cookie, auth_token) + `bearerAuth` (http bearer) securityScheme 추가.
  - **CORS**: allowHeaders 에 `Cookie` 추가. allowCredentials 는 wildcard origin 일 때 false.
  - **신규 회귀 가드**: `apps/build-server/tests/auth-routes.test.ts` 14 case (login 4 / whoami 3 / logout 2 / HMAC edge case 4 / legacy-headers default OFF 1).
- 회귀: build-server **257/257 PASS** (기존 243 + 신규 14). TS 5 packages `--noEmit` clean.
- 운영 자산 무변경: build-monitor / runner / SQL / schema / migration / 5 package.json 변경 0.
- follow-up (3·4·5단계, 본 세션 보류):
  - 3단계: build-routes 의 owner policy 강제. `getBuild`/`getBuildLogs`/`storeSourceArchive*`/`getSourceArchive`/`deleteSourceArchive`/`listBuilds`/`createBuild` 의 principal.subject 매칭 + body `requestedBy` 위조 차단.
  - 4단계: admin-routes 의 requireAdmin preHandler 통합. `AUTH_LEGACY_HEADERS=true` 일 때만 cookie 우선 + legacy header 보조. admin allow-list membership 검증.
  - 5단계: 운영 가이드 `docs/operations/identity-cookie-hmac-2026-08-06.md` 신규 + PROJECT_PROFILE §3.13 + CHANGELOG v0.10.0 + compose/k8s manifest env (`AUTH_HMAC_SECRET`) + release-checklist §3 Verify 에 `whoami` curl.

## 표준 워크플로우 키트 v1.0.0-beta 이식

- 상태: in_progress
10- 범위: `~/repos/standard_ai_workflow` 의 `workflow-source/` (v1.0.0, 2026-07-22 released) 와 본 프로젝트 `ai-workflow/` 의 차이를 식별하고, 부재 자산을 일괄 이식 + 메타 헤더 정합.

## 표준 워크플로우 키트 v1.0.0-beta 이식

- 상태: in_progress
- 범위: `~/repos/standard_ai_workflow` 의 `workflow-source/` (v1.0.0, 2026-07-22 released) 와 본 프로젝트 `ai-workflow/` 의 차이를 식별하고, 부재 자산을 일괄 이식 + 메타 헤더 정합.
- 본 세션 비교 결과 (Phase 13 close-out 시점):
  - 표면 7종 skills + 6종 MCP 의 SKILL.md / .py 는 헤더 1줄(`<!-- standard-ai-workflow-kit: v1.0.0-beta -->`)만 본 프로젝트에 추가돼 있고 본문은 표준 키트와 동일.
  - 부재 자산 7종 skills (`automated-repro-scaffold`, `git-conflict-resolver`, `memory-freeze`, `memory-index-query`, `project-status-assessment`, `robust-patcher`, `workers`, `workflow-linter`) 와 5종 MCP (`apply_robust_patch`, `create-environment-record-stub`, `create-session-handoff-draft`, `git-history-summarizer`, `smart-context-reader`) 일괄 이식.
  - core 문서 3종 (`phase_13_followup.md`, `stable_guarantee.md`, `v1_0_0_entry_evaluation.md`) 이식. MEMORY_GOVERNANCE.md 는 표준 키트와 동일 본문임을 확인.
  - `workflow_kit/common/maturity.py` 모듈을 표준 키트에서 가져옴 — `session-end/scripts/run_session_end.py` 가 boot-time 에 import 하므로 모듈 부재 시 가드 9종 전체가 죽었던 결함 해소.
- 메타 헤더 정합 (v0.15.19-beta → v1.0.0-beta):
  - 진입점 4종: `CLAUDE.md`, `GROK.md`, `MiniMax.md`, `AGENTS.md`
  - 운영 표면: `docs/PROJECT_PROFILE.md`, `ai-workflow/README.md`, `ai-workflow/skills/session-end/SKILL.md`, `ai-workflow/memory/active/memory_index/README.md`
  - 의도적으로 보존: `backlog/2026-07-*` 일자 + `backlog/2026-08-{03,04,05}.md` + `state.json` 본문 (이력은 그대로, 키트 release tag 만이 본 세션의 정합 범위).
- 회귀 영향 없음: 본 세션은 워크플로우 메타 + 키트 자산만 갱신. `build-server` / `build-monitor` / `apps/runner` / SQL / schema / migration / 5 package.json (`0.9.0`) 모두 변경 없음.
- session-end 가드 9종 실행: 8/9 PASS, G3 latest_rev drift 만 잔존 (오늘자 백로그 등록 + work_backlog 인덱스 갱신으로 해소).

## 본 프로젝트 자체 자산 (v0.8.13 부터 봉인)

- `ai-workflow/skills/session-end/` — 표준 키트 v1.0.0 배포판엔 부재한 자체 자산 (TASK-178, 가드 9종은 v0.8.16 으로 확장). 표준 키트가 이 자산을 채택하면 통합 가능. 현재는 본 프로젝트 단독 운영.
- `apps/build-monitor/dist-react` 단일 SPA 운영 baseline — TASK-101 부터 유지. 본 세션 무관.
- service database 격리 (`migration 0017`+`0018`) + lifecycle(purge/rotate) — 2026-08-05 봉인. 본 세션 무관.

## 후속 (TASK 후보)

- 표준 키트 v1.0.0 부재 13종 harness 정합 — `harnesses/_template`, `aider`, `antigravity`, `codewhale`, `custom`, `gemini-cli`, `grok-build`, `minimax-code`, `pi-dev` 등 본 프로젝트엔 `codex`/`opencode` 만 있음. 본 세션 범위 밖.
- `workflow_kit/` 의 v0.15.20 → v1.0.0 모듈 갱신 — `cache_lfu_decay*.py`, `cache_*.py`, `cli/*`, `upgrade_diff.py` 등 일부가 v1.0.0 의 모듈 구조와 갈라져 있음. 본 세션 범위 밖.
- `workflow_kit/__init__.py` 의 `version` 상수 / `pyproject.toml` 동기 — 본 프로젝트 `ai-workflow/VERSION` 은 `v0.15.20-beta` 인데 본 세션 후속 release 에서 v1.0.0 으로 끌어올릴지 결정 필요.