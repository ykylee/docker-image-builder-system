<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-07-23 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (TASK-164 봉인 — 원격 발산 조정 + k8s adapter 이식)**
- 최종 수정일: 2026-07-23 (rev 1: 신규 일일 백로그. §1 TASK-164 봉인 — 원격 `origin/main` 이 P2-M1 이후 로컬과 다른 축으로 갈라진 것(로컬 3커밋 vs 원격 12커밋, 공통 조상 `060cc0b`, 33파일 충돌)을 확인·조정. 두 라인은 **같은 작업 중복이 아니라 설계 분기** — 로컬은 preview-era 완전 청산(엔드포인트 `/container-test/*` 통합 + errorCode 채널 + skill_mcp 실서버 검증 + NullableBuildError), 원격은 preview 최소 rename + **실제 k8s 배포 어댑터**. 엔드포인트 설계가 정면 대립(`/container-test/*` vs `/preview`+`/test-deployment/*`)해 자동 병합 불가. 사용자 결정: **로컬 baseline 유지 + 원격 고유분(k8s adapter)만 이식**. 이식(commit `6014322`): `deploy/k8s.go`+`k8s_test.go` 그대로 / `config.go` `RUNNER_K8S_*` env 4종 추가 / `build_service.go` k8sDeployer 필드+WithK8sDeployer+deployImage k8s 분기 — 원격의 ProcessClaim 인라인 raw-error 방식을 로컬의 `*stageFailure`/단일 `fail()` 구조에 맞춰 재작성. 프로덕션 미배선(worker/main 미주입, nil skip)이라 기존 동작 불변 — skeleton 단계(P2-M5 실제 client-go 대기). 원격 반영은 `merge -s ours`(원격 12커밋을 히스토리상 흡수, 트리는 로컬 유지 — 원격 엔드포인트/계약 변경분 의도적 폐기, commit `7c85a18`) 후 일반 push(비파괴, force-push 없음). 검증: `go build`/`vet` clean / `go test ./...` **8 pkg PASS**(신규 k8s 테스트 deploy 6 + services 5 포함). SQL / migration / version / git tag 변경 0. TS / frontend / skill_mcp 영향 0(런너 한정).)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-21.md)

## 1. TASK-164 원격 발산 조정 + k8s adapter 이식 (runner)

- 상태: completed
- 모드: Integration (git 히스토리 조정 + 원격 고유 코드 이식)
- 우선순위: high (원격과 로컬 main 이 33파일 규모로 갈라져 push/pull 불가 상태였음)
- 담당: Claude Code
- 영향 파일:
  - `apps/runner/internal/deploy/k8s.go` (신규, 원격에서 그대로)
  - `apps/runner/internal/deploy/k8s_test.go` (신규, 원격에서 그대로)
  - `apps/runner/internal/config/config.go` (`RUNNER_K8S_*` env 4종 추가)
  - `apps/runner/internal/services/build_service.go` (k8sDeployer 필드 + WithK8sDeployer + deployImage k8s 분기)
  - `apps/runner/internal/services/build_service_test.go` (k8s 통합 테스트 5건)
- commit: `6014322` (이식) + `7c85a18` (merge -s ours)

### 1.1 진단 (Diagnose)

- 공통 조상 `060cc0b`(P2-M1 완료)에서 로컬 3커밋 / 원격 12커밋으로 발산. 33파일 충돌.
- **핵심 판별**: 양쪽이 같은 마일스톤(P2-M2/M3/M4)을 병렬로 **다르게** 구현 — `status.ts` 최종 차이가 "같은 결정(previewStatuses 제거), 다른 주석"임을 확인.
- **결정적 대립**: 엔드포인트 설계. 로컬 = `/container-test/{start,result}` 2종 통합, 원격 = `/preview` + `/test-deployment/{ready,status}` 유지. 둘 다 가질 수 없음.
- **직교 산출물**: 로컬만 = `verify-live-server.sh` / 스킬 개명 / 운영 문서 3종 / errorCode 채널. 원격만 = `deploy/k8s.go` k8s 어댑터 / RELEASE_NOTES.
- 어느 쪽도 상대의 상위집합 아님.

### 1.2 결정 (Decide)

- 사용자 결정: **로컬 baseline(M2→M3→M4 청산 축이 더 앞섬) 유지 + 원격 고유 가치(k8s)만 이식**.
- 원격 반영 방법: **merge -s ours 후 일반 push**(비파괴, 원격 히스토리 DAG 보존, force-push 회피).

### 1.3 이식 (Graft)

- 신규 파일 2종: `git show cb4e482:...` 로 그대로 가져옴(K8sDeployer 인터페이스 + noopSkeleton).
- `config.go`: `K8sMode`/`K8sCluster`/`K8sNamespace`/`K8sManifest` + `RUNNER_K8S_*` env — 순수 추가.
- `build_service.go`: 원격은 `ProcessClaim` 인라인에서 `return fmt.Errorf(...)` 방식이었으나, 로컬은 `deployImage` 가 `*stageFailure` 를 반환하는 단계 함수 구조 → k8s 분기를 로컬 컨벤션에 맞춰 재작성. k8s 실패 시 `TargetType="K8S"` FAILED 보고 후 `stageFailure` 반환(단일 `fail()` 이 phase FAILED 보고), 성공 시 docker registry 결과와 merge 해 단일 SUCCESS 보고(`TargetType += ",K8S"` + payload.k8s section).
- `WithK8sDeployer` 는 gofmt-clean 으로 정리(원격 diff 의 leading-tab 잔재 제거).

### 1.4 검증 (Verify)

- `gofmt -w` 5파일 / `go build ./...` clean / `go vet ./...` clean.
- `go test ./...` **8 pkg PASS**. 신규 k8s 테스트 실행 확인: deploy `TestNewK8sDeployer_Modes`(6 서브) + services 5종(`NilByDefault`/`Setter`/`MergedIntoReportDeployment`/`Failure_ReportsFAILED`/`NilSkipsK8sPath`).
- 프로덕션 동작 불변: k8sDeployer 는 `WithK8sDeployer`(테스트 전용)로만 주입, worker/main 미배선 → 실운영은 nil skip.

### 1.5 남긴 것 (follow-up)

- k8s adapter 는 skeleton(noop + ResultRef emit) 단계. P2-M5 에서 실제 k8s client-go 구현체 + worker 배선(cfg.K8sMode 분기) 필요.
- 원격의 엔드포인트/계약 라인은 폐기됨 — 협업자가 그 라인 기준 작업 중이었다면 로컬 계약(`/container-test/*`)으로 재정렬 필요.

## scope_creep_warnings

- `[]` (PURPOSE.md 부재 → graceful skip, advisory only)
