<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-08-04 작업 백로그

## v0.9.0 release candidate 정리

- 상태: done
- 범위: Helm/ArgoCD adapter 실 e2e 완료, PROJECT_PROFILE 복구, v0.9.0 release notes 및 package version 준비.
- 완료:
  - `bash apps/runner/scripts/e2e-helm-deploy.sh` — kind Helm install/status/uninstall PASS.
  - `bash apps/runner/scripts/e2e-hosting.sh` — hosted HTTP path/subdomain/lifecycle PASS.
  - `bash apps/runner/scripts/e2e-argocd-deploy.sh` — Application Synced/Healthy 및 managed Deployment/Service 생성·삭제 PASS.
  - `docs/PROJECT_PROFILE.md`를 원본 이력 기준으로 복구하고 v0.9.0 현황 반영.
  - 5개 배포 package version을 `0.9.0`으로 준비하고 `CHANGELOG.md`/release notes 갱신.
- 검증:
  - frontend Vitest 282/282 PASS.
  - build-server node:test 205/205 PASS.
  - runner Go 8 packages PASS, TypeScript `--noEmit` clean.
  - `git diff --check` PASS.
- 남은 작업:
- workflow state/backlog/session handoff 정합 갱신 완료. 실제 tag 전까지 stable baseline은 `v0.8.16`으로 유지한다.
  - release 후보 최종 리뷰 후 사용자 승인 시 commit/tag/push 결정.
- 주의:
  - 현재 변경사항은 아직 commit/tag/push하지 않았다.
  - `ai-workflow/skills/session-end/scripts/run_session_end.py`는 `workflow_kit.common.maturity` 모듈 누락으로 자동 실행이 막혀 있어 수동 정합 검토가 필요하다.
- release 결과: v0.9.0 release commit/tag 발급 및 원격 push 완료.

## 통합 deployment e2e nightly CI

- 상태: done
- `.github/workflows/nightly-e2e.yml`의 기존 hosting job을 `deployment-e2e`로 확장했다.
- Helm install/status/uninstall, hosted HTTP path/subdomain/lifecycle, ArgoCD Application sync/health/cleanup을 한 job에서 순차 실행한다.
- Helm은 `azure/setup-helm`, kind/kubectl/Go/Node/pnpm을 공통 준비한다.
- ArgoCD local Git source는 Linux에서 kind Docker network gateway를 자동 사용하고 Docker Desktop에서는 `host.docker.internal`을 사용한다.
- YAML/shell 정적 검증 PASS. 실제 GitHub scheduled/manual run 결과 관찰이 후속이다.

## 호스팅 3티어 정책 컨셉

- 상태: done (concept)
- 서비스의 규모와 자원 할당을 기준으로 `sandbox` / `standard` / `production`을
  구분하는 정책 컨셉을 [`docs/design/hosting-tiers.md`](../../../docs/design/hosting-tiers.md)에 고정했다.
- `hostingTier`는 자원·운영 프로파일이고 `hostingScheme(path/subdomain)`은 별도
  라우팅 축으로 분리했다.
- 실제 CPU/memory/replica/quota 수치는 클러스터 capacity와 비용 기준 확인 뒤
  다음 정책 개정에서 확정한다.
- 다음 작업: 티어별 자원 matrix와 판정 입력(`serviceSize`/resource request)을
  구체화한다.

## 호스팅 3티어 정책 설계

- 상태: done (design draft)
- [`docs/design/hosting-tiers-design.md`](../../../docs/design/hosting-tiers-design.md)에
  `effectiveTier = max(serviceSize, cpuRequest, memoryRequest, replicaRequest)`
  판정, policy version/snapshot, API·DB 영향, adapter 전달, lifecycle 및 완료
  기준을 정리했다.
- 실제 수치 확정 전까지 resolver profile의 min/default/max 슬롯으로 유지한다.
- 다음 작업: 클러스터 capacity·비용 기준을 바탕으로 티어별 resource matrix를
  확정한다.

## 호스팅 3티어 초기 수치 제안

- 상태: proposed
- 소형 shared cluster 기준 v1 baseline을 설계안에 추가했다.
- 기본값: sandbox `100m/128Mi`, standard `250m/512Mi`, production `500m/1Gi`.
- 상한: sandbox `500m/512Mi`, standard `1 CPU/1Gi`, production `2 CPU/2Gi`.
- replica: sandbox `1`, standard `1~2`, production `2~4`.
- 이 수치는 운영 SLA 확정값이 아니라 capacity 관측 전 제안값이다.
- 다음 검토: 실제 cluster node capacity와 동시 서비스 수를 대입해 quota/밀도를
  검증하고 수치를 확정한다.

## 호스팅 3티어 API/UI 및 배포 연결

- 상태: done (v1 baseline)
- `BuildRequest`에 `serviceSize`, `requestedTier`, `resources`를 추가했다.
- build-server resolver가 `effectiveTier`와 resource profile을 판정하고,
  migration `0013_hosting_tier.sql`로 build/hosted service에 policy snapshot을
  저장한다(memory/Postgres 공통).
- Build Request UI에서 tier/size/CPU·memory request/limit/replica를 설정할 수
  있고, Admin Hosting UI에서 effective tier와 자원 프로파일을 조회한다.
- claim → runner → kubectl/Helm/ArgoCD까지 replica와 resource request/limit이
  전달되도록 연결했다.
- 검증: shared/db/build-server/build-monitor TypeScript clean, build-monitor
  283 tests PASS, build-server 전체 테스트 PASS, runner Go 전체 PASS,
  Helm lint/template PASS, `git diff --check` PASS.
- 남은 운영 검토: 실제 cluster capacity와 동시 서비스 밀도를 대입해 v1 수치를
  확정하고 namespace quota/HPA를 후속 정책으로 연결한다.

## 호스팅 3티어 capacity 검증

- 상태: done (local baseline)
- `kind-dib` 관측: 단일 노드 4 CPU / 약 7.7Gi allocatable, system pod requests
  약 950m CPU / 360Mi memory.
- system overhead와 25% reserve를 제외한 보수적 서비스 budget을 약 2 CPU /
  5.5Gi로 계산했다.
- 이론상 동시 활성 서비스 후보: sandbox 20개, standard 8개, production 2개.
- 이는 requests 기반 개발 클러스터 admission 후보이며 SLA 보장값이 아니다.
- 다음 작업: 혼합 tier 가중치와 namespace ResourceQuota admission을 구현하고,
  kind에서 경계 초과 요청을 거부하는 e2e를 추가한다.

## 호스팅 3티어 ResourceQuota admission

- 상태: done
- kubectl manifest와 Helm/ArgoCD chart values에 tier별 aggregate
  `ResourceQuota`를 연결했다.
- Standard quota는 `requests/limits 2 CPU/2Gi, pods 2`, Production은
  `requests/limits 8 CPU/8Gi, pods 4`로 replica 전체를 수용한다.
- 실제 Helm kind e2e에서 Standard `replicas=2`, resource request/limit,
  ResourceQuota 생성 및 `2/2 Ready`를 확인했다.
- 검증: runner Go 전체 PASS, Helm lint/template PASS, Helm kind e2e PASS.
- 보완 발견: 중단된 e2e가 kind cluster를 남길 수 있어 수동 cleanup 후 종료했다.
