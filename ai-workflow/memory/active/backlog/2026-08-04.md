<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-08-04 작업 백로그

## v0.9.0 release candidate 정리

- 상태: done
- 범위: Helm/ArgoCD adapter 실 e2e 완료, PROJECT_PROFILE 복구, v0.9.0 release notes 및 package version 준비.
- 완료:
  - `bash apps/runner/scripts/e2e-helm-deploy.sh` — kind Helm install/status/uninstall PASS.
  - `bash apps/runner/scripts/e2e-hosting.sh` — hosted HTTP path/subdomain/lifecycle PASS.
  - `bash apps/runner/scripts/e2e-argocd-deploy.sh` — Application Synced/Healthy 및 managed Deployment/Service 생성·삭제 PASS.
  - `docs/PROJECT_PROFILE.md`를 원본 이력 기준으로 복구하고 v0.9.0 현황 반영.
  - 5개 배포 package version을 `0.9.0`으로 준비하고 `CHANGELOG.md`/release notes 갱신.
- 검증:
  - frontend Vitest 282/282 PASS.
  - build-server node:test 205/205 PASS.
  - runner Go 8 packages PASS, TypeScript `--noEmit` clean.
  - `git diff --check` PASS.
- 남은 작업:
- workflow state/backlog/session handoff 정합 갱신 완료. 실제 tag 전까지 stable baseline은 `v0.8.16`으로 유지한다.
  - release 후보 최종 리뷰 후 사용자 승인 시 commit/tag/push 결정.
- 주의:
  - 현재 변경사항은 아직 commit/tag/push하지 않았다.
  - `ai-workflow/skills/session-end/scripts/run_session_end.py`는 `workflow_kit.common.maturity` 모듈 누락으로 자동 실행이 막혀 있어 수동 정합 검토가 필요하다.
- release 결과: v0.9.0 release commit/tag 발급 및 원격 push 완료.

## 통합 deployment e2e nightly CI

- 상태: done
- `.github/workflows/nightly-e2e.yml`의 기존 hosting job을 `deployment-e2e`로 확장했다.
- Helm install/status/uninstall, hosted HTTP path/subdomain/lifecycle, ArgoCD Application sync/health/cleanup을 한 job에서 순차 실행한다.
- Helm은 `azure/setup-helm`, kind/kubectl/Go/Node/pnpm을 공통 준비한다.
- ArgoCD local Git source는 Linux에서 kind Docker network gateway를 자동 사용하고 Docker Desktop에서는 `host.docker.internal`을 사용한다.
- YAML/shell 정적 검증 PASS. 실제 GitHub scheduled/manual run 결과 관찰이 후속이다.

## 호스팅 3티어 정책 컨셉

- 상태: done (concept)
- 서비스의 규모와 자원 할당을 기준으로 `sandbox` / `standard` / `production`을
  구분하는 정책 컨셉을 [`docs/design/hosting-tiers.md`](../../../docs/design/hosting-tiers.md)에 고정했다.
- `hostingTier`는 자원·운영 프로파일이고 `hostingScheme(path/subdomain)`은 별도
  라우팅 축으로 분리했다.
- 실제 CPU/memory/replica/quota 수치는 클러스터 capacity와 비용 기준 확인 뒤
  다음 정책 개정에서 확정한다.
- 다음 작업: 티어별 자원 matrix와 판정 입력(`serviceSize`/resource request)을
  구체화한다.

## 호스팅 3티어 정책 설계

- 상태: done (design draft)
- [`docs/design/hosting-tiers-design.md`](../../../docs/design/hosting-tiers-design.md)에
  `effectiveTier = max(serviceSize, cpuRequest, memoryRequest, replicaRequest)`
  판정, policy version/snapshot, API·DB 영향, adapter 전달, lifecycle 및 완료
  기준을 정리했다.
- 실제 수치 확정 전까지 resolver profile의 min/default/max 슬롯으로 유지한다.
- 다음 작업: 클러스터 capacity·비용 기준을 바탕으로 티어별 resource matrix를
  확정한다.

## 호스팅 3티어 초기 수치 제안

- 상태: proposed
- 소형 shared cluster 기준 v1 baseline을 설계안에 추가했다.
- 기본값: sandbox `100m/128Mi`, standard `250m/512Mi`, production `500m/1Gi`.
- 상한: sandbox `500m/512Mi`, standard `1 CPU/1Gi`, production `2 CPU/2Gi`.
- replica: sandbox `1`, standard `1~2`, production `2~4`.
- 이 수치는 운영 SLA 확정값이 아니라 capacity 관측 전 제안값이다.
- 다음 검토: 실제 cluster node capacity와 동시 서비스 수를 대입해 quota/밀도를
  검증하고 수치를 확정한다.

## 호스팅 3티어 API/UI 및 배포 연결

- 상태: done (v1 baseline)
- `BuildRequest`에 `serviceSize`, `requestedTier`, `resources`를 추가했다.
- build-server resolver가 `effectiveTier`와 resource profile을 판정하고,
  migration `0013_hosting_tier.sql`로 build/hosted service에 policy snapshot을
  저장한다(memory/Postgres 공통).
- Build Request UI에서 tier/size/CPU·memory request/limit/replica를 설정할 수
  있고, Admin Hosting UI에서 effective tier와 자원 프로파일을 조회한다.
- claim → runner → kubectl/Helm/ArgoCD까지 replica와 resource request/limit이
  전달되도록 연결했다.
- 검증: shared/db/build-server/build-monitor TypeScript clean, build-monitor
  283 tests PASS, build-server 전체 테스트 PASS, runner Go 전체 PASS,
  Helm lint/template PASS, `git diff --check` PASS.
- 남은 운영 검토: 실제 cluster capacity와 동시 서비스 밀도를 대입해 v1 수치를
  확정하고 namespace quota/HPA를 후속 정책으로 연결한다.

## 호스팅 3티어 capacity 검증

- 상태: done (local baseline)
- `kind-dib` 관측: 단일 노드 4 CPU / 약 7.7Gi allocatable, system pod requests
  약 950m CPU / 360Mi memory.
- system overhead와 25% reserve를 제외한 보수적 서비스 budget을 약 2 CPU /
  5.5Gi로 계산했다.
- 이론상 동시 활성 서비스 후보: sandbox 20개, standard 8개, production 2개.
- 이는 requests 기반 개발 클러스터 admission 후보이며 SLA 보장값이 아니다.
- 다음 작업: 혼합 tier 가중치와 namespace ResourceQuota admission을 구현하고,
  kind에서 경계 초과 요청을 거부하는 e2e를 추가한다.

## 호스팅 3티어 ResourceQuota admission

- 상태: done
- kubectl manifest와 Helm/ArgoCD chart values에 tier별 aggregate
  `ResourceQuota`를 연결했다.
- Standard quota는 `requests/limits 2 CPU/2Gi, pods 2`, Production은
  `requests/limits 8 CPU/8Gi, pods 4`로 replica 전체를 수용한다.
- 실제 Helm kind e2e에서 Standard `replicas=2`, resource request/limit,
  ResourceQuota 생성 및 `2/2 Ready`를 확인했다.
- 검증: runner Go 전체 PASS, Helm lint/template PASS, Helm kind e2e PASS.
- 보완 발견: 중단된 e2e가 kind cluster를 남길 수 있어 수동 cleanup 후 종료했다.

## 호스팅 3티어 capacity 계산기

- 상태: done
- `apps/build-server/src/services/hosting-capacity.ts`에 tier 기본 resource를
  replica 수만큼 aggregate해 CPU/memory 중 더 작은 동시 서비스 수를 반환하는
  계산기를 추가했다.
- 기본 local capacity `2000m CPU / 5632Mi memory`에서 sandbox 20, standard 8,
  production 2를 재현한다.
- 단위 테스트 3건과 기존 hosting policy 테스트를 PASS했다.
- 다음 작업: 이 계산기를 reservation 저장소와 `POST /builds` admission에 연결한다.

## 호스팅 3티어 capacity reservation/admission

- 상태: done (v1 admission)
- memory/Postgres 저장소에 빌드별 aggregate reservation을 연결했다.
- 기본 capacity `2000m CPU / 5632Mi memory`를 초과하는 요청은 `POST /builds`에서
  `409 HOSTING_CAPACITY_EXCEEDED`로 거부한다.
- Postgres는 `hosting_capacity_reservation` 테이블과 advisory lock으로 동시 요청을
  직렬화하고, memory도 동일한 resource request × replica 계산을 적용한다.
- FAILED 빌드와 hosted service의 stop/remove 시 예약을 해제하고, start 시 재예약한다.
- 검증: admission 단위/서비스 테스트 6건 PASS, build-server/shared-contract/db
  TypeScript clean.
- 다음 작업: capacity 사용량 조회 API와 Admin UI capacity panel을 추가한다.

## 호스팅 capacity 사용량 API/Admin UI

- 상태: done
- 관리자 전용 `GET /admin/hosting-capacity`를 추가했다.
- 응답은 aggregate capacity, active reservation 사용량/잔여량, sandbox·standard·production
  이론 수용량을 함께 제공한다.
- Admin Hosted Services 화면에 CPU/memory 사용량과 tier별 수용량 panel을 추가했다.
- OpenAPI `HostingCapacityResponse` component/path와 React API helper를 동기화했다.
- 검증: build-server 전체 217 tests PASS, frontend Vitest 284 tests PASS, 관련 TypeScript clean.
- 다음 작업: 운영 capacity 설정을 env/config로 외부화하고 cluster allocatable 관측값과 연결한다.

## 호스팅 capacity 설정 외부화 및 cluster 관측

- 상태: done
- `HOSTING_CAPACITY_CPU_MILLICORES`와 `HOSTING_CAPACITY_MEMORY_MI`를 shared-config
  runtime settings로 추가했다. 기본값은 `2000m / 5632Mi`다.
- memory/Postgres admission과 Admin capacity API가 동일한 runtime capacity를 사용한다.
- `apps/build-server/scripts/observe-hosting-capacity.sh`가 현재 Kubernetes context의
  node allocatable을 합산하고 기본 25% reserve를 제외한 export 값을 출력한다.
- 운영 설계 문서에 환경변수와 관측 절차를 추가했다.
- 검증: build-server 220 tests PASS, frontend Vitest 284 tests PASS, TypeScript 5
  package clean, shell syntax PASS.
- 다음 작업: reservation/cluster capacity drift를 감시하는 주기적 관측 또는 alerting을
  운영 workflow에 연결한다.

## 호스팅 capacity drift monitoring

- 상태: done (log alert)
- `HOSTING_CAPACITY_DRIFT_CHECK_INTERVAL_MS` 설정 시 build-server가 주기적으로
  `kubectl get nodes -o json`을 조회한다. 기본값 `0`으로 비활성이다.
- `HOSTING_CAPACITY_RESERVE_RATIO`를 적용한 관측 capacity와 configured capacity를
  비교하고, `HOSTING_CAPACITY_DRIFT_THRESHOLD`(기본 `0.2`) 이상 하락하면 structured
  warning log를 기록한다.
- quantity parser와 CPU/memory 양축 drift 판정, 명시적 check callback을 테스트했다.
- 검증: build-server 224 tests PASS, TypeScript clean, shell syntax PASS.
- 다음 작업: warning log를 Slack/webhook 등 외부 alert channel로 전달한다.

## 호스팅 capacity drift 외부 alert

- 상태: done (webhook)
- `HOSTING_CAPACITY_DRIFT_ALERT_WEBHOOK_URL` 설정 시 drift payload를 HTTP POST한다.
- payload는 `type`, `severity`, `observedAt`, configured/observed capacity와 CPU/memory
  ratio/reason을 포함한다.
- 5초 timeout, non-2xx 및 네트워크 오류는 warning log로만 처리해 admission 흐름을
  방해하지 않는다.
- 검증: webhook stub 포함 capacity monitor/config 테스트 9건 PASS, build-server
  전체 226 tests PASS, TypeScript clean.
- 다음 작업: 반복 drift webhook alert dedup/cooldown을 운영 기준으로 확정한다.

## 호스팅 capacity drift webhook cooldown

- 상태: done
- `HOSTING_CAPACITY_DRIFT_ALERT_COOLDOWN_MS`를 추가했으며 기본값은 15분이다.
- 같은 drift reason은 cooldown 동안 webhook을 억제하고, CPU→memory 등 reason 변경은
  즉시 재전송한다.
- cooldown 억제 이벤트는 info log로 남기며, webhook 오류는 기존처럼 best-effort 처리한다.
- 검증: capacity monitor/config 11건 PASS, build-server 전체 228 tests PASS,
  TypeScript clean.
- 다음 작업: recovery(정상 capacity 복귀) 이벤트를 별도 webhook으로 보낼지 결정한다.

## Dockerfile 자동 생성 정책 1차 구현

- 상태: done (v1 foundation)
- `BuildRequest.dockerfileMode`를 `required`(기본) / `auto`로 추가하고 migration
  `0015_dockerfile_mode.sql`, memory/Postgres 저장 및 claim→Runner 전달을 연결했다.
- Runner `auto` generator는 `index.html` 정적 사이트와 `package.json.scripts.start`
  Node 앱만 지원한다. 판정 불가 source는 자동 추측하지 않고 실패한다.
- 생성 Dockerfile은 고정 버전 template을 사용하고 marker, build manifest, Runner log에
  생성 사실을 남긴다.
- Build Request UI에서 dockerfile mode를 선택할 수 있다.
- 검증: Runner Go 전체 PASS, build-server/shared-contract/db TypeScript clean,
  Build Request 테스트 18건 PASS, generator 테스트 4건 PASS.
- 다음 작업: auto-generated Dockerfile의 실제 Docker CLI e2e와 실패/재현성 검증.

## Dockerfile 자동 생성 실제 CLI e2e

- 상태: done
- `apps/runner/scripts/e2e-dockerfile-auto.sh`를 추가하고 Docker daemon이 있을 때만
  opt-in 실행하도록 구성했다.
- `index.html` source → pinned nginx image build → manifest provenance 확인 → image cleanup
  전체 흐름이 실제 Docker CLI에서 PASS했다.
- 기본 Go 회귀는 Docker daemon 없이도 통과하며, CLI e2e는 별도 명시 실행한다.
- 검증: Go 전체 PASS, 실제 auto Dockerfile CLI e2e PASS, TS 5 package clean,
  Build Request 테스트 18건 PASS.
- 다음 작업: Node auto template의 실제 CLI e2e와 생성 image 재현성 검증.

## 2026-08-04 Node Dockerfile auto CLI e2e 완료

- Node source(`package.json`의 `scripts.start`)에서 pinned `node:22.14.0-alpine3.21`
  template을 생성하고 실제 Docker CLI build를 완료했다.
- 같은 source를 두 번 build해 image ID가 동일함을 확인했고, manifest provenance의
  `dockerfileGenerated=true`, `dockerfileTemplate=node` 계약을 유지했다.
- 검증: static + Node auto CLI e2e PASS, Go 전체 PASS, TS 5 package clean,
  Build Request 테스트 18건 PASS, `git diff --check` PASS.
- 주의: Docker CLI는 현재 legacy builder 경고를 출력한다. BuildKit 전환은 별도 운영
  작업으로 남긴다.
- 다음 작업: 전체 API→claim→Runner lifecycle e2e에서 mode 전달과 unsupported source
  실패 경로를 검증한다.
