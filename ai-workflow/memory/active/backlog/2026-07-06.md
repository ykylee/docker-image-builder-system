<!-- standard-ai-workflow-kit: v0.11.21-beta -->

# 2026-07-06 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (PR #27 + PR #28 + PR #29 squash merge main 합류, TASK-076/077/078/079 봉인)**
- 최종 수정일: 2026-07-06 (rev 1: 신규 일일 백로그. §1 TASK-076 (admin 권한 자동 부여 + Admin Login/Logout 제거) + §2 TASK-077 (admin 메뉴 단일 진입점 + 페이지 탭) 묶음 PR #27 open + self-review amend 봉인. 사용자 요청 "admin 기능을 정비" 의 연속 TASK. **rev 2: PR #27 squash merge main 합류 (`0eb0ad7`) — `gh pr merge --squash --delete-branch` 으로 main 에 통합 + branch 자동 삭제. self-review follow-up amend 두 차례 봉인 (1차 `85a8ab5` trailing newline 4건 + raw rgba → `--shadow-glow` + doc 코멘트 정합 / 2차 `aab8282` AdminBuilds `$effect` 일관성 + AdminTabs `.active` class 회귀 테스트 2건). 회귀 baseline: build-monitor vitest 80/80 PASS (TASK-075 baseline 71 → +11). workflow meta sync (state rev 89→90, handoff 68→69, work_backlog 62→63, backlog index 37→38 / latest 10→11) 같은 sync commit 안에 포함.** **rev 3: TASK-078 (container self-dogfood) PR #28 squash merge main 합류 (`661925a`, 2026-07-06T05:30:00Z, by ykylee) — `gh pr merge --squash --delete-branch` 으로 main 에 통합 + branch 자동 삭제. 8 file / 927 insertions. self-review amend 두 차례 봉인 — 1차 (`81263cc`): Docker + Runner Dockerfile + compose + .dockerignore + docs + 백로그 5 file. 2차 (`2c15e9a`): amend 보완 10건 봉인 — (C-1) USER app/runner@1500 + docker group supplementary, (C-2) ADMIN_IDS default 부재 + compose ${ADMIN_IDS:?...} strict, (C-3) DOCKER_SOCKET_GID silent fallback 제거, (C-4) apk add git 제거, (C-5) HEALTHCHECK start_period 5s → 15s, (I-1) prod-deps stage 분리 (309 MB → 169 MB, -45%), (I-2) .dockerignore 의 Dockerfile/compose 라인 제거, (I-3) .github/workflows/docker-build.yml 신규 (PR/main trigger, image build + size 200 MB threshold + USER non-root 검증 + /health + smoke), (I-4) Runner claimBackoff exponential (1×5s → 2×10s → 3×20s, 5분 cap) + log throttle 5s + TestWorker_ClaimBackoff_ExponentialWithCap 7 sub-test, (I-5) Runner EXPOSE 3000 제거. 회귀 baseline: TS 4 packages clean, build-monitor vitest 80/80 PASS (TASK-077 baseline 유지), build-server 123/123 PASS (TASK-077 baseline 유지), runner go test 7 packages 모두 PASS (신규 backoff test 포함), svelte-check 0 errors / 0 warnings, vite build OK (gzip js 32.77KB / css 5.15KB), e2e-single-port PASS. container image size: dibs/build-server:dev 169 MB (was 309 MB, -45%), dibs/runner:dev 41 MB (was 42.7 MB). 시뮬레이션 7 단계로 self-dogfood 시나리오 실 검증 완료. workflow meta sync (state rev 90→91, handoff 69→70, work_backlog 63→64, backlog index 38→39 / latest 11→12) 같은 sync commit 안에 포함.**)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-05.md)

## 1. PR #27 (TASK-076/077 admin 진입점 + 메뉴 통합) squash merge main 합류 + self-review amend 두 차례 봉인

- 상태: completed (PR #27 squash merge main 합류, 모든 amend 봉인 완료)
- 모드: Code
- 우선순위: high
- 담당: Codex
- 영향 파일: 19 file (신규 2 + 수정 15 + 삭제 2) / 536 insertions / 393 deletions

### 1.1 계획 (Plan)

- 작업 목표: 사용자 요청 "admin 기능을 정비" 의 연속 TASK 묶음.
  - TASK-076: 별도 `/admin/login` 단계 제거. `userId` 가 곧 admin id. userId 가 admin allow-list (`ADMIN_IDS`) 에 들어가면 자동으로 admin 권한 부여. 일반 Logout 한 번에 admin 메뉴도 사라진다.
  - TASK-077: Header 의 4개 admin nav 링크 (Admin · Builds / Users / Admins / Runners) 를 단일 진입점 `Admin` 으로 정리. 섹션 간 이동은 페이지 상단 `<AdminTabs />` 가 담당.
- 완료 기준: TS 4 packages clean / build-monitor vitest 78/78 / build-server 123/123 / svelte-check 0/0 / vite build OK / e2e-single-port PASS. 사용자 직관 — admin 사용자 헤더는 깔끔, 섹션 이동을 탭에서.

### 1.2 실행 (Act)

#### TASK-076 — admin 권한 자동 부여 + Admin Login/Logout 제거

- 진행 현황: 완료.
- 수정 사항:
  - `apps/build-monitor/src/routes/AdminLogin.svelte` (183 lines) + `AdminLogin.test.ts` (2 tests) 파일 삭제.
  - `apps/build-monitor/src/App.svelte` `/admin/login` 라우트 제거.
  - `apps/build-monitor/src/lib/session.ts` `adminIdStore`, `ADMIN_ID_KEY` 제거. writable store 하나로 `userId` / admin id 통합.
  - `apps/build-monitor/src/lib/admin-store.ts` 코멘트 갱신.
  - `apps/build-monitor/src/components/Header.svelte` `adminIdStore` 의존 제거 + 별도 AdminLogin 링크 / Admin Logout 버튼 제거.
  - `apps/build-monitor/src/routes/Admin{Builds,Users,Admins,Runners}.svelte`:
    - Build/Users: `localStorage.getItem('adminId')` 체크 → `userIdStore` 직접 구독으로 교체. redirect target `/admin/login` → `/`.
    - Admins/Runners: `effectiveAdminId = userId` 단순화, `adminIdStore` import 제거.
  - 테스트 4종: localStorage 키 `adminId` → `userId` + redirect target `/admin/login` → `/`. Header.test.ts 신규 TASK-076 회귀 가드 3건 (admin login link / admin logout button / 로그아웃 정리) 추가.

#### TASK-077 — admin 메뉴 단일 진입점 + 페이지 탭

- 진행 현황: 완료.
- 수정 사항:
  - `apps/build-monitor/src/components/AdminTabs.svelte` 신규 (79 lines): 4 탭 (Builds / Users / Admins / Runners) + svelte-spa-router `use:link` + `$location` store 구독으로 active tab 하이라이트. 라우트 URL 그대로 유지 (`/admin/builds` 등) — deep link / bookmark 정상 동작.
  - `apps/build-monitor/src/components/AdminTabs.test.ts` 신규 (4 tests): 4탭 렌더링 + href 매핑 + active `aria-current="page"` + 라우트 변경 시 active 전환.
  - `apps/build-monitor/src/components/Header.svelte`: 4 nav 링크 → 단일 `Admin` 진입점 (`/admin/builds`).
  - `apps/build-monitor/src/routes/Admin{Builds,Users,Admins,Runners}.svelte` 각 페이지 상단에 `<AdminTabs />` 추가. `<h1>` 도 페이지 특화 텍스트로 단축 (예: `Admin · Admins` → `Admins`).
  - `Admin{Builds,Users,Admins,Runners}.test.ts` 에 `$location` writable store mock 추가 (`vi.hoisted` + `require` hoist-safe 패턴).

#### 부수 — 발견된 mock 인프라 결함 동시 봉인

- `Header.test.ts` 의 `setAdminAllowList` 가 mock `__set` 을 optional chaining 으로 호출했으나 mock 의 `__set` 이 inner writable 에만 붙어 silent no-op 이었던 것을, mock 반환 객체에 `__set` 을 직접 노출해서 helper 가 항상 inner store 까지 함께 set 하도록 수정 (회귀 안전성 회복).

#### self-review follow-up amend 봉인

- `App.svelte` / `Header.svelte` / `AdminTabs.svelte` / `AdminBuilds.svelte` trailing newline 복구.
- `AdminTabs.svelte` raw `rgba(99, 102, 241, 0.4)` → `var(--shadow-glow)` 디자인 토큰 정렬.
- `AdminTabs.svelte` doc 코멘트 (underline 언급) 실제 fill 스타일과 정합.
- amend SHA: `aab8282` (base `49dac8c`). force-with-lease push 완료. PR #27 amend 두 차례 봉인 — 1차 (`85a8ab5`) trailing newline 4건 + raw rgba → `--shadow-glow` + doc 코멘트 정합 / 2차 (`aab8282`) AdminBuilds `$effect` 일관성 + AdminTabs `.active` class 회귀 테스트 2건 추가.

### 1.3 검증 (Validate)

- 테스트 결과:
  - TS 4 packages tsc clean.
  - build-monitor vitest **78/78 PASS** (TASK-075 baseline 71 → +9: AdminTabs 신규 4 + Header 신규 TASK-077 회귀 가드 1 + admin 페이지 4종 tab 노출 가드 4; AdminLogin 2건 TASK-076 에서 파일 삭제; Header 9건 동일 — 신규 TASK-076 회귀 가드 3건이 기존 admin login/logout 관련 3건을 대체).
  - svelte-check 0 errors / 0 warnings.
  - vite build OK (gzip js 32.79KB / css 5.15KB).
  - build-server tests **123/123 PASS** (TASK-075 baseline 유지).
  - e2e-single-port PASS (단일 port reverse proxy + SPA fallback 정합).
- PR #27: https://github.com/ykylee/docker-image-builder-system/pull/27 — **MERGED (`0eb0ad7`, 2026-07-06T03:38:46Z, by ykylee)**

### 1.4 결과 (Result)

- 일반 사용자: 변화 없음 (Login 페이지 → Builds 리스트).
- admin 사용자: Header admin 메뉴가 단일 `Admin` 링크로 정리. 섹션 이동은 페이지 진입 후 페이지 상단 탭 바에서 수행.
- 비-admin 사용자가 `/admin/*` 로 deep link 진입 시: backend 가 401/403 으로 거부하고 화면에 에러 노출 (redirect target 없음 — Login 페이지와 admin 진입점이 동일).
- 다음: 사용자 다른 지시 대기 (TASK-073 private registry auth / TASK-074 다른 deploy target 모두 보류). TASK-076/077 봉인으로 admin UX 흐름 정리 종료 — TASK-017/035/036/043/051~077 모두 main 합류.


## 2. PR #28 (TASK-078 container self-dogfood — Dockerfile + compose + CI workflow) squash merge main 합류 + self-review amend 두 차례 봉인

- 상태: completed (PR #28 squash merge main 합류, 모든 amend 봉인 완료)
- 모드: Code
- branch: `codex/task-078-container-self-dogfood-2026-07-06` (main `13bf191` base) → squash merge → `--delete-branch` 자동 삭제
- merge SHA: `661925a` (2026-07-06T05:30:00Z, by ykylee)

### 2.1 컨텍스트 (Context)

사용자 요청 "이 프로젝트의 dockerfile 을 만들고 self dogfood 로 사용해서 시나리오 실제 동작시켜보자" 의 응답. Build Server + Runner + (옵션) postgres 를 container 로 띄워서 기존 smoke.sh 의 시나리오를 그대로 실행. PR review 환경에서도 운영 환경과 동일한 흐름으로 validation 가능하도록 containerization.

본질은 두 축:
- (1) Build Server + Runner Dockerfile 작성 및 self-dogfood 시나리오 검증 (1차 amend `81263cc`)
- (2) self-review 10건 amend 봉인 (2차 amend `2c15e9a`)

### 2.2 변경 사항 (Changes)

#### 2.2.1 신규 파일 (5)

- `Dockerfile` (root) — Build Server multi-stage 4-stage (builder → prod-deps → runtime). pnpm workspace + shared packages (contract / config / db) + apps/build-server + apps/build-monitor. non-root USER `app` (uid 1500). ADMIN_IDS default 부재 (운영자 강제). HEALTHCHECK start_period 15s.
- `apps/runner/Dockerfile` — Go multi-stage. golang:1.22-alpine → alpine:3.20 + docker-cli + tini + ca-certs. non-root USER `runner` (uid 1500) + `addgroup runner docker` 로 docker group supplementary. EXPOSE 3000 제거 (HTTP server 없음).
- `compose.dev.yaml` — build-server + runner + (profile) postgres. `${ADMIN_IDS:?...}` + `${DOCKER_SOCKET_GID:?...}` strict 강제 (silent fallback 없음). runner `restart: on-failure` (socket 권한 미스매치 무한 restart 방지). `mem_limit: 512m` per service.
- `.dockerignore` — node_modules / dist / docs report / .generated 등 제외. `Dockerfile` / `docker-compose*.yml` 라인 제거 (관용 패턴).
- `.github/workflows/docker-build.yml` — PR / main push trigger. image build + size 200 MB threshold + USER non-root 검증 + /health + smoke scenario (POST /builds + GET /builds/:id + Admin 가드).

#### 2.2.2 amend 파일 (6)

- `Dockerfile` + `apps/runner/Dockerfile` — USER directive + apk add git 제거 + ADMIN_IDS default 부재 + start_period 15s.
- `compose.dev.yaml` — strict mode + mem_limit + restart on-failure.
- `.dockerignore` — Dockerfile/compose 라인 제거.
- `apps/runner/internal/worker/worker.go` — `claimBackoff` exponential (1×5s → 2×10s → 3×20s, 5분 cap) + log throttle 5s + `TestWorker_ClaimBackoff_ExponentialWithCap` 7 sub-test 추가.
- `docs/operations/container-self-dogfood.md` — Quick Start + strict mode 안내 + 비-root + size 169 MB + runner backoff + CI 가이드.

### 2.3 검증 (Validate)

- 테스트 결과:
  - TS 4 packages `tsc --noEmit` clean.
  - `apps/build-monitor` vitest **80/80 PASS** (TASK-077 baseline 유지).
  - `apps/build-server` node:test **123/123 PASS** (TASK-077 baseline 유지).
  - `apps/runner` go test **7 packages 모두 PASS** (incl. 신규 `TestWorker_ClaimBackoff_ExponentialWithCap`).
  - `apps/build-monitor` svelte-check 0 errors / 0 warnings.
  - `apps/build-monitor` vite build OK (gzip js 32.77KB / css 5.15KB).
  - `e2e-single-port.sh` PASS.
  - CI workflow `build + smoke`: **SUCCESS** (PR #28 mergeable 확인, image build + size 169 MB / USER non-root 검증 통과).
- container image size:
  - `dibs/build-server:dev` **169 MB** (was 309 MB, -45%, prod-deps stage 분리 효과).
  - `dibs/runner:dev` **41 MB** (was 42.7 MB).
- Self-dogfood 시나리오 (7 PHASE 실 검증):
  - PHASE 0: 환경 / 이미지 / 의존성 상태 (docker 29.1.3, host docker gid 116).
  - PHASE 1: standalone 부팅 — USER app@1500, ADMIN_IDS env 주입, HEALTHCHECK `starting` → `healthy` (t+5s), `/health` 200.
  - PHASE 2: OpenAPI 16 paths / 39 schemas / 6 tags. Admin 가드 5/5 (401/403/200/200/403). `/docs` 200. SPA fallback 200.
  - PHASE 3: 5 build 생성 (alice/bob/charlie) → 5 list → 중복 POST → 409 ACTIVE_BUILD_EXISTS → phaseHistory REQUEST_ACCEPTED → admin view 5 builds.
  - PHASE 4: ADMIN_IDS unset strict fail → with env compose up → build-server USER app@1500 + runner USER runner@1500 + groups 101(docker), 116(host gid via group_add) → docker socket 매칭.
  - PHASE 5: POST → QUEUED → Runner claim 5s 이내 → QUEUE_CLAIMED → SOURCE_PREPARED → FAILED (404 source) → admin view 노출.
  - PHASE 6: build-server stop → Runner backoff failure=1,2,3 → 5s, 10s, 20s → 재기동 → `claim recovered after 3 consecutive failures`.
  - PHASE 7: amend 보완 통합 검증 + 회귀 baseline + cleanup. `docker info` from runner container → host 데몬 RPC 정상.
- PR #28: https://github.com/ykylee/docker-image-builder-system/pull/28 — **MERGED (`661925a`, 2026-07-06T05:30:00Z, by ykylee)**

### 2.4 결과 (Result)

#### 보안 / 운영 가드 (amend 의 의도)

- non-root 실행: build-server `app@1500`, runner `runner@1500`. container 안에서 root 권한은 빌드 시점에만 사용.
- ADMIN_IDS default 부재: image 의 `ENV` 에 `ADMIN_IDS=` 박지 않음. PROJECT_PROFILE §3 (admin secret 정책) 정합.
- DOCKER_SOCKET_GID strict: silent fallback (`-999`) 제거. `:-` → `:?` 로 강제. silent permission error 방지.
- HEALTHCHECK start_period 15초: postgres backend 의 cold start (applyMigrations) 가 5초보다 길 수 있어 여유.

#### 효율성

- prod-deps stage 분리: runtime image 가 devDependencies (tsc, vite, puppeteer 등) 제외 → image size **309 MB → 169 MB (45% 감소)**.
- Runner backoff / circuit-breaker: build-server 가 down 됐을 때 매 5초마다 "connection refused" 폭주 대신 exponential backoff 적용. `attempt 1 → 5s, 2 → 10s, 3 → 20s, ..., cap 5m`. log throttle 5초.

#### CI 보호

`.github/workflows/docker-build.yml` 가 PR / main push 시 자동 검증:
- 두 image 가 build 가능한지
- build-server image size 가 200 MB 이하인지
- container 가 `id -u` 0 (root) 가 아닌지
- `/health` 통과
- smoke scenario (`POST /builds` + `GET /builds/:id` + Admin 가드)

trigger: `Dockerfile`, `apps/runner/Dockerfile`, `compose.dev.yaml`, `packages/**`, `apps/**`, `pnpm-lock.yaml`, `package.json` 변경 시만.

### 2.5 다음 (Next)

- 사용자 다른 지시 대기 (TASK-079 alpine → distroless 검토 / TASK-080 runner DinD·rootless / TASK-081 CI 기반 OpenAPI 동기화 모두 보류).
- 본 TASK 봉인으로 TASK-017/035/036/043/051~078 모든 후속 TASK main 합류.


## 3. PR #29 (TASK-079 skill 측 build request UI + API Console + Header nav 보완) squash merge main 합류 + self-review amend 두 차례 봉인

- 상태: completed (PR #29 squash merge main 합류, 모든 amend 봉인 완료)
- 모드: Code
- branch: `codex/task-079-build-request-ui-2026-07-06` (main `661925a` base) → squash merge → `--delete-branch` 자동 삭제
- merge SHA: `074a80c` (2026-07-06T07:30:00Z, by ykylee)

### 3.1 컨텍스트 (Context)

사용자 요청 "상단 네비바의 API, Docs는 아직 동작을 안하는거 같은데? 그리고 skill 측에서 담당하는 빌드 요청 기능을 웹에서 테스트할 수 있도록 테스트용 버튼과 요청 제출용 페이지를 만들어줘" 의 응답. 두 축 작업:

- (1) Header nav 보완 — API/Docs 의도 명확화 + New Build / API Console 진입점 추가
- (2) 신규 페이지 2종 — skill 측 build request 를 웹에서 직접 시험할 수 있는 `/build-request` + Swagger UI in-app iframe 임베드 `/api-console`

### 3.2 변경 사항 (Changes)

#### 3.2.1 신규 파일 (5)

- `apps/build-monitor/src/routes/BuildRequest.svelte` (713 lines) — POST /builds payload 직접 작성 페이지. 3 preset (Hello World / Minimal Python / TypeScript) + Random appName + form validation + accepted/duplicate 결과 패널 + Build Detail 이동 링크 + Raw JSON payload preview
- `apps/build-monitor/src/routes/BuildRequest.test.ts` (366 lines, 15 tests)
- `apps/build-monitor/src/routes/ApiConsole.svelte` (242 lines) — Swagger UI iframe 임베드 (`/docs/`). Refresh + Raw OpenAPI JSON + Go to Build Request 액션
- `apps/build-monitor/src/routes/ApiConsole.test.ts` (74 lines, 6 tests)
- `apps/build-monitor/src/lib/api.test.ts` (114 lines, 7 tests) — parseApiError unit tests

#### 3.2.2 amend 파일 (4)

- `BuildRequest.svelte` — StatusPill 사용 (I-1) + resetForm + field validation + Reset 버튼 + lifecycleStatus mock
- `BuildRequest.test.ts` — +7 tests (field error, reset, validation, default)
- `ApiConsole.svelte` — description 명확화 ("iframe 안의 Swagger UI")
- `lib/api.ts` — `parseApiError` helper (zod field error → field-level banner via brace-count JSON parser)

### 3.3 검증 (Validate)

- 테스트 결과:
  - TS 4 packages `tsc --noEmit` clean.
  - build-monitor vitest **114/114 PASS** (TASK-077 baseline 102 → +12: BuildRequest 5 + parseApiError 7).
  - build-server node:test 123/123 PASS (TASK-077 baseline 유지).
  - runner go test 7 packages 모두 PASS.
  - svelte-check 0 errors / 0 warnings.
  - vite build OK (gzip js 37.27 KB / css 6.31 KB).
  - e2e-single-port.sh PASS.
- PR #29: https://github.com/ykylee/docker-image-builder-system/pull/29 — **MERGED (`074a80c`, 2026-07-06T07:30:00Z, by ykylee)**

### 3.4 결과 (Result)

#### Header nav 변경

| Before | After |
|---|---|
| `API` → `/openapi.json` (raw JSON) | `OpenAPI` → `/openapi.json` (raw spec, target=_blank) |
| `Docs` → `/docs` (Swagger UI, target=_blank) | `Docs` → `/docs/` (Swagger UI, target=_blank) |
| (없음) | `API Console` → `/api-console` (in-app iframe) |
| (없음) | `New Build` → `/build-request` (userId 있을 때만) |

#### 사용자 친화 개선

- zod error 메시지 → field-level banner (`req-field-errors`): path chip + message line 별 표시
- chip status 4 → 14 (StatusPill 컴포넌트로 통일)
- "cancel" 버튼 → "Reset form" + "View Builds list →" 분리 (의도 명확화)
- client-side validation 완성 (previewTtlMinutes positive integer, dockerfilePath non-empty)
- default previewTtlMinutes 30 → 60 (backend zod `default(60)` 정합)

### 3.5 다음 (Next)

- 사용자 다른 지시 대기 (TASK-079 follow-up 외 별도 TASK 결정 대기).
- 본 TASK 봉인으로 TASK-017/035/036/043/051~079 모든 후속 TASK main 합류.
