<!-- standard-ai-workflow-kit: v0.11.25-beta -->

# 2026-07-20 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (TASK-102 PROJECT_PROFILE §3 baseline 양축 동기화 docs only 봉인 + workflow meta sync)**
- 최종 수정일: 2026-07-20 (rev 10: TASK-111 신규. §10 production-semantic Postgres backend 동등 보강. 신규 compose.dev.e2e-production-postgres.yaml + 신규 e2e-production-semantic-postgres.sh (8 단계) + 신규 운영 가이드. amend 1 file (PROJECT_PROFILE.md §3.5 postgres backend follow-up 후속 TASK 해제 + 본 TASK 신규 항목). SQL / schema / migration / version 변경 0. 회귀 영향: TS 5 clean + vitest 130/130 (frontend 0 변경) + build-server 165/165 (변경 0) + Go 7+ + vite build:react 동일 + postgres migration 0001~0006 적용 정상. 운영 영향 0 (memory variant callers) — TASK-085 의 memory variant 가 그대로 동등 운영; 신규 postgres variant 는 release staging 에서 선택적 검증. 메타 sync (state rev 140→141, handoff 110→111, work_backlog 104→105, backlog index 74→75 / latest 52→53) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 9: TASK-110 신규. §9 STRICT_CONTENT_RANGE env flag 의 운영적 strict 모드 봉인. 신규 create-app.ts 의 `parseStrictContentRangeFlag` helper + BuildService instantiation 시 `{ strictContentRange }` 위임 + build-service.ts constructor amend + build-repository.ts 6 번째 parameter + memory/postgres repository 의미 C branch 4 종 dispatch + build-source-chunked.test.ts +4 cases + 신규 운영 가이드. amend 7 file. build-routes.ts amend 0. SQL / schema / migration / version 변경 0. 회귀 영향: TS 5 clean + vitest 130/130 (frontend 0 변경) + build-server 161 → 165 (+4 신규 strict cases, 의미 B / 의미 C / `*` 0 변경) + Go 7+ + vite build:react 동일 + postgres migration 0001~0006 적용 정상. 운영 영향 0 (default — env unset / `"false"` callers TASK-109 동작 보존; strict 모드 활성은 deployment 정책 결정). 메타 sync (state rev 139→140, handoff 109→110, work_backlog 103→104, backlog index 73→74 / latest 51→52) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 8: TASK-109 신규. §8 RFC 7233 §4.2 `*` 케이스 dynamic boundary check 봉인. memory + postgres repository amend (의미 C branch 3 종 분기) + build-source-chunked.test.ts +3 cases + 신규 운영 가이드. amend 4 file. build-routes.ts amend 0. SQL / schema / migration / version 변경 0. 회귀 영향: TS 5 clean + vitest 130/130 (frontend 0 변경) + build-server 158 → 161 (+3 신규 `*` cases, 의미 B / 의미 A numeric 0 변경) + Go 7+ + vite build:react 동일 + postgres migration 0001~0006 적용 정상. 운영 영향 0 (의미 B / 의미 A callers 0 변경, `*` 케이스 boundary-check 정교화만). 메타 sync (state rev 138→139, handoff 108→109, work_backlog 102→103, backlog index 72→73 / latest 50→51) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 7: TASK-108 신규. §7 RFC 7233 Content-Range chunked wire-format 의미 C bipartite 완전 봉인. 신규 ContentRangeParts / ContentRangeParse / parseContentRange / MAX_CONTENT_RANGE_TOTAL_BYTES + StoreSourceChunkResult 신규 에러 케이스 2 종 + 신규 회귀 가드 4 case + 신규 운영 가이드. amend 7 file. SQL / schema / migration / version 변경 0 — wire-format 처리 amend 만. 회귀 영향: TS 5 clean + vitest 130/130 (frontend 0 변경) + build-server 154 → 158 (+4 신규 RFC 7233 의미 C cases, 의미 B 0 변경) + Go 7+ + vite build:react 동일 + postgres migration 0001~0006 적용 정상. 운영 영향 0 (의미 B callers = TASK-106 callers 0 변경). 메타 sync (state rev 137→138, handoff 107→108, work_backlog 101→102, backlog index 71→72 / latest 49→50) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 6: TASK-107 신규. §6 RFC 7233 Content-Range chunked wire-format follow-up 권장 봉인 (docs only — PROJECT_PROFILE.md §3 source archive 부분 끝에 한 항목 + 다음에 읽을 문서 reference). SQL / schema / migration / version 변경 0. 회귀 영향 0. 메타 sync (state rev 136→137, handoff 106→107, work_backlog 100→101, backlog index 70→71 / latest 48→49) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 5: TASK-106 신규. §5 source archive chunked split + multi-row schema 봉인 (옵션 Y). 신규 migrations/0006_build_source_chunked.sql + packages/db 신규 schema + build-server/tests +11 cases + e2e 2 종 + 운영 가이드. amend 8 file. 회귀 영향: TS 5 clean + vitest 130/130 (frontend 변경 0) + build-server 143 → 154 (+11 신규 chunked) + Go 7+ + vite build:react 동일 + postgres migration 0001~0006 적용 정상. 운영 영향 0 (legacy endpoint 보존). 메타 sync (state rev 135→136, handoff 105→106, work_backlog 99→100, backlog index 69→70 / latest 47→48) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 4: TASK-105 신규. §4 운영 배포 체크리스트 운영 가이드 봉인. SQL / schema / migration / version / git tag 변경 0 (옵션 B/C 미봉인 — 운영자가 본 운영 가이드를 따르면서 자연스럽게 발견하는 후속 TASK 권장). 회귀 영향 0 (docs only). 메타 sync (state rev 134→135, handoff 104→105, work_backlog 98→99, backlog index 68→69 / latest 46→47) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 3: TASK-104 신규. §3 build_source SLO + TOAST follow-up 운영 가이드 봉인. SQL / schema / migration 변경 0 — 본 TASK 가 SLO 확장 봉인 안 함, 후속 결정 분리 절차만 봉인. 회귀 영향 0 (docs only). 메타 sync (state rev 133→134, handoff 103→104, work_backlog 97→98, backlog index 67→68 / latest 45→46) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 2: TASK-103 신규. §2 scripts/migrate.ts standalone CLI 운영 workflow + scripts/db-migrate.sh 권고 스크립트 봉인. SQL / schema 변경 0 (전부 기존 standalone CLI 위임 wrapper). 회귀 영향 0. 메타 sync (state rev 132→133, handoff 102→103, work_backlog 96→97, backlog index 66→67 / latest 44→45) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 1: 신규 일일 백로그. §1 TASK-102 봉인 — PROJECT_PROFILE.md 의 §3 명령 박스 default backend 정합 + §3.5 / §3.6 / §3.10 / §3.11 / §3.12 회귀 baseline 양축 동기화 + §4 검증 포인트 Postgres 운영 baseline 추가 + 다음에 읽을 문서 reference 추가. 운영 가이드 신규 1 종. 회귀 영향 0 (docs only). 메타 sync (state rev 131→132, handoff 101→102, work_backlog 95→96, backlog index 65→66 / latest 43→44) 같은 sync commit 안에 포함.)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-18.md)

## 1. TASK-102 PROJECT_PROFILE §3 baseline 양축 동기화 (docs only)

- 상태: completed
- 모드: Doc (docs only)
- 우선순위: medium (결정 대기 항목 §3 명령 보정 + memory vs Postgres baseline 갱신 — 두 항목을 한 PR 안에서 동시 봉인)
- 담당: Mavis
- 영향 파일: `docs/PROJECT_PROFILE.md` (수정) + `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md` (신규)
- commit: `4ad57f2` (`docs(project-profile): TASK-102 §3 baseline 양축 동기화 (memory/Postgres)`)

### 1.1 계획 (Plan)

- 작업 목표: TASK-066 follow-up batch 3 (PR #59, main `9c9bbe3`) 가 §3.2 의 "Postgres default 개발 경로" 권장과 `run_local_postgres` 의 단일 명령 박스를 공식 운영 baseline 으로 정립했지만 본 PROJECT_PROFILE.md 본문은 두 영역에서 memory backend 단일 baseline 으로 남아 정합이 어긋남. 본 TASK 가 두 영역의 정합을 한 PR 안에서 docs only 로 봉인.
- 완료 기준:
  - §3 명령 박스의 run_local / run_local_postgres 의 default backend 정합 (memory = 보조, Postgres = default)
  - §3.5 / §3.6 / §3.10 / §3.11 / §3.12 회귀 baseline 의 memory-only 표기에 Postgres 동등 baseline 한 줄씩 추가
  - §4 검증 포인트 UI 변경 항목에 Postgres 운영 baseline 한 줄 추가
  - 다음에 읽을 문서에 신규 운영 가이드 reference 추가
  - 운영 가이드 신규: `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md` (의도 / 변경 / 사전 결함 + 보강 / 회귀 baseline / follow-up)
  - 회귀 영향 0 (docs only): TS 5 packages clean / vitest 130 / build-server 143 / Go 7+ / vite build:react 동일

### 1.2 실행 (Act)

#### 1.2.1 §3 명령 박스 보강

- §3 명령 박스의 `run_local` (memory backend) 에 "단일 runner / 단일 build / 빠른 smoke / CI / 디버깅용 보조 경로" 명시.
- §3 명령 박스의 `run_local_postgres` (Postgres backend) 의 의도 줄을 굵은 텍스트 "**Postgres backend 가 default 개발 경로**" + 운영 환경(production deployment)이 Postgres 만 사용함을 명시 + bytea round-trip 회귀 / FK CASCADE + migration / multi-runner 운영 검증이 Postgres 환경에서만 가능함을 정합.
- Postgres backend 명령의 풀 shell block (compile 4 packages + DATABASE_URL + BUILD_REPOSITORY_BACKEND=postgres + DB_AUTO_BOOTSTRAP=true + node 명령) 노출.

#### 1.2.2 §3 회귀 baseline 양축 동기화 — 5개 섹션

- **§3.5 (TASK-085 production-semantic)** — 기존 memory backend baseline 보존 + 한 줄 추가: `e2e-source-archive-postgres.sh` 5 단계 모두 PASS + postgres migration 0001~0005 모두 적용 정상 + TASK-082 `e2e-multi-runner-postgres.sh` ALL PASS + `e2e-production-semantic.sh` 는 memory backend 전용 회귀 가드 (Postgres 동등은 후속 TASK).
- **§3.6 (TASK-086 e2e-multi-runner.sh BASE + heredoc 결함 봉인)** — 기존 memory baseline 보존 + 한 줄 추가: TASK-082 `e2e-multi-runner-postgres.sh` ALL PASS — §3.5 / §3.6 memory backend e2e 와 동등 baseline.
- **§3.10 (TASK-076 insecure-registry)** — 한 줄 추가: insecure-registry 운영 모델은 memory / postgres 두 backend 와 직교하므로 별도 postgres 회귀 가드 추가 불필요. 후속 TASK 권장: insecure-registry + Postgres backend 의 e2e 가드 신규.
- **§3.11 (TASK-077 admin-initiated runner registration)** — 한 줄 추가: 401/403/400/201/409 응답은 memory `Map.has` + postgres `INSERT … ON CONFLICT DO NOTHING` 양 backend 에서 동등 회귀 가드.
- **§3.12 (TASK-093 + TASK-094 + TASK-100 + TASK-101 React 빌드 mount 운영 패턴)** — 운영 명령 박스의 step 2 를 step 2-a (memory) + step 2-b (Postgres) 로 분리. 회귀 baseline 끝 한 줄 추가: backend 선정과 무관하게 동일하게 통과 + Postgres 환경의 default 운영 패턴에서도 React 단일 SPA 운영 baseline 유지.

#### 1.2.3 §4 검증 포인트 + 다음에 읽을 문서

- §4 검증 포인트 UI 변경 항목의 끝에 한 줄 추가: "Postgres backend 운영 baseline (TASK-102 양축 동기화) — vitest 130 / build-server 143 / e2e-source-archive-postgres ALL PASS / TASK-082 multi-runner-postgres ALL PASS / `applyMigrations` 자동 부팅 정상. frontend 변경 0 이므로 React 측 운영 baseline 영향 없음."
- 다음에 읽을 문서 끝에 신규 운영 가이드 reference 추가.

#### 1.2.4 운영 가이드 신규

- `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md` — 의도 / 변경 (2 file: 1 수정 + 1 신규) / 사전 결함 + 보강 3건 / 회귀 baseline (docs only — 영향 없음) / follow-up / 관련 문서.

### 1.3 사전 결함 + 보강 (3건, docs only)

1. **§3 명령 박스의 default 부재** — `run_local` / `run_local_postgres` 가 어느 backend 가 default 인지 명시 안 함. 본 TASK 가 §3.2 의 default 권장과 정합하도록 각 명령의 의도 + 권장 사용처 명시.
2. **§3 회귀 baseline 의 memory-only 표기** — TASK-082 + TASK-066 의 Postgres 운영 검증이 봉인됐지만 PROJECT_PROFILE 본문에는 메모되지 않음. 본 TASK 가 5개 §3 블록 각각에 한 줄만 (Postgres) 추가.
3. **§3.12 의 memory-only 운영 명령** — React 단일 SPA 의 운영 명령 박스가 memory backend 만 예시로 노출. 본 TASK 가 step 2 를 2-a (memory) + 2-b (Postgres) 로 분리하여 두 backend 의 운영 명령을 모두 노출.

### 1.4 결정 (옵션 비교)

- A. ✅ **옵션 A (docs only 채택)** — §3 명령 박스 + §3 baseline 표 (4개 블록) + §4 + 다음에 읽을 문서 + 운영 가이드 신규 1 종. 회귀 영향 0 보장.
- B. 옵션 A+ (Postgres e2e 추가 검증) — §3.5 의 e2e-production-semantic.sh 의 Postgres backend 동등 보강. 작업량 중간.
- C. 옵션 B (§3 명령만 보정) — 가장 작은 scope. 본 TASK 의 항목 2 (memory vs Postgres baseline 갱신) 미봉인.

### 1.5 다음 작업

- 사용자 결정 대기:
  - 옵션 (a) scripts/migrate.ts standalone CLI 운영 workflow 정립
  - 옵션 (b) postgres migration 0004 build_source 의 TOAST 전략 (256 MiB body limit 충분 — 보류 가능)
  - 옵션 (c) 운영 안정화 (release workflow / CHANGELOG / 운영 배포 체크리스트)
  - 옵션 (d) 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선

### 1.6 회귀 baseline (본 TASK 봉인 시점, docs only)

- **변경 없음** — TS 5 packages `tsc --noEmit` clean + vitest 130 + build-server 143 + Go 7+ + vite build:react 정상 (gzip js 99.01KB / css 30.62KB) + `e2e-source-archive-postgres.sh` ALL PASS + `e2e-multi-runner-postgres.sh` ALL PASS + `e2e-production-semantic.sh` ALL PASS + postgres migration 0001~0005 모두 적용 정상.
- **변경 표면**: `docs/PROJECT_PROFILE.md` 33 insertions + 6 deletions + `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md` 90 insertions (신규).
- **메타 sync**: state rev 131→132 / handoff 101→102 / work_backlog 95→96 / backlog index 65→66 / latest 43→44.

## 2. TASK-103 scripts/migrate.ts standalone CLI 운영 workflow + scripts/db-migrate.sh 권고 스크립트

- 상태: completed
- 모드: Script + Doc (wrapper + docs only, SQL / schema 변경 0)
- 우선순위: medium (TASK-102 의 결정 대기 항목 해소 — standalone CLI 운영 workflow 정립)
- 담당: Mavis
- 영향 파일: `scripts/db-migrate.sh` (신규) + `docs/PROJECT_PROFILE.md` (수정) + `docs/operations/migration-cli-workflow-2026-07-20.md` (신규)
- commit: `7b74614` (`feat(scripts): TASK-103 migration CLI 운영 workflow + db-migrate.sh 권고 스크립트`)

### 2.1 계획 (Plan)

- 작업 목표: TASK-064 (PR #19) 가 봉인한 `apps/build-server/scripts/migrate.ts` standalone CLI 의 운영 workflow 정립 — 6 종 시나리오 gate 를 단일 entrypoint 스크립트로 노출. SQL / schema 변경 0 (전부 기존 standalone CLI 위임 wrapper).
- 완료 기준:
  - `scripts/db-migrate.sh` 신규 — 6 게이트 (`--plan` / `--status` / `--apply-all` / `--apply-up-to <ver>` / `--bootstrap` / `--dr-stop`) 단일 entrypoint.
  - `docs/PROJECT_PROFILE.md` §3 명령 박스 끝에 "Migration 운영 권장 (TASK-103)" 참조 항목 추가 + 다음에 읽을 문서에 신규 운영 가이드 reference 추가.
  - `docs/operations/migration-cli-workflow-2026-07-20.md` 신규 (의도 / 변경 / 자주 쓰는 5종 시나리오 / 기존 standalone CLI 와의 관계 / 사전 결함 + 보강 2건 / follow-up).
  - 회귀 영향 0 (wrapper + docs only): TS 5 clean / vitest 130 / build-server 143 / Go 7+ / vite build:react 동일.
  - scripts/db-migrate.sh 자체 검증: bash -n syntax OK / --help exit 0 OK / 게이트 부재 exit 2 OK / --dr-stop 안내만 출력 OK / DATABASE_URL 미설정 hint + exit 2 OK.

### 2.2 실행 (Act)

#### 2.2.1 scripts/db-migrate.sh 신규

- 6 게이트 단일 entrypoint 스크립트. 모든 gate 가 `apps/build-server/scripts/migrate.ts` 위임 wrapper.
- `--apply-all` 게이트가 dry-run 자동 1회 선행 (안전장치). `DB_MIGRATE_VERBOSE=1` 일 때 skip.
- `--dr-stop` 는 실행 안 함, 안내만 출력 (의도하지 않은 데이터 손실 / 컨테이너 중단 결함 방지).
- `set -euo pipefail` + `dispatch` 함수 + `case` 문으로 typo / 알 수 없는 게이트 모두 즉시 exit 2.
- color helpers는 `-t 1` (TTY) + `NO_COLOR` 미설정 시에만 활성 (CI / 비대화형 환경에서는 색 없음).

#### 2.2.2 docs/PROJECT_PROFILE.md §3 끝 migration 운영 권장 참조 항목

- §3 명령 박스 끝 (실행 확인 줄 다음) 에 "Migration 운영 권장 (TASK-103)" 굵은 텍스트 + 6 게이트 + 단일 entrypoint 설명 + 운영 가이드 link 추가.

#### 2.2.3 다음에 읽을 문서 reference 추가

- 신규 운영 가이드 reference 추가.

#### 2.2.4 운영 가이드 신규

- `docs/operations/migration-cli-workflow-2026-07-20.md` — 의도 / 결정 옵션 A / 변경 (3 file) / 자주 쓰는 5종 시나리오 (신규 migration 추가 staging verify / 운영 배포 전 dry-run / 운영 DB 일괄 적용 / 특정 version 까지만 단계 적용 / DR / staging 중단 + 복구) / 기존 standalone CLI 직접 호출과의 관계 / 사전 결함 + 보강 2건 / follow-up / 관련 문서.

### 2.3 사전 결함 + 보강 2건

1. **`--apply-all` 의 dry-run 자동 선행 정책** — `DB_MIGRATE_VERBOSE=1` 일 때만 dry-run skip. CI / 자동화 환경에서 의도하지 않은 double 호출 회피. 운영자는 verbose=0 (안전) / verbose=1 (성능) 환경에 따라 결정.
2. **`--dr-stop` 의 안내만 출력 정책** — 본 게이트는 실제 stop / down 명령을 실행하지 않음. dry-run 도 안 함 — 명시적으로 가이드만 표시. 운영자가 DR 절차의 의미를 모르고 스크립트를 실행해 의도하지 않은 데이터 손실 / 컨테이너 중단을 일으키는 결함 방지.

### 2.4 결정 (옵션 비교)

- A. ✅ **옵션 A (운영 가이드 + 권고 명령 스크립트, 채택)** — 운영 가이드 1 file + 권고 스크립트 1 file + PROJECT_PROFILE.md 보강 + 운영 가이드 reference. SQL / schema 변경 0. 회귀 영향 0.
- B. 옵션 B (운영 가이드만) — 신스크립트 없이 기존 standalone CLI 를 그때 그때 조합해 사용. 가장 작은 scope.
- C. 옵션 C (standalone CLI 기능 보강) — `scripts/migrate.ts` 에 `--rollback` / `--checksum-verify` / `--export` / `--import` 기능 추가. 작업량 증가, 신규 회귀 테스트 필요.

### 2.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - 옵션 (a) postgres migration 0004 build_source 의 TOAST 전략 (256 MiB body limit 충분 — 보류 가능)
  - 옵션 (b) 운영 안정화 (release workflow / CHANGELOG / 운영 배포 체크리스트)
  - 옵션 (c) 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선

### 2.6 회귀 baseline (본 TASK 봉인 시점, wrapper + docs only)

- **변경 없음** — TS 5 packages `tsc --noEmit` clean + vitest 130 + build-server 143 + Go 7+ packages 모두 PASS + vite build:react 정상 (gzip js 99.01KB / css 30.62KB) + `e2e-source-archive-postgres.sh` ALL PASS + `e2e-multi-runner-postgres.sh` ALL PASS + `e2e-production-semantic.sh` ALL PASS + postgres migration 0001~0005 적용 정상.
- **변경 표면**: `scripts/db-migrate.sh` 신규 (206 lines) + `docs/PROJECT_PROFILE.md` 2 insertions + `docs/operations/migration-cli-workflow-2026-07-20.md` 158 insertions (신규). 3 file / 366 insertions.
- **메타 sync**: state rev 132→133 / handoff 102→103 / work_backlog 96→97 / backlog index 66→67 / latest 44→45.


## 3. TASK-104 build_source SLO + TOAST follow-up 운영 가이드 (docs only)

- 상태: completed
- 모드: Doc (docs only, SQL / schema / migration 변경 0)
- 우선순위: medium (결정 대기 항목 해소 — 본 TASK 가 SLO 확장 봉인 안 함, 후속 결정 분리 절차만 봉인)
- 담당: Mavis
- 영향 파일: `docs/operations/build-source-toast-strategy-2026-07-20.md` (신규) + `docs/PROJECT_PROFILE.md` (§3.5 회귀 baseline 끝 1 line + 다음에 읽을 문서 1 line)
- commit: `d4fb554` (`docs(project-profile): TASK-104 build_source SLO + TOAST follow-up 운영 가이드 (현 주소 / 트리거 / 후속 후보 / 결정 분리 절차)`)

### 3.1 계획 (Plan)

- 작업 목표: TASK-066 follow-up batch 3 의 source archive Postgres 운영 가이드가 봉인한 `bodyLimit: 256 * 1024 * 1024` (256 MiB) SLO + PostgreSQL bytea column 의 TOAST (1 GB inline split) 동작이 현 운영 환경에서 충분함을 명시. 본 TASK 는 SLO 확장 자체를 봉인하지 않고, 후속 결정 후보 3종 (단순 SLO 확장 / chunked split / 외부 object storage) 의 트리거 5종과 결정 분리 절차만 정립.
- 완료 기준:
  - 운영 가이드 신규: `docs/operations/build-source-toast-strategy-2026-07-20.md` (현 주소 / 256 MiB 충분한 이유 / 트리거 5종 / 후속 후보 옵션 3종 / 후속 결정 분리 절차 5단계).
  - PROJECT_PROFILE.md §3.5 회귀 baseline 끝 follow-up 한 줄 + 다음에 읽을 문서 reference 추가.
  - 결정 대기 항목 해소: postgres migration 0004 build_source TOAST 전략 (TASK-104 봉인, 본 TASK 가 SLO 확장 봉인 안 함).
  - 회귀 영향 0 (docs only): TS 5 clean / vitest 130 / build-server 143 / Go 7+ / vite build:react 동일 / build_source bytea column + bodyLimit 256 MiB 동일 / e2e ALL PASS 유지.

### 3.2 실행 (Act)

#### 3.2.1 운영 가이드 신규

- `docs/operations/build-source-toast-strategy-2026-07-20.md` — 6 섹션.
  - §1 의도 + 옵션 A 결정 (현 운영 가이드 + 후속 결정 분리 절차)
  - §2 현 주소 (`bodyLimit: 256 MiB` + PostgreSQL `bytea` TOAST 1 GB + 활용 패턴)
  - §3 256 MiB 가 충분한 이유 (평균 1~50 MB / 5-50x 여유)
  - §4 트리거 5종 (신규 기능 size 요구 / 200+ MiB regular / 신규 SLO / docker context inline receive / PG 17+ TOAST 변경)
  - §5 후속 후보 옵션 3종 (단순 SLO 확장 X / chunked split Y / 외부 object storage Z)
  - §6 후속 결정 분리 절차 5단계 (trigger 감지 → TASK 등록 → 회귀 가드 → follow-up 갱신 → commit + workflow meta sync)

#### 3.2.2 PROJECT_PROFILE §3.5 회귀 baseline 끝 follow-up 한 줄

- §3.5 의 TASK-102 baseline 양축 동기화 줄 끝에 "**build_source SLO + TOAST follow-up (TASK-104 봉인)**" 굵은 텍스트 + 운영 가이드 link 추가.
- 다음에 읽을 문서에 신규 운영 가이드 reference 추가.

### 3.3 결정 (옵션 비교)

- A. ✅ **옵션 A (현 운영 가이드 + 후속 결정 분리 절차, 채택)** — docs only. SQL / schema / migration 변경 0. 본 TASK 가 SLO 확장 봉인 안 함.
- B. 옵션 B (운영 가이드 + migration stub + 코멘트) — chunked layout 코멘트 + migration 0006 stub. SQL 변경 0.
- C. 옵션 C (지금 강력히 보류) — 추가 docs only 변경 없음. follow-up 항목에서 X. 결정 대기 (b) X.

### 3.4 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - 운영 안정화 (release workflow / CHANGELOG / 운영 배포 체크리스트)
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선
  - TASK-104 trigger 5종 (신규 기능 size / 200+ MiB regular / 신규 SLO / docker context inline receive / PG 17+ TOAST 변경) 중 1+ 발동 시 별도 TASK

### 3.5 회귀 baseline (본 TASK 봉인 시점, docs only)

- **변경 없음** — TS 5 packages `tsc --noEmit` clean + vitest 130/130 PASS + build-server 143/143 PASS + Go 7+ packages 모두 PASS + vite build:react 정상 (gzip js 99.01KB / css 30.62KB) + `e2e-source-archive-postgres.sh` ALL PASS + `e2e-multi-runner-postgres.sh` ALL PASS + `e2e-production-semantic.sh` ALL PASS + postgres migration 0001~0005 적용 정상.
- **변경 표면**: `docs/operations/build-source-toast-strategy-2026-07-20.md` 신규 (120 insertions) + `docs/PROJECT_PROFILE.md` 2 insertions. 2 file / 122 insertions.
- **메타 sync**: state rev 133→134 / handoff 103→104 / work_backlog 97→98 / backlog index 67→68 / latest 45→46.

## 4. TASK-105 운영 배포 체크리스트 운영 가이드 (docs only)

- 상태: completed
- 모드: Doc (docs only, SQL / schema / migration / version / git tag 변경 0)
- 우선순위: medium (결정 대기 항목 해소 — 운영 안정화 1 종)
- 담당: Mavis
- 영향 파일: `docs/operations/release-checklist-2026-07-20.md` (신규) + `docs/PROJECT_PROFILE.md` (§3 끝 1 line + 다음에 읽을 문서 1 line)
- commit: `4de639c` (`docs(project-profile): TASK-105 운영 배포 체크리스트 운영 가이드 신규`)

### 4.1 계획 (Plan)

- 작업 목표: main HEAD `7b9d6ef` 의 운영 환경 배포 절차 단일 entrypoint 운영 가이드 신규. 운영자가 따르면서 release notes / CHANGELOG / git tag 같은 후속 TASK 의 필요성을 자연스럽게 발견할 수 있게 정함. 본 TASK 는 SQL / schema / migration / version / git tag 변경 0 — 운영 deploy workflows 의 한 자리에 정립 후 release notes 후속 TASK 가 자연스럽게 발견.
- 완료 기준:
  - 운영 가이드 신규: `docs/operations/release-checklist-2026-07-20.md` (8 섹션: Pre-deploy / Deploy / Verify / Post-deploy Monitoring / DR-Rollback / Operational Safety / 운영 환경 baseline 갱신 / follow-up).
  - 7 종 회귀 가드 (health / OpenAPI / React SPA + admin + API / e2e-source-archive-postgres / e2e-multi-runner-postgres / e2e-production-semantic / e2e-single-port / db-migrate.sh --status) + 4 종 운영 안전장치.
  - PROJECT_PROFILE.md §3 끝 운영 배포 체크리스트 참조 + 다음에 읽을 문서 reference.
  - 결정 대기 항목 해소: 운영 안정화 1 종 (TASK-105 봉인, Release notes / CHANGELOG / git tag + version bump 는 본 TASK 가 봉인 안 함).
  - 회귀 영향 0 (docs only): TS 5 clean / vitest 130 / build-server 143 / Go 7+ / vite build:react 동일.

### 4.2 실행 (Act)

#### 4.2.1 운영 가이드 신규

- `docs/operations/release-checklist-2026-07-20.md` — 8 섹션.
  - §1 Pre-deploy: git log / git status / state.json 동기성 / 4 packages TS / Go build / db-migrate.sh --plan
  - §2 Deploy: build-server 4 packages TS + Vite build + Go build + compose/k8s 배포 + applyMigrations 자동 부팅 검증
  - §3 Verify: 7 종 회귀 가드 (health / OpenAPI / React SPA + admin + API / e2e-source-archive-postgres / e2e-multi-runner-postgres / e2e-production-semantic / e2e-single-port / db-migrate.sh --status)
  - §4 Post-deploy Monitoring: lifecycle_status 분포 + runner ACTIVE status + source archive bytea round-trip + 24 시간 신규 build FAILED 비율
  - §5 DR / Rollback: 직전 commit checkout + 재부팅 + 회귀 baseline 재검증 + workflow meta sync
  - §6 Operational Safety: 4 종 안전장치 (workflow meta sync / 운영 가이드 cross-reference / destructive migration 안전 / 운영 환경 baseline drift)
  - §7 운영 환경 baseline 갱신 (후속 TASK 권장: Release notes / CHANGELOG / git tag + version bump / monitors 자동화)
  - §8 follow-up

#### 4.2.2 PROJECT_PROFILE §3 끝 운영 배포 체크리스트 참조 항목

- §3 명령 박스 끝의 TASK-103 운영 권장 + 본 TASK 운영 배포 체크리스트 (TASK-105) 두 줄 추가. 다음에 읽을 문서에 신규 운영 가이드 reference 추가.

### 4.3 결정 (옵션 비교)

- A. ✅ **옵션 A (운영 배포 체크리스트 운영 가이드, 채택)** — docs only. 운영 안정화 1 종 결정 해소. 옵션 B/C 미봉인.
- B. 옵션 B (운영 가이드 + CHANGELOG.md 신규) — 운영자가 release version 노트를 누적. 작업량 중간.
- C. 옵션 C (운영 가이드 + CHANGELOG + git tag v0.1.0 + version bump) — release version 운영 환경과 동기화. 작업량 큼.

### 4.4 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선
  - TASK-104 trigger 5종 발동 시 별도 TASK
  - TASK-105 운영 후 release notes 후속 TASK 권장 (옵션 B/C 의 자연스러운 후속 결정)

### 4.5 회귀 baseline (본 TASK 봉인 시점, docs only)

- **변경 없음** — TS 5 packages `tsc --noEmit` clean + vitest 130/130 PASS + build-server 143/143 PASS + Go 7+ packages 모두 PASS + vite build:react 정상 (gzip js 99.01KB / css 30.62KB) + 4 종 e2e ALL PASS (source-archive-postgres / multi-runner-postgres / production-semantic / single-port) + postgres migration 0001~0005 적용 정상.
- **변경 표면**: `docs/operations/release-checklist-2026-07-20.md` 신규 (236 insertions) + `docs/PROJECT_PROFILE.md` 2 insertions. 2 file / 238 insertions.
- **메타 sync**: state rev 134→135 / handoff 104→105 / work_backlog 98→99 / backlog index 68→69 / latest 46→47.

## 5. TASK-106 source archive chunked split + multi-row schema (옵션 Y)

- 상태: completed
- 모드: Code + Doc (신규 endpoint + memory/postgres repository amend + 신규 schema + 신규 회귀 가드 + 운영 가이드)
- 우선순위: high (TASK-104 trigger 5종 중 1+ 2 — multi-GB source archive / 200+ MiB regular pattern 흡수)
- 담당: Mavis
- 영향 파일 (14): 신규 6 + amend 8
  - 신규: `migrations/0006_build_source_chunked.sql` / `packages/db/src/schema/build-source-chunk.ts` / `apps/build-server/tests/build-source-chunked.test.ts` / `e2e-source-archive-chunked.sh` / `e2e-source-archive-chunked-postgres.sh` / `docs/operations/source-archive-chunked-2026-07-20.md`
  - amend: `build-repository.ts` / `memory-build-repository.ts` / `postgres-build-repository.ts` / `build-service.ts` / `build-routes.ts` / `packages/db/src/bootstrap.ts` / `packages/db/src/index.ts` / `docs/PROJECT_PROFILE.md`
- commit: `fadddcf` (`feat(build-server): TASK-106 source archive chunked split + multi-row schema (TASK-104 trigger 5종 trigger)`)

### 5.1 계획 (Plan)

- 작업 목표: TASK-104 운영 가이드 §5 의 옵션 Y (chunked split + multi-row schema) 봉인. TASK-104 의 trigger 5종 중 1 (multi-GB / docker context inline receive) + 2 (200+ MiB regular pattern) 를 chunked upload 로 흡수. SQL 신규 migration 0006 + memory/postgres repository amend + 신규 endpoint `POST /builds/:buildId/source/chunk` + 신규 회귀 가드 + 운영 가이드 신규.
- 완료 기준:
  - migrations/0006_build_source_chunked.sql 신규 (id / build_id FK CASCADE / idx / bytes / size_bytes / checksum_sha256 / created_at + (build_id, idx) unique index + build_id FK idempotent) — 운영 환경 `ensureDbSchema` + `applyMigrations` 자동 부팅
  - packages/db/src/schema/build-source-chunk.ts 신규 + index.ts export + bootstrap.ts ensureDbSchema 신규 DDL 3 종
  - build-repository.ts 에 StoreSourceChunkResult type + storeSourceChunk interface 추가
  - memory-build-repository.ts sourceArchivesChunked Map + storeSourceChunk + getSourceArchive 우선 chunked + deleteSourceArchive 두 side 동시
  - postgres-build-repository.ts storeSourceChunk (ON CONFLICT DO UPDATE last-write-wins) + getSourceArchiveFromChunks + deleteSourceArchive Promise.all
  - build-service.ts storeSourceChunk 위임
  - build-routes.ts 신규 endpoint `POST /builds/:buildId/source/chunk` (per-chunk X-Source-Checksum-Sha256 + X-Chunk-Is-Final response header + 400/404/409/415 분기)
  - build-source-chunked.test.ts 신규 11 test cases
  - e2e-source-archive-chunked.sh (memory 6 단계) + e2e-source-archive-chunked-postgres.sh (postgres 6 단계 + bytea direct verify)
  - 운영 가이드 신규 + PROJECT_PROFILE.md §3 source archive 라운드트립 갱신 + 다음에 읽을 문서 reference
  - 회귀 영향: TS 5 clean / vitest 130/130 (frontend 변경 0) / build-server legacy 143/143 (변경 0) + chunked 11/11 신규 / Go 7+ / vite build:react 동일 / postgres migration 0001~0006 적용 정상

### 5.2 실행 (Act)

#### 5.2.1 신규 schema + migration 0006

- packages/db/src/schema/build-source-chunk.ts — buildSourceChunkTable (id UUID PK / buildId UUID / idx INTEGER / bytes bytea / sizeBytes INTEGER / checksumSha256 TEXT / createdAt TIMESTAMPTZ defaultNow) + uniqueIndex (buildId, idx) + index (buildId)
- apps/build-server/migrations/0006_build_source_chunked.sql — CREATE TABLE IF NOT EXISTS + UNIQUE INDEX + INDEX + DO $$ ... ALTER TABLE ... ADD CONSTRAINT ... (idempotent pattern with build_source / build_test / deployment_attempt 정합)
- packages/db/src/bootstrap.ts — ensureDbSchema 의 bootstrapStatements 배열에 신규 DDL 3 종 추가

#### 5.2.2 build-repository.ts interface amend

- StoreSourceChunkResult type (ok with idx + isFinalChunk / not_found / checksum_mismatch / size_mismatch / idx_out_of_range)
- ContentRangeParts wire format type
- BuildRepository interface 에 storeSourceChunk 메서드 추가

#### 5.2.3 memory-build-repository.ts amend

- sourceArchivesChunked Map + StoredSourceChunked envelope type
- storeSourceChunk: 새로 uuid 검증 → declaredTotalSizeBytes 검증 → envelope 생성 (legacy wipe) → idx = envelope.chunks.size → totalChunks cap = ceil(declaredTotalSizeBytes/1024) → idx >= totalChunks 면 idx_out_of_range → chunks.set + isFinalChunk = cumulative >= totalSizeBytes
- getSourceArchive: chunked envelope 우선 reassemble → legacy fallback
- deleteSourceArchive: 두 side Map.delete + 0/1 hit 보고

#### 5.2.4 postgres-build-repository.ts amend

- import buildSourceChunkTable + MAX_CHUNK_SIZE constant (16 MiB — chunked envelope 의 chunk size 권장 상한)
- storeSourceChunk: build 존재 검증 → per-chunk SHA-256 recompute → declaredTotalSizeBytes 검증 → prior count = chunks.size → idx = priorCount.length → totalChunks cap (1KiB 기반) → ON CONFLICT (build_id, idx) DO UPDATE last-write-wins → idx=0 일 때 legacy build_source wipe → isFinalChunk = cumulative >= declaredTotalSizeBytes
- getSourceArchiveFromChunks: build row 의 source_archive_checksum_sha256 조회 + rows ordered by idx + concat
- getSourceArchive: getSourceArchiveFromChunks 우선 → legacy fallback
- deleteSourceArchive: Promise.all [legacy.delete, chunked.delete]

#### 5.2.5 build-service.ts + build-routes.ts amend

- build-service.ts: storeSourceChunk 메서드 위임
- build-routes.ts: POST /builds/:buildId/source/chunk 신규 — invalid buildId 400 / non-buffer 415 / metadata not_found 404 / storeSourceChunk result 분기 (not_found 404 / checksum_mismatch 400 / size_mismatch 400 / idx_out_of_range 409 / ok 201 + X-Chunk-Is-Final header) / Fastify header 는 string|string[] narrow 후 fallback

#### 5.2.6 신규 회귀 가드

- build-source-chunked.test.ts — 11 test cases (모두 본 TASK 작성)
- e2e-source-archive-chunked.sh — memory backend 6 단계 (build / N chunks / in-memory envelope verify (out-of-range 409) / GET reassemble SHA-256 match / DELETE chunk rows)
- e2e-source-archive-chunked-postgres.sh — postgres backend 6 단계 + psql bytea direct verify

#### 5.2.7 운영 가이드 + PROJECT_PROFILE 갱신

- 운영 가이드 신규: docs/operations/source-archive-chunked-2026-07-20.md (의도 / 결정 옵션 Y / Wire format / Schema / Repository amend / Routes amend / Build service amend / 신규 회귀 가드 / 신규 e2e / 사전 결함 + 보강 4건 / 회귀 baseline / 운영 마이그레이션 절차 / follow-up / 관련 문서)
- docs/PROJECT_PROFILE.md §3 의 source archive 라운드트립 부분 chunked 동작 추가 (POST /source/chunk + build_source_chunk + 4 종 e2e variant)
- 다음에 읽을 문서에 신규 운영 가이드 reference

### 5.3 사전 결함 + 보강 4건

1. **chunk index derivation 의 floor 기반 (MAX_CHUNK_SIZE) 이 다양한 chunk size / out-of-order upload 에서 깨짐** — 해결: monotonically increasing sequence (idx = current chunks.size) 로 단순화. Caller 의 chunk size 자유 보장.
2. **isFinalChunk 의 totalChunks 기반 검출이 per-1KiB cap 과 일치하지 않음** — 해결: isFinalChunk = cumulative >= declaredTotalSizeBytes 로 변경.
3. **legacy ↔ chunked cross-talk (양 side 데이터 동시 존재 시 operational ambiguity)** — 해결: storeSourceArchive ↔ storeSourceChunk (idx=0) 시 cross-wipe — 한 쪽 write 가 다른 쪽을 강제 정리.
4. **Fastify header 가 string|string[] union type** — 해결: typeof x === "string" && x.length > 0 narrow 후 fallback.

### 5.4 결정 (옵션 비교)

- A. ✅ **옵션 Y (chunked split + multi-row schema 완전 봉인, 채택)** — chunked upload 가 multi-GB source 까지 흡수 + 기존 단일-shot 호환 보존.
- B. 옵션 B (외부 object storage) — single-host + Postgres 인프라 정합 상태에서는 over-engineering.
- C. 옵션 C (지금 강력히 보류) — 본 TASK 가 봉인 안 함. TASK-104 trigger 발동 시 별도 TASK.

### 5.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - 옵션 Z (외부 object storage — S3 / MinIO) TASK 후속 결정 — 본 TASK 가 chunked split 으로 GB 단위 source 흡수 가능하므로 발동 가능성 낮음
  - RFC 7233 Content-Range 호환 wire format 표준화 (현재는 idx = chunks.size 만 사용)
  - TASK-066 follow-up batch 4 (release notes 후속 TASK 권장)
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선

### 5.6 회귀 baseline (본 TASK 봉인 시점)

- **변경 0 + 신규 +11** — TS 5 packages clean + vitest 130/130 (frontend 변경 0) + build-server **legacy 143/143 (변경 0) + chunked 11/11 신규 = 154/154** + Go 7+ packages PASS + vite build:react 동일 (gzip js 99.01KB / css 30.62KB) + postgres migration **0001~0006 적용 정상** + e2e-source-archive-chunked 6 단계 + e2e-source-archive-chunked-postgres 6 단계 + bytea direct verify 검증.
- **변경 표면**: 신규 6 file (migrations/0006 41 lines / schema 51 lines / test 245 lines / e2e 2 file 약 314 lines / 운영 가이드 270 lines) + amend 8 file (build-repository.ts + memory repository 약 120 lines 신규 + postgres repository 약 180 lines 신규 + build-service.ts +10 lines + build-routes.ts +65 lines + bootstrap.ts +22 lines + index.ts +1 line + PROJECT_PROFILE.md source archive 갱신) = 14 file / 1636 insertions + 22 deletions.
- **메타 sync**: state rev 135→136 / handoff 105→106 / work_backlog 99→100 / backlog index 69→70 / latest 47→48.

## 6. TASK-107 RFC 7233 Content-Range chunked wire-format follow-up 권장 (docs only)

- 상태: completed
- 모드: Doc (docs only — PROJECT_PROFILE.md §3 source archive 라운드트립 부분 끝에 한 항목 + 다음에 읽을 문서 reference)
- 우선순위: medium (TASK-066 follow-up batch 4 의 RFC 7233 결정 분리)
- 담당: Mavis
- 영향 파일: `docs/PROJECT_PROFILE.md` (§3 source archive 부분 +6 lines / 다음에 읽을 문서 +1 line). docs only.
- commit: `7c25a06` (`docs(project-profile): TASK-107 RFC 7233 Content-Range chunked wire-format follow-up 권장`)

### 6.1 계획 (Plan)

- 작업 목표: TASK-106 follow-up 의 RFC 7233 Content-Range 호환 결정 분리. TASK-106 의 chunked endpoint 가 `Content-Range: bytes <start>-<end>/<total>` 헤더를 wire-format 정의로 적시만, 실제 구현은 idx = monotonic sequence 만 사용. 본 TASK 가 의미를 3 종 (의미 A/B/C) 으로 분리 + 권장 의미 C 의 bipartite 결정 후속 TASK 권장.
- 완료 기준:
  - PROJECT_PROFILE.md §3 source archive 라운드트립 부분 끝에 한 항목 신규 (의미 A/B/C 3 종 + 권장 의미 C)
  - 다음에 읽을 문서 끝에 후속 TASK 권장 reference 신규 (TASK-066 follow-up batch 4 의 RFC 7233 후속)
  - 결정 대기 항목 분석 완료: TASK-106 follow-up 의 옵션 Z 외부 object storage + 본 TASK 의 RFC 7233 + PROJECT_PROFILE §3.5 의 e2e-production-semantic-postgres (TASK-066 follow-up batch 4 후보) + chunked multi-runner 회귀 가드 4 종
  - 회귀 영향 0 (docs only)

### 6.2 실행 (Act)

#### 6.2.1 PROJECT_PROFILE §3 source archive 라운드트립 부분 끝에 한 항목 신규

- 의미 A: caller 의 Content-Range 신뢰 (Out-of-order 강함, corrupt 위험)
- 의미 B: chunked size 만 신뢰 + monotonically increasing (현 상태 정합 = TASK-106)
- 의미 C: Content-Range 가 있으면 의미 A, 없으면 의미 B (bipartite, 권장)
- 신규 회귀 가드 (RFC 7233 wire-format 4 case — partial / total / multipart-byte-range / start-offset mismatch) + 운영 가이드

#### 6.2.2 다음에 읽을 문서 끝에 후속 TASK 권장 reference 신규

- 후속 TASK 에서 신규 운영 가이드 / 신규 회귀 가드 / route layer 의 wire-format 처리 amend 예정
- SQL / schema / migration 변경 0 — wire-format amend 만

### 6.3 결정 (옵션 비교)

- A. ✅ **본 TASK 분석으로 종료 (B 후속 TASK 권장 — syntax RFC 7233 완전 침조)** — PROJECT_PROFILE §3 한 항목 + 다음에 읽을 문서 reference 신규 (6 insertions). 후속 TASK 권장 외 추가 봉인 안 함.
- B. 운영 가이드 본체 신규 — choice A 의 권장만 docs only, 추가 가이드 봉인 안 함.
- C. RFC 7233 의 route layer amend — wire-format 처리 + 신규 회귀 가드 4 case 모두 봉인 (SQL 변경 0, route layer 만 amend). 작업량 큼.

### 6.4 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - TASK-107 의 의미 C 권장 후속 TASK — RFC 7233 의 route layer amend (옵션 C)
  - 옵션 Z 외부 object storage 후속 TASK (TASK-104 운영 가이드 §5 의 Z)
  - TASK-066 follow-up batch 4 e2e-production-semantic-postgres 후보
  - chunked multi-runner 회귀 가드 (e2e-multi-runner-postgres 의 chunked path 보강)
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선
  - release notes 후속 TASK (TASK-105 운영 후 권장)

### 6.5 회귀 baseline (본 TASK 봉인 시점, docs only)

- **변경 없음** — TS 5 packages clean + vitest 130/130 (frontend 0 변경) + build-server legacy 143/143 + chunked 11/11 신규 = 154/154 + Go 7+ packages PASS + vite build:react 정상 (gzip js 99.01KB / css 30.62KB) + postgres migration 0001~0006 적용 정상 + e2e-source-archive 4 종 ALL PASS.
- **변경 표면**: `docs/PROJECT_PROFILE.md` §3 source archive 부분 +6 lines + 다음에 읽을 문서 +1 line. 1 file / 6 insertions.
- **메타 sync**: state rev 136→137 / handoff 106→107 / work_backlog 100→101 / backlog index 70→71 / latest 48→49.

## 7. TASK-108 RFC 7233 Content-Range chunked wire-format 의미 C bipartite 완전 봉인

- 상태: completed
- 모드: Code + Doc (wire-format processing amend + 신규 회귀 가드 + 운영 가이드)
- 우선순위: high (TASK-107 follow-up 권장 의 후속)
- 담당: Mavis
- 영향 파일 (8): 신규 1 + amend 7
  - 신규: `docs/operations/content-range-rfc-7233-2026-07-20.md`
  - amend: `build-repository.ts` / `memory-build-repository.ts` / `postgres-build-repository.ts` / `build-service.ts` / `build-routes.ts` / `build-source-chunked.test.ts` / `docs/PROJECT_PROFILE.md`
- commit: `4e5161e` (`feat(build-server): TASK-108 RFC 7233 Content-Range chunked wire-format 의미 C bipartite 완전 봉인`)

### 7.1 계획 (Plan)

- 작업 목표: TASK-107 의 의미 C bipartite 결정 (RFC 7233 §4.2 의 `bytes <start>-<end>/<total>` + `*` total, 의미 A = start offset 신뢰, 의미 B = monotonic fallback). 신규 TypeScript parser + 의미 C branch + 신규 회귀 가드 4 case + 운영 가이드 신규.
- 완료 기준:
  - 신규 `ContentRangeParts` / `ContentRangeParse` / `parseContentRange` / `MAX_CONTENT_RANGE_TOTAL_BYTES` (1 GiB hard cap) in build-repository.ts
  - `StoreSourceChunkResult` 신규 에러 케이스 2 종 (content_range_invalid + content_range_mismatch)
  - `storeSourceChunk` 5 번째 parameter `contentRange?: ContentRangeParts` (의미 A path dispatch)
  - memory + postgres repository amend (의미 C branch + 의미 A path: idx = floor(start / 16 MiB), end = start + length - 1 cross-check, total = declared cross-check, 기존 의미 B monotonic fallback 정합)
  - build-routes.ts 의 `POST /source/chunk` 가 `Content-Range` 헤더 파싱 + 의미 C dispatch + 416 Range Not Satisfiable 분기 (RFC 7233 §4.4)
  - build-source-chunked.test.ts 신규 4 case (의미 A start=0 / 의미 A start=16 MiB → idx=1 / 의미 A end mismatch → content_range_invalid / 의미 C bipartite semantic B fallback)
  - 운영 가이드 신규 + PROJECT_PROFILE.md §3 source archive 라운드트립 TASK-108 의미 C bipartite 항목
  - 회귀 영향: TS 5 clean / vitest 130/130 (frontend 0 변경) / build-server legacy 143 + chunked 11 (의미 B 0 변경) + RFC 7233 신규 4 = 158/158 / Go 7+ / vite build:react 동일 / postgres migration 0001~0006 적용 정상 / SQL / schema / migration / version / git tag 변경 0 — wire-format 처리 amend 만

### 7.2 실행 (Act)

#### 7.2.1 신규 TypeScript in build-repository.ts

```ts
// RFC 7233 §4.2 의 single-range Content-Range + '*' total 케이스
export interface ContentRangeParts { start: number; end: number; total: number; }
export type ContentRangeParse =
  | { kind: "ok"; parts: ContentRangeParts; totalIsStar: boolean }
  | { kind: "invalid_format" }
  | { kind: "invalid_range"; reason: "start_after_end" | "negative_offset" | "non_numeric" };

export const MAX_CONTENT_RANGE_TOTAL_BYTES = 1_073_741_824; // 1 GiB hard cap

export function parseContentRange(header: string | null): ContentRangeParse | null {
  // ... /^bytes\s+(\d+)-(\d+)\/(\d+|\*)$/ parse + range validation
}
```

`StoreSourceChunkResult` 신규 에러 케이스: `content_range_invalid`, `content_range_mismatch`.

#### 7.2.2 memory + postgres repository amend

- `storeSourceChunk` 의 5 번째 parameter `contentRange?: ContentRangeParts`
- **의미 A path** (truthy `contentRange`): `idx = Math.floor(start / 16 MiB)`, `expectedEnd = start + size - 1` cross-check, total mismatch 검증
- **의미 B path** (falsy `contentRange`): `idx = envelope.chunks.size` monotonic (TASK-106 default — `priorCount.length` in postgres)
- 두 path 공유 `(build_id, idx)` uniqueness invariant + cross-table cleanup (legacy ↔ chunked wipe)

#### 7.2.3 build-routes.ts 의 parseContentRange dispatch

- `parseContentRange(content-range header)` 결과에 따라 dispatch — 의미 A / 의미 B / 416 Range Not Satisfiable 분기
- result 분기: `not_found` → 404 / `content_range_invalid` → 400 / `content_range_mismatch` → 400 / `checksum_mismatch` → 400 / `size_mismatch` → 400 / `idx_out_of_range` → 409 / `ok` → 201 + `X-Chunk-Is-Final: cumulative >= declaredTotalSizeBytes`

#### 7.2.4 신규 회귀 가드 4 case

1. 의미 A — start offset 0, 1 chunk (`idx=0`)
2. 의미 A — start offset > 16 MiB → idx > 0 (multi-chunk 분리)
3. 의미 A — Content-Range end 가 start + length - 1 과 불일치 → content_range_invalid
4. 의미 C bipartite — 헤더 부재 시 의미 B fallback (monotonic)

#### 7.2.5 운영 가이드 신규

`docs/operations/content-range-rfc-7233-2026-07-20.md` — 의도 / 결정 / Wire format (RFC 7233 §4.2 single-range + `*` total) / Repository amend / Routes amend / 신규 회귀 가드 4 case / 사전 결함 + 보강 3건 / 회귀 baseline / follow-up / 관련 문서.

#### 7.2.6 PROJECT_PROFILE.md 갱신

§3 source archive 라운드트립 부분에 TASK-108 의미 C bipartite 항목 추가 + 다음에 읽을 문서에 TASK-108 운영 가이드 reference 추가.

### 7.3 사전 결함 + 보강 3건

1. **의미 A 의 idx = 0 일 때 메모리 누락 위험** — MAX_CHUNK_SIZE 진눈 부채 의미 A floor + 의미 B envelope.chunks.size 시 idx 정합 + cross-table cleanup 정합.
2. **Content-Range total `*` 케이스** — parseContentRange 의 totalIsStar flag 로 declared cross-check skip (후속 TASK 에서 `*` 동적 검증 추가 가능).
3. **Fastify header `string | string[]` union type** — typeof narrow 후 fallback.

### 7.4 결정 (옵션 비교)

- A. ✅ **의미 C bipartite 완전 봉인 (채택)** — wire-format 처리 amend + 신규 회귀 가드 4 case + 운영 가이드. SQL / schema / migration 변경 0.
- B. 옵션 B (의미 A strict + 의미 B 보존 fallback 폐기) — caller breaking change 가능.
- C. 옵션 C (분석만 후속은 후속 TASK 에서 결정) — TASK-107 의 의미 C 권장만 보존.

### 7.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - TASK-108 `*` 케이스 후속 검증 (의미 C 의 declared cross-check 동적 추가)
  - chunked multi-runner 회귀 가드 (e2e-multi-runner-postgres 의 chunked path + 의미 C 보강)
  - TASK-066 follow-up batch 4 e2e-production-semantic-postgres
  - 옵션 Z 외부 object storage
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK

### 7.6 회귀 baseline (본 TASK 봉인 시점)

- **변경 0 + 신규 +4** — TS 5 packages clean + vitest 130/130 (frontend 0 변경) + build-server **legacy 143/143 + chunked 11/11 (의미 B 0 변경) + RFC 7233 신규 4/4 = 158/158** + Go 7+ packages PASS + vite build:react 동일 (gzip js 99.01KB / css 30.62KB) + postgres migration 0001~0006 적용 정상 + 5 종 e2e ALL PASS (source-archive / source-archive-postgres / source-archive-chunked / source-archive-chunked-postgres / single-port).
- **변경 표면**: 신규 1 file (운영 가이드 270 lines) + amend 7 file (build-repository.ts ~50 lines 신규 + memory-build-repository.ts 의미 C branch + postgres-build-repository.ts 의미 C branch + build-service.ts 5 번째 parameter amend + build-routes.ts parseContentRange dispatch + 416 분기 + build-source-chunked.test.ts +4 cases + PROJECT_PROFILE.md source archive 갱신) = 8 file / 475 insertions + 23 deletions.
- **메타 sync**: state rev 137→138 / handoff 107→108 / work_backlog 101→102 / backlog index 71→72 / latest 49→50.

## 8. TASK-109 RFC 7233 §4.2 `*` 케이스 dynamic boundary check 봉인

- 상태: completed
- 모드: Code + Doc (memory / postgres repository amend + 신규 회귀 가드 + 운영 가이드 + PROJECT_PROFILE.md 갱신)
- 우선순위: medium (TASK-108 follow-up 의 후속)
- 담당: Mavis
- 영향 파일 (5): 신규 1 + amend 4
  - 신규: `docs/operations/content-range-rfc-7233-star-2026-07-20.md`
  - amend: `memory-build-repository.ts` / `postgres-build-repository.ts` / `build-source-chunked.test.ts` / `docs/PROJECT_PROFILE.md`
- commit: `0663785` (`feat(build-server): TASK-109 RFC 7233 ' * ' 케이스 (unknown total) dynamic boundary check 봉인`)

### 8.1 계획 (Plan)

- 작업 목표: TASK-108 의 Content-Range wire-format 정의의 `bytes <start>-<end>/*` (RFC 7233 §4.2 unknown total) 케이스 dynamic boundary check. Content-Range.total 부재 시 numeric equality check skip, 단 `end + 1 ≤ declaredTotalSizeBytes` 검증으로 silent accept 위험 봉인.
- 완료 기준:
  - memory + postgres repository amend — 의미 C branch 의 3 종 분기 추가 (numeric total mismatch / `*` total + boundary over / 그 외)
  - build-routes.ts amend 0 (기존 분기 흡수)
  - build-source-chunked.test.ts 신규 +3 cases (`*` 안 넘는 chunk / `*` boundary over → size_mismatch / numeric total regression)
  - 신규 운영 가이드
  - PROJECT_PROFILE.md §3 source archive 라운드트립 TASK-109 항목 + 다음에 읽을 문서 reference
  - 회귀 영향: TS 5 clean / vitest 130/130 (frontend 0 변경) / build-server legacy 143 + chunked 11 + 의미 C 4 + `*` 3 = 161/161 / Go 7+ / vite build:react 동일 / postgres migration 0001~0006 적용 정상 / SQL / schema / migration / version 변경 0

### 8.2 실행 (Act)

#### 8.2.1 memory + postgres repository amend

의미 C branch 의 3 종 분기:
1. **numeric total + declared mismatch** → 400 `content_range_mismatch` (TASK-108 정합)
2. **`*` total + boundary over** (`contentRange.total === 0 && contentRange.end + 1 > declaredTotalSizeBytes`) → 400 `size_mismatch` (TASK-109 신규)
3. 그 외 → 정상 진행 (의미 A 또는 의미 B 의 wire-format 진행)

#### 8.2.2 build-routes.ts 변경 0

기존 400 분기 (`not_found` / `content_range_invalid` / `content_range_mismatch` / `checksum_mismatch` / `size_mismatch` / `idx_out_of_range`) 가 신규 결과를 흡수. size_mismatch 결과가 기존 분기로 처리.

#### 8.2.3 신규 회귀 가드 3 case

1. `*` total + declared 안 넘는 chunk → ok (TASK-109 의 정상 경로)
2. `*` total + chunk 의 end 가 declared 초과 → size_mismatch (boundary over 검증)
3. numeric total + declared cross-check (TASK-108 정합 유지 regression 가드)

#### 8.2.4 운영 가이드 신규

`docs/operations/content-range-rfc-7233-star-2026-07-20.md` — 의도 / 결정 lenient default / Wire format / Repository amend / Routes amend 변경 0 / 신규 회귀 가드 3 case / 사전 결함 + 보강 2건 / 회귀 baseline / follow-up / 관련 문서.

#### 8.2.5 PROJECT_PROFILE.md 갱신

§3 source archive 라운드트립 TASK-108 항목 끝에 TASK-109 신규 항목 추가 + 다음에 읽을 문서에 TASK-109 운영 가이드 reference.

### 8.3 사전 결함 + 보강 2건

1. **`*` 케이스 silent accept 위험** — caller 가 잘못된 offset 으로 chunk 를 보내면 chunk 위치 corrupt 가능. 해결: dynamic boundary check `end + 1 ≤ declaredTotalSizeBytes` 가 silent accept 위험 봉인.
2. **strict alternative 잠재 정밀화** — env `STRICT_CONTENT_RANGE=true` 같은 flag 로 `*` 자체를 invalid_format 처리 + numeric total 의무화. 본 TASK 의 lenient default 가 RFC 7233 incremental adoption 의 진입 장벽을 낮춤. strict mode 후속 TASK 권장.

### 8.4 결정 (옵션 비교)

- A. ✅ **Lenient default (채택)** — `*` 케이스 callers 가 기존 동작과 동일하게 동작 (단 boundary check 추가). 동적 검증이 silent accept 위험만 봉인.
- B. Strict alternative — `*` 자체를 reject 하고 caller 가 numeric total 을 의무화. 강한 cross-check 이지만 caller breaking change 가능.
- C. 보류 — 잠재 trigger 발동 시 후속 결정.

### 8.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - strict mode env flag (STRICT_CONTENT_RANGE=true) 후속 TASK
  - chunked multi-runner 회귀 가드 (e2e-multi-runner-postgres amend 의 의미 C 보강)
  - TASK-066 follow-up batch 4 e2e-production-semantic-postgres 후보
  - 옵션 Z 외부 object storage
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK

### 8.6 회귀 baseline (본 TASK 봉인 시점)

- **변경 0 + 신규 +3** — TS 5 packages clean + vitest 130/130 (frontend 0 변경) + build-server **legacy 143/143 + chunked 11/11 (의미 B 0 변경) + 의미 C 4/4 (TASK-108 0 변경) + `*` 신규 3/3 = 161/161** + Go 7+ packages PASS + vite build:react 동일 (gzip js 99.01KB / css 30.62KB) + postgres migration 0001~0006 적용 정상 + 5 종 e2e ALL PASS.
- **변경 표면**: 신규 1 file (운영 가이드 240 lines) + amend 4 file (memory-build-repository.ts 의미 C branch 3 종 분기 + postgres-build-repository.ts 일대일 정합 + build-source-chunked.test.ts +3 cases + PROJECT_PROFILE.md source archive 갱신) = 5 file / 283 insertions + 12 deletions.
- **메타 sync**: state rev 138→139 / handoff 108→109 / work_backlog 102→103 / backlog index 72→73 / latest 50→51.

## 9. TASK-110 STRICT_CONTENT_RANGE env flag 의 운영적 strict 모드 봉인

- 상태: completed
- 모드: Code + Doc (create-app.ts / build-service.ts / build-repository.ts / memory / postgres repository amend + 신규 회귀 가드 + 운영 가이드 + PROJECT_PROFILE.md 갱신)
- 우선순위: medium (TASK-109 follow-up 의 후속)
- 담당: Mavis
- 영향 파일 (8): 신규 1 + amend 7
  - 신규: `docs/operations/content-range-rfc-7233-strict-mode-2026-07-20.md`
  - amend: `create-app.ts` / `build-service.ts` / `build-repository.ts` / `memory-build-repository.ts` / `postgres-build-repository.ts` / `build-source-chunked.test.ts` / `docs/PROJECT_PROFILE.md`
- commit: `1105a8e` (`feat(build-server): TASK-110 STRICT_CONTENT_RANGE env flag 의 운영적 strict 모드 봉인`)

### 9.1 계획 (Plan)

- 작업 목표: 운영자가 deployment 시점에 env flag 한 개 (`STRICT_CONTENT_RANGE`) 로 RFC 7233 §4.2 `*` total 케이스 거절 + caller 가 numeric total 의무화. SQL / schema / migration / version 변경 0 — boot-time env flag + BuildService runtime field + repository 분기 amend 만.
- 완료 기준:
  - create-app.ts 의 `parseStrictContentRangeFlag` helper (truthy "true" / "1" / "yes" case-insensitive)
  - BuildService instantiation 시 `{ strictContentRange }` 인자 위임 (default `{ strictContentRange: false }`)
  - build-repository.ts 의 `storeSourceChunk` 6 번째 parameter `strictContentRange?: boolean`
  - memory + postgres repository amend — 의미 C branch 4 종 dispatch (strict ON + `*` → content_range_invalid / strict ON + numeric + mismatch → content_range_mismatch / 비-strict + `*` + boundary over → size_mismatch / 그 외)
  - build-service.ts constructor 의 두 번째 인자 `{ strictContentRange: boolean }` (default `{ strictContentRange: false }`) + `storeSourceChunk` 의 repository 위임 시 `this.runtime.strictContentRange`
  - build-routes.ts amend 0 (BuildService runtime field 자동 전파)
  - build-source-chunked.test.ts 신규 +4 cases (strict ON + numeric ok / strict ON + `*` → content_range_invalid / strict OFF + `*` ok / strict ON + numeric + mismatch)
  - 신규 운영 가이드
  - PROJECT_PROFILE.md §3 source archive 라운드트립 TASK-110 항목
  - 회귀 영향: TS 5 clean / vitest 130/130 (frontend 0 변경) / build-server 161 → 165 (+4 신규 strict cases, 의미 B / 의미 C / `*` 0 변경) / Go 7+ / vite build:react 동일 / postgres migration 0001~0006 적용 정상

### 9.2 실행 (Act)

#### 9.2.1 create-app.ts amend — env loader

```ts
function parseStrictContentRangeFlag(env: NodeJS.ProcessEnv): boolean {
  const raw = env.STRICT_CONTENT_RANGE;
  if (typeof raw !== "string") return false;
  const normalised = raw.trim().toLowerCase();
  return normalised === "true" || normalised === "1" || normalised === "yes";
}

const strictContentRange = parseStrictContentRangeFlag(process.env);
const buildService = new BuildService(buildRepository, {
  strictContentRange
});
```

#### 9.2.2 build-service.ts amend — BuildService 의 runtime field

- constructor 의 두 번째 인자 `{ strictContentRange: boolean }` (default `{ strictContentRange: false }`)
- `storeSourceChunk` 의 repository 위임 시 `this.runtime.strictContentRange` 추가

#### 9.2.3 build-repository.ts amend — interface signature

- `storeSourceChunk` 의 6 번째 parameter `strictContentRange?: boolean`

#### 9.2.4 memory + postgres repository amend — 4 종 dispatch

```ts
if (contentRange) {
  if (strictContentRange && contentRange.total === 0) {
    return { kind: "content_range_invalid" };
  }
  if (contentRange.total > 0 && contentRange.total !== declaredTotalSizeBytes) {
    return { kind: "content_range_mismatch", declared, supplied };
  }
  if (contentRange.total === 0 && contentRange.end + 1 > declaredTotalSizeBytes) {
    return { kind: "size_mismatch", expected, actual };
  }
}
```

#### 9.2.5 build-routes.ts 변경 0

BuildService runtime field 자동 전파 — strict 모드 flag 는 routes 의 storeSourceChunk 호출 시 자동 위임.

#### 9.2.6 신규 회귀 가드 4 case

1. strict ON + numeric + declared 일치 → ok (`idx=0`, `isFinalChunk=true`)
2. strict ON + `*` total → content_range_invalid
3. strict OFF + `*` total → ok (TASK-109 lenient default 정합)
4. strict ON + numeric + declared mismatch → content_range_mismatch

#### 9.2.7 운영 가이드 신규

`docs/operations/content-range-rfc-7233-strict-mode-2026-07-20.md` — 의도 / 결정 / 운영 rollout playbook 3 단계 / Wire format / Schema 변경 0 / Build service amend / Repository amend / Routes amend 0 / 신규 회귀 가드 4 case / 사전 결함 + 보강 2건 / 회귀 baseline / follow-up / 관련 문서.

#### 9.2.8 PROJECT_PROFILE.md 갱신

§3 source archive 라운드트립 TASK-109 항목 끝에 TASK-110 신규 항목 추가 + 다음에 읽을 문서에 TASK-110 운영 가이드 reference.

### 9.3 사전 결함 + 보강 2건

1. **운영 restart 없이 dynamic toggle 불가** — env loader 가 boot-time 1 회 실행이라 운영 중에 flag 를 toggle 하려면 build-server restart 필요. 해결 의도적 결정 — 운영 정책 결정에 restart 가 자연스러운 경계. 후속 TASK 에서 admin runtime API 같은 dynamic toggle 추가 가능.
2. **strict 모드 ON 시 기존 callers 의 breakdown 위험** — client 가 `*` total 만 보내는 코드가 운영 환경의 strict 모드 flip 후 모두 400 으로 fail. 해결: 운영 가이드 §3 (rollout playbook) 의 부팅 시작점으로 strict OFF 권장 — 운영자가 client 정렬을 검증한 뒤 flip.

### 9.4 결정 (옵션 비교)

- A. ✅ **운영 정책 toggle (채택)** — env flag 한 개 (`STRICT_CONTENT_RANGE`) 로 봉인. 운영자가 deployment 시점에 정책 결정. SQL / schema / migration 변경 0.
- B. minimum 봉인 — env loader 없이 process.env 직접 조회. helper 의 일관성 약화 + testability 약화.
- C. 보류 — 잠재 trigger 발동 시 후속 결정.

### 9.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - 운영자 client 정렬 staging checklist (strict 모드 ON 시점)
  - chunked multi-runner 회귀 가드 (e2e-multi-runner-postgres amend 의 의미 C + `*` + strict 보강)
  - TASK-066 follow-up batch 4 의 e2e-production-semantic-postgres 후보
  - 옵션 Z 외부 object storage
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK

### 9.6 회귀 baseline (본 TASK 봉인 시점)

- **변경 0 + 신규 +4** — TS 5 packages clean + vitest 130/130 (frontend 0 변경) + build-server **legacy 143/143 + chunked 11/11 (의미 B 0 변경) + 의미 C 4/4 (TASK-108 0 변경) + `*` 3/3 (TASK-109 0 변경) + strict 신규 4/4 = 165/165** + Go 7+ packages PASS + vite build:react 동일 (gzip js 99.01KB / css 30.62KB) + postgres migration 0001~0006 적용 정상 + 5 종 e2e ALL PASS.
- **변경 표면**: 신규 1 file (운영 가이드 240 lines) + amend 7 file (create-app.ts `parseStrictContentRangeFlag` helper + BuildService instantiation amend + build-service.ts constructor amend + build-repository.ts 6 번째 parameter amend + memory/build-repo.ts 4 종 dispatch + postgres-build-repo.ts memory 와 일대일 정합 + build-source-chunked.test.ts +4 cases + PROJECT_PROFILE.md source archive 갱신) = 8 file / 355 insertions + 6 deletions.
- **메타 sync**: state rev 139→140 / handoff 109→110 / work_backlog 103→104 / backlog index 73→74 / latest 51→52.

## 10. TASK-111 Production-semantic Postgres backend 동등 보강

- 상태: completed
- 모드: Script + Doc (신규 e2e 스크립트 + 신규 compose override + 운영 가이드 + PROJECT_PROFILE.md 갱신)
- 우선순위: high (TASK-066 follow-up batch 4 의 후속 결정 해소)
- 담당: Mavis
- 영향 파일 (4): 신규 3 + amend 1
  - 신규: `compose.dev.e2e-production-postgres.yaml` / `apps/build-server/scripts/e2e-production-semantic-postgres.sh` / `docs/operations/production-semantic-postgres-2026-07-20.md`
  - amend: `docs/PROJECT_PROFILE.md`
- commit: `3369d0a` (`feat(e2e): TASK-111 production-semantic postgres backend 동등 보강`)

### 10.1 계획 (Plan)

- 작업 목표: TASK-085 의 production-semantic 운영 검증 (busybox Dockerfile + 실제 tar.gz source archive 의 build COMPLETED 까지 운영 시나리오)의 postgres backend 동등 보강. 신규 compose override (`compose.dev.e2e-production-postgres.yaml`) + 신규 e2e 스크립트 (`e2e-production-semantic-postgres.sh`) + 운영 가이드. SQL / schema / migration / version 변경 0.
- 완료 기준:
  - 신규 `compose.dev.e2e-production-postgres.yaml` (postgres profile + build-server 의 postgres backend 전환)
  - 신규 `e2e-production-semantic-postgres.sh` 8 단계: postgres + busybox warm-up → compose up → build-server cold start + applyMigrations → psql direct verify → source archive 작성 → source archive upload + bytea round-trip verify → build lifecycle 대기 → 10 phase + preview URL + deploy mode 검증 → terminal-phase bytea 재검증 + container cleanup
  - 신규 운영 가이드 (정확한 운영 rollout playbook 5 단계 + 사전 결함 + 보강 3건 + 회귀 baseline + follow-up)
  - PROJECT_PROFILE.md §3.5 postgres backend follow-up 후속 TASK 해제 + 본 TASK 신규 항목
  - 회귀 영향 0 (TASK-085 memory variant 0 변경 + 신규 postgres variant 0 영향) — build-server vitest 165/165 동일 유지, TS 5 clean, Go 7+, vite build:react 동일, postgres migration 0001~0006 적용 정상

### 10.2 실행 (Act)

#### 10.2.1 신규 compose override

```yaml
# compose.dev.e2e-production-postgres.yaml
services:
  build-server:
    environment:
      BUILD_REPOSITORY_BACKEND: postgres
      DATABASE_URL: postgres://dibs:dibs@postgres:5432/dibs
      DB_AUTO_BOOTSTRAP: "true"
    depends_on:
      postgres:
        condition: service_healthy
```

`compose.dev.yaml` + `compose.dev.e2e-production.yaml` + `compose.dev.e2e-production-postgres.yaml` 3 layer compose. `--profile postgres` 로 postgres service 활성화.

#### 10.2.2 신규 e2e 스크립트

`e2e-production-semantic-postgres.sh` 8 단계 (위 참조). TASK-085 의 7 단계에 postgres-specific 3 step 추가:
- [0/8] postgres + busybox warm-up
- [3/8] psql direct verify (schema_migrations + build_request table)
- [8/8] terminal-phase bytea 재검증 (build COMPLETED 후 data integrity)

#### 10.2.3 운영 가이드 신규

`docs/operations/production-semantic-postgres-2026-07-20.md` — 의도 / 결정 / 신규 회귀 가드 8 단계 / Wire format 변경 0 / Schema 변경 0 / 운영 rollout playbook 5 단계 / 사전 결함 + 보강 3건 / 회귀 baseline / follow-up / 관련 문서.

#### 10.2.4 PROJECT_PROFILE.md 갱신

§3.5 의 "Postgres backend 회귀 (TASK-102 baseline 양축 동기화)" 줄에서 "TASK-066 follow-up batch 4 후보" 언급을 "TASK-111 신규 봉인 완료" 로 전환 + 본 TASK 신규 항목 추가 (의미 / 결정 / 신규 회귀 가드 / 운영 rollout playbook). 다음에 읽을 문서 reference 신규.

### 10.3 사전 결함 + 보강 3건

1. **3 layer compose 의 검증 부담** — 운영자가 3 개 compose 파일을 명시적으로 지정해야 함. 해결: 운영 가이드 §2 의 예시 명령으로 모든 layer + profile 을 나열. 의도적 — 각 layer 가 독립적으로 staged 환경에서 사용 가능하도록 분리 보존.
2. **postgres volume 자동 drop (`-v`)** — staging 환경 검증 시 의도된 cleanup 이지만 운영자가 실수로 운영 환경에서 실행하면 위험. 해결: 운영 가이드의 `--project-name` prefix 를 staging 인지 검증한 뒤 사용.
3. **build 가 5분 안에 COMPLETED 못할 때** — busybox pull latency + postgres cold start latency 의 합. 해결: 상한 5분 (TASK-085 의 memory variant 와 동일). 운영자가 staging 환경에서 pull latency 가 반복되면 `docker pull busybox:1.36` 사전 warm-up.

### 10.4 결정 (옵션 비교)

- A. ✅ **Postgres backend 동등 보강 (채택)** — TASK-085 의 모든 7 단계 lifecycle 을 그대로 보존하면서 backend 만 memory → postgres 로 swap. SQL / schema / migration 변경 0.
- B. 옵션 B (docs only) — 운영 가이드만. 운영자가 실제 도입 시 신규 e2e 작성.
- C. 옵션 C (통합 운영 가이드 본체) — TASK-085 운영 가이드에 postgres variant inline 추가.

### 10.5 다음 작업 (남은 결정 대기)

- 사용자 결정 대기:
  - TASK-085 production-semantic 운영 가이드 postgres variant cross-reference
  - chunked multi-runner 회귀 가드 (e2e-multi-runner-postgres amend 의 의미 C + `*` + strict 보강)
  - 옵션 Z 외부 object storage
  - 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK

### 10.6 회귀 baseline (본 TASK 봉인 시점)

- **변경 0 + 신규 e2e + 신규 운영 가이드** — TS 5 packages clean + vitest 130/130 (frontend 0 변경) + build-server vitest 165/165 (변경 0) + Go 7+ packages PASS + vite build:react 동일 + postgres migration 0001~0006 적용 정상 + 6 종 e2e ALL PASS (source-archive / source-archive-postgres / source-archive-chunked / source-archive-chunked-postgres / production-semantic / single-port).
- **변경 표면**: 신규 3 file (compose override 47 lines + e2e 스크립트 488 lines + 운영 가이드 200 lines) + amend 1 file (PROJECT_PROFILE.md §3.5 postgres backend follow-up 1 line 해제 + 본 TASK 신규 6 line 항목 + 다음에 읽을 문서 1 line reference) = 4 file / 609 insertions + 1 deletion.
- **메타 sync**: state rev 140→141 / handoff 110→111 / work_backlog 104→105 / backlog index 74→75 / latest 52→53.
