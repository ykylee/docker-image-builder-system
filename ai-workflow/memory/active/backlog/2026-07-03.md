<!-- standard-ai-workflow-kit: v0.11.21-beta -->

# 2026-07-03 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress (close after wrap-up)
- 최종 수정일: 2026-07-03 (rev 45→46: `TASK-059` 구현 및 workflow 메타 동기화. **rev 46→47: PR #13 follow-up 보완 — `reportDeploymentResult` / `reportPreviewStatus` transaction wrap, `getTestDeployment` left join, `0003` SQL FK + DEFAULT, Go claim `Reason omitempty`, OpenAPI deployment 응답 schema, docker `BuildImage` 중복 PrepareSource 제거, memory repo EXPIRED case 코멘트 보강. rev 47→48: PR #14 self-review follow-up 보완 5건 amend — StatusPill 색상 통일 / BuildDetail .kv breakpoint / section 이름 차별화 / Legacy preview 안내문 명확화 / BuildRow lifecycleStatus prop**)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

## 1. TASK-016 문서 정합성 보정 및 스캐폴드 진입 준비

- 상태: done
- 모드: Review | Planning | Documentation
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/GLOSSARY_AND_STATE_MODEL.md, docs/IDENTITY_MODEL.md, docs/sdlc/SRS/04-policy-and-constraints.md, docs/review/01-sdlc-review.md, docs/report/02-sdlc-review-report.html, ai-workflow/memory/active/session_handoff.md

### 1.1 계획 (Plan)
- 작업 목표: 리뷰 findings를 반영해 canonical 문서 정합성을 먼저 보정하고 스캐폴드 진입점을 확정한다.
- 완료 기준: 문서 충돌이 줄어들고 다음 코드 진입점이 확정된다.

### 1.2 실행 (Act)
- 진행 현황: `2026-07-03` review/report와 기준 문서를 함께 대조해 상태 모델, 제약 조건, workflow 메타 최신성 보정 진행
- 수정 사항:
  - `docs/GLOSSARY_AND_STATE_MODEL.md`에서 `TEST_READY` handoff, preview `QUEUED`, canonical source 우선순위 반영
  - `docs/IDENTITY_MODEL.md` active build 기준에서 `TEST_READY` 제거
  - `docs/sdlc/SRS/04-policy-and-constraints.md`의 stale 저장소 제약 문구 정정
  - `ai-workflow/memory/active/repository_assessment.md`, `docs/PROJECT_PROFILE.md`를 `docs/sdlc/` 중심 구조로 정리
  - `docs/review/01-sdlc-review.md`, `docs/report/01-assignment-plan.md`, `docs/report/02-sdlc-review-report.html`에 canonical source와 반영 결과 명시
  - `docs/report/02-sdlc-review-report.html`을 검토 결과 보고서 톤에서 구현 착수 기획안 톤으로 재작성

### 1.3 검증 (Validate)
- 테스트 결과: root 개념 문서, SDLC contract/SRS, workflow 메타, review/report 문서 간 상태 모델과 현재 focus 정합성 확인
- 미실행 사유: 애플리케이션 코드와 실행 환경이 아직 없어 런타임 검증 대상이 없음

### 1.4 결과 (Result)
- 작업 결과: 문서 canonical source가 `docs/sdlc/`와 shared contract 기준으로 정리되었고, 보고 자료는 구현 착수 기획안 관점으로 재구성되었다.
- 다음 세션 시작 포인트: TASK-017 shared package 또는 API 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - 애플리케이션 코드와 실행 명령은 아직 미구현
  - Runner와 Docker 실행 환경 요구사항은 여전히 문서 단계
  - `OI-006`, `OI-008`, `OI-009` 후속 decision은 구현 차단 시점에 선별 대응 필요

## 2. TASK-017 shared package 또는 API 스캐폴드 착수

- 상태: done
- 모드: Planning | Scaffolding | Documentation
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/sdlc/09-repository-package-structure-baseline.md, docs/sdlc/contracts/01-shared-build-contract-baseline.md, docs/sdlc/14-backend-development-target-selection.md, docs/PROJECT_PROFILE.md

### 2.1 계획 (Plan)
- 작업 목표: `packages/shared-contract`, `packages/shared-config`, `packages/db` 또는 `apps/build-server` 중 첫 코드 진입점을 확정하고 스캐폴드를 시작한다.
- 완료 기준: 저장소 기본 package/app 디렉터리와 최소 엔트리포인트 구조가 생성된다.

### 2.2 실행 (Act)
- 진행 현황: `2026-07-03` root workspace, shared packages, `apps/build-server`, `apps/runner` 최소 골격을 한 번에 생성하고 검증
- 수정 사항:
  - root에 `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json` 추가
  - `packages/shared-contract`에 request/status/phase/error/response schema 초안 추가
  - `packages/shared-config`에 env/runtime/constants baseline 추가
  - `packages/db`에 Drizzle schema, DB client, drizzle config 추가
  - `apps/build-server`에 Fastify bootstrap, `/health`, `POST /builds`, `GET /builds/:buildId`, `GET /builds/:buildId/logs` 골격 추가
  - `apps/build-server`는 초기 단계에서는 in-memory repository 로 동작시켜 API contract smoke test가 가능하도록 구성
  - `apps/runner`에 `go.mod`, signal-aware main, poll loop, queue/docker/repository/service placeholder 추가
  - `.gitignore`에 `node_modules/`, `dist/`, `coverage/`, `*.tsbuildinfo` 추가
  - `docs/PROJECT_PROFILE.md` 실행/검증 명령을 실제 스캐폴드 기준으로 갱신
  - `packages/shared-config`에 `BUILD_REPOSITORY_BACKEND`, `DB_AUTO_BOOTSTRAP` runtime 설정 추가
  - `packages/db`의 `build_request` schema를 source archive size, preview TTL, metadata, last error 컬럼까지 확장
  - `packages/db`에 `ensureDbSchema` bootstrap helper와 query helper export 추가
  - `apps/build-server`에 `PostgresBuildRepository` 추가, `memory|postgres` backend 선택 및 app close 시 pool 종료 처리 추가
  - `apps/build-server`의 `postgres` 경로는 schema auto-bootstrap 후 `build_request` / `build_log` 저장 구조를 사용할 준비 상태로 연결
  - 아키텍처 결정을 `DB = Host Server 전용 PostgreSQL`, `Runner = Host Server API only` 로 고정하고 `apps/runner` 골격에서 DB repository 자리를 `internal/hostclient` 로 치환

### 2.3 검증 (Validate)
- 테스트 결과:
  - `./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `./node_modules/.bin/tsc -p packages/shared-config/tsconfig.json --noEmit`
  - `./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `./node_modules/.bin/tsc -p apps/build-server/tsconfig.json --noEmit`
  - `./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json`
  - `./node_modules/.bin/tsc -p packages/shared-config/tsconfig.json`
  - `./node_modules/.bin/tsc -p packages/db/tsconfig.json`
  - `./node_modules/.bin/tsc -p apps/build-server/tsconfig.json`
  - `(cd apps/runner && go build ./...)`
  - `BUILD_REPOSITORY_BACKEND=memory node apps/build-server/dist/apps/build-server/src/index.js` 기동 후 `GET /health`, `POST /builds`, `GET /builds/:buildId`, `GET /builds/:buildId/logs` smoke 확인
  - Colima 기동 후 Docker로 `docker-image-builder-postgres` 컨테이너(`127.0.0.1:15432`) 실행
  - `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:15432/docker_image_builder BUILD_REPOSITORY_BACKEND=postgres DB_AUTO_BOOTSTRAP=true node apps/build-server/dist/apps/build-server/src/index.js` 기준 live Postgres smoke 확인
  - Postgres 경로에서 `build_request`, `build_log`, `test_deployment` 테이블 auto-bootstrap, `POST /builds` insert, `GET /builds/{id}`, `GET /builds/{id}/logs`, duplicate `409` 응답, DB row count 확인
- 미실행 사유:
  - `pnpm -r check` 는 환경의 `ERR_PNPM_IGNORED_BUILDS` 정책 때문에 direct `tsc` 검증으로 우회
  - Drizzle migration 운영 기준과 SQL artifact 생성은 후속 `PKG-003` 단계에서 진행

### 2.4 결과 (Result)
- 작업 결과: 문서 전용 저장소였던 루트가 실제 monorepo 코드 골격으로 전환되었고, Build Server/Runner/shared package 의 첫 실행 단위가 생성되었다.
- 마감 판단: `TASK-017` 범위의 스캐폴드, postgres live smoke, Runner-Host Server 경계 결정까지 반영되어 본 TASK는 종료한다.
- 다음 세션 시작 포인트: postgres 경로를 기본 개발 경로로 둘지 결정하고, 그 위에서 `PKG-002`/`PKG-003`/`PKG-004`를 persistence 기준으로 마감
- 남은 리스크 및 후속 작업:
  - shared-contract 의 Go 측 generated binding 전략은 아직 미정
  - `tsconfig` 산출물 경로가 `dist/apps/build-server/src/index.js` 형태라 추후 빌드 산출 구조 정리가 필요할 수 있음
  - `pnpm` dev watch 경로는 `esbuild` 계열 승인 정책 정리 전까지 제한적

## 3. TASK-018 보고자료 재구성 및 기획안 재작성

- 상태: done
- 모드: Planning | Documentation
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/report/01-assignment-plan.md, ai-workflow/memory/active/session_handoff.md, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/state.json

### 3.1 계획 (Plan)
- 작업 목표: 기존 Build Server 중심 착수 메모를 프로젝트 전체 기획안 구조로 재작성한다.
- 완료 기준: 프로젝트 개요, 구성, 예상 산출물, 추진 전략이 한 문서에서 읽히도록 정리된다.

### 3.2 실행 (Act)
- 진행 현황: `2026-07-03` 기존 `docs/report/01-assignment-plan.md`를 검토하고 프로젝트 개요 중심 발표 흐름으로 전면 재구성
- 수정 사항:
  - 제목을 Build Server 구현 착수 계획안에서 프로젝트 기획안으로 변경
  - 프로젝트 개요, 추진 배경, 목표, 프로젝트 구성, 추진 범위, 추진 전략, 예상 산출물, 기대 효과, 리스크 구조로 재편
  - 구현 착수 논의를 전체 프로젝트 설명 뒤에 배치해 보고자료용 흐름 강화
  - workflow 메타 문서에 이번 기획안 재작성 사실 반영

### 3.3 검증 (Validate)
- 테스트 결과: `docs/sdlc/01~03`, `README.md`, `docs/PROJECT_PROFILE.md`와 비교해 프로젝트 목적, MVP 범위, 다음 착수점 정합성 확인
- 미실행 사유: 런타임 또는 코드 검증 대상 없음

### 3.4 결과 (Result)
- 작업 결과: `docs/report/01-assignment-plan.md`가 발표/보고용 프로젝트 기획안 형태로 재작성되었다.
- 다음 세션 시작 포인트: TASK-017 shared package 또는 API 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - HTML 보고자료도 같은 서사 구조로 추가 동기화가 필요할 수 있음
  - 구현 단계 진입 시 계획안의 예상 산출물을 실제 산출물 기준으로 갱신해야 함

## 4. TASK-019 리더 소개용 HTML 보고자료 시각화 재작성

- 상태: done
- 모드: Planning | Documentation | Visualization
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/report/02-sdlc-review-report.html, ai-workflow/memory/active/session_handoff.md, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/state.json

### 4.1 계획 (Plan)
- 작업 목표: 리더 대상 소개 자료로 쓸 수 있도록 HTML 보고자료를 개요, 구성, 흐름, 산출물이 도식적으로 보이는 구조로 재작성한다.
- 완료 기준: 기술 세부 구현 메모가 아니라 프로젝트 소개 슬라이드로 읽히는 HTML 자료가 생성된다.

### 4.2 실행 (Act)
- 진행 현황: `2026-07-03` 기존 구현 착수형 HTML을 제거하고 리더 브리프형 slide deck으로 전면 재작성
- 수정 사항:
  - hero, 프로젝트 개요, 사용자 흐름, 시스템 구성, 필요성, 범위, 추진 단계, 예상 산출물, 리스크, 의사결정 포인트 섹션으로 재편
  - CSS 기반 flow diagram과 architecture diagram을 추가해 개요와 구성을 시각적으로 설명
  - 리더 보고용 문장으로 톤을 단순화하고 핵심 승인 포인트를 마지막 슬라이드에 집약

### 4.3 검증 (Validate)
- 테스트 결과: HTML 문서 내 핵심 섹션 존재 여부와 전체 구조를 재확인
- 미실행 사유: 브라우저 렌더링 검증 도구는 이번 턴에서 사용하지 못했고 정적 구조 점검만 수행

### 4.4 결과 (Result)
- 작업 결과: `docs/report/02-sdlc-review-report.html`이 리더 대상 프로젝트 소개 자료 형태로 재작성되었다.
- 다음 세션 시작 포인트: 필요 시 copy polishing 또는 실제 브라우저 렌더 검토 후 shared package 또는 API 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - 실제 발표 환경 해상도에서 폰트/간격 최종 점검이 한 번 더 필요할 수 있음
  - 발표 톤에 맞춰 문구를 더 공격적으로 줄이는 후속 polishing 여지가 있음

## 5. TASK-020 HTML 시각화 보강 및 오프라인 에셋 내장화

- 상태: done
- 모드: Documentation | Visualization
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/report/02-sdlc-review-report.html, ai-workflow/memory/active/session_handoff.md, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/state.json

### 5.1 계획 (Plan)
- 작업 목표: HTML 소개 자료의 시각 밀도를 높이고, 외부 네트워크 없이도 동일하게 동작하도록 필요한 도식과 아이콘을 문서 내부에 포함한다.
- 완료 기준: hero와 흐름 설명의 시각 설득력이 높아지고, 에셋 의존성이 로컬 HTML 내부로 정리된다.

### 5.2 실행 (Act)
- 진행 현황: `2026-07-03` CSS 강화와 인라인 SVG 자산 추가를 통해 오프라인 완결형 자료로 보강
- 수정 사항:
  - shadow, section divider, flow icon, hero ribbon 등 CSS 기반 시각 레이어 추가
  - hero에 인라인 SVG 기반 개념도 추가
  - 흐름도 카드에 SVG symbol sprite 기반 아이콘 추가
  - 외부 asset 없이 오프라인에서도 동일하게 보이도록 아이콘과 도식을 HTML 내부에 포함

### 5.3 검증 (Validate)
- 테스트 결과: 신규 클래스와 SVG symbol/hero visual/flow icon 존재 여부 정적 확인
- 미실행 사유: 실제 브라우저 렌더 및 화면 캡처 검증은 이번 턴에서 수행하지 않음

### 5.4 결과 (Result)
- 작업 결과: `docs/report/02-sdlc-review-report.html`이 CSS와 인라인 SVG를 활용한 오프라인 완결형 소개 자료로 고도화되었다.
- 다음 세션 시작 포인트: 브라우저 렌더 검토 또는 발표용 copy polishing 후 구현 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - 실제 프로젝터/발표 화면에서 대비와 줄바꿈 확인이 추가로 필요할 수 있음
  - 구조는 충분히 잡혔으므로 이후에는 문장 압축 중심 polishing이 더 효과적일 수 있음

## 6. TASK-021 발표용 카피 압축 및 승인안 톤 보정

- 상태: done
- 모드: Documentation | Visualization
- 우선순위: medium
- 담당: Codex
- 영향 문서: docs/report/02-sdlc-review-report.html, ai-workflow/memory/active/session_handoff.md, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/state.json

### 6.1 계획 (Plan)
- 작업 목표: 리더 발표용 문장을 더 짧고 직접적으로 다듬어 “소개 자료”보다 “소개 및 승인안”에 가까운 톤으로 보정한다.
- 완료 기준: 장황한 설명이 줄고 승인 포인트가 더 빠르게 읽힌다.

### 6.2 실행 (Act)
- 진행 현황: `2026-07-03` hero, overview, flow, scope, roadmap, conclusion 카피를 중심으로 문장 압축 수행
- 수정 사항:
  - 제목을 소개 자료에서 소개 및 승인안 톤으로 보정
  - hero one-liner와 핵심 메시지를 더 짧게 정리
  - flow, why, scope, risk, conclusion 문장을 발표형 문장으로 압축
  - 마지막 제안 문구를 즉시 진행형 승인안 문장으로 조정

### 6.3 검증 (Validate)
- 테스트 결과: 주요 섹션 제목과 요약 문장이 리더용 승인 흐름에 맞게 유지되는지 정적 확인
- 미실행 사유: 브라우저 렌더 검토는 수행하지 않음

### 6.4 결과 (Result)
- 작업 결과: `docs/report/02-sdlc-review-report.html`의 카피가 더 짧고 직접적인 승인안 톤으로 정리되었다.
- 다음 세션 시작 포인트: 브라우저 렌더 검토 또는 구현 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - 실 발표 시간에 맞춘 더 강한 축약은 추가 가능
  - 발표자 멘트와 맞춰 특정 문장을 더 공격적으로 바꿀 여지는 있음

## 7. TASK-023 워크플로우 skill/MCP 셋업 (워크플로우 온보딩 마무리)

- 상태: done
- 모드: Documentation | Configuration
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/PROJECT_PROFILE.md, ai-workflow/memory/active/.codex/config.toml.example, ai-workflow/memory/active/.agents/README.md, ai-workflow/memory/active/session_handoff.md, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/state.json

### 7.1 계획 (Plan)
- 작업 목표: 표준 워크플로우 키트(`ai-workflow/`) prototype skill/MCP를 우리 프로젝트 운영에 어떻게 묶을지(active/deferred, transport 상태, 진입점) 명문화하고, Codex 측 진입 메모까지 둔다.
- 완료 기준: `docs/PROJECT_PROFILE.md` §3.1에 active/deferred 표가 추가되고, `.codex/config.toml.example`(운영 폴더 미러)과 `.agents/README.md`(운영 폴더 미러)가 생성되며, `state.json`/`work_backlog`/`session_handoff`가 current_focus 변경에 맞춰 동기화된다.
- 직전 배경: TASK-022에서 placeholder·status assessment·report index 등 메타 정합성을 좁혔고, 본 TASK는 그 다음 prerequisite으로 TASK-017 스캐폴드 진입 전 워크플로우 셋업을 닫는다.

### 7.2 실행 (Act)
- 진행 현황: `2026-07-03` 표준 키트 `ai-workflow/skills/` 6종, `ai-workflow/mcp_servers/` 6종을 재점검하고 본 프로젝트에 active로 묶을 3+3종을 선정
- 수정 사항:
  - `docs/PROJECT_PROFILE.md`에 §3.1 "활성 워크플로우 자산" 신설, active skills(`session-start`/`backlog-update`/`doc-sync`) + active MCPs(`latest-backlog`/`check-doc-links`/`check-doc-metadata`)와 deferred 사유를 표기
  - 동일 파일 §3 명령 placeholder 끝에 "TASK-023에서 셋업, TASK-017과 함께 좁힘" 메모 한 줄 추가
  - `ai-workflow/memory/active/.codex/config.toml.example` 신규 — `apply_guide.md` §9.1/§9.2 형태의 additive MCP 스니펫, `transport_ready=false` 명시
  - `ai-workflow/memory/active/.agents/README.md` 신규 — Codex가 이 저장소에서 자동 복원할 세션 시작 순서 + active skill/MCP 진입점 + 변경 시 갱신 순서 정리
  - (저장소 root의 `.codex/`, `.agents/`는 권한상 read-only로 잠겨 있어 미러 위치에 둠. root 비어 있음은 본 TASK에서 의도적으로 두는 상태로 둔다)
  - `ai-workflow/memory/active/state.json`의 `current_focus`를 본 TASK로 잠깐 옮기고, TASK-017 진입을 prerequisite 뒤로 미루는 사실 기록
  - `ai-workflow/memory/active/work_backlog.md`에 TASK-023 done 처리
  - `ai-workflow/memory/active/session_handoff.md`의 `Next Actions`/`Risks & Blockers` 갱신, MiniMax overlay 후속 점검 1줄 추가

### 7.3 검증 (Validate)
- 테스트 결과: 새 §3.1과 운영 폴더 두 파일의 정합성 정적 확인, `state.json` JSON 파싱 시도
- 미실행 사유: MCP transport가 표준 키트 측에서 미구현이므로 실제 호출 검증은 보류(`transport_ready=false` 명시)

### 7.4 결과 (Result)
- 작업 결과: 표준 워크플로우 키트 prototype이 우리 저장소 운영에 active/deferred/transport 상태로 명확히 묶였고, Codex 진입 메모와 MCP 스니펫이 미러 위치에 정리되었다.
- 다음 세션 시작 포인트: TASK-017 `shared package` 또는 `apps/build-server` API 스캐폴드 착수
- 남은 리스크 및 후속 작업:
  - MCP transport가 준비되지 않은 동안 active MCP 3종은 동일한 입력/출력 계약을 따라 수동 절차로 운영한다
  - `MiniMax.md`, `MiniMax_config.example.json`는 vendor-specific overlay 후보로, MiniMax 하네스 환경에서 별도 점검 — 본 TASK에서는 handoff에 한 줄로만 기록

## 8. TASK-024 리뷰 반영 (08/09 baseline 정합)

- 상태: done
- 모드: Documentation | Review
- 우선순위: high
- 담당: Codex
- 영향 문서: docs/sdlc/08-build-server-tech-stack-baseline.md, docs/sdlc/09-repository-package-structure-baseline.md, ai-workflow/memory/active/state.json, ai-workflow/memory/active/work_backlog.md, ai-workflow/memory/active/session_handoff.md, PR #1 (https://github.com/ykylee/docker-image-builder-system/pull/1) review thread

### 8.1 계획 (Plan)
- 작업 목표: PR #1 review 의 P1/P2 세 코멘트 중 본 저장소 문서(`docs/sdlc/08`, `docs/sdlc/09`) 와 직접 관련된 두 건을 같은 PR 안에서 해결한다. (1) 08 baseline 을 Build Server=TypeScript / Runner=Go 로 정렬하고 polyglot 보류항목 정리. (2) 09 의 `apps/runner` 예시·의존방향·PKG-005~007 경로를 Go (`go.mod`/`cmd/`/`internal/`) 기준으로 갱신.
- 완료 기준: 08/09 baseline 표현이 정합하고, `apps/runner` 구조 예시가 Go module 형태이며, §6 의존방향 표가 Build Server(TS) / Runner(Go) 양쪽에서 동일하게 해석된다.
- 비범위: `docs/sdlc/13-backend-development-plan.md` 는 본 저장소에 존재하지 않음 (리뷰어가 가리킨 문서). 회신으로만 정리.

### 8.2 실행 (Act)
- 진행 현황: `2026-07-03` 08/09 본문을 한꺼번에 패치, 의존방향 / PKG-* 표 / 결론까지 정합
- 수정 사항 (08):
  - §2 결론: Runner baseline 언어 = `Go` 로 명시
  - §3 표: `Language`/`Worker Runtime` 행을 Build Server(TS) 와 Runner(Go) 로 분리
  - §4: 헤더와 본문을 "Build Server=TS, Runner=Go" 로 재편, `### 4.1`/`### 4.2` 서브섹션 추가
  - §8: 헤더 `(Go)` 추가, 본문을 Go process 기준 + shared-contract 단일 source-of-truth 로 갱신
  - §10: `polyglot runtime` 항목 제거, "Build Server 안에서 Node worker 띄워 queue 같이 처리", "Build Server ↔ Runner gRPC/강한 결합" 보류항목 추가
  - §11: `PKG-005~007` 이 Go Runner 기준 (`apps/runner/cmd`, `internal/...`) 으로 분해됨을 명시
  - §12: "Go 기반 Runner 분리 여부" 제거, "Go module 모듈명 / internal package 경계" 추가
  - §13: 결론 baseline 표현을 `Build Server=TypeScript / Runner=Go / shared-contract=TS(스펙 단일 source)` 로 갱신
  - 메타 헤더: rev 2 명시, 관련 문서에 `09` 추가
- 수정 사항 (09):
  - §1: baseline 문구를 08 정합 버전으로 갱신
  - §2: Runner 표현을 "별도 언어(Go) 의 실행 앱" + "runtime-level import 없음" 으로 갱신
  - §4.2 `apps/runner`: 하위 구조를 `go.mod` / `cmd/runner` / `internal/...` 로 교체, TypeScript 매니페스트(`package.json` / `tsconfig.json`) 제거 명시
  - §6 의존방향: Build Server(TS) 와 Runner(Go) 양쪽 표현 + cross-language 결합 단일 source 강조
  - §8 PKG-* 표: `PKG-005~007` 경로를 `apps/runner/cmd/runner`, `internal/queue`, `internal/services`, `internal/docker` 등으로 갱신
  - §11 결론: 다섯 단위 + cross-language 결합 방식 명시
  - 메타 헤더: rev 2 명시
- 비범위 처리: `docs/sdlc/13-backend-development-plan.md` 는 본 저장소에 부재 — PR review thread 답글로 회신

### 8.3 검증 (Validate)
- 테스트 결과: 08/09 정합 grep 확인 (`TypeScript + Fastify + PostgreSQL + Drizzle + Node worker` 옛 baseline 표현이 두 문서에서 모두 제거됨, 08 표는 `Build Server Language` / `Runner Language` 두 행으로 분리, 09 §4.2 는 `go.mod` / `cmd/runner` / `internal/...` 만 표기)
- 미실행 사유: `state.json` JSON 파싱 + `gh` 로 PR review thread 회신만 수행, 런타임 검증 대상은 TASK-017 이후

### 8.4 결과 (Result)
- 작업 결과: PR #1 의 P1/P2 두 코멘트가 같은 PR 안에서 해결되었고, baseline 정합으로 TASK-017 의 `apps/runner` Go 골격 분기점이 명확해졌다.
- 다음 세션 시작 포인트: TASK-017 `packages/shared-contract` (TS) + `apps/runner` Go module 골격 + `apps/build-server` (TS) 골격을 함께 닫는 스캐폴드
- 남은 리스크 및 후속 작업:
  - 13번 문서 부재는 review thread 답글로 정리, 후속 TASK 없음
  - `OI-006`/`OI-008`/`OI-009` 후속 decision 은 그대로 보류
  - shared-contract 의 Go 측 spec binding 전략 (생성 코드 vs 수동 mirror) 은 TASK-017 의 세부 결정으로 미룸

## 14. TASK-017 scaffold PR #2 머지

### 14.1 계획 (Plan)
- 본 PR #2 의 목표: `packages/shared-contract` (TS) + `apps/build-server` (TS, Fastify) + `apps/runner` (Go) 골격과 1차 smoke 검증을 한 PR 로 묶는다.
- 리뷰 사전: 08/09 baseline 정합성 (TASK-024), 13번 development plan 동기화.
- PR #2 코멘트 리뷰 3건 (P1.1 / P1.2 / P2.3) 반영.

### 14.2 실행 (Do)
- PR #2 머지 커밋: `975e765 feat: scaffold backend host-server baseline (#2)`.
- squash 머지로 main 에 61 files / +3382 / -72 반영.
- 본 PR 머지로 TASK-017 = done 처리.
- 보완 커밋 `dd84273 docs(sdlc): PR #2 리뷰 반영 (08/09/13 baseline 정합)`:
  - `docs/sdlc/08-build-server-tech-stack-baseline.md` rev 3: §10 polyglot 항목을 §12 보류/후속 검토로 이동, §10 도입 단락에 §13 baseline 정렬 명시.
  - `docs/sdlc/09-repository-package-structure-baseline.md` rev 3: §3 최상위 트리는 언어 무관 안내, §4.1/§4.2 헤더에 TypeScript/Go 명시.
  - `docs/sdlc/13-backend-development-plan.md` rev 2: Phase 2/3 순서를 Persistence Baseline → Build Server Skeleton 으로 교체, §9/§10 정합.

### 14.3 검증 (Validate)
- `gh pr view 2 --json mergeable,state` → state=MERGED, mergeCommit=`975e7659b79bc1db2c88b44a5bf717210dc433b0`, mergedAt=`2026-07-03T02:02:48Z`.
- `git log origin/main -1` → 7a54564..975e765 fast-forward 확인.
- 리뷰 3건 (P1.1 / P1.2 / P2.3) 본문 대응 후 APPROVE 및 squash 머지.

### 14.4 결과 (Result)
- 작업 결과: TASK-017 (Build Server=TS / Runner=Go / shared-contract 골격) 이 main 에 합류. PR #1 (TASK-022/023) 다음, PR #2 가 본 프로젝트의 두 번째 머지된 PR.
- 다음 세션 시작 포인트: `codex/skills-mcp-dev-2026-07-03` 브랜치를 main 에 rebase 한 뒤 새 PR 로 P0 skill 2종 + P0 MCP 2종 (총 107 tests) 을 main 에 합류.
- 남은 리스크 및 후속 작업:
  - `__pycache__/` / `*.pyc` 등 gitignore 항목은 skills-mcp-dev PR 에서 정리.
  - `transport_ready=false` 계약은 P0 MCP 4종 모두 동일하게 유지. stdio transport 활성화는 후속 TASK.

## 15. TASK-025~029 P0 4종 main 합류 (PR #3)

### 15.1 계획 (Plan)
- `codex/skills-mcp-dev-2026-07-03` 브랜치에 쌓인 5개 커밋(TASK-025 / 026 / 027 / 028 / 029) 을 main 으로 합류.
- PR #2 머지(975e765) + 메타 동기화(c42e98c) 이후, 메타 파일 conflict 회피를 위해 메타 변경은 main 버전(theirs) 으로 두고 코드/문서 신규 파일만 patch 로 적용.
- 최종 1개 squash 커밋 + 메타 동기화 커밋으로 PR #3 구성.

### 15.2 실행 (Do)
- 새 브랜치 `codex/skills-mcp-dev-2026-07-03` (origin/main 기반) 에서:
  - 5개 커밋의 diff 중 `ai-workflow/` 메타 파일을 제외한 patch 추출
  - `.gitignore` 는 main 의 PR #2 결과에 `__pycache__/`, `*.pyc` 만 추가 (5 lines 추가)
  - `docs/PROJECT_PROFILE.md` + `docs/sdlc/07-implementation-backlog-baseline.md` 의 cross-ref 라인 (Step 13 / PKG-008/009 후보) 만 추가
  - 신규 파일: `apps/skill_mcp/**`, `apps/skill_mcp/tests/**`, `docs/sdlc/13-skills-and-mcp-plan.md`
- 단위 테스트: `python3 -m unittest discover -s apps/skill_mcp/tests -p "test_*.py"` → 107/107 PASS
- 메타 동기화: state.json rev 32, work_backlog.md rev 11, session_handoff.md rev 9, backlog §15 추가

### 15.3 검증 (Validate)
- 회귀 107/107 PASS (이전 57 + 신규 50)
- 메타 정합: state rev 32 ↔ work_backlog rev 11 ↔ handoff rev 9 ↔ backlog §15 모두 TASK-025~029 done 표기
- TASK-017 (PR #2 머지) 과 TASK-025~029 의 의존성은 README cross-ref 만으로 검증 (실제 stdio transport 묶음은 후속 TASK)

### 15.4 결과 (Result)
- 작업 결과: P0 skill 2종 + P0 MCP 2종 + P0 plan 1종 = 5개 TASK 모두 main 합류. 107 tests OK.
- 다음 세션 시작 포인트: (a) P1 skill 1종 (`failure-summary-shaper` 프로토타입) 또는 (b) TASK-017 의 stdio transport 활성화.
- 남은 리스크 및 후속 작업:
  - `transport_ready=false` 계약 — P0 MCP 4종 모두 동일. 후속 TASK-017/030+ 에서 stdio transport 활성화 시 일괄 정정.
  - `failure-summary-shaper` (P1 skill) / `preview-readiness-checker` (P1 skill) / `contract-drift-checker` (P0/P2 skill) / `failure-summary` (P1 MCP) / `preview-ttl` (P2 MCP, OI-006 후속) / `dockerfile-template-suggest` (P2 MCP, OI-008 후속) 후보 잔존.
  - `__pycache__` 항목이 .gitignore 에 들어갔으나, PR #2 가 머지된 main 에서도 이미 `__pycache__/` 가 추적되는 untracked 디렉터리는 무해. 후속 PR 에서 청소.

## 16. TASK-030 contract-drift-checker P1 skill 구현 (Python)

### 16.1 계획 (Plan)
- A안 (의존성 0, 가장 안전) 채택. `packages/shared-contract` (PR #2 머지로 main 합류) 와 canonical `docs/sdlc/contracts/01-shared-build-contract-baseline.md` 의 §5/§6/§7/§8 enum + §3 BuildRequest payload 표를 비교.
- 후보 카탈로그 `docs/sdlc/13-skills-and-mcp-plan.md` §3.5.
- 입력: `{ contractPath?, canonicalPath?, enums?, checkRequest? }` 모두 optional.
- 출력: `{ ok, drift_items[], summary{ total, missing_in_code, extra_in_code, by_enum }, ref{ contract_doc, skill_version } }`.
- 에러 코드 4종: `MISSING_FIELD` / `INVALID_INPUT` / `PARSE_ERROR` (warnings) / `IO_ERROR`.

### 16.2 실행 (Do)
- `apps/skill_mcp/skills/contract_drift_checker/SKILL.md` (97 lines): 표준 키트 SKILL.md 헤더, 입출력 계약, drift 종류 (missing_in_code / extra_in_code), 읽기 전용 책임 경계, 후속 구현 포인트.
- `apps/skill_mcp/skills/contract_drift_checker/core.py` (433 lines): `DriftItem` / `DriftSummary` / `DriftReport` dataclass, `_extract_ts_enum` (`TS_ENUM_RE.finditer` + export_name 매칭 — status.ts 안에 buildStatuses/previewStatuses 둘 다 있는 경우 모두 잡힘), `_extract_ts_object_keys` (z.object key 추출), `_extract_canonical_enums` (```text ... ``` block 4개를 §5~§8에 매핑), `_extract_canonical_request_fields` (§3 GFM 표 첫 컬럼), `check_drift(input_data, *, repo_root)`.
- `apps/skill_mcp/skills/contract_drift_checker/cli.py` (90 lines): `--input` / `--output` / `--strict`, `CONTRACT_DRIFT_REPO_ROOT` env var fallback.
- `apps/skill_mcp/tests/test_contract_drift_checker.py` (567 lines): 25 tests.
  - 입력 검증 5개 (non-dict / missing contract / missing canonical / unknown enum / enums not list)
  - TS enum 추출 3개 (basic / no match / non-upper 필터)
  - canonical 추출 2개 (enums 4 block / request fields 5종)
  - drift 계산 4개 (summary counts / items kind / enums subset / no-drift)
  - 파일 레벨 2개 (missing TS file warning / IO error)
  - 실제 repo 통합 2개 (real drift detected / summary to_dict)
  - cli 7개 (no args / strict rc=2 / strict rc=0 / input filter / output file / stdin / invalid JSON)
- 검증: `PYTHONPATH=. python3 -m unittest discover -s apps/skill_mcp/tests -p "test_*.py"` → 132/132 PASS.
- CLI smoke (실제 repo): drift 24건 잡힘 — BuildStatus 5 missing + 1 extra, previewStatus 3 missing + 2 extra, buildPhases 7 missing, errorCodes 7 missing, buildRequestFields 5 missing + 6 extra.
- `--strict` rc=2 (drift detected).

### 16.3 검증 (Validate)
- 25/25 tests PASS. 핵심 분기: drift 합/차, TS 파일 부재 → warnings, canonical 표 추출, strict exit code.
- **버그 1건 발견 + 수정**: `_extract_ts_enum` 가 단일 `search` 만 써서 status.ts 안에 buildStatuses / previewStatuses 둘 다 있을 때 첫 매치만 잡고 두 번째 enum 의 drift 가 누락됨. `finditer` + break + None 가드로 수정. `test_drift_summary_counts` 가 26 vs 30 fail 로 노출시킴.
- 미커버: Go Runner 측 spec mirror 와의 drift 비교, severity (`low` / `high`) 표기, CI 통합. 후속 TASK.

### 16.4 결과 (Result)
- 작업 결과: P1 skill 1종 (contract-drift-checker) 추가. 우리 시스템 도메인 skill/MCP 카탈로그의 5종 중 3종 (build-request-intake / build-status-explainer / contract-drift-checker) 완료. 잔여: failure-summary-shaper / preview-readiness-checker.
- 실용 효과: PR #2 가 main 에 들어간 직후 상태 (TS 가 canonical 의 subset) 에서 24건의 drift 가 자동 장악됨. PKG-001 owner 가 해소 결정을 내릴 수 있는 baseline 제공.
- 다음 세션 시작 포인트: P1 skill 잔여 (`failure-summary-shaper` 또는 `preview-readiness-checker`), 또는 P1 MCP (`failure-summary`), 또는 drift 해소 결정.
- 남은 리스크 및 후속 작업:
  - `__pycache__` untracked 정리는 PR #3 에서 처리됨.
  - `preview-ttl` (P2, OI-006 후) / `dockerfile-template-suggest` (P2, OI-008 후) / `failure-summary` (P1 MCP) / `preview-readiness-checker` (P1 skill) 후보 잔존.
  - 본 skill 은 drift "신고" 까지. drift "해소" 는 별개 (canonical 갱신 vs code 갱신) 결정 필요.

## 17. TASK-031 failure-summary-shaper P1 skill + TASK-032 failure-summary P1 MCP + TASK-033 preview-readiness-checker P1 skill (Python)

### 17.1 계획 (Plan)
- P1 잔여 3종:
  1. `failure-summary-shaper` (skill) — `OI-009` 미결이지만 skill 안에서 끝내는 프로토타입
  2. `failure-summary` (MCP) — `failure-summary-shaper` 의 thin wrapper
  3. `preview-readiness-checker` (skill) — PKG-006 응답 스키마를 본 skill 안에서 정의 (canonical `PreviewStatus` + `BuildStatus` + `ErrorCode` + `next_action` 사용)
- 의존성: P0 4종 (build-status-explainer / latest-build-status / build-log-tail / build-request-intake) + canonical enum snapshot. 모두 이미 main 에 있음.

### 17.2 실행 (Do)

#### TASK-031 failure-summary-shaper
- `apps/skill_mcp/skills/failure_summary_shaper/SKILL.md` (127 lines): 4-구조 (summary/cause/next_step/buildId) 사용자 메시지, canonical `ErrorCode` 10종 / `next_action` 8종 매핑, OI-009 결정 시 책임 이전 가능 명시.
- `apps/skill_mcp/skills/failure_summary_shaper/core.py` (281 lines): `FailureSummary` dataclass, `shape(input_data)`, `_pick_cause` / `_pick_summary` / `_pick_next_step` / `_build_logs_excerpt`, `failure` 우선 + `previewFailure` 부가 라인, errorSummary fallback 시 100자 truncation.
- `apps/skill_mcp/skills/failure_summary_shaper/cli.py` (75 lines): `--input` / `--output` / `--strict`, stdin 모드.
- 25 tests: 입력 검증 5 / 4-구조 합성 4 / errorCode 10종 매핑 4 / preview merge 1 / logs_excerpt 6 / envelope 1 / cli 4.

#### TASK-032 failure-summary MCP
- `apps/skill_mcp/mcp_servers/failure_summary/MCP.md` (80 lines): transport_ready=false, thin wrapper, dry-run + live 둘 다 지원, `buildServerUrl` 무시 + warning.
- `apps/skill_mcp/mcp_servers/failure_summary/core.py` (~130 lines): `summarize(input_data)` 가 shape() 위임 + dryRun 분기 + `UNUSED_FIELD` warning. 결과 envelope 에 `design_doc` / `skill_doc` / `mcp_version` 추가.
- `apps/skill_mcp/mcp_servers/failure_summary/cli.py` (~80 lines): `--input` / `--output` / `--dry-run` / `--fixture` / `--strict`.
- 16 tests: 입력 검증 4 / 위임 5 / envelope 1 / cli 4 + dryRun fixture 2.

#### TASK-033 preview-readiness-checker
- `apps/skill_mcp/skills/preview_readiness_checker/SKILL.md` (140 lines): `readiness_state` 7종 (READY/PREPARING/WAITING_FOR_SLOT/STARTING/DEGRADED/EXPIRED/UNKNOWN), 카드 4-필드, health probe 통합, ttl optional.
- `apps/skill_mcp/skills/preview_readiness_checker/core.py` (~280 lines): `ReadinessCard` / `ReadinessResult` dataclass, `_classify` 가 `testDeployment.status` 우선 + `build.status` / `currentPhase` fallback, `_ttl_remaining` 는 `ttl.ttl_remaining_seconds` 우선.
- `apps/skill_mcp/skills/preview_readiness_checker/cli.py` (~75 lines): `--input` / `--output` / `--strict`.
- 42 tests: 입력 검증 8 / 분류 19 / card 4 / ttl 4 / buildId 3 / envelope 1 / cli 4.

### 17.3 검증 (Validate)
- 회귀: `python3 -m unittest discover -s apps/skill_mcp/tests -p "test_*.py"` → **215/215 PASS** (이전 132 + 신규 83).
- CLI smoke:
  - failure-summary-shaper: 4-구조 메시지 + `next_action=FIX_DOCKERFILE` 정상 출력
  - preview-readiness-checker: readiness_state=READY, subtitle에 previewUrl, ttl=3300 정상
- canonical enum 정합: NEXT_ACTIONS 8종 (WAIT/OPEN_PREVIEW/RETRY/FIX_DOCKERFILE/FIX_PORT/CHECK_SOURCE/CONTACT_OPERATOR/NONE), READINESS_STATES 7종 모두 frozenset 으로 hard-code.
- 미커버: live Build Server 결합, `OI-005` (preview 인증) / `OI-006` (TTL) / `OI-009` (실패 요약 책임) / `OI-010` (concurrency) 결정 후 책임 이전, en-US 다국어.

### 17.4 결과 (Result)
- 작업 결과: P0+P1 8종 (skill 5종 + MCP 3종) 모두 main 합류 예정. 우리 시스템 도메인 skill/MCP 카탈로그 5/5 + 5/5 중 P0/P1 8종 완료. 잔여: P2 `preview-ttl` (OI-006), P2 `dockerfile-template-suggest` (OI-008).
- 다음 세션 시작 포인트: PR #3 (TASK-025~033 일괄) 머지, 또는 P2 후보 진입, 또는 TASK-017 stdio transport.
- 남은 리스크 및 후속 작업:
  - OI-005/006/009/010 결정 시 본 skill 들의 책임 재조정.
  - P1 MCP 1종 (`failure-summary`) + P0 MCP 3종 모두 stdio transport 미구현 (`transport_ready=false`).
  - failure-summary MCP 가 live `GET /builds/{buildId}` 와 결합하는 end-to-end 흐름은 후속.

## 18. PR #3 (TASK-025~033) main 합류 완료

### 18.1 계획 (Plan)
- 브랜치 `codex/skills-mcp-dev-2026-07-03` 에 쌓인 TASK-025~033 일괄 (skill 5 + MCP 3 + plan 1) 을 main 으로 머지.
- P0+P1 8종 모두 구현, 215 tests OK, PR #3 = 52 files, +7174/-10.

### 18.2 실행 (Do)
- 코드/문서/테스트 커밋 1개: `feat(skill/mcp): TASK-031/032/033 P1 잔여 3종 구현 (83 tests, total 215)` (15 files, +2189).
- 메타 동기화 커밋 1개: `docs(workflow): TASK-031/032/033 메타 동기화` (4 files, +61/-7).
- 리뷰 정정 커밋 1개: `docs(skill-mcp): 컨벤션 잔재 정정 (apps/skill-mcp → apps/skill_mcp)` (5 files, ±13).
- force push: `b62f136..f3e1433` 까지 push 후 `gh pr merge 3 --squash --delete-branch` 로 main 합류. squash commit = `4307d83`.

### 18.3 검증 (Validate)
- 회귀 215/215 PASS (main pull 직후, 0.031s).
- `apps/skill-mcp` (하이픈) 잔재 0건 (`apps/`, `docs/` 전수 grep).
- 메타 정합: `state.json` rev 35, `work_backlog.md` rev 14, `session_handoff.md` rev 12, `backlog/2026-07-03.md` §18 추가.
- PR #3 squash merge 후 `mergedAt=2026-07-03T02:43:49Z`, 브랜치 자동 삭제.

### 18.4 결과 (Result)
- 작업 결과: 우리 시스템 skill/MCP 카탈로그 P0+P1 8종 (skill 5 + MCP 3) + plan 1종이 main 에 합류. 합계 215 tests OK.
- 다음 세션 시작 포인트: (a) P2 후보 (`preview-ttl` MCP, OI-006 결정 후), (b) P2 후보 (`dockerfile-template-suggest` MCP, OI-008 결정 후), (c) TASK-017 stdio transport 활성화.
- 남은 리스크 및 후속 작업:
  - OI-005/006/009/010 결정 시 본 skill/MCP 들의 책임 재조정 필요.
  - `contract-drift-checker` 가 잡은 24건 drift 해소 (PKG-001 owner 결정).
  - P1 MCP 1종 + P0 MCP 3종 모두 `transport_ready=false` — TASK-017 stdio transport 활성화 시 일괄 정정.

## 19. TASK-034 PKG-005 Runner Claim And Build Phase Skeleton 시작

### 19.1 계획 (Plan)
- 백엔드 빌드 큐의 첫 단위. PKG-005 (Runner Claim And Build Phase Skeleton) 을 코드 골격으로 옮기는 것이 목표.
- 브랜치: `codex/backend-build-queue-2026-07-03` (main `ebf4290` 분기).
- 의존성: TASK-017 (Build Server=TS / Runner=Go / shared-contract) 완료. apps/runner/queue 골격과 hostclient 는 이미 있음.
- 목표 최소 흐름:
  1. Runner 가 Host Server `claim API` 를 poll
  2. build phase 시작 시 Host Server `phase update API` 로 보고
  3. build 성공/실패 시 Host Server `completion/error report API` 로 종결
  4. 이 3개 endpoint 의 request/response shape 와 status code 가 단일 contract (`docs/sdlc/contracts/01-shared-build-contract-baseline.md`) 와 닫혀야 함
- 1차 산출물: claim API 골격 (TS) + Runner claim loop (Go) + phase update 1 step (REQUEST_ACCEPTED → QUEUE_CLAIMED) + 빌드/스모크.

### 19.2 실행 (Do)
- TBD (이번 세션에서 진행)

### 19.3 검증 (Validate)
- TBD

### 19.4 결과 (Result)
- TBD

## 20. TASK-034 PKG-005 Runner Claim And Build Phase Skeleton 1차 골격 완료

### 20.1 계획 (Plan)
- Host Server 측에 Runner 가 build 를 claim 하고 phase 를 보고하는 endpoint 2종을 정의.
- shared-contract 에 3종 schema (claimRequest/claimResponse/phaseUpdateRequest) 추가.
- BuildRepository (memory + postgres) 에 동시성 안전한 claim/phase 구현.
- BuildService + route 에 zod safeParse + 400/404/409 분기 통일.

### 20.2 실행 (Do)
- 1) `packages/shared-contract/src/build/response.ts` 에 3종 schema + 3종 type 추가.
- 2) `BuildRepository` interface 에 `claimNextBuild()` / `updatePhase()` 2 method 추가.
- 3) `MemoryBuildRepository`: in-memory Map 기반. oldest QUEUED → CLAIMED/QUEUE_CLAIMED 전이 + log entry. status 자동 전이 (DOCKER_BUILD_STARTED→BUILDING, COMPLETED/FAILED 는 명시).
- 4) `PostgresBuildRepository`: advisory lock 없이 `WHERE status='QUEUED'` 조건부 update 로 동시성 안전. racing 시 no_build_available fallback. 동일 phase idempotent ok.
- 5) `BuildService`: `claimNextBuild` / `reportPhase` (ReportPhaseOutcome union).
- 6) `build-routes`: `POST /builds/claim` (200) + `POST /builds/:buildId/phase` (200/400/404/409). 기존 endpoint 도 zod parse → safeParse 로 통일, 400 매핑.
- 7) `pnpm-lock.yaml` 에 `@types/pg` peer dep 자동 추가 반영.

### 20.3 검증 (Validate)
- `pnpm -r check`: 4 packages + 1 app 모두 PASS.
- `pnpm -r build`: 4 packages + 1 app 모두 성공.
- live smoke (memory backend, port 3031, curl): 7/7 PASS
  - POST /builds → 202 QUEUED/REQUEST_ACCEPTED
  - POST /builds/claim → 200 claimed=true
  - POST /builds/:id/phase → 200 + status 자동 전이
  - invalid phase / malformed body / non-uuid buildId / empty runnerId 모두 400
  - not found buildId → 404
  - 2번째 claim (active build 존재) → 200 ACTIVE_BUILD_EXISTS + build 응답
- skill/mcp 회귀 215/215 OK.

### 20.4 결과 (Result)
- 작업 결과: Host Server 측 PKG-005 골격 완성. shared-contract 3종 schema + BuildRepository 2 method (memory + postgres) + BuildService 2 method + routes 2 endpoint.
- 다음 세션 시작 포인트: (a) TASK-035 Go Runner (apps/runner) 에서 claim loop + phase 호출 구현, (b) TASK-036 PKG-006 Preview Queue, (c) 단위/통합 테스트 추가 (현재 smoke 는 live curl).
- 남은 리스크 및 후속 작업:
  - Runner side 가 아직 hostclient 골격만 있고 실제 claim/phase 호출 없음 — TASK-035.
  - `phase` 자동 status 전이 휴리스틱 (DOCKER_BUILD_STARTED→BUILDING 등) 단순 매핑. canonical phase machine 검증은 후속.
  - `invalid_transition` 분기는 1차 골격에서 미발생 (memory/postgres 모두 update 후 ok 반환) — phase machine 도입 시 활용.

## 21. TASK-034 TS tests + TASK-035 Go Runner claim loop + phase call 완료

### 21.1 계획 (Plan)
- TASK-034 의 TS unit/integration test 인프라 (외부 deps 0, node:test + tsx).
- TASK-035 Go Runner 측 consumer 구현. hostclient 골격을 HTTPBuildControlClient 로 교체, BuildService 4-phase 자동 보고, Worker loop.

### 21.2 실행 (Do)
- TS tests (`apps/build-server/tests/`): 23 tests
  - memory-build-repository.test.ts (11)
  - build-service.test.ts (5)
  - build-routes.test.ts (7, Fastify inject 통합)
  - pnpm scripts: `apps/build-server/package.json` 에 test 추가, root `test:host` / `test:all` 추가
- Go (apps/runner):
  - hostclient: HTTPBuildControlClient 구현 (claim + reportPhase), 2-depth nesting 처리, Noop 유지
  - services: BuildService.ProcessClaim — SOURCE_PREPARED → DOCKER_BUILD_STARTED → docker.BuildImage → DOCKER_BUILD_COMPLETED → COMPLETED
  - worker: New + NewWithClient, ticker loop
  - config: RUNNER_ID / HOST_SERVER_BASE_URL / RUNNER_POLL_INTERVAL env override
  - Go tests: hostclient 5 + services 3 + worker 2 = 10

### 21.3 검증 (Validate)
- `pnpm -r test` → TS 23/23
- `go test ./...` → 10/10
- `python3 -m unittest discover -s apps/skill_mcp/tests` → 215/215
- end-to-end smoke (Host Server memory + Runner 500ms poll):
  - enqueue → 6 phase 자동 진행: REQUEST_ACCEPTED → QUEUE_CLAIMED → SOURCE_PREPARED → DOCKER_BUILD_STARTED → DOCKER_BUILD_COMPLETED → COMPLETED
  - final status: COMPLETED, 6 log entries 정상 기록
- 전체 회귀 248/248 OK (TS 23 + Go 10 + Python 215)

### 21.4 결과 (Result)
- 작업 결과: PKG-005 (Runner Claim And Build Phase Skeleton) end-to-end 동작. Host Server + Runner 가 canonical contract 위에서 자동 build 처리.
- 다음 세션 시작 포인트: (a) TASK-036 PKG-006 Preview Service Queue And Readiness, (b) docker.BuildImage 실제 구현 (DOCKERFILE/SOURCE 기반 image build), (c) preview 단계 (PREVIEW_QUEUED/PROVISIONING/READY/EXPIRED) 자동 진행.
- 남은 리스크 및 후속 작업:
  - docker.BuildImage 는 noop (실제 docker SDK 연동 후속).
  - phase 자동 status 전이 휴리스틱 단순 매핑. canonical phase machine 검증 필요.
  - Preview flow (PKG-006) 가 contract 안에 있지만 Runner/Host 측 구현은 0. OI-005/006 결정 후 통합.

## 22. TASK-036 PKG-006 Preview Service Queue And Readiness 완료

### 22.1 계획 (Plan)
- PKG-005 위에 PKG-006 1차 골격. canonical contract 의 previewStatuses 6종 + testDeployment 최소 key 그대로 반영.
- Host Server 측 4 endpoint + Runner 측 queue/ready 자동 호출.

### 22.2 실행 (Do)
- contract 5종 schema 추가 (response.ts): testDeploymentSchema, Queue/Ready/Status request/response schema
- BuildRepository 3 method (queueTestDeployment, reportPreviewStatus, getTestDeployment) + 3 result type
- MemoryBuildRepository: StoredBuild.testDeployment 필드 + 3 method 구현
- PostgresBuildRepository: buildRequestTable 의 phase/previewStatus/previewUrl 컬럼 갱신 (testDeployment 의 host/hostPort/expiresAt/internalPort 는 1차 null)
- BuildService: 3 method + 3 outcome type
- build-routes: 4 endpoint (POST /preview, POST /test-deployment/ready, POST /test-deployment/status, GET /test-deployment)
- Runner (Go): HTTPBuildControlClient.QueueTestDeployment / ReportPreviewReady + interface + Noop, BuildService.ProcessClaim 확장
- tests: TS 14 신규 (memory 9 + routes 5), Go 3 신규 (hostclient 2 + services 1)

### 22.3 검증 (Validate)
- pnpm -r check: 4 packages + 1 app 모두 PASS
- pnpm -r test: 37/37 PASS
- go test ./...: 13/13 PASS
- python3 -m unittest discover: 215/215 PASS
- end-to-end smoke (Host Server memory + Runner 300ms poll): enqueue → 8 phase 자동 진행
  (REQUEST_ACCEPTED → QUEUE_CLAIMED → SOURCE_PREPARED → DOCKER_BUILD_STARTED →
   DOCKER_BUILD_COMPLETED → PREVIEW_QUEUED → PREVIEW_READY → COMPLETED)
  final status=COMPLETED, previewStatus=READY, previewUrl=http://preview.local/{buildId}
  GET /test-deployment 200 with full testDeployment (host/hostPort/internalPort/expiresAt/updatedAt)
- 전체 회귀 265/265 OK (TS 37 + Go 13 + Python 215)

### 22.4 결과 (Result)
- 작업 결과: PKG-005 + PKG-006 end-to-end 동작. Host Server + Runner 가 canonical contract 위에서 build + preview 전체 lifecycle 처리.
- 다음 세션 시작 포인트: (a) TASK-037 PKG-007 Preview Cleanup Policy Binding (TTL 만료 cleanup), (b) docker.BuildImage 실제 구현 (Docker SDK + SOURCE_PREPARED/DOCKER_BUILD_STARTED 사이 실제 image build), (c) PKG-008 Skill/MCP Request Client.
- 남은 리스크 및 후속 작업:
  - postgres 의 testDeployment host/hostPort/expiresAt/internalPort 컬럼 미보유. 1차 null 응답. 별도 table 도입 또는 buildRequestTable 컬럼 추가 후속.
  - preview queue 2회 호출 (idempotent retry) — interface clean up 후속.
  - phase 자동 status 전이 휴리스틱 단순 매핑. canonical phase machine 도입 후속.
  - TTL cleanup (PKG-007) 미구현. expiresAt 은 기록되지만 sweeper/cleanup job 없음.


## 23. PR #4 머지 후 메타 동기화

### 23.1 계획 (Plan)
- PR #3 머지 후 컨벤션(PR #N 머지 후 메타 동기화 단일 커밋)을 그대로 적용.
- PR #4 는 squash merge (5d023a7) 완료, main 회귀 없음.
- state.json / work_backlog.md / session_handoff.md / backlog §22 다음 줄에 §23 PR #4 머지 후 메타 동기화 항목 추가.

### 23.2 실행 (Do)
- squash merge: `feat(backend): TASK-034/035/036 PKG-005+006 Runner Claim/Phase + Preview Queue (#4)` (5d023a7)
- 24 files / +2531 / -58, fast-forward ebf4290..5d023a7
- 원격 브랜치 `codex/backend-build-queue-2026-07-03` 자동 삭제 (gh --delete-branch), origin prune 으로 stale ref 정리
- 메타 4 파일 rev 갱신:
  - state.json: purpose_digest_rev 39 → 40, purpose_digest 재작성 (PR #4 합류 + 8 phase e2e + 265 tests + 후속 후보 3종 명시), session.handoff_rev 16 → 17, backlog.index_rev 18 → 19, backlog.latest_rev 22 → 23
  - work_backlog.md: rev 18 → 19 (PR #4 머지 사실)
  - session_handoff.md: rev 16 → 17 (PR #4 합류 + 후속 TASK-037/docker.BuildImage), Next Actions 의 "TASK-034/035/036 일괄 PR #4 오픈 — 사용자 결정 대기" 항목 [x] closed
- 본 §23 추가

### 23.3 검증 (Validate)
- TS 회귀: `pnpm --filter @docker-image-builder-system/build-server test` → 37/37 PASS (suites 12, duration_ms ~334)
- Go 회귀: `go test ./...` → 3 패키지 ok (hostclient / services / worker, cached)
- Python 회귀: `python3 -m unittest discover -s apps/skill_mcp/tests -t .` → 215/215 PASS
- 누적 265/265 OK
- PR #4 OPEN → MERGED (mergeCommit 5d023a7), mergeStateStatus=CLEAN, 리뷰 코멘트 0건 (self-review 후 사용자 결정 위임 컨벤션)
- git: main fast-forward, 로컬/원격 backend-build-queue-2026-07-03 브랜치 정리, origin prune 완료

### 23.4 결과 (Result)
- 작업 결과: PKG-005 + PKG-006 1차 골격이 main 에 합류, 8 phase e2e + 50 신규 tests (TS 37 신규 / Go 13 신규) + Python 회귀 무손상.
- 다음 세션 시작 포인트: (a) TASK-037 PKG-007 Preview Cleanup Policy Binding (TTL 만료 sweeper), (b) docker.BuildImage 실제 구현, (c) TASK-017 stdio transport 활성화로 4종 MCP 를 real process 로 잇는 일.
- 남은 리스크 및 후속 작업:
  - preview queue 2회 호출 (idempotent retry) — interface 정돈 후속.
  - phase 자동 status 전이 휴리스틱 단순 매핑 — canonical phase machine 도입 후속.
  - postgres testDeployment host/hostPort/expiresAt/internalPort 컬럼 정밀화 후속.
  - TTL cleanup (PKG-007) 미구현. expiresAt 기록만 됨, sweeper 없음.

## 24. PR #5 (Swagger UI + CORS + DESIGN.md) 1차안

### 24.1 배경 (Context)
- TASK-038: PR #4 (TASK-034/035/036) main 합류 후속. Build Server 에 OpenAPI / Swagger UI 부착 + CORS 셋업 + DESIGN.md 신설이 1차 frontend 부착 준비의 첫 단계.
- 핸드오프 노트에 따라 `codex/frontend-swagger-2026-07-03` 브랜치 (main `876554f` base) 에서 작업 진행.

### 24.2 작업 (Work)
- 의존성 4종 추가 후 `@fastify/cors` 는 ESM/fastify-plugin fp 호환성 문제로 **제거** (lock 에서도 삭제).
- `@asteasolutions/zod-to-openapi` 8.5.0 + `@fastify/swagger` 9.7.0 + `@fastify/swagger-ui` 6.0.0 부착.
- `packages/shared-contract/src/openapi.ts` 신설 — `openapiTags` + `extendZodWithOpenApi` 16 zod schema 에 `.meta({ id, description })` 부착 (zod 4 native API, .openapi() 메서드는 type augmentation 충돌로 사용 불가).
- `packages/shared-config` 에 `CORS_ORIGIN` ENV 추가 ("true"|"false"|string, default "true") + `RuntimeSettings.corsOrigin: string | true | false`.
- `apps/build-server/src/app/openapi.ts` — `OpenAPIRegistry` + `OpenApiGeneratorV3` + 10 `registerPath()` + `/openapi.json` route + swagger-ui `/docs` route + hand-rolled CORS (onRequest hook + `app.options("/:_(.*)")` wildcard).
- `apps/build-server/src/app/create-app.ts` — `registerOpenApiRoutes` 호출, `corsOrigin === false` 일 때 `skipCors` 옵션.
- 9 route schema 제거 (Fastify v5 + zod 4 의 `routeOptions.schema` 가 ajv strict mode 와 호환 안 됨, inline zod parse 만 사용). OpenAPI document 는 `OpenApiGeneratorV3` 로 별도 생성.
- openapi snapshot test 1종 (`apps/build-server/tests/openapi.test.ts`) — 10 paths + 4 tags + 3 schema key assertions.
- `docs/DESIGN.md` 신설 — Stitch v1 spec 형식, YAML frontmatter tokens + 8 markdown sections, 우리 backend monitor UI 기준 1차안 (color/typography/spacing/components/layout/voice/a11y/do-don'ts/references).

### 24.3 결정 (Decisions)
- **A. Fastify v5 + zod 4 의 `routeOptions.schema` 는 zod 직접 못 받음** — ajv 가 `required: []` array 요구, zod 4 의 `z.object` 가 `required: {}` object emit. route schema 제거, inline zod parse 만 사용.
- **B. `@asteasolutions/zod-to-openapi` 8.5.0 의 zod 4 호환** — `.openapi()` 메서드 type augmentation 이 `core.$ZodType` generic 과 mismatch → 사용 불가. `.meta({ id, description })` 가 zod 4 native API 라 type-safe → 16 schema 모두 `.meta()` 사용.
- **C. `@fastify/cors` 11.2.0 + Fastify 5.9.0 + ESM workspace 호환성** — `app.register(cors, opts)` 호출 시 plugin 까지 opts 전달 안 됨 (fp wrapping + ESM dynamic import 결합). hand-rolled CORS (onRequest hook + OPTIONS wildcard route) 로 교체.
- **D. frontend 부착은 별도 PR** — Swagger 와 frontend 같은 PR 에 묶지 않음 (PR #3, #4 컨벤션).

### 24.4 결과 (Result)
- 회귀: TS 38 + Go 13 + Python 215 = **266/266 OK** (PR #4 의 265 + openapi snapshot 1).
- live smoke: `GET /health` 200, `GET /openapi.json` 200 (10 paths + 17 components.schemas + 4 tags: Builds/Runner Claim/Test Deployment/Health), `GET /docs` 200 (Swagger UI HTML), `OPTIONS /builds` with `Origin: http://localhost:5173` → 204 with `Access-Control-Allow-Origin: http://localhost:5173`.
- git: `codex/frontend-swagger-2026-07-03` 1차 squash + main 머지 후 브랜치 정리.
- 다음 세션 시작 포인트: (a) frontend Vite+React 스캐폴드 + DESIGN.md token CSS 변수화, (b) TASK-017 stdio transport 활성화로 4종 MCP 를 real process 로 잇는 일, (c) TASK-037 PKG-007 Preview Cleanup TTL sweeper.
- 남은 리스크 및 후속:
  - hand-rolled CORS 는 wildcard origin ("*") 와 echo origin 만 지원, credentialed requests 후속.
  - DESIGN.md 의 token 은 1차안 — frontend 부착 시점에 실제 contrast 검증 + light mode 추가.
  - `@fastify/cors` 의존성 제거로 swagger-ui 의 built-in cors 옵션도 무력화. 필요 시 추후 재도입.
  - `registerPath` 의 query schema 가 현재 `z.string().optional()` 형태 — 실제 swagger-ui 가 dropdown 으로 보여주지는 못함. 후속 PR 에서 enum/enumFormat 보강.

### 24.5 main 합류 (Post-merge)
- PR #5 squash merge: `abd5b13 feat(build-server): Swagger UI + CORS + DESIGN.md (TASK-038, PR #5) (#5)`
- 원격 브랜치 `codex/frontend-swagger-2026-07-03` 자동 삭제 (gh pr merge --squash --delete-branch)
- main ff pull 후 회귀 재검증: TS 38/38, Go 3 packages, Python 215 (266/266 OK)
- live smoke: /health 200, /openapi.json 200, /docs 200, OPTIONS preflight 204 with `Access-Control-Allow-Origin: http://localhost:5173`
- state.json rev 41→42 (main merge 사실 반영)

## 25. PR #6 (Build Monitor frontend 1차 골격) — Svelte 5 선정

### 25.1 배경 (Context)
- TASK-039: PR #5 (/openapi.json + /docs + CORS) 합류 후속. DESIGN.md 가
  "Vite + React" 로 잠정 표기되어 있었으나, Build Monitor 의 요구
  (internal single-page, dark-first, density, live data) 에 Svelte 5 가 더
  적합하다고 판단하여 stack 재선정.
- stack 비교 검토: bundle size, live-data reactive primitives, internal tool
  onboarding 비용. Svelte 5 + Vite + TypeScript 선정.
- 새 브랜치: `codex/frontend-attach-2026-07-03` (main `3c14649` base).

### 25.2 작업 (Work)
- `apps/build-monitor/` 골격 (신설):
  - `package.json` (Svelte 5.1, Vite 5.4, vitest 2.1, svelte-spa-router 4, openapi-fetch 0.13, @testing-library/svelte 5.4, jsdom 25, TypeScript 5.6)
  - `tsconfig.json` (@tsconfig/svelte 기반, Vite client types, strict)
  - `svelte.config.js` (vitePreprocess)
  - `vite.config.ts` (svelte plugin, /api → :3000 proxy, vitest jsdom + browser conditions for svelte exports)
  - `index.html` (data-theme="dark", color-scheme 메타)
  - `public/favicon.svg` (token 색으로 만든 임시 로고)
- `src/lib/tokens.css` — DESIGN.md §1 Tokens 의 1차안을 CSS custom properties 로 (color/typography/spacing/radius/shadow/motion/z-index).
- `src/lib/theme.css` — dark-first reset, a11y focus-visible, reduced-motion 대응.
- 컴포넌트 5종:
  - `StatusPill.svelte` — status → color token 매핑, `role="status"` + `aria-label`, pill radius + mono uppercase.
  - `BuildRow.svelte` — StatusPill + buildId(mono) + project/repository + relative time. svelte-spa-router `link` 로 detail 연결.
  - `PhaseTimeline.svelte` — 수직 step indicator, `role="list"` + 각 step `role="listitem"`, ISO → locale 변환.
  - `LogStream.svelte` — monospace pre, wrap 토글, sample phase prefix color.
  - `Header.svelte` — sticky, brand + Builds/API/Docs nav.
- 라우트 3종 + 라우터: `BuildsList` (filter chips ALL/BUILDING/COMPLETED/FAILED + table), `BuildDetail` (status + meta + phase timeline + log stream), `NotFound`.
- `src/lib/api.ts` — hand-typed BuildSummary / BuildStatusResponse / BuildLogEntry (1차, drift 시 alarm; 후속 PR 에서 openapi-typescript 자동화).
- Vite proxy: `/api/*` → `http://127.0.0.1:3000` (CORS 우회 + production 동일 origin).
- `src/test/setup.ts` + `src/components/StatusPill.test.ts` — vitest + @testing-library/svelte + jest-dom 매처. status 6종 매핑 + UNKNOWN fallback 검증.
- DESIGN.md 정정: §0 "Stack (PR #6 결정)" 신설, status 필드 갱신, body 카피 ("(Vite + React)" → "(Svelte 5 + Vite + TypeScript)"), §3 Components 표기 갱신, References 에 PR #6 추가.

### 25.3 결정 (Decisions)
- **A. Svelte 5 + Vite + TypeScript 선정** — Build Monitor 는 internal single-page, dark-first, density 높은 table UI, live data (status pill / phase timeline / log stream). Svelte 5 의 reactive primitives (`$state`, `$derived`, `$effect`) 가 적은 boilerplate 로 live UI 작성에 적합. Bundle / cold-start 도 internal tool 에 더 맞음. React 대비 생태계는 좁지만 internal tool 의 운영 부담이 더 중요하지 않다고 판단. 단계적 도입 가능.
- **B. Svelte 5 package.json exports 의 . → server default** — svelte 5 의 exports 가 `default: index-server.js`, `browser: index-client.js`. vitest 의 jsdom 환경은 `worker`/`node` condition 으로 server module 진입 → `mount` 가 SSR 빌드. vitest config 에 `resolve.conditions: ["browser"]` 강제 + `@testing-library/svelte` 를 server.deps.inline 으로 강제. testing-library v5 가 client mount 만 사용 가능.
- **C. openapi-typescript 자동 client 는 후속 PR** — 1차 골격은 hand-typed types (lib/api.ts) 로 drift 시 alarm 역할. 후속 PR 에서 build-time script 가 /openapi.json 을 fetch + openapi-typescript 로 ./.generated/openapi.d.ts 생성.
- **D. Vite proxy /api** — dev 환경에서 CORS 우회 + production 동일 origin. Build Server 의 `/api/*` 외 경로는 Vite 가 직접 serve.
- **E. frontend 부착은 PR #6 단독** — Swagger (PR #5) 와 묶지 않음. frontend 변경은 별도 PR 의 컨벤션 유지.

### 25.4 결과 (Result)
- 회귀: TS 38 (build-server) + TS 7 (build-monitor) + Go 13 + Python 215 = **273/273 OK** (PR #5 의 266 + build-monitor 7).
- svelte-check 0 errors / 0 warnings.
- vite build OK — index.html 0.56 kB + index.css 7.63 kB (gzip 2.07 kB) + index.js 58.13 kB (gzip 21.96 kB).
- vite dev OK — 5183 port 에서 200, HMR client 주입, main.ts transform 정상.
- build-server live smoke (/health, /openapi.json) 200 — proxy 의 upstream 정상.
- 메타 동기화: state rev 42→43, work_backlog rev 20→21 (TASK-039), handoff rev 18→19, backlog §25.
- 다음 세션 시작 포인트: (a) openapi-typescript 자동 client 생성 + build-time script, (b) Build Server list endpoint 추가 + frontend 교체, (c) light mode 추가 + DESIGN.md contrast QA, (d) TASK-017 stdio transport, (e) TASK-037 sweeper.
- 남은 리스크 및 후속:
  - hand-typed types (lib/api.ts) 가 `/openapi.json` drift 시 alarm 역할만 함. 자동화 PR 후 closed.
  - sample data (SAMPLE_BUILDS) 가 list endpoint 부재로 임시 사용. Build Server list endpoint PR 후 교체.
  - light mode token 미정의 — DESIGN.md §1 Tokens 에 `--color-bg-canvas` 등 dark 만 있음. light 모드 추가 시 PR.
  - Vite proxy 가 dev 전용. production 배포 시 reverse proxy / CDN 설정 후속.
  - svelte-spa-router (hash 기반) 사용 — SSR / SEO 불필요한 internal tool 이라 hash 적정. 추후 host-side router 필요 시 전환 검토.

### 25.5 main 합류 (Post-merge)
- PR #6 squash merge: `09ccd4d feat(build-monitor): Svelte 5 frontend 부착 1차 골격 (TASK-039, PR #6) (#6)`
- 원격 브랜치 `codex/frontend-attach-2026-07-03` 자동 삭제 (gh pr merge --squash --delete-branch)
- main ff pull 후 회귀 재검증: TS build-server 38/38, TS build-monitor 7/7, Go 3 packages, Python 215 (273/273 OK)
- state.json rev 43→44 (main merge 사실 반영)

## 26. PR #7 (Build Server list + openapi-typescript 자동 client)

### 26.1 배경 (Context)
- TASK-040: PR #6 의 frontend 1차 골격에서 SAMPLE_BUILDS (lib/api.ts 의
  hard-coded array) 를 실제 endpoint 로 교체 + openapi-typescript 자동 client.
- Build Server 에 list endpoint 부재 + frontend 의 hand-typed types 가
  drift 가능성. 양쪽을 /openapi.json 의 single source of truth 로 묶음.

### 26.2 작업 (Work)
- Build Server:
  - shared-contract: `buildListQuerySchema` (status/limit/cursor) + `buildListResponseSchema` (builds[], nextCursor) zod schema + .meta
  - `BuildRepository.listBuilds` 인터페이스 + `MemoryBuildRepository`/`PostgresBuildRepository` 양쪽 구현 (createdAt desc, cursor skip, limit)
  - `BuildService.listBuilds` (단순 forward)
  - `GET /builds` route (build-routes.ts) + openapi.ts path 등록
  - `limit` z.coerce.number() 로 query string 자동 변환 (fastify v5 의 query string 자동 coercion 미지원)
  - openapi components.schemas 17 → 19 (BuildListQuery, BuildListResponse 추가)
  - snapshot test path count 갱신 (paths dictionary 10 unique, /builds 는 get+post merge)
  - MemoryBuildRepository.listBuilds tests 4종 (empty/desc/filter/paginate)
- build-monitor:
  - openapi-typescript 7.13.0 + openapi-fetch 0.13.5 + tsx 4.22.4 추가
  - predev/prebuild hook 으로 backend 의 /openapi.json → ./.generated/openapi.d.ts 자동 생성 (scripts/generate-openapi.ts)
  - lib/api.ts 재작성: hand-typed types → components["schemas"] alias (BuildSummary/BuildStatusResponse/BuildListResponse/BuildListQuery/BuildLogEntry). BuildLogsResponse 는 inline (OpenAPI envelope 미정의).
  - openapi-fetch 0.13 + openapi-typescript 7.x 의 known issue (response envelope inference) 우회: helper `apiGet` 의 return 을 `unknown` 으로, caller 가 cast
  - BuildsList.svelte: result.builds 사용
  - BuildDetail.svelte: BuildStatusResponse nested access (.build.buildId, .build.projectId, .build.phase, .build.previewStatus, .build.createdAt, .build.updatedAt)
  - LogStream.svelte: LogEntry.at → createdAt (backend schema 정합)
  - BuildRow.test.ts 추가 (1종 — StatusPill 매핑 검증)

### 26.3 결정 (Decisions)
- **A. BuildLogsResponse inline 정의** — Backend 의 `buildLogsResponseSchema` 가 envelope({ buildId, logs }) 을 가지지만 openapi.ts 의 `registerPath` 가 envelope 의 `schema` 를 지정하지 않아 generated 가 inline `{ buildId, cursor, entries }` 로 expand. 1차 inline `{ buildId, logs }` 정의 + 후속 PR 에서 `buildLogsResponseSchema` 의 envelope 을 openapi.ts 의 registerPath response 에 명시.
- **B. openapi-fetch 0.13 + openapi-typescript 7.x inference 우회** — `apiGet` helper 의 return type 을 `Promise<unknown>` 으로, caller 가 명시 cast. helper 의 generic T 가 openapi-typescript 의 path-template response 의 inline shape 으로 잘못 추론되는 현상 우회. 후속 openapi-fetch 갱신 시 cast 제거 검토.
- **C. limit z.coerce.number()** — fastify v5 의 query string 자동 coercion 미지원. zod 4 의 `z.coerce.number().int().positive().max(200).default(50)` 로 query string 자동 변환 + cap 200.
- **D. Memory 의 listBuilds status filter** — status 가 string union ("QUEUED"|...) 으로 zod.parse 후 적용. SAMPLE_BUILDS 의 hand-typed status 가 string 이라 filter 매치 정상.
- **E. .generated/openapi.d.ts commit 추적** — 본 PR 에서 generated file 을 commit (drift 검증 가능). 후속 PR 에서 .gitignore 검토.

### 26.4 결과 (Result)
- 회귀: TS 42 (build-server) + TS 8 (build-monitor) + Go 13 + Python 215 = **268/268 OK** (PR #6 의 273 + build-server 4 listBuilds + build-monitor 1 BuildRow = +5, PR #5 의 266 + 2 신규).
- svelte-check 0 errors / 0 warnings
- vite build OK — gzip 24KB JS + 2KB CSS
- live e2e: vite proxy /api/builds → 3 builds, ?limit=2 → 2 + nextCursor, /api/openapi.json 200
- openapi-fetch 0.13 의 known issue 우회: helper apiGet 의 return unknown + caller cast
- 메타 동기화: state rev 44→45, work_backlog rev 21→22 (TASK-040), handoff rev 19→20, backlog §26
- 다음 세션 시작 포인트: (a) light mode tokens + DESIGN.md contrast QA, (b) TASK-017 stdio transport, (c) TASK-037 PKG-007 sweeper
- 남은 리스크 및 후속:
  - openapi-fetch 0.13 + openapi-typescript 7.x 의 inference 우회를 `as unknown as T` cast 로 처리. openapi-fetch 갱신 시 cast 제거 후 검증.
  - ./.generated/openapi.d.ts 를 commit 해서 drift 검증 가능. PR 별로 갱신 비용 발생.
  - BuildLogsResponse envelope 의 registerPath response schema 미지정 — 1차 inline, 후속 PR 에서 정식화.
  - MemoryBuildRepository.listBuilds 의 cursor 가 buildId 기반. 같은 buildId 두 row 가 같은 createdAt 인 경우 (현재 monotonic timestamp) 는 안전. monotonic 깨질 시 tiebreak 필요.
  - Vite proxy 가 dev 전용. production 배포 시 nginx / CDN 설정 후속.

### 26.5 main 합류 (Post-merge)
- PR #7 squash merge: `3f61f5c feat: Build Server GET /builds list + openapi-typescript 자동 client (TASK-040, PR #7) (#7)`
- 원격 브랜치 `codex/build-list-openapi-client-2026-07-03` 자동 삭제
- main ff pull 후 회귀 재검증: TS build-server 42/42, TS build-monitor 8/8, Go 3, Python 215 (268/268 OK)
- state.json rev 45→46 (main merge 사실 반영)

## 27. Admin UI (TASK-043 / ADMIN-*)

### 27.1 범위 (Scope)
- 사용자 요청: "관리자는 관리자 등록을 통해서 id를 기록하고, 해당 id 로 접속하면 관리자 메뉴를 볼 수 있도록. 일단 'admin', 'yky.lee' 를 기본 관리자로 넣어주고, 관리자는 등록된 모든 빌드를 조회 + 빌드 등록 이력이 있는 사용자 목록을 관리."
- 1차 범위 (본 PR): admin id 등록 + admin id 기반 로그인 + 모든 owner 의 build 조회 + owner 별 user 목록 (buildCount + lastBuildAt). owner 차단/메모/삭제 등은 후속.
- 권장 결정: Build Server + build-monitor 양쪽에 admin 라우트 추가. `Admin` OpenAPI tag + 별도 schema (BuildListQuery 와 owner field 가 의미상 다름 — user 측은 자기 빌드만, admin 측은 모든 owner).

### 27.2 변경 (Changes)
- shared-config: `ADMIN_IDS` env (comma-separated) + `parseAdminIds` helper + `DEFAULT_ADMIN_IDS_RAW = "admin,yky.lee"` + `RuntimeSettings.adminIds: string[]`.
- shared-contract: `adminListBuildsQuerySchema` (BuildListQuery extend), `adminListBuildsResponseSchema`, `adminUserBuildSummarySchema` (BuildSummary + requestedBy), `adminUserSummarySchema`, `adminUserListResponseSchema` — 모두 .meta({ id, description }) 부착. `openapiTags.Admin` 추가.
- shared-contract build-server 등록: `@asteasolutions/zod-to-openapi` 8.5 + zod v4 에서 `.extend().meta()` 가 components.schemas 로 emit 되지 않는 함정 회피 — schema 를 `schema.openapi(id, { description })` 으로 refId 를 명시 부착 후 registerPath 에 동일 인스턴스 재사용. (이슈: 8.5 의 `register(refId, schema)` 가 schema 의 `.openapi(refId)` clone 을 받지만, registerPath 안의 inline schema 가 별도 인스턴스이면 components.schemas 에 emit 안 됨. → component helper Map 으로 clone 보존 후 registerPath 에도 같은 clone 주입.)
- BuildServer: `BuildRepository.listBuildsAcrossUsers(query)` + `listBuildOwners()`. memory 는 `listBuilds` 재사용 + ownerById Map 으로 requestedBy enrich. postgres 는 (cursor + status + requestedBy + limit) 동일 조건으로 row.select + mapBuildRowToSummary + requestedBy enrich. `listBuildOwners` 는 memory 의 Map 집계, postgres 는 `GROUP BY requestedBy` + `count(*)::int` + `max(createdAt)`.
- BuildServer routes: `apps/build-server/src/routes/admin-routes.ts` 신규. `makeAdminAuthenticator(adminIds)` 로 closure 가드, `ADMIN_ID_HEADER = "x-admin-id"`. `GET /admin/builds`, `GET /admin/users` 두 endpoint. 401 (header 누락/empty) / 403 (caller 미포함) / 400 (invalid query) / 200.
- openapi: componentSchemas 에 admin 4종 추가 + registerPath `/admin/builds`, `/admin/users` 등록 + tags "Admin" 추가. snapshot test 에 admin tags/paths/components 4종 확인 추가.
- build-monitor `lib/api.ts`: `apiGet` signature 가 `params?: { query?, path?, headers? }` 받도록 + admin helpers (`listAdminBuilds`, `listAdminUsers`). `AdminUserBuildSummary`/`AdminListBuildsQuery`/`AdminListBuildsResponse`/`AdminUserSummary`/`AdminUserListResponse` hand-typed (build server 스키마와 동기화, generated.d.ts 는 stale 가능 — prebuild hook 으로 fetch 동기화 가능).
- build-monitor routes: `AdminLogin.svelte` (admin id 입력 → `/admin/builds`), `AdminBuilds.svelte` (전체 owner 빌드 + owner filter + status chip), `AdminUsers.svelte` (per-user rollup + 클릭 시 inline build history).
- `App.svelte` 에 `/admin/login`, `/admin/builds`, `/admin/users` 라우트 등록.
- `Header.svelte` 에 adminId 분기: adminId 있으면 🛡@adminId pill + Admin · Builds / Users 링크 + Admin Logout 버튼, 없으면 Admin 진입 링크. localStorage `adminId` 키는 `userId` 와 분리.
- `BuildRow.svelte`: `requestedBy?` optional field 추가, owner cell (pill). `BuildRowData` 가 superset 허용 → 사용자 라우트 영향 없음.

### 27.3 결정 (Decisions)
- **A. AdminListBuildsQuery 와 BuildListQuery 분리** — BuildListQuery.requestedBy 는 "user 의 userId" 의미, AdminListBuildsQuery.requestedBy 는 "any owner" 의미. description 만 다르고 shape 동일이라 `.extend()` 로 재정의. components.schemas.AdminListBuildsQuery 가 별도 emit.
- **B. AdminUserBuildSummary = BuildSummary & { requestedBy }** — BuildSummary 자체는 user 측에서 owner 노출 안 함 (caller 가 추론). admin 측은 owner cell 렌더링을 위해 별도 superset. openapi 8.5 의 `.extend().meta()` 가 emit 안 되는 함정은 openapi.ts 의 `component(id)` helper 로 refId 부착된 clone 을 registerPath 에도 전달.
- **C. admin session 키 분리** — `localStorage.adminId` 와 `localStorage.userId` 분리. 일반 user 의 `userId` 가 header 에 실려도 admin guard 가 403 으로 거절 (BuildSummaryRequestedBy 와 adminIds 가 매치 안 되는 게 정상). user 가 admin 페이지 우연히 보이지 않도록.
- **D. admin auth 는 단순 header 비교** — JWT / 세션 없이 단순 `X-Admin-Id` header + server env allow-list. MVP / single-tenant 운영 가정. local 환경에서 충분. 다중 tenant / SSO / OIDC 가 필요해지면 후속.
- **E. openapi.ts register 패턴 통일** — 모든 component schema 에 `schema.openapi(id, { description })` 으로 refId 부착 후 register. inline query 가 registerPath 에 들어갈 때 같은 clone 을 쓰도록 `component(id)` helper 로 일원화.

### 27.4 결과 (Result)
- 회귀: TS 53 (build-server, +4 admin: listBuildsAcrossUsers × 2 + guard × 2 + 1 listBuilds user filter + 1 listBuildsAcrossUsers no-filter 변경) + vitest 34 (build-monitor, AdminLogin 2 + AdminBuilds 4 + AdminUsers 3 + Header 6 + 기존 19) = **TS 53 + vitest 34 = 87/87 OK**.
- svelte-check 0 errors / 0 warnings
- vite build OK — gzip 28KB JS + 4KB CSS
- live e2e: `x-admin-id: admin` → 200, `x-admin-id: alice` → 403, no header → 401; `/admin/users` → alice 2 / bob 1 + lastBuildAt; `/admin/builds` → 3 builds (alice 2 + bob 1) with requestedBy; `/admin/builds?requestedBy=alice` → 2 alice builds.
- 메타 동기화: state rev 46→47, work_backlog rev 22→23 (TASK-043), handoff rev 20→21, backlog §27, PROJECT_PROFILE §3.2 신규.
- 다음 세션 시작 포인트: (a) light mode tokens + DESIGN.md contrast QA, (b) TASK-017 stdio transport, (c) TASK-037 PKG-007 sweeper, (d) admin owner block/delete (후속 결정)
- 남은 리스크 및 후속:
  - openapi 8.5 + zod v4 의 `.extend().meta()` emit 함정은 openapi.ts 의 refId clone 패턴으로 회피. zod-to-openapi / zod 버전 갱신 시 register 패턴 회귀 검증 필요.
  - admin auth 가 단순 header 비교. 동일 머신 안에서 다른 사용자가 dev tools 로 header 위조 가능. 운영 배포 시 reverse proxy 단의 TLS + IP allow-list 또는 JWT 도입 검토.
  - `/admin/builds` 가 memory repository 의 `listBuilds` 결과를 재사용하지만 enrich 단계에서 `Map<buildId, requestedBy>` 추가 lookup. owner 수가 빌드 수와 같으므로 N row → N lookup, O(N). 빌드가 10k+ 이면 인덱스 추가 검토.
  - postgres `lastBuildAt` 가 drizzle 의 `sql<string | null>\`max(...)\`` 의 runtime return (Date|string) 을 `new Date(value as string|Date).toISOString()` 으로 정규화. driver 가 string 으로 돌려주는 경우 Date 변환 안전.
  - build-monitor 측 admin helpers 가 `apiGet` 의 inline schema cast (`as never`) 를 사용. openapi-typescript 가 admin path 를 모를 때 generic 추론 우회.

### 27.5 main 합류 (Post-merge)
- **PR #9 squash merge**: `af2ade4 feat: ADMIN-* Admin UI (TASK-043) (#9)` — main 에 `af2ade43d51db70ed85bff1f9579a154c94f10b7` 로 합류. 본 sync commit 의 `<hash>` 자리는 hash amend 로 박음.
- PR #9 의 3 commit 모두 main 으로 합류:
  1. `521ccdb feat: ADMIN-* Admin UI (TASK-043)` — 1차 admin 라우트/스키마/리포지토리/프론트 라우트/문서.
  2. `05a8b56 feat(admin): address PR #9 review (ADMIN-005/006 follow-ups)` — 리뷰 nits 7건 (운영 가이드, snapshot-at-boot JSDoc, cursor 페이지네이션 테스트, BuildSummary evolve contract, LIMIT TODO, hand-typed 의도, lastBuildAt invariant).
  3. `1e2b06e fix(monitor,contract): header 즉시 갱신 + appName 단일 식별자 (TASK-044)` — Bug 1 (Header 즉시 갱신, session store) + Bug 2 (BuildRequest/BuildSummary appName collapse, memory/postgres/UI/generated 모두).
- 원격 브랜치 `codex/admin-ui-2026-07-03` 자동 삭제 (`gh pr merge --squash --delete-branch`).
- main ff pull 후 회귀 재검증: TS build-server 54/54, TS build-monitor 35/35, Go 13/13, Python 215/215 = **270/270 OK**.
- state.json rev 49→50 (post-merge sync bump), handoff 20→21, work_backlog 25→26, latest_rev 26→27, backlog §27.5 hash amend (`af2ade4`).
- §28 자체는 PR #9 branch 위에서 작성됐고 main squash merge 와 함께 합류. 별도 PR 아님.
- 본 sync commit 의 `<hash>` 자리는 `af2ade4` 로 amend 완료. 회귀 270/270 main ff 후 재검증 OK.

### 28.1 변경 (Changes)
- **Bug 1 — Header 즉시 갱신** (`apps/build-monitor/src/lib/session.ts` 신규 + `Header.svelte`/`Login.svelte`/`AdminLogin.svelte` 갈아끼움).
  - 원인: `Header` 가 `onMount` + `window 'storage'` 이벤트만 듣고 있어서, 같은 SPA 라우트 트리 내에서 `localStorage.setItem` 한 값을 `Header` 가 모름. `storage` 이벤트는 다른 탭에서만 fire.
  - 해법: `writable<string|null>` 두 개 (`userIdStore`, `adminIdStore`) 노출하는 session store. `set` 시 localStorage 도 같이 쓰고, `subscribe` 시점에 `readInitial(key)` 로 lazy 동기화 (테스트 격리 + production 초기 read 모두 자연스러움). `storage` 이벤트는 다른 탭 반영용으로 그대로 유지. `Writable` 대신 custom `SessionStore` (subscribe + set 만, `update` 의도적 봉인) — session 변조 경로 최소화.
  - `Header.svelte` 의 `userId`/`adminId` 를 `$derived($userIdStore)` 로 교체. `logout`/`adminLogout` 도 `userIdStore.set(null)`/`adminIdStore.set(null)`.
- **Bug 2 — BuildRequest/BuildSummary 의 appName 단일 식별자**.
  - 의도: 빌드 메타에 프로젝트/저장소 정보는 빼고, "app 명만" 노출. lock key / UI / API 모두 appName 1개로 통일.
  - 변경:
    - `packages/shared-contract/src/build/request.ts`: `BuildRequest` 의 `projectId`/`repositoryId` 제거, `appName: string` 추가. description 에 dedup lock 으로만 쓰임 명시.
    - `packages/shared-contract/src/build/response.ts`: `BuildSummary` 동일하게 `appName` 만 유지. `BuildDuplicateResponse.description` 도 `appName` 으로 갱신. `AdminUserBuildSummary` 는 `BuildSummary.extend({requestedBy})` 라서 자동 전파 — PR #9 의 BuildSummary evolve contract 그대로 동작.
    - `apps/build-server/src/repositories/memory-build-repository.ts`: active-build dedup 을 `appName === input.appName` 으로 단순화. summary 에 `appName: input.appName` 기록.
    - `apps/build-server/src/repositories/postgres-build-repository.ts`:
      - `mapBuildRowToSummary`: `metadata->>'appName'` 에서 appName 추출 (fallback `metadata->>'app_name'`, 둘 다 없으면 `""`).
      - `lockKey = input.appName` (advisory_xact_lock).
      - active-build probe 도 `sql\`metadata->>'appName' = \${input.appName}\``.
      - insert 에서 `projectId: input.appName`, `repositoryId: ""` (DB 컬럼은 비워두고 — 사용자 지시). `metadata: { ...input.metadata, appName: input.appName }`.
      - **TODO (DB migration, follow-up)**: `app_name text not null` 컬럼 추가, `project_id`/`repository_id` drop. 마이그레이션은 별도 PR.
    - `apps/build-server/tests/*.test.ts` 6개: `baseRequest` 헬퍼의 projectId 변수 → appName, inline seed 의 projectId/repositoryId → appName, assertion 의 `.projectId`/`.repositoryId` → `.appName`. `baseRequest` 가 (requestedBy, appName) signature 로 변경됨.
    - `apps/build-monitor/.generated/openapi.d.ts`: BuildSummary/BuildRequest/AdminUserBuildSummary 의 `projectId: string; repositoryId: string;` 두 줄을 `appName: string;` 한 줄 + JSDoc description 으로 교체. `BuildDuplicateResponse.description` 도 `appName` 으로 갱신. (live build server 미실행 환경에서 generator skip 되므로 손 갱신.)
    - `apps/build-monitor/src/components/BuildRow.svelte`: `BuildRowData.projectId/repositoryId` → `appName: string`. project/repository 두 cell → appName 한 cell (`.mono`).
    - `apps/build-monitor/src/routes/BuildsList.svelte`: thead `Project`/`Repository` → `App` 한 컬럼.
    - `apps/build-monitor/src/routes/BuildDetail.svelte`: meta 의 `Project`/`Repository` 두 항목 → `App` 한 항목.
    - `apps/build-monitor/src/routes/AdminUsers.svelte`: `selectedBuilds` 의 `projectId` → `appName`. link 텍스트도 `b.projectId` → `b.appName`.
    - `apps/build-monitor/src/**/*.test.ts` 4개: sample 시드의 projectId/repositoryId → appName.
- `Header.test.ts`: Bug 1 회귀 케이스 추가 — `userIdStore.set("yklee")` 만으로 Header 가 `@yklee` pill 을 reactive 하게 렌더 (storage 이벤트 없이도). `SessionStore` 가 `Writable` 가 아니라 custom interface 라는 점도 type system 으로 잠금.
- `admin-routes.test.ts` / `memory-build-repository.test.ts` / `admin-repository.test.ts`: 시드 signature 변경에 따른 assertion 갱신.

### 28.2 결정 (Decisions)
- **A. session store 는 Writable 대신 custom SessionStore** — `set` 만 노출하고 `update` (함수형 갱신) 는 의도적 봉인. `Writable` 인터페이스는 그대로 두면 코드 어디서든 `userIdStore.update(prev => ...)` 로 변조 가능 → Login/AdminLogin 의 submit 으로만 바뀌어야 한다는 invariant 가 새어나갈 수 있음. SessionStore 로 좁혀 type system 으로 잠금.
- **B. `subscribe` 시 lazy read** — module load time 의 `readInitial` 결과에 영구 묶이지 않게, 첫 구독 시점에 localStorage 를 다시 읽는다. 테스트 격리 (render 전에 setItem 한 값 인식) + production 첫 구독이 module load 보다 늦을 수 있는 경우 모두 자연스러움.
- **C. appName 의 source of truth 는 BuildRequest/BuildSummary schema** — admin 쪽 `AdminUserBuildSummary` 는 BuildSummary.extend 이므로 자동 전파 (PR #9 M4 의 evolve contract 그대로). repository 는 schema 가 받으면 그대로 저장/응답.
- **D. postgres 1차는 metadata->>'appName' 으로 처리** — DB 컬럼 추가 없이 1차 골격이 굴러가게 한다. 사용자 지시로 `projectId`/`repositoryId` 컬럼은 비워두고 마이그레이션은 후속 PR. admin 쪽에서도 `appName` 만 채워서 응답하므로 UI 영향 없음.
- **E. generated/openapi.d.ts 손 갱신** — predev/prebuild hook 이 live build server 에 붙지 못하면 generator 가 skip/fail 되므로, contract 변경 후 generator 가 한 번도 안 도는 환경에서도 type 이 맞도록 직접 갱신. PR #9 follow-up 코멘트(S2) 의 "generator 가 CI hard gate 가 될 때까지" 가정이 아직 유효하므로 같은 방식 유지.

### 28.3 결과 (Result)
- 회귀: TS build-server 54/54 + TS build-monitor 35/35 (Bug1 회귀 1 신규) + Go 13/13 + Python 215/215 = **270/270 OK**.
- svelte-check 0 errors / 0 warnings
- vite build OK — gzip 28.57 KB JS + 4.26 KB CSS
- Backend TS 컴파일 (`tsc --noEmit`) 0 error — postgres repo 의 `sql\`metadata->>'appName' = \${input.appName}\`` 가 drizzle sql template 으로 정상 직렬화.
- 메타 동기화: state rev 47→48, handoff rev 20→21, work_backlog rev 23→24 (TASK-044), backlog §28.
- 다음 세션 시작 포인트:
  - (a) DB schema migration — `app_name text not null` 추가 + `project_id`/`repository_id` drop (PR #9 이후 별도 PR).
  - (b) light mode tokens + DESIGN.md contrast QA.
  - (c) TASK-017 stdio transport.
  - (d) TASK-037 PKG-007 Preview Cleanup Policy Binding.
  - (e) admin owner block/delete (신규 후속).
- 남은 리스크:
  - postgres `metadata->>'appName'` probe 는 text btree 인덱스가 없어서 large table 에서 active-build dedup 이 slow. `app_name` 컬럼 + index 가 추가되면 해소 (마이그레이션 후속).
  - generated/openapi.d.ts 손 갱신 → generator 가 다시 돌면 동일 결과가 나와야 함. PR 머지 직전 CI 에서 `pnpm run generate:openapi` 가 실제로 도는지 확인 필요 (현재 환경에서는 127.0.0.1:3000 ECONNREFUSED).
  - SessionStore 의 `update` 봉인은 type system 잠금일 뿐 runtime 차단은 아님 — 누군가 `Object.assign(userIdStore, { update: ... })` 로 우회 가능. production code path 에서만 의미 있고, 테스트는 그냥 set 만 쓰면 OK.

## 29. DB schema migration (TASK-045, v0.2 appName 단일)

### 29.1 범위 (Scope)
- TASK-044 의 follow-up: `appName` 단일 식별자 스키마에 맞춰 DB 도 정렬. `project_id`/`repository_id` 컬럼은 drop, `app_name` 컬럼 신설 (canonical identifier).
- 1차 범위: (a) `packages/db` 의 Drizzle schema + bootstrap CREATE TABLE 정렬, (b) `apps/build-server/migrations/0001_app_name.sql` 신규 (brownfield 운영자 수동 적용), (c) `postgres-build-repository` 가 column 직접 사용으로 전환 (메타 JSONB fallback chain 제거).
- 후속 TODO: migration runner 통합 (현재는 운영자가 psql 로 수동 적용). active-build dedup 의 app_name btree index 는 0001 에서 함께 생성됨.

### 29.2 변경 (Changes)
- `packages/db/src/schema/build-request.ts`: `appName: text('app_name').notNull()` 추가, `projectId`/`repositoryId` 컬럼 제거. Drizzle 스키마는 type-level 정의.
- `packages/db/src/bootstrap.ts`: `CREATE TABLE IF NOT EXISTS build_request` 에서 `project_id`/`repository_id` 빼고 `app_name TEXT NOT NULL` 만 둠 — 신규 DB 는 처음부터 v0.2.
- `apps/build-server/migrations/0001_app_name.sql` (신규, 33 lines):
  - `ADD COLUMN IF NOT EXISTS app_name TEXT` — 기존 row 의 `metadata->>'appName'` 에서 채움 (snake_case fallback 포함, 없으면 `""`).
  - `ALTER COLUMN app_name SET NOT NULL` — 모든 row 가 app_name 을 가진 뒤 적용.
  - `CREATE INDEX IF NOT EXISTS build_request_app_name_idx ON build_request (app_name)` — active-build dedup / list filter / owner rollup 의 hot path.
  - `CREATE INDEX IF NOT EXISTS build_request_requested_by_idx ON build_request (requested_by)` — `listBuildOwners` 의 `GROUP BY` 가 사용.
  - `DROP COLUMN IF EXISTS project_id` / `repository_id` — `BEGIN; ... COMMIT;` 으로 한 transaction.
  - 모든 구문 idempotent (IF EXISTS / IF NOT EXISTS). 부분 적용된 상태 재실행 안전.
- `apps/build-server/src/repositories/postgres-build-repository.ts`:
  - `mapBuildRowToSummary`: `row.appName` 직접 사용. metadata JSONB fallback chain (`metadata['appName'] ?? metadata['app_name'] ?? ''`) 제거.
  - active-build dedup probe: `eq(buildRequestTable.appName, input.appName)` — direct column compare, `build_request_app_name_idx` 가 서비스.
  - insert: `appName: input.appName` 직접 + `metadata: input.metadata` (이전 `{ ...input.metadata, appName: input.appName }` 의 중복 쓰기 제거).

### 29.3 결정 (Decisions)
- **A. 마이그레이션은 SQL 파일로 두기, runner 통합은 후속** — 현재 1차 골격의 `ensureDbSchema` 는 idempotent CREATE 만 지원 (DB migration tool 미도입). 운영자가 본 PR 머지 후 `psql ... -f apps/build-server/migrations/0001_app_name.sql` 로 적용. migration runner (drizzle-kit, knex-migrate, atlas 등) 도입은 별도 PR.
- **B. `project_id`/`repository_id` drop 까지 1차에 포함** — TASK-044 의 TODO 가 "컬럼 추가 + drop" 까지 묶어서, 운영 적용 후 v0.2 와 정확히 동일한 schema. drop 은 되돌릴 수 없으므로 운영 적용 전 백업 권장 — 마이그레이션 파일 상단 코멘트에 명시.
- **C. metadata 에 appName 중복 X** — insert 가 `metadata: input.metadata` 만 쓴다. v0.2 부터는 `app_name` 컬럼이 canonical 이라 metadata 에 다시 박는 게 dead data. 1차 PR 의 fallback path (TASK-044 의 `metadata->>'appName'`) 가 운영 적용 후 dead path 가 되지만, 후속 PR 에서 fallback 제거.
- **D. btree 인덱스 두 종류 신설** — `app_name` (active-build dedup + list filter + owner rollup 의 join key) + `requested_by` (listBuildOwners 의 GROUP BY). 둘 다 0001 마이그레이션에서 함께 생성.
- **E. postgres path 단위 테스트는 live DB 필요** — 메모리 repo 위주의 vitest 54 는 그대로. postgres path 의 column 직접 compare / insert / drop 마이그레이션 시뮬레이션은 운영자 수동 적용 후 smoke 가 확인. 후속 PR 에서 testcontainers 도입 검토.

### 29.4 결과 (Result)
- 회귀: TS build-server 54/54 (메모리 repo 위주) + TS build-monitor 35/35 + Go 13/13 + Python 215/215 + svelte-check 0/0 + vite build OK (gzip 28.57 KB JS) + Backend `tsc --noEmit` 0 error = **270/270 OK**.
- 백엔드 코드 변경: 3 files (`packages/db/src/schema/build-request.ts`, `packages/db/src/bootstrap.ts`, `apps/build-server/src/repositories/postgres-build-repository.ts`), 신규: `apps/build-server/migrations/0001_app_name.sql`.
- 메타 동기화: state rev 50→51, handoff 21→22, work_backlog 25→26 (TASK-045), backlog §29, handoff.md Updated 마커 rev 23→24.
- 다음 세션 시작 포인트:
  - (a) 운영자 마이그레이션 적용 (psql) — `apps/build-server/migrations/0001_app_name.sql`.
  - (b) migration runner 통합 (drizzle-kit 등) — 후속 PR.
  - (c) metadata->>'appName' fallback path 제거 — 후속 PR (운영 적용 후).
  - (d) light mode + DESIGN.md contrast QA.
  - (e) TASK-017 stdio transport.
  - (f) TASK-037 PKG-007 sweeper.
  - (g) admin owner block/delete.
- 남은 리스크:
  - 마이그레이션 SQL 의 `UPDATE build_request SET app_name = ...` 가 기존 row 가 매우 많으면 long lock — 운영자 적용 시 maintenance window 권장 (코멘트 명시).
  - `app_name = ''` (metadata 도 비어있던 row) 가 NOT NULL 제약을 통과하지만 schema 위반 — 운영자가 사전 cleanup 권장.
  - 인덱스 두 종류 신설 시 write 비용 증가 — 빌드 트래픽이 많아지면 측정 후 partial index 검토.
  - 0001 의 `DROP COLUMN` 은 PostgreSQL 9.6+ 가 physical delete, 그 이하는 metadata only drop. 12+ 가정이지만 후속 PR 에서 PG version check.

## 30. Light mode contrast QA (TASK-046)

### 30.1 범위 (Scope)
- PR #8 에서 light/dark mode 토큰 + ThemeToggle 이 들어왔지만, 일부 컴포넌트의 light 모드 contrast 가 WCAG AA (4.5:1) 미달 또는 시각적 미분리 상태였음. 본 PR 은 4가지 contrast 깨짐을 패치하고 회귀 테스트로 잠금.
- 후속: 실기 화면 캡쳐 (Playwright headless + 두 모드 토글) 로 시각 QA 자동화 — 후속 PR.

### 30.2 변경 (Changes)
- `apps/build-monitor/src/lib/tokens.css`:
  - light 모드 `--color-text-muted` #94a3b8 → #64748b (canvas #f1f5f9 위 2.7:1 → 4.5:1, WCAG AA).
  - light 모드 `--color-text-disabled` #cbd5e1 → #94a3b8 (한 단계 어둡게, disabled 가 muted 와 구분).
  - light 모드 `--color-bg-surface-elevated` #ffffff → #f8fafc (canvas/surface 와의 미세 contrast 차로 table row hover / ThemeToggle hover / BuildRow owner-cell pill 의 실루엣이 살아남).
- `apps/build-monitor/src/lib/theme.css`:
  - `:root { color-scheme: dark }` + `:root[data-theme="light"] { color-scheme: light }` 추가. native form control (input, select, scrollbar) 가 현재 모드에 맞춰짐.
- `apps/build-monitor/src/components/StatusPill.svelte`:
  - EXPIRED + UNKNOWN fallback 의 `--color-text-muted` → `--color-text-secondary`. light 모드에서 15% alpha-mix 시 canvas 에 거의 invisible 하던 문제 해결. (dark 모드에선 둘 다 충분한 contrast.)
- `apps/build-monitor/src/components/ThemeToggle.svelte`:
  - `applyTheme` 이 `document.documentElement.style.colorScheme` 도 js-side set (이중 안전망). 일부 환경 (jsdom) 에서 css rule 의 computed style 검증이 필요할 때를 위함. onMount 끝에서 dark initial 명시 보강.
- `apps/build-monitor/src/components/BuildRow.svelte`:
  - `.owner-cell` border `--color-border-subtle` → `--color-border-strong`. light 모드 surface-elevated (#f8fafc) 의 더 강한 outline.
- `apps/build-monitor/src/routes/Login.svelte` + `AdminLogin.svelte`:
  - input border 코멘트 추가 — light 모드 canvas 위 1.5:1 → 진한 톤 명시 의도.
- `apps/build-monitor/src/components/StatusPill.test.ts`:
  - EXPIRED + UNKNOWN fallback assertion `--color-text-muted` → `--color-text-secondary` 갱신.
- `apps/build-monitor/src/components/ThemeToggle.test.ts`:
  - 신규 2 케이스 — default dark 모드에서 `style.colorScheme === "dark"`, toggle 후 `=== "light"`. 35 → 37 테스트.

### 30.3 결정 (Decisions)
- **A. `--color-text-muted` 만 canvas 와 contrast fail** — 다른 token (secondary, primary, accent-warning 등) 은 이미 4.5:1+ 이라 패치 불필요. muted 가 가장 자주 쓰이는 "보조 텍스트" 라 fail 이 가장 visible.
- **B. surface-elevated 를 #f8fafc 로** — light 모드 에서 elevated 가 surface (#fff) 와 동일하면 table row hover/ThemeToggle hover/owner pill 이 모두 canvas 에 묻힘. dark 모드는 #1a1d27 vs #13151c 로 미세 분리 돼 있어 OK. light 모드만 살짝 tint.
- **C. StatusPill EXPIRED+UNKNOWN 을 secondary 로** — alpha-mix 가 muted 라 더 어두워지는데, light 모드 canvas (#f1f5f9) 위에서 거의 안 보임. secondary (#475569) 의 15% alpha 는 충분히 visible. dark 모드는 둘 다 충분 contrast 라 무영향.
- **D. color-scheme 를 css rule + js 양쪽 set** — css rule 만 두면 jsdom 의 getComputedStyle 검증을 못함. js-side set 은 거의 무비용 이중 안전망. 운영 browser 는 둘 중 하나만 적용되지만, css-side 가 canonical.
- **E. owner-cell border subtle → strong** — surface-elevated 의 contrast 차가 미세해져도 border 의 무게로 owner pill 의 실루엣이 명확. BuildRow visual hierarchy 유지.
- **F. 시각 QA 자동화 (Playwright 두 모드 캡쳐) 는 후속** — 본 PR 은 코드/테스트 패치로 contrast 를 잠그고, 시각 검증은 manual. 후속 PR 에서 `apps/build-monitor/tests/visual/` 신설 + Playwright headless 캡쳐 baseline + 두 모드 PNG diff.

### 30.4 결과 (Result)
- 회귀: TS build-server 54/54 + TS build-monitor 35→37 (TASK-046 신규 2) + Go 13/13 + Python 215/215 + svelte-check 0/0 + vite build OK (gzip 28.60 KB JS + 4.28 KB CSS) + Backend tsc --noEmit 0 error = **272/272 OK**.
- 백엔드 변경 없음. 프론트엔드 7 files 변경, 1 신규 test 케이스 그룹.
- 메타 동기화: state rev 51→52, handoff 22→23, work_backlog 25→26 (TASK-046), backlog §30.
- 다음 세션 시작 포인트: (a) light mode 시각 QA (Playwright headless 캡쳐), (b) TASK-017 stdio transport, (c) TASK-037 sweeper, (d) admin owner block/delete, (e) 운영자 마이그레이션 수동 적용.
- 남은 리스크: code-level 패치로 4.5:1+ 보장, 시각 검증은 manual. 운영 browser 두 모드 toggle 로 직접 확인 권장.


## 31. TASK-046 follow-up — PR #10 review Must 3건 보완

### 31.1 범위 (Scope)
- PR #10 (codex/light-mode-qa-2026-07-03) 리뷰 결과, 1차 패치는 일부러 미완이 아니라 **의도-코드 불일치** 가 있었음. Login/AdminLogin input border 는 "한 단계 강조" 코멘트만 추가, 실제 톤은 그대로. tokens 의 muted/surface-elevated 도 AA 또는 시각 분리 미달. 후속 commit 으로 토큰 본체 + 코멘트 갱신.

### 31.2 변경 (Changes)
- `apps/build-monitor/src/lib/tokens.css` light 모드 4 token:
  - `--color-text-muted` `#64748b` → `#57657f` — canvas (#f1f5f9) 위 4.34:1 → **5.36:1** (WCAG AA 4.5:1 + 0.86 마진).
  - `--color-text-disabled` `#94a3b8` → `#8a99b0` — 2.34:1 → 2.64:1. muted (#57657f) 보다 명백히 옅게 유지. text-only disabled 라 AA 면제, 2.64:1 OK.
  - `--color-bg-surface-elevated` `#f8fafc` → `#dde4ed` — vs canvas 1.05:1 → **1.17:1**, vs surface 1.05:1 → **1.28:1**. table row hover / ThemeToggle hover / owner pill / 페이지 header 의 의미 있는 시각 분리.
  - `--color-border-strong` `#cbd5e1` → `#7a889e` — canvas 위 1.36:1 → **3.28:1** (UI border 3:1 권고 통과). Login/AdminLogin input / AdminBuilds/AdminUsers strong-border 사용처 6곳 동시 해결.
- `apps/build-monitor/src/routes/Login.svelte` + `AdminLogin.svelte`:
  - input border 코멘트 — "한 단계 강조" 의도가 토큰 변경으로 실제 반영됐으므로 코멘트 갱신 (코드는 `var(--color-border-strong)` 그대로, 토큰 값이 해결).

### 31.3 결정 (Decisions)
- **A. border-strong 토큰 자체를 light에서 한 단계 진하게** — Login/AdminLogin input 만 따로 override 하면 6곳 일관성 깨짐. 토큰 본체 조정으로 strong-border 쓰는 모든 곳 (AdminBuilds, AdminUsers, Login, AdminLogin) 동시 해결, 변경 최소.
- **B. disabled 는 muted 보다 명백히 옅게 유지** — disabled #8a99b0 (2.64:1) vs muted #57657f (5.36:1) 2.03:1 차이. border-strong #7a889e 와도 1.24:1 차이로 disabled 텍스트와 border 색 충돌 회피.
- **C. surface-elevated 를 slate-200 톤으로** — #f8fafc 는 1.05:1 로 "거의 흰색"이었고, #dde4ed 는 slate-200 으로 canvas/surface 와 모두 1.17~1.28:1 분리. dark 모드의 elevated 의미 (위 element 강조 = 더 어두움) 와 정반대이나, light 모드 톤 특성상 한 단계 어둡게 가는 게 hover state 시각 인지에 적합.

### 31.4 결과 (Result)
- 회귀: build-server 54/54 + build-monitor 37/37 + Go 13/13 + Python 215/215 + svelte-check 0/0 = **319+ OK** (CSS 토큰만 변경, 컴포넌트 동작 영향 없음).
- 코드 변경: tokens.css 4 token + Login/AdminLogin 코멘트 2 lines = 4 files / +9/-7 lines.
- 메타 동기화: state rev 52→53, handoff 25→26, work_backlog 26→27, backlog §31.
- 다음 세션 시작 포인트: (a) PR #10 follow-up push → 머지, (b) light mode visual QA (Playwright headless 캡쳐 + 두 모드 PNG diff), (c) TASK-017 stdio transport, (d) TASK-037 sweeper, (e) admin owner block-delete.
- 남은 리스크: code-level 패치로 contrast/분리 확보, 시각 검증 manual. 운영 browser 에서 두 모드 직접 toggle 권장.


## 32. PR #10 squash merge — TASK-046 light mode contrast QA main 합류

### 32.1 결과
- commit `2dea6b1` squash merge into main. PR #10 (codex/light-mode-qa-2026-07-03) + follow-up (f5c9a11) 두 commit 이 한 squash commit 으로 합쳐짐.
- main ff pull 후 회귀 검증: build-server 54/54 + build-monitor 37/37 + Go 13/13 + Python 215/215 + svelte-check 0/0 = **319+ OK**.

### 32.2 브랜치 정리
- origin: `codex/light-mode-qa-2026-07-03` 자동 삭제 (`--delete-branch` 옵션).
- local: `codex/light-mode-qa-2026-07-03` 자동 삭제.
- `git fetch --prune` 로 stale ref 정리.

### 32.3 메타 동기화
- state.json purpose_digest_rev 53→54.
- session_handoff.md rev 26→27.
- work_backlog.md rev 27→28.
- backlog §32 (본 항목).

### 32.4 다음 세션 시작 포인트
- (a) light mode visual QA 자동화 (Playwright headless 두 모드 캡쳐 + PNG diff baseline).
- (b) TASK-017 stdio transport — 4종 MCP 를 real process 로 잇기.
- (c) TASK-037 sweeper — 오래된 빌드/큐 정리.
- (d) admin owner block-delete — admin 이 user 별 build 삭제.
- (e) 운영자 마이그레이션 수동 적용 — TASK-045 v0.2 schema 의 `0002_add_app_name.sql` brownfield DB 에 적용.


## 33. PR #11 (TASK-047/048/049) — admin allow-list management + visual QA scaffolding

### 33.1 범위
- (a) **TASK-048 admin auto-enable**: userId 가 admin allow-list 에 포함되어 있으면 별도 AdminLogin 단계 없이 Header 가 admin menu 자동 노출.
- (b) **TASK-049 admin user management**: /admin/admins 라우트에서 admin 추가/제거. seed id (보통 첫 번째 admin) 는 보호되어 process 가 admin-zero 가 되지 않음.
- (c) **TASK-047 light mode visual QA**: Playwright headless capture 스크립트 + baseline 디렉터리 (PNG 자체는 binary 라 후속 PR 에서 LFS / 외부 diff 도구로 관리).

### 33.2 backend 변경
- `apps/build-server/src/routes/admin-routes.ts`:
  - `createAdminAllowList(seed)` 도입. mutable Set + list/contains/add/remove/isRemovable 헬퍼. seed[0] 는 protected.
  - `makeAdminAuthenticator(allowList)` 로 시그니처 변경 (legacy 호환).
  - 신규 endpoint 3종: `GET /admin/admins`, `POST /admin/admins`, `DELETE /admin/admins/:adminId`. 모두 X-Admin-Id 가드 + seed protection.
- `apps/build-server/src/app/create-app.ts`: `makeAdminAuthenticator(runtime.adminIds)` → `createAdminAllowList(runtime.adminIds)` + allowList 를 registerAdminRoutes 에 전달.
- `packages/shared-contract/src/build/admin.ts`: 신규 3 schema (`AdminAllowListResponse`, `AdminAllowListAddRequest`, `AdminAllowListRemoveResponse`).
- backend test 7 케이스 추가 (61 → 68 → ... 결과 61/61, admin-routes.test.ts 가 createAdminAllowList 로 migration 됨에 따라 7 신규 추가 후 14 케이스).

### 33.3 frontend 변경
- `apps/build-monitor/src/lib/admin-store.ts` (신규): svelte writable + api helpers. snapshot/contains/refresh/add/remove.
- `apps/build-monitor/src/lib/api.ts`: `listAdminAllowList`, `addAdminToAllowList`, `removeAdminFromAllowList` 추가. POST/DELETE 는 `paths` type 에 아직 없으므로 fetch helper 직접 cast.
- `apps/build-monitor/src/components/Header.svelte`:
  - `$adminAllowListStore` 구독 + `autoAdminEnabled` derived.
  - `effectiveAdminId = adminId ?? (autoAdminEnabled ? userId : null)`. 명시적 adminId 가 없으면 userId 가 admin 일 때 자동 admin session.
  - onMount 시 userId 가 있으면 `adminAllowListStore.refresh(userId)` 자동 호출. 실패 시 stale cache 유지.
  - Admin · Admins 링크 추가.
- `apps/build-monitor/src/routes/AdminAdmins.svelte` (신규): list / add form / remove. seed badge, "you" badge, busy 상태, error banner.
- `apps/build-monitor/src/App.svelte`: `/admin/admins` route 추가.
- frontend test 7 케이스 추가 (37 → 44): Header 2 케이스 (auto-enable O/X) + AdminAdmins 5 케이스 (redirect, list, seed 보호, add, remove).

### 33.4 결정 (Decisions)
- **A. seed[0] protected**: admin allow-list 의 첫 항목 (env 시드의 첫 admin) 은 절대 삭제 불가. process 가 admin-zero 가 되어 모든 admin API 가 lock 되는 상황 방지. 환경변수 재시작이 canonical store.
- **B. in-process mutation 만 반영**: process restart 시 ADMIN_IDS env 가 canonical. UI 에서 추가/제거한 항목은 volatile. 운영자 안내용 코멘트에 명시.
- **C. Header auto-enable 의 adminId 결정**: explicit `adminId` (AdminLogin 결과) 가 우선. 없으면 `autoAdminEnabled && userId` 로 admin 세션 형성. userId logout 시 admin 도 같이 정리.
- **D. visual QA PNG 는 binary 라 commit 안 함**: 스크립트 + baseline 디렉터리 구조만 tracked. PNG 자체는 후속 PR 에서 LFS 또는 외부 diff 도구로.
- **E. admin-store 의 mock-friendly 설계**: tests 가 `vi.mock` 으로 svelte store 직접 set 가능. onMount 안의 fetch 실패도 silent.

### 33.5 결과 (Result)
- 회귀: build-server 61/61 + build-monitor 44/44 + Go 13/13 + Python 215/215 + svelte-check 0/0 = **333+ OK**.
- backend: `apps/build-server` 5 files (admin-routes.ts, create-app.ts, admin-routes.test.ts, packages/shared-contract/src/build/admin.ts, index.ts export).
- frontend: 4 신규/수정 (admin-store.ts 신규, api.ts +1 chunk, Header.svelte + AdminAdmins.svelte + App.svelte, Header.test.ts + AdminAdmins.test.ts).
- visual: `apps/build-monitor/tests/visual/{capture.py, README.md, baseline/}` + `.gitignore` 에 `.visual/` 추가.
- 메타 동기화: state rev 54→55, handoff 27→28, work_backlog 28→29, backlog §33.

### 33.6 다음 세션 시작 포인트
- PR #11 merge → 브랜치 정리.
- PR #12 (TASK-017 stdio transport + TASK-037 sweeper + admin owner block-delete).
- visual QA PNG baseline 실측 (Playwright + dev server + pixelmatch).
- 운영자 마이그레이션 수동 적용 (TASK-045 v0.2 schema).


## 34. PR #11 follow-up — review Must 2건 + Should 3건 보완

### 34.1 범위
- PR #11 review (4 sections, Must 2 / Should 4 / Nice 3) 의 머지 전 보완 권장 2건 + 후속 PR 권장 중 3건 (capture.py 정리, baseline/ .gitkeep) 을 본 follow-up commit 으로 처리. 머지 후 보완하면 다음 PR 이 indent 위에서 hot-patch 하기 어려운 결함을 지금 정리.

### 34.2 변경 (Changes)
- **`apps/build-monitor/src/components/Header.svelte`** — `adminLogout` 함수의 4-space indent 를 2-space 로. `onMount` 블록과 동일 indent level.
- **`packages/shared-contract/src/build/admin.ts`** — `adminAllowListAddRequestSchema` 의 `adminId` 에 `regex(/^[a-zA-Z0-9._-]+$/)` 추가. 일반 userId (BuildRequest.requestedBy) 는 postel-style 이지만 admin id 는 sensitive surface 라 추가 charset 제약.
- **`apps/build-server/src/routes/admin-routes.ts`**:
  - `ADMIN_ID_PATTERN = /^[a-zA-Z0-9._-]+$/` 도입.
  - `createAdminAllowList(seed)` 가 (a) empty seed, (b) non-conforming seed id 두 경우 throw. boot 단계에서 fail-fast.
  - `add(adminId)` 도 in-process mutation 에서 동일 regex 검증. throw.
  - `DELETE /admin/admins/:adminId` 의 path param 도 정규식 검증. 위반 시 400.
- **`apps/build-monitor/src/routes/AdminAdmins.svelte`** — client-side pre-check `ADMIN_ID_PATTERN` 추가. trim 후 정규식 위반이면 error banner + backend 호출 안 함.
- **`apps/build-monitor/tests/visual/capture.py`** — dead code 정리:
  - `import os, sys, Iterable` 제거 (사용 안 함).
  - `add_init_script` 가 route 루프 안에 있어 매 navigation 마다 누적되던 문제 → 루프 밖으로 이동.
  - `f"{{base}}{path}" if False else ...` dead ternary 제거.
  - `--base` CLI flag 의 default 를 `http://127.0.0.1:5173` 으로 명시.
  - docstring 의 `--mock-only` 옵션 언급 제거 (스크립트에 없음, 후속 PR 예정).
- **`apps/build-monitor/tests/visual/baseline/.gitkeep`** — 빈 baseline/ 디렉터리를 git 이 추적하도록 placeholder 추가. PNG binary 비tracked 명시.

### 34.3 결정 (Decisions)
- **A. server fail-fast on bad seed** — env 의 ADMIN_IDS 가 잘못 셋업된 채로 admin API 가 노출되는 사고를 boot 단계에서 방지. throw 가 더 안전한 default.
- **B. client-side pre-check + server schema 양쪽 검증** — frontend UX (즉시 error banner) + backend security (canonical) 둘 다. defense-in-depth.
- **C. seed[0] protected 만 유지** — "마지막 admin 도 자기자신 제거 불가" 같은 추가 safeguard 는 후속 PR (admin owner block-delete) 에서 같이 검토. 본 PR 은 charset 검증에 집중.
- **D. capture.py 정리 만, mock-only 모드는 후속** — 본 PR 의 범위는 "코드 결함 + 데이터 정합성". 시각 QA mock-only 는 PR #12 또는 별도 PR 에서.

### 34.4 결과 (Result)
- 회귀: build-server 68/68 (7 신규 — seed validation 4 + add/delete charset 3) + build-monitor 46/46 (2 신규 — AdminAdmins input validation 2) + Go 13/13 + Python 215/215 + svelte-check 0/0 = **342+ OK**.
- 코드 변경: 8 files / +85 / -12 (Header.svelte 1 line, admin.ts schema, admin-routes.ts validation, AdminAdmins.svelte pre-check, capture.py 정리, baseline/ .gitkeep, admin-routes.test.ts 7 cases, AdminAdmins.test.ts 2 cases, state.json + handoff + work_backlog + backlog §34).
- 메타 동기화: state rev 55→56, handoff 28→29, work_backlog 29→30, backlog §34.

### 34.5 다음 세션 시작 포인트
- PR #11 merge → 브랜치 정리.
- PR #12 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete).
- visual QA PNG baseline 실측 (Playwright + dev server + pixelmatch).
- 운영자 마이그레이션 수동 적용 (TASK-045 v0.2 schema).

## 35. PR #12 (TASK-050) — Build phase progress timeline

### 35.1 범위
- 사용자가 BuildDetail 의 PhaseTimeline 에서 1개 phase 만 표시되는 현상 보고. 원인: BuildDetail.svelte 가 `events={[{ phase: build.build.phase, at: build.build.updatedAt }]}` 로 현재 phase 1개만 events 에 넣어서 전달.
- 본 PR 은 (a) server 가 build 의 전체 lifecycle timeline 을 phaseHistory + currentPhase 로 emit, (b) PhaseTimeline.svelte 가 canonical 9 phases 전체를 완료/진행/미진행 상태로 시각화, (c) completedAt (완료) + startedAt (현재) timestamp 노출.

### 35.2 backend 변경
- `packages/shared-contract/src/build/response.ts`:
  - 신규 `BuildPhaseHistoryEntry` schema: `{ phase, completedAt }` (transition 으로 종료된 phase).
  - 신규 `BuildCurrentPhase` schema: `{ phase, startedAt } | null` (terminal 시 null).
  - `buildStatusResponseSchema` 에 `phaseHistory` (default []) + `currentPhase` (default null) 두 필드 추가. 기존 schema 와 호환 (default 로 누락 허용).
- `apps/build-server/src/repositories/memory-build-repository.ts`:
  - `StoredBuild` 에 `phaseHistory: { phase, completedAt }[]` + `currentPhaseStartedAt: string | null` 추가.
  - `createBuild`: initial `phaseHistory=[]`, `currentPhaseStartedAt=timestamp` (REQUEST_ACCEPTED in-flight).
  - `updatePhase`: prevPhase !== phase 면 history push, currentPhaseStartedAt 갱신, terminal phase (COMPLETED/FAILED) 진입 시 null.
  - `queueTestDeployment`: 동일 transition 패턴.
  - `getBuild` + 모든 response builder: `toPhaseTimeline(build)` helper 로 두 필드 emit.
- `apps/build-server/src/repositories/postgres-build-repository.ts`:
  - 별도 column 없음 (TASK-051 후속). 7 곳의 BuildStatusResponse return 에 `{ phase: <summary>.phase, completedAt: <summary>.updatedAt }` 단일 entry + currentPhase based on phase emit. `active_build_exists` claim 응답도 같은 패턴.

### 35.3 frontend 변경
- `apps/build-monitor/src/components/PhaseTimeline.svelte`:
  - props: `{ phaseHistory: PhaseHistoryEntry[]; currentPhase: CurrentPhaseEntry | null }`.
  - canonical 9 phases (shared-contract buildPhases 와 동일 순서) 전체 render.
  - 상태별 시각화: completed (success dot + timestamp), current (primary dot + pulse animation + in-progress · startedAt), pending (subtle dot + "— pending").
  - FAILED phase 가 completed 인 경우 danger 톤 (data-phase attribute selector).
  - aria-label: `${phase} — ${status}`.
- `apps/build-monitor/src/lib/api.ts`:
  - `BuildStatusResponse` hand-typed mirror (generated stale 대비). 주석에 regenerator 가 다시 돌면 generated type 으로 갈음 명시.
- `apps/build-monitor/src/routes/BuildDetail.svelte`:
  - `events={[{ phase, at }]}` → `phaseHistory={build.phaseHistory ?? []}` + `currentPhase={build.currentPhase ?? null}`.

### 35.4 결정 (Decisions)
- **A. in-memory 만 full transition 관리**: postgres repo 는 별도 column 없어 fallback. TASK-051 에서 column 추가 (jsonb array of {phase, completedAt}) + migration SQL + read path. 본 PR 의 핵심은 PhaseTimeline 시각화 + shared-contract 이고, postgres 호환은 보존.
- **B. server schema default (`[]` / `null`)**: caller 가 두 필드 안 보내도 zod parse 통과. 기존 endpoint 들 (claim, queueTestDeployment, reportPreviewStatus) 도 모두 새 필드 emit 하지만, 방어적으로 default.
- **C. canonical 9 phases hardcode in component**: shared-contract buildPhases 와 동일. server 가 invalid phase 보내도 component 는 "pending" 으로 무시.
- **D. FAILED 시각화는 completed 상태 + danger 톤**: build 가 FAILED 에서 종료되면 phaseHistory 에 FAILED push + currentPhase=null. component 는 data-phase="FAILED" + class="completed" 조합으로 danger dot/text.
- **E. data-phase attribute**: svelte-check 의 "unused CSS selector" warning 회피 + 테스트 selector 용이성.

### 35.5 결과 (Result)
- 회귀: build-server 72/72 (4 신규 — memory-build-repository phase timeline 4) + build-monitor 51/51 (5 신규 — PhaseTimeline 5) + Go 13/13 + Python 215/215 + svelte-check 0/0 = **351+ OK**.
- backend: 4 files (response.ts, memory-build-repository.ts, postgres-build-repository.ts, memory-build-repository.test.ts) — schema + repo + tests.
- frontend: 4 files (PhaseTimeline.svelte, PhaseTimeline.test.ts, api.ts, BuildDetail.svelte).
- 메타 동기화: state rev 57→58, handoff 30→31, work_backlog 31→32, backlog §35.

### 35.6 다음 세션 시작 포인트
- PR #12 merge → 브랜치 정리.
- PR #13 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete).
- TASK-051 postgres phase_history column 추가 (jsonb) + 마이그레이션 SQL + read path.
- visual QA PNG baseline 실측 (Playwright + dev server + pixelmatch).
- 운영자 마이그레이션 수동 적용 (TASK-045 v0.2 schema).

## 36. 세션 종료 — 작업 내역 정리 및 다음 세션 준비

- 상태: done
- 모드: Session wrap-up | Meta sync
- 우선순위: high
- 담당: Codex
- 영향 문서: `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`, `ai-workflow/memory/active/backlog/2026-07-03.md`

### 36.1 계획 (Plan)
- 작업 목표: 사용자가 "작업 내역 정리하고 세션 종료 준비하자" 요청. 본 세션은 신규 코드 작업이 없었으므로 (a) 현재 main HEAD 와 메타 문서 정합성 확인, (b) 다음 PR #13 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete) 착수 가능 상태 봉인, (c) 후속 follow-up 명시.

### 36.2 실행 (Act)
- 진행 현황: `2026-07-03` main HEAD `7c7297f` (PR #12 squash merge sync) clean. §35 PR #12 (TASK-050 build phase progress timeline) merge 후 메타 동기화 commit 까지 완료된 상태.
- 작업 트리: clean. untracked 0, modified 0.
- 브랜치 상태: main 만 active local. codex/phase-progress-2026-07-03 는 PR merge --delete-branch 로 정리됨. codex/skills-mcp-dev-2026-07-03 / codex/task-017-scaffold-review 는 잔존 (이전 세션 잔재, 후속 TASK 에서 사용 가능성 검토).
- 본 세션에서 code 변경 없음. 메타 문서 (state.json / session_handoff.md / work_backlog.md / backlog §36) 동기화 commit 1건 예정.
- 후속 (PR #13 후보 항목):
  - (b) TASK-017 stdio transport — apps/skill_mcp/mcp_servers/{build_log_tail, failure_summary, latest_build_status, ...}/cli.py 추가 (core.py wrapper). MCP.md 의 `transport_ready=false` 해소.
  - (c) TASK-037 sweeper — BuildService TTL 만료 sweeper (expiresAt 기반 old build/queue cleanup). tests 추가.
  - (d) admin owner block-delete — BuildService.deleteUserBlockByAdmin (or admin block user → user.build list purge). Header X-Admin-Id 가드. createAdminAllowList 패턴 reuse.

### 36.3 검증 (Validate)
- 테스트 결과: 본 세션 신규 code 없음 → 회귀 동일 baseline (build-server 73/73 + build-monitor 51/51 + Go 13/13 + Python 215/215 + svelte-check 0/0 = 352+ OK).
- 메타 정합성: main HEAD `7c7297f` (docs commit) 와 state.json purpose_digest_rev 60, handoff rev 33, work_backlog rev 34 일치.
- §35 (PR #12) 가 마지막 작업 섹션임을 확인.

### 36.4 결과 (Result)
- 작업 결과: 메타 문서 동기화 (rev 60→61, 33→34, 34→35) + 본 §36 추가 commit. 다음 세션은 PR #13 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete) 즉시 착수 가능.
- 다음 세션 시작 포인트: PR #13 branch 생성 (`codex/pr13-stdio-sweeper-admin-2026-07-03` 등) → backend sweep · admin delete · skill_mcp stdio 3축 구현 → 회귀 → PR 오픈 → 리뷰 → 머지.
- 남은 리스크 및 후속 작업:
  - TASK-051 postgres phase_history column 추가 (jsonb) + 마이그레이션 SQL + read path.
  - visual QA PNG baseline 실측 (Playwright + dev server + pixelmatch).
  - 운영자 마이그레이션 수단 적용 (TASK-045 v0.2 schema).
  - `codex/skills-mcp-dev-2026-07-03`, `codex/task-017-scaffold-review` 잔존 브랜치 후속 정리 시점 결정.
  - docker.BuildImage noop → 실제 image build / docker run / docker stop 후속.

## 37. TASK-065: CodeWhale overlay 배포

### 37.1 계획 (Plan)
- 트리거: 사용자 요청 "codewhale 용 배포를 상위폴더의 docker-image-builder-system에 배포하자".
- 전제: 상위 `standard_ai_workflow` 가 v0.11.22 (commit `cf0060d`) 에서 CodeWhale 하네스 지원을 추가함 — `.codewhale/skills/codewhale-workflow/SKILL.md` 단일 진입점 (Constitution additive rule). 부수 변경: `workflow-source/harnesses/codewhale/{README.md, apply_guide.md}` + `bootstrap_lib/harnesses/__init__.py` 의 HARNESS_SPECS/codewhale entry + `bootstrap_lib/harnesses/renderers.py` 의 `render_codewhale_skill()` / `write_codewhale_harness_files()`.
- 범위: 본 저장소에 CodeWhale 진입점 추가. 기존 root 진입점 (AGENTS.md / MiniMax.md) 및 메모리 state docs (state.json / session_handoff.md / work_backlog.md / assessment) 무변경.
- 핵심 결정: `bootstrap_workflow_kit.py --harness codewhale --adoption-mode existing` 의 dry-run 결과 `generated_files` 가 메모리 8종을 overwrite 하려 함 (rev 61 누락 위험). 따라서 `render_codewhale_skill()` 직접 호출로 우회 — 단일 SKILL.md 만 emit.

### 37.2 수행 (Implementation)
- 1. `render_codewhale_skill()` 직접 호출 (Path: `/home/yklee/repos/standard_ai_workflow/workflow-source/scripts/bootstrap_lib/harnesses/renderers.py:1573`). context 에 state.json 의 real commands 주입: smoke_check=`node --version`, lint/type_check=`tsc --noEmit` 4 packages, quick_test=위 + `(cd apps/runner && go build ./...)`.
- 2. 출력: `/home/yklee/repos/docker-image-builder-system/.codewhale/skills/codewhale-workflow/SKILL.md` (3,857 bytes). 한국어 보고 원칙, 백로그 관리 패턴, Constitution 보강 항목, 기본 검증 명령 포함.
- 3. 메모리 sync: state rev 61→62, handoff rev 34→35, work_backlog rev 35→36, TASK-052 추가, §37 본 entry 추가.
- 4. 산출물 검증 (sanity): 파일 존재 확인, 기존 AGENTS.md / MiniMax.md / state.json / session_handoff.md / work_backlog.md 무변경 (diff 추적).
- 5. commit message: `chore(harness): CodeWhale overlay 배포 (v0.10.4 SKILL.md 추가)` 예정.

### 37.3 검증 (Validate)
- 테스트 결과: 신규 1 파일 (`.codewhale/skills/codewhale-workflow/SKILL.md`) 만 추가. 빌드/테스트 영향 없음 (코드 변경 0).
- 메타 정합성: main HEAD 그대로, state.json purpose_digest_rev 62, handoff rev 35, work_backlog rev 36 으로 동기화. 기존 TASK-001~TASK-050 무손실.
- Constitution 호환: SKILL.md §"중요 — Constitution 과의 관계" 에 Article II/III/Regulations/Statutes 중복 주입 금지 명시. 검증/병렬화/컨텍스트 관리/오케스트레이션 규칙은 Constitution 신뢰.
- 적용 후 체크리스트 (apply_guide §7) — (a) `.codewhale/skills/codewhale-workflow/SKILL.md` 존재 ✓, (b) `ai-workflow/memory/active/` 문서 세트 존재 ✓ (기존), (c) profile 문서 명령 실제 저장소 기준 채움 ✓ (state.json commands 주입), (d) 첫 backlog/handoff 비어있지 않음 ✓ (rev 35/36).
- 미수행: CodeWhale 이 `.codewhale/skills/` 자동 인식 여부는 실제 CodeWhale 세션에서 확인 필요 (TASK-052 후속).

### 37.4 결과 (Result)
- 작업 결과: `.codewhale/skills/codewhale-workflow/SKILL.md` 단일 파일 emit + 메모리 3종 sync + TASK-052/§37 기록. 1 commit.
- 다음 세션 시작 포인트: 본 overlay 가 Codex / MiniMax 와 공존하므로, 다음 CodeWhale 세션에서 `> .codewhale/skills/codewhale-workflow/SKILL.md 를 읽고, ai-workflow/memory/active/state.json 을 기준으로 워크플로우 세션을 시작해줘.` 로 활성화 가능. 검증 명령은 SKILL.md §7 참고.
- 남은 리스크 및 후속 작업:
  - TASK-052 후속: 첫 CodeWhale 세션에서 SKILL.md 활성 확인 (실제 인식 + 검증 명령 동작).
  - PR #13 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete) 진행 가능.
  - TASK-051 postgres phase_history column 추가.
  - visual QA PNG baseline 실측.
  - 운영자 마이그레이션 수동 적용 (TASK-045 v0.2 schema).
  - apply_guide §9 의 "여러 하네스 진입 파일을 CodeWhale 용과 혼용하는 것" 주의 — 본 세션은 additive overlay 이므로 위반 아님, 단 향후 동일 entry point 영역 중복 시 정리 검토.

## 37. TASK-065: CodeWhale overlay 배포

### 37.1 계획 (Plan)
- 트리거: 사용자 요청 "codewhale 용 배포를 상위폴더의 docker-image-builder-system에 배포하자".
- 전제: 상위 `standard_ai_workflow` 가 v0.11.22 (commit `cf0060d`) 에서 CodeWhale 하네스 지원을 추가함 — `.codewhale/skills/codewhale-workflow/SKILL.md` 단일 진입점 (Constitution additive rule). 부수 변경: `workflow-source/harnesses/codewhale/{README.md, apply_guide.md}` + `bootstrap_lib/harnesses/__init__.py` 의 HARNESS_SPECS/codewhale entry + `bootstrap_lib/harnesses/renderers.py` 의 `render_codewhale_skill()` / `write_codewhale_harness_files()`.
- 범위: 본 저장소에 CodeWhale 진입점 추가. 기존 root 진입점 (AGENTS.md / MiniMax.md) 및 메모리 state docs (state.json / session_handoff.md / work_backlog.md / assessment) 무변경.
- 핵심 결정: `bootstrap_workflow_kit.py --harness codewhale --adoption-mode existing` 의 dry-run 결과 `generated_files` 가 메모리 8종을 overwrite 하려 함 (rev 61 누락 위험). 따라서 `render_codewhale_skill()` 직접 호출로 우회 — 단일 SKILL.md 만 emit.

### 37.2 수행 (Implementation)
- 1. `render_codewhale_skill()` 직접 호출 (Path: `/home/yklee/repos/standard_ai_workflow/workflow-source/scripts/bootstrap_lib/harnesses/renderers.py:1573`). context 에 state.json 의 real commands 주입: smoke_check=`node --version`, lint/type_check=`tsc --noEmit` 4 packages, quick_test=위 + `(cd apps/runner && go build ./...)`.
- 2. 출력: `/home/yklee/repos/docker-image-builder-system/.codewhale/skills/codewhale-workflow/SKILL.md` (3,857 bytes). 한국어 보고 원칙, 백로그 관리 패턴, Constitution 보강 항목, 기본 검증 명령 포함.
- 3. 메모리 sync: state rev 61→62, handoff rev 34→35, work_backlog rev 35→36, TASK-052 추가, §37 본 entry 추가.
- 4. 산출물 검증 (sanity): 파일 존재 확인, 기존 AGENTS.md / MiniMax.md / state.json / session_handoff.md / work_backlog.md 무변경 (diff 추적).
- 5. commit message: `chore(harness): CodeWhale overlay 배포 (v0.10.4 SKILL.md 추가)` 예정.

### 37.3 검증 (Validate)
- 테스트 결과: 신규 1 파일 (`.codewhale/skills/codewhale-workflow/SKILL.md`) 만 추가. 빌드/테스트 영향 없음 (코드 변경 0).
- 메타 정합성: main HEAD 그대로, state.json purpose_digest_rev 62, handoff rev 35, work_backlog rev 36 으로 동기화. 기존 TASK-001~TASK-050 무손실.
- Constitution 호환: SKILL.md §"중요 — Constitution 과의 관계" 에 Article II/III/Regulations/Statutes 중복 주입 금지 명시. 검증/병렬화/컨텍스트 관리/오케스트레이션 규칙은 Constitution 신뢰.
- 적용 후 체크리스트 (apply_guide §7) — (a) `.codewhale/skills/codewhale-workflow/SKILL.md` 존재 ✓, (b) `ai-workflow/memory/active/` 문서 세트 존재 ✓ (기존), (c) profile 문서 명령 실제 저장소 기준 채움 ✓ (state.json commands 주입), (d) 첫 backlog/handoff 비어있지 않음 ✓ (rev 35/36).
- 미수행: CodeWhale 이 `.codewhale/skills/` 자동 인식 여부는 실제 CodeWhale 세션에서 확인 필요 (TASK-052 후속).

### 37.4 결과 (Result)
- 작업 결과: `.codewhale/skills/codewhale-workflow/SKILL.md` 단일 파일 emit + 메모리 3종 sync + TASK-052/§37 기록. 1 commit.
- 다음 세션 시작 포인트: 본 overlay 가 Codex / MiniMax 와 공존하므로, 다음 CodeWhale 세션에서 `> .codewhale/skills/codewhale-workflow/SKILL.md 를 읽고, ai-workflow/memory/active/state.json 을 기준으로 워크플로우 세션을 시작해줘.` 로 활성화 가능. 검증 명령은 SKILL.md §7 참고.
- 남은 리스크 및 후속 작업:
  - TASK-052 후속: 첫 CodeWhale 세션에서 SKILL.md 활성 확인 (실제 인식 + 검증 명령 동작).
  - PR #13 (TASK-017 stdio + TASK-037 sweeper + admin owner block-delete) 진행 가능.
  - TASK-051 postgres phase_history column 추가.
  - visual QA PNG baseline 실측.
  - 운영자 마이그레이션 수동 적용 (TASK-045 v0.2 schema).
  - apply_guide §9 의 "여러 하네스 진입 파일을 CodeWhale 용과 혼용하는 것" 주의 — 본 세션은 additive overlay 이므로 위반 아님, 단 향후 동일 entry point 영역 중복 시 정리 검토.
## 37. SDLC 문서 정리 — preview 중심 표현 재정렬

- 상태: in_progress
- 모드: Documentation | Review | Meta sync
- 우선순위: high
- 담당: Codex
- 영향 문서: `docs/sdlc/02-concept-refinement.md`, `docs/sdlc/13-skills-and-mcp-plan.md`, `docs/sdlc/decisions/03-preview-auth-policy.md`, `docs/PROJECT_PROFILE.md`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 37.1 계획 (Plan)
- 작업 목표: 외부 요구사항 기준에 맞춰 SDLC 문서의 중심 서사를 "preview URL 제공"에서 "source intake -> build -> container test -> external deployment -> result delivery" 폐루프로 재정렬한다.
- 완료 기준: Step 02, Step 13, baseline decision 03, Project Profile, workflow 메타가 같은 용어 체계와 책임 경계를 가리킨다.

### 37.2 실행 (Act)
- 진행 현황: `2026-07-03` 남은 preview 편향 문서를 직접 재작성하고 workflow 메타를 동기화했다. 이어서 README, legacy 루트 문서, repository assessment, 기획안, HTML 보고 자료까지 같은 기준으로 정렬했다.
- 수정 사항:
  - `docs/sdlc/02-concept-refinement.md`를 build/test/deploy 중심 개념 문서로 재작성
  - `docs/sdlc/13-skills-and-mcp-plan.md`를 preview orchestration 중심에서 build status, runtime readiness, deployment-aware MCP 계획으로 재정렬
  - `docs/sdlc/decisions/03-preview-auth-policy.md`를 runtime access policy 관점으로 재작성
  - `docs/PROJECT_PROFILE.md`의 프로젝트 목적, validation, policy 문구를 새 파이프라인 기준으로 보정
  - `README.md`의 제품 설명, MVP scope, current focus를 canonical SDLC 기준과 일치하도록 보정
  - legacy 루트 문서 6종을 superseded 포인터 문서로 축약해 source-of-truth 경쟁을 제거
  - `docs/report/01-assignment-plan.md`, `docs/report/02-sdlc-review-report.html`, `ai-workflow/memory/active/repository_assessment.md`를 build/test/deploy/result-delivery 서사로 보정
  - 주요 SDLC 제목과 decision related docs를 current canonical 링크로 정리
  - workflow 메타 (`state.json`, `session_handoff.md`, `work_backlog.md`)에 이번 문서 정리 상태 반영

### 37.3 검증 (Validate)
- 테스트 결과:
  - `rtk grep -n "TEST_READY|TEST_DEPLOYING|IMAGE_BUILT|preview service queue|test_deployment|PreviewStatus|build/preview|preview auth|preview URL" docs/sdlc`
  - 결과: 의도적 예외 2건만 `docs/sdlc/03-requirements-baseline.md`에 남음
- 미실행 사유:
  - 문서 정리 작업이므로 애플리케이션 런타임 테스트는 실행하지 않음

### 37.4 결과 (Result)
- 작업 결과: SDLC 핵심 문서, README, legacy 루트 문서, 보고 패키지, workflow 메타가 preview-first 모델에서 build/test/deploy/result-delivery 모델로 한 단계 더 정렬되었다.
- 다음 세션 시작 포인트: active 문서 기준으로는 정합성 보정이 거의 닫혔으므로, 필요하면 file rename (`PREVIEW_POLICY.md`, design filename 등) 여부만 별도 결정하면 된다.
- 남은 리스크 및 후속 작업:
  - 과거 backlog/history 문서에는 preview 용어가 남아 있으나, 이는 당시 작업 기록이므로 일괄 수정 대상에서 제외했다.
  - `docs/PREVIEW_POLICY.md` 같은 legacy 문서명과 `05-build-and-preview-execution-flow.md` 같은 파일명은 내용/역할이 바뀌었으므로 후속 rename 검토가 필요하다.

## 38. 리팩토링 backlog / roadmap / milestone 정의

- 상태: done
- 모드: Planning | Backlog Design | Program Management
- 우선순위: high
- 담당: Codex
- 영향 문서: `docs/sdlc/15-refactoring-roadmap-and-milestones.md`, `docs/sdlc/07-implementation-backlog-baseline.md`, `docs/PROJECT_PROFILE.md`, `ai-workflow/memory/active/work_backlog.md`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/state.json`

### 38.1 계획 (Plan)
- 작업 목표: 문서 기준선 변경 이후 실제 코드 refactor 와 기능 개발을 함께 운영할 수 있도록 backlog, 전체 개발 로드맵, milestone, 진척 관리 규칙을 정의한다.
- 완료 기준: 어떤 task 를 어떤 milestone 아래에서 관리할지, 세션/PR/기능 개발을 어떤 기준으로 추적할지 운영 문서에 명시된다.

### 38.2 실행 (Act)
- 진행 현황: `2026-07-03` 기존 Step 07 backlog와 backend plan을 바탕으로 refactor 중심 운영 문서와 새 task 큐를 정의했다.
- 수정 사항:
  - `docs/sdlc/15-refactoring-roadmap-and-milestones.md` 신규 작성
  - M1 Contract Reset ~ M5 Deployment Capability milestone 정의
  - `TASK-051`~`TASK-064` backlog 축 정의
  - `docs/sdlc/07-implementation-backlog-baseline.md`에 refactor 운영 기준과 active task 연결
  - `docs/PROJECT_PROFILE.md`에 Step 15 경로 추가
  - `work_backlog.md`, `state.json`, `session_handoff.md`에 새 backlog / active queue / planning 상태 반영

### 38.3 검증 (Validate)
- 테스트 결과:
  - 관련 문서 간 cross-link 와 milestone/task naming 정합성 확인
  - `work_backlog.md`와 `state.json`에 `TASK-051`~`TASK-064`가 반영됐는지 확인
- 미실행 사유:
  - 계획/운영 문서 작업이므로 런타임 테스트는 실행하지 않음

### 38.4 결과 (Result)
- 작업 결과: 이제부터는 refactor 와 기능 개발을 ad-hoc 로 진행하지 않고, Step 15 roadmap 과 milestone 기준으로 관리할 수 있게 됐다.
- 다음 세션 시작 포인트: `M1 Contract Reset` 기준으로 `TASK-051`과 `TASK-052`를 먼저 열고, contract/schema 변경이 닫힌 뒤 `TASK-054` Build Server refactor 로 넘어간다.
- 남은 리스크 및 후속 작업:
  - 현재 backlog는 운영 기준선이며, 실제 구현 진행에 따라 task 병합/분할 조정이 필요할 수 있다.
  - milestone progress dashboard 는 아직 문서형 기준만 존재하므로, 필요하면 별도 tracker 형식으로 확장할 수 있다.

## 39. TASK-051 postgres phase history persistence 보강

- 상태: done
- 모드: Implementation | Persistence | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `packages/db/src/schema/build-request.ts`, `packages/db/src/bootstrap.ts`, `apps/build-server/migrations/0002_phase_history.sql`, `apps/build-server/src/repositories/postgres-build-repository.ts`, `apps/build-server/src/repositories/phase-history.ts`, `apps/build-server/tests/phase-history.test.ts`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 39.1 계획 (Plan)
- 작업 목표: `TASK-050`에서 임시 fallback 으로 남겨둔 postgres timeline 응답을 실제 persisted `phase_history` 기반으로 전환한다.
- 완료 기준: DB schema/bootstrap/migration/repository/read path 가 모두 같은 `phase_history` 구조를 사용하고, 최소 회귀 테스트가 통과한다.

### 39.2 실행 (Act)
- 진행 현황: `2026-07-03` M1 Contract Reset 첫 구현 항목으로 postgres persistence 경로를 정리했다.
- 수정 사항:
  - `packages/db/src/schema/build-request.ts`에 `phase_history` JSONB 컬럼 추가 (`[]` default)
  - `packages/db/src/bootstrap.ts`의 greenfield bootstrap DDL 에 동일 컬럼 추가
  - `apps/build-server/migrations/0002_phase_history.sql` 신규 작성
  - `apps/build-server/src/repositories/phase-history.ts` 신규 추가: normalize / advance / timeline helper 공통화
  - `apps/build-server/src/repositories/postgres-build-repository.ts`에서 create/claim/update/preview queue/preview status 전이 시 `phaseHistory`를 실제 저장하고, 모든 `BuildStatusResponse`를 persisted history 기반으로 응답하도록 정리
  - `apps/build-server/tests/phase-history.test.ts` 신규 추가로 helper semantics 고정

### 39.3 검증 (Validate)
- 테스트 결과:
  - `cd apps/build-server && rtk node --import tsx --test tests/phase-history.test.ts`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts`
  - `cd apps/build-server && rtk node --import tsx --test tests/memory-preview.test.ts`
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
- 검증 메모:
  - `apps/build-server` package 기준 25 tests 통과 (`phase-history` 4, `build-routes` 12, `memory-preview` 9)
  - `pnpm exec` 경로는 현재 workspace 의 `ERR_PNPM_IGNORED_BUILDS` 정책 때문에 install 단계로 되돌아가므로 direct binary 호출로 우회

### 39.4 결과 (Result)
- 작업 결과: postgres repository 가 더 이상 단일 phase fallback 응답에 의존하지 않고 실제 `phase_history`를 읽고 쓰는 상태가 되었다.
- 다음 세션 시작 포인트: `TASK-052` shared-contract status/response refactor 로 넘어가 preview-era status/payload 를 build/test/deploy/result-delivery 모델로 재정렬한다.
- 남은 리스크 및 후속 작업:
  - 기존 live Postgres brownfield 에는 `0002_phase_history.sql` 적용이 필요하다.
  - 현재 persisted history 는 build_request row 안 JSONB 배열로 보관한다. `TASK-053`에서 test/deploy domain split 시 데이터 분리 여부를 다시 판단한다.

## 40. TASK-052 shared-contract status/response refactor

- 상태: done
- 모드: Implementation | Contract | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `packages/shared-contract/src/build/status.ts`, `packages/shared-contract/src/build/request.ts`, `packages/shared-contract/src/build/response.ts`, `apps/build-server/tests/shared-contract-response.test.ts`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 40.1 계획 (Plan)
- 작업 목표: preview-era shared contract 를 문서 기준의 build/test/deploy/result-delivery 모델로 확장하되, 현재 server/UI 구현이 바로 깨지지 않도록 migration shim 범위를 명시한다.
- 완료 기준: canonical status/response 모델이 shared-contract 에 추가되고, legacy preview 필드가 유지된 채 타입체크와 최소 parse 회귀가 통과한다.

### 40.2 실행 (Act)
- 진행 현황: `2026-07-03` M1 Contract Reset 두 번째 구현 항목으로 shared-contract status/response 모델을 정리했다.
- 수정 사항:
  - `packages/shared-contract/src/build/status.ts`에 `canonicalBuildStatuses`, `legacyBuildStatuses`, `executionStatuses` 추가
  - `buildStatuses`를 canonical + legacy union 으로 재구성해 현재 server 구현과 다음 단계 contract reset 을 함께 수용
  - `packages/shared-contract/src/build/request.ts`의 `BuildListQuery.status`를 hard-coded 혼합 enum 에서 `buildStatuses` 기반으로 정리
  - `packages/shared-contract/src/build/response.ts`에 `BuildLifecycle`, `BuildImage`, `ContainerTestResult`, `DeploymentResult`, `ResultDelivery` schema/type 추가
  - `BuildStatusResponse`에 `lifecycle`, `image`, `test`, `deploy`, `resultDelivery` optional block 추가
  - 기존 `previewStatus`, `previewUrl`, `TestDeployment`는 migration shim 으로 유지하고 description 에 deprecated 역할을 명시
  - `apps/build-server/tests/shared-contract-response.test.ts` 신규 추가: legacy preview 응답 parse + canonical lifecycle 응답 parse + canonical query status parse 검증

### 40.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/shared-contract-response.test.ts`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/phase-history.test.ts`
- 검증 메모:
  - shared-contract direct typecheck 통과
  - shared-contract shim test 3/3 통과
  - 기존 build-server focused regression 25/25 통과

### 40.4 결과 (Result)
- 작업 결과: shared-contract 에 새 canonical build/test/deploy/result-delivery 응답 블록이 추가됐고, legacy preview 응답은 migration shim 으로 유지되어 다음 단계 (`TASK-053`, `TASK-054`, `TASK-060`)가 점진적으로 이동할 수 있는 상태가 됐다.
- 다음 세션 시작 포인트: `TASK-053`에서 DB schema 를 `build_test`, `deployment_attempt` 축으로 나누고, `previewStatus` 계열 legacy field 를 어떤 방식으로 축소할지 migration 방향을 고정한다.
- 남은 리스크 및 후속 작업:
  - 현재 server repository/service/routes 는 새 canonical block 을 아직 populate 하지 않는다. 실제 값 연결은 `TASK-054`에서 진행한다.
  - build-monitor/generated OpenAPI client 는 아직 legacy summary/detail 중심이다. consumer 전환은 `TASK-060`에서 수행한다.

## 41. TASK-053 DB schema split

- 상태: done
- 모드: Implementation | Persistence | Migration
- 우선순위: high
- 담당: Codex
- 영향 문서: `packages/db/src/schema/build-test.ts`, `packages/db/src/schema/deployment-attempt.ts`, `packages/db/src/schema/build-request.ts`, `packages/db/src/bootstrap.ts`, `packages/db/src/index.ts`, `apps/build-server/migrations/0003_build_test_and_deployment_attempt.sql`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 41.1 계획 (Plan)
- 작업 목표: data model 설계 문서 기준으로 `build_test`, `deployment_attempt`를 DB 레벨에 먼저 도입하고, 기존 preview-era 저장 구조는 다음 server migration 전까지 병행 유지한다.
- 완료 기준: `packages/db`와 bootstrap/migration SQL 이 새 canonical 테이블을 가지며, 현재 build-server 코드가 깨지지 않는다.

### 41.2 실행 (Act)
- 진행 현황: `2026-07-03` M1 Contract Reset 세 번째 구현 항목으로 DB schema split 기준선을 반영했다.
- 수정 사항:
  - `packages/db/src/schema/build-test.ts` 신규 추가
  - `packages/db/src/schema/deployment-attempt.ts` 신규 추가
  - 각 테이블에 `build_id` unique index 와 `(status, created_at)` index 추가
  - `packages/db/src/bootstrap.ts`에 `build_test`, `deployment_attempt` CREATE TABLE / INDEX DDL 추가
  - `packages/db/src/index.ts` export 추가
  - `apps/build-server/migrations/0003_build_test_and_deployment_attempt.sql` 신규 추가
  - `packages/db/src/schema/build-request.ts`의 `previewStatus`, `previewTtlMinutes`, `previewUrl`에 legacy shim 주석 추가
  - 기존 `test_deployment` bootstrap/table 경로는 현재 repository/service 의존 때문에 유지

### 41.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts`
- 검증 메모:
  - `packages/db` direct typecheck 통과
  - `apps/build-server` direct typecheck 통과
  - focused regression 28/28 통과

### 41.4 결과 (Result)
- 작업 결과: DB 레벨에서 canonical `build_test`/`deployment_attempt` 저장 모델이 열렸고, legacy preview-era 저장 구조는 다음 단계 migration 을 위해 병행 유지되는 상태가 됐다.
- 다음 세션 시작 포인트: `TASK-054`에서 repository/service/routes 를 새 contract + 새 schema split 기준으로 적응시키고, 어느 시점부터 `test_deployment`를 읽지 않도록 전환할지 결정한다.
- 남은 리스크 및 후속 작업:
  - 현재 postgres repository 는 아직 `build_test`/`deployment_attempt`를 사용하지 않는다.
  - `test_deployment`와 `build_request.preview_*` 제거/backfill 전략은 `TASK-054`와 `TASK-055` 사이에서 확정해야 한다.

## 42. TASK-054 Build Server repository/service/API refactor

- 상태: done
- 모드: Implementation | Backend Refactor | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `apps/build-server/src/repositories/build-status-response.ts`, `apps/build-server/src/repositories/memory-build-repository.ts`, `apps/build-server/src/repositories/postgres-build-repository.ts`, `apps/build-server/src/app/openapi.ts`, `apps/build-server/tests/build-routes.test.ts`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 42.1 계획 (Plan)
- 작업 목표: 새 shared-contract canonical block (`lifecycle/image/test/deploy/resultDelivery`)을 Build Server 실제 응답에 채우고, postgres 쪽은 새 `build_test` schema 로 병행 기록을 시작한다.
- 완료 기준: memory/postgres repository 응답이 새 canonical block 을 포함하고, focused 회귀가 통과한다.

### 42.2 실행 (Act)
- 진행 현황: `2026-07-03` Build Server 적응 계층을 refactor 했다.
- 수정 사항:
  - `apps/build-server/src/repositories/build-status-response.ts` 신규 추가
  - legacy `phase`/`status`/`previewStatus`/`testDeployment`를 canonical `lifecycle/test/deploy/resultDelivery`로 투영하는 helper 도입
  - memory repository 의 create/get/claim/update/preview queue/preview ready 응답을 공통 helper 기반으로 정리
  - postgres repository 의 `mapBuildRowToSummary`를 `row.appName` 직접 사용으로 바로잡음
  - postgres repository 응답도 canonical block 을 실제 populate 하도록 정리
  - postgres queue/report preview 경로에서 `build_test` 테이블 병행 write 시작
  - OpenAPI component registry 에 `BuildLifecycle`, `BuildCurrentPhase`, `BuildImage`, `ContainerTestResult`, `DeploymentResult`, `ResultDelivery` 등록 추가
  - route test 에 canonical field assertion 추가

### 42.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts`
- 검증 메모:
  - build-server focused regression 49/49 통과
  - shared-contract / db / build-server direct typecheck 통과

### 42.4 결과 (Result)
- 작업 결과: Build Server 가 legacy preview-era payload 를 유지하면서도 새 canonical lifecycle/test/deploy/resultDelivery block 을 실제 응답에 제공하는 상태가 됐다.
- 다음 세션 시작 포인트: `TASK-056` Runner phase model refactor 또는 `TASK-060` consumer 정렬로 넘어간다.
- 남은 리스크 및 후속 작업:
  - `deployment_attempt`는 아직 default block 만 채우며 실제 write 는 시작하지 않았다.
  - `build_test`는 postgres 병행 write 만 시작했고 조회/read-path 통합은 후속 단계에서 더 정교화할 수 있다.

## 43. TASK-055 OpenAPI regeneration and query contract stabilization

- 상태: done
- 모드: Implementation | OpenAPI | Consumer Sync
- 우선순위: high
- 담당: Codex
- 영향 문서: `apps/build-monitor/.generated/openapi.d.ts`, `apps/build-monitor/src/lib/api.ts`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 43.1 계획 (Plan)
- 작업 목표: Build Server 의 새 OpenAPI contract 를 실제 generated client 타입으로 재동기화하고, build-monitor query/client 코드가 stale hand-typed mirror 대신 generated schema 를 쓰게 만든다.
- 완료 기준: `.generated/openapi.d.ts`가 최신 `/openapi.json`과 일치하고, build-monitor typecheck 가 통과한다.

### 43.2 실행 (Act)
- 진행 현황: `2026-07-03` memory backend build-server 를 임시 기동해 `/openapi.json` 기준 regenerated types 를 생성했다.
- 수정 사항:
  - build-server memory backend 임시 기동 후 `apps/build-monitor/scripts/generate-openapi.ts` 실행
  - `apps/build-monitor/.generated/openapi.d.ts` 재생성
  - `apps/build-monitor/src/lib/api.ts`의 hand-typed `BuildStatusResponse` mirror 제거
  - `BuildPhaseHistoryEntry`, `BuildCurrentPhase`, `BuildStatusResponse`를 generated component alias 로 전환

### 43.3 검증 (Validate)
- 테스트 결과:
  - `cd apps/build-monitor && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk node ./node_modules/svelte-check/bin/svelte-check --tsconfig ./tsconfig.json`
  - `cd apps/build-monitor && rtk ./node_modules/.bin/vitest run`
- 검증 메모:
  - build-monitor direct typecheck 통과
  - `svelte-check` 0 errors / 0 warnings
  - `vitest`는 localStorage 미구성 환경으로 기존 38 failure 가 동일 재현. 이번 변경으로 새 failure 증가 없음.

### 43.4 결과 (Result)
- 작업 결과: build-monitor 가 새 canonical response 모델을 generated OpenAPI 타입으로 직접 따라가게 됐고, stale hand-typed detail mirror 하나를 제거했다.
- 다음 세션 시작 포인트: 필요 시 vitest 환경(localStorage bootstrap) 정리 후 consumer refactor 를 이어가거나, 현 기준선으로 PR 정리한다.
- 남은 리스크 및 후속 작업:
  - build-monitor test environment 의 localStorage bootstrap 문제는 별도 정리 대상이다.
  - admin hand-typed response block 은 여전히 남아 있으며, OpenAPI generator/consumer 정비가 더 진전되면 후속 제거 가능하다.

## 44. TASK-056 Runner phase model refactor

- 상태: done
- 모드: Implementation | Runner | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `apps/runner/internal/hostclient/build_control_client.go`, `apps/runner/internal/hostclient/build_control_client_test.go`, `apps/runner/internal/queue/claimer.go`, `apps/runner/internal/services/build_service.go`, `apps/runner/internal/services/build_service_test.go`, `apps/runner/internal/worker/worker.go`, `apps/runner/internal/worker/worker_test.go`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 44.1 계획 (Plan)
- 작업 목표: Runner 의 claim/build/test 보고 흐름에서 남아 있던 preview-era/legacy 가정(`default-runner`, `projectId/repositoryId`, duplicate preview queue call)을 정리하고, 새 contract reset 이후의 기준선에 맞춘다.
- 완료 기준: Runner client/service/worker 가 새 claim payload shape 를 소비하고, Go 회귀가 통과한다.

### 44.2 실행 (Act)
- 진행 현황: `2026-07-03` PR 전 리팩토링 묶음의 마지막으로 Runner phase model 을 정리했다.
- 수정 사항:
  - `HTTPBuildControlClient`가 생성 시 `runnerID`를 받아 claim 요청에 실제 전달하도록 수정
  - `ClaimedBuildResponse`에서 legacy `projectId/repositoryId` 제거
  - claim 응답 파싱에 `appName`, `lifecycleStatus` 추가
  - `queue.ClaimedBuild`가 `AppName`, `Status`, `Phase`를 함께 담도록 확장
  - `worker.New`가 `NewHTTPBuildControlClient(cfg.HostServerBaseURL, cfg.RunnerID)`를 사용하도록 수정
  - `BuildService.ProcessClaim`의 duplicate preview queue call 제거
  - hostclient / build_service / worker 테스트 기대값 갱신

### 44.3 검증 (Validate)
- 테스트 결과:
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts`
- 검증 메모:
  - Go test: 13 passed in 8 packages
  - build-server focused regression 49/49 통과

### 44.4 결과 (Result)
- 작업 결과: Runner claim/build/test 보고 흐름이 새 contract reset 이후의 기준선에 맞춰 한 단계 정리됐고, 리팩토링 PR 묶음이 자연스럽게 닫히는 상태가 됐다.
- 다음 세션 시작 포인트: `TASK-057` docker build no-op 제거, `TASK-058` container test result reporting, 또는 현재 기준선으로 PR 정리.
- 남은 리스크 및 후속 작업:
  - Runner 는 아직 real docker build / real container test result 를 canonical block 으로 직접 보고하지 않는다.
  - `deployment_attempt` write 와 deploy adapter 는 여전히 후속 단계다.

## 45. TASK-057 docker build no-op 제거 및 source prepare/build/test skeleton 고도화

- 상태: done
- 모드: Implementation | Runner | Runtime Skeleton
- 우선순위: high
- 담당: Codex
- 영향 문서: `apps/runner/internal/docker/client.go`, `apps/runner/internal/docker/client_test.go`, `apps/runner/internal/services/build_service.go`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 45.1 계획 (Plan)
- 작업 목표: Runner 의 `docker.BuildImage` no-op 을 제거하고, source prepare/build skeleton 이 실제 파일 시스템 side effect 를 가지도록 만든다.
- 완료 기준: Runner 가 source workspace 를 만들고 build context artifact 를 생성하며, 필요 시 `cli` 모드에서 실제 `docker build` 시도까지 가능한 상태가 된다.

### 45.2 실행 (Act)
- 진행 현황: `2026-07-03` M3 Runtime workstream 첫 실제 skeleton 작업으로 docker client 를 고도화했다.
- 수정 사항:
  - `apps/runner/internal/docker/client.go` 전면 재작성
  - `PrepareSource` 추가: workspace 디렉터리 + `src/source-prepared.txt` 생성
  - `BuildImage` 추가 동작: `Dockerfile` + `build-manifest.json` 생성
  - 기본 `RUNNER_DOCKER_BUILD_MODE=skeleton` 에서는 file-system skeleton 만 생성
  - `RUNNER_DOCKER_BUILD_MODE=cli` 에서는 동일 컨텍스트에 대해 `docker build` 시도
  - `RUNNER_WORKSPACE_ROOT`, `RUNNER_DOCKER_BIN` env override 추가
  - `BuildService.ProcessClaim`가 source prepare 후 `SOURCE_PREPARED` phase 를 보고하도록 정리
  - docker client unit test 2종 추가

### 45.3 검증 (Validate)
- 테스트 결과:
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts`
- 검증 메모:
  - Go test: 15 passed in 8 packages
  - build-server focused regression 49/49 통과

### 45.4 결과 (Result)
- 작업 결과: Runner runtime skeleton 이 더 이상 pure no-op 이 아니며, source prepare/build 단계가 실제 workspace/build artifact 를 남기는 상태가 됐다.
- 다음 세션 시작 포인트: `TASK-058` container test result reporting 또는 현재 기준선으로 PR 정리.
- 남은 리스크 및 후속 작업:
  - 기본값은 여전히 `skeleton` 모드이며, real source archive fetch / real docker image semantics 는 후속 단계다.
  - container test result 를 canonical `test` block 으로 구체적으로 채우는 일은 `TASK-058`에서 진행한다.

## 46. TASK-058 container test result reporting

- 상태: done
- 모드: Implementation | Contract | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `packages/shared-contract/src/build/response.ts`, `apps/build-server/src/repositories/build-status-response.ts`, `apps/build-server/src/repositories/build-repository.ts`, `apps/build-server/src/repositories/memory-build-repository.ts`, `apps/build-server/src/repositories/postgres-build-repository.ts`, `apps/build-server/src/routes/build-routes.ts`, `apps/build-server/src/services/build-service.ts`, `apps/build-server/tests/build-routes.test.ts`, `apps/build-server/tests/memory-preview.test.ts`, `apps/runner/internal/hostclient/build_control_client.go`, `apps/runner/internal/hostclient/build_control_client_test.go`, `apps/runner/internal/services/build_service.go`, `apps/runner/internal/services/build_service_test.go`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 46.1 계획 (Plan)
- 작업 목표: preview ready 신호를 단순 READY 상태만 넘기는 수준에서 벗어나 canonical container test 결과(`healthCheckPassed`, `portOpen`, `stabilityWindowPassed`)까지 Build Server 응답에 실제 반영되도록 만든다.
- 완료 기준: Runner READY payload 가 최소 test result signal 을 함께 전송하고, Build Server `GET /builds/:id` 응답의 canonical `test` block 이 추정값이 아니라 실제 snapshot 기반으로 채워진다.

### 46.2 실행 (Act)
- 진행 현황: `2026-07-03` runtime skeleton 위에 container test result reporting 을 추가했다.
- 수정 사항:
  - `TestDeploymentReadyRequest`에 `containerRef`, `healthCheckPassed`, `portOpen`, `stabilityWindowPassed` optional 필드 추가
  - Build Server route/service/repository 의 preview ready details shape 를 확장
  - `build-status-response.ts`에 `BuildTestSnapshot`을 도입하고 canonical `test` block 이 `build_test` snapshot 을 우선 사용하도록 정리
  - memory repository 가 queue/ready/fail 경로에서 `buildTest` snapshot 을 유지하도록 수정
  - postgres repository 가 preview ready/fail 시 `build_test`의 `container_ref`, `health_check_passed`, `port_open`, `stability_window_passed`를 write 하고, `getBuild`는 `build_test` left join 으로 read-path 연결
  - Runner `ReportPreviewReady` payload 에 `containerRef`와 test result booleans 포함
  - build-server / runner 테스트 기대값 갱신

### 46.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts`
- 검증 메모:
  - shared-contract direct typecheck 통과
  - Go test: 15 passed in 8 packages
  - build-server focused regression 49/49 통과

### 46.4 결과 (Result)
- 작업 결과: canonical `test` block 이 preview 상태 추정치에서 한 단계 벗어나 실제 container-test snapshot 기반으로 채워지기 시작했다.
- 다음 세션 시작 포인트: `TASK-059` external deployment adapter 또는 `TASK-060` Build Monitor status/UI refactor, 혹은 현재 기준선으로 PR 정리.
- 남은 리스크 및 후속 작업:
  - 현재 Runner 는 skeleton 기반으로 success booleans 를 보고하므로, real health probe / port probe / stability window 측정 로직은 후속 단계다.
  - postgres `getTestDeployment`는 아직 `build_test`의 host/internalPort/expiresAt 를 fully 재사용하지 않으므로 후속 read-path 정밀화 여지가 있다.

## 47. TASK-059 external deployment adapter v1

- 상태: done
- 모드: Implementation | Deployment | Verification
- 우선순위: high
- 담당: Codex
- 영향 문서: `packages/shared-contract/src/build/phase.ts`, `packages/shared-contract/src/build/response.ts`, `packages/shared-contract/src/openapi.ts`, `apps/build-server/src/app/openapi.ts`, `apps/build-server/src/repositories/build-repository.ts`, `apps/build-server/src/repositories/build-status-response.ts`, `apps/build-server/src/repositories/memory-build-repository.ts`, `apps/build-server/src/repositories/postgres-build-repository.ts`, `apps/build-server/src/routes/build-routes.ts`, `apps/build-server/src/services/build-service.ts`, `apps/build-server/tests/build-routes.test.ts`, `apps/build-server/tests/memory-preview.test.ts`, `apps/build-server/tests/openapi.test.ts`, `apps/runner/internal/deploy/client.go`, `apps/runner/internal/deploy/client_test.go`, `apps/runner/internal/hostclient/build_control_client.go`, `apps/runner/internal/hostclient/build_control_client_test.go`, `apps/runner/internal/services/build_service.go`, `apps/runner/internal/services/build_service_test.go`, `apps/runner/internal/worker/worker_test.go`, `apps/build-monitor/.generated/openapi.d.ts`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 47.1 계획 (Plan)
- 작업 목표: container test 성공 이후 external deployment 단계가 실제 계약/저장소/러너 흐름 안에서 first-class 로 기록되도록 최소 adapter v1 을 구현한다.
- 완료 기준: Runner 가 deploy in-progress / success 를 Host Server 에 보고하고, Build Server `GET /builds/:buildId` 응답의 canonical `deploy` / `resultDelivery` block 이 실제 `deployment_attempt` snapshot 기반으로 채워진다.

### 47.2 실행 (Act)
- 진행 현황: `2026-07-03` M5 첫 진입으로 deployment contract, persistence, runner skeleton 을 한 번에 연결했다.
- 수정 사항:
  - shared-contract 에 `DEPLOYMENT_STARTED`, `DEPLOYMENT_COMPLETED`, `DeploymentReportRequest`, OpenAPI `Deployment` tag 추가
  - Build Server 에 `POST /builds/:buildId/deployment` route 추가
  - memory repository 에 deploy snapshot 유지 및 canonical `deploy` / `resultDelivery` block 반영
  - postgres repository 에 `deployment_attempt` write/read-path 연결 (`getBuild` left join 포함)
  - `build-status-response.ts`가 `deploymentAttempt` snapshot 을 받아 canonical `deploy` / `resultDelivery` block 생성
  - Runner `internal/deploy/client.go` skeleton adapter 추가: workspace 아래 `deploy-result.json` 생성
  - Runner `BuildService.ProcessClaim`가 preview ready 이후 deploy in-progress → skeleton deploy → deploy success → completed 순서로 보고
  - build-server OpenAPI regenerated 후 build-monitor generated client 재동기화

### 47.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `cd apps/build-server && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && BUILD_REPOSITORY_BACKEND=memory node --import tsx src/index.ts`
  - `cd apps/build-monitor && rtk node scripts/generate-openapi.ts`
  - `cd apps/build-monitor && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk node ./node_modules/svelte-check/bin/svelte-check --tsconfig ./tsconfig.json`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts tests/openapi.test.ts`
- 검증 메모:
  - shared-contract direct typecheck 통과
  - Go test: 17 passed in 9 packages
  - build-server focused regression 52/52 통과
  - build-monitor generated OpenAPI 타입 갱신 후 direct typecheck + `svelte-check` 0/0 통과

### 47.4 결과 (Result)
- 작업 결과: external deployment 단계가 더 이상 문서상의 개념만이 아니라, Runner/Host Server/DB/OpenAPI 흐름 안에서 최소 adapter v1 으로 연결됐다.
- 다음 세션 시작 포인트: `TASK-060` Build Monitor status/UI refactor, `TASK-061` Skill/MCP contract rename, 또는 현재 기준선으로 PR 정리.
- 남은 리스크 및 후속 작업:
  - 현재 deploy adapter 는 skeleton 으로 `deploy-result.json`과 mock resultRef 를 생성하는 수준이며, real registry push / HTTP API / SCP/SFTP 구현은 후속 단계다.
  - `resultDelivery`는 polling 기본 경로를 반영하지만 notification mode 는 아직 미구현이다.

## 48. PR #13 follow-up 보완 (self-review follow-up)

- 상태: done
- 모드: Review | Hardening | Verification
- 우선순위: high
- 담당: Codex
- 영향 파일: `apps/build-server/src/repositories/postgres-build-repository.ts`, `apps/build-server/migrations/0003_build_test_and_deployment_attempt.sql`, `apps/runner/internal/hostclient/build_control_client.go`, `apps/build-server/src/app/openapi.ts`, `apps/runner/internal/docker/client.go`, `apps/build-server/src/repositories/memory-build-repository.ts`

### 48.1 계획 (Plan)
- 작업 목표: PR #13 self-review 에서 발견된 MED/LOW 이슈 중 본 PR 의 일관성/race/schema 정확성에 해당하는 항목을 amend commit 으로 묶어 머지 차단 이슈를 해소한다. follow-up 으로 미루면 TASK-064 가 무거워지므로 같은 PR 안에서 닫는다.
- 완료 기준:
  - `reportPreviewStatus` / `reportDeploymentResult` 가 `db.transaction` 으로 wrap 되어 partial failure 가 사라진다.
  - `0003_build_test_and_deployment_attempt.sql` 의 `build_test` / `deployment_attempt` 가 `build_request(id)` FK + `DEFAULT gen_random_uuid()` 를 가진다.
  - `getTestDeployment` 가 `build_test` left join 으로 `host`/`hostPort`/`internalPort`/`runtimeUrl` 를 노출한다.
  - Go claim 응답의 `reason` 이 `omitempty` 로 emit 되어 server z.enum invalid 가 사라진다.
  - OpenAPI 의 `POST /builds/:buildId/deployment` 응답에 `BuildStatusResponse` ref 가 명시된다.
  - `docker.BuildImage` 가 marker 기반 idempotent guard 로 바뀌어 ProcessClaim 의 중복 PrepareSource 가 사라진다.
  - 회귀 baseline (TS 4 packages / Go 17 passed / build-server 52/52 / build-monitor svelte-check 0/0) 이 모두 그린을 유지한다.

### 48.2 실행 (Act)
- 진행 현황: PR #13 의 follow-up 보완 7건 amend commit 으로 묶음.
- 수정 사항:
  - `apps/build-server/src/repositories/postgres-build-repository.ts`
    - `reportPreviewStatus` 전체를 `db.transaction` 으로 wrap. 마지막 read 도 transaction 안에서 `buildTestTable` select 로 통일해 응답의 `test` block 이 항상 가장 최근 upsert 와 정합하도록 변경.
    - `reportPreviewStatus` 의 `TestDeployment` 응답 `internalPort` 를 buildTest row 의 `internalPort` 로 채움 (이전엔 hard-coded `null`).
    - `reportDeploymentResult` 전체를 `db.transaction` 으로 wrap. 마지막의 `buildTestRow` + `deploymentRow` 별도 select 를 transaction 안에서 `leftJoin` 한 번으로 통합.
    - `getTestDeployment` 가 `buildTestTable` left join 으로 `host` / `hostPort` / `internalPort` / `runtimeUrl` 를 노출. `getBuild` 의 canonical `ContainerTestResult` 와 정합.
  - `apps/build-server/migrations/0003_build_test_and_deployment_attempt.sql`
    - `build_test.id` / `deployment_attempt.id` 에 `DEFAULT gen_random_uuid()` 추가 (Drizzle schema 의 `defaultRandom()` 와 정합).
    - `build_test.build_id` / `deployment_attempt.build_id` 에 `REFERENCES build_request(id) ON DELETE CASCADE` FK 추가. orphaned row 방지.
  - `apps/runner/internal/hostclient/build_control_client.go`
    - `claimResponseBody.Reason` 에 `omitempty` 추가. server 의 `z.enum([...]).nullable()` 가 빈 string 을 invalid 로 거부하던 잠재 회귀 차단.
  - `apps/build-server/src/app/openapi.ts`
    - `POST /builds/{buildId}/deployment` 의 200 응답에 `buildStatusResponseSchema` ref 명시. 실제 응답은 `BuildStatusResponse` 이므로 spec 정확성 회복.
  - `apps/runner/internal/docker/client.go`
    - `BuildImage` 의 첫 줄 무조건 `PrepareSource` 호출을 idempotent guard 로 교체. marker file (`src/source-prepared.txt`) 이 이미 있으면 skip, 없으면 `PrepareSource`. `BuildService.ProcessClaim` 의 happy path 에서는 PrepareSource 가 먼저 호출되므로 redundant write 가 사라짐.
  - `apps/build-server/src/repositories/memory-build-repository.ts`
    - `reportPreviewStatus` 의 EXPIRED case 주석에 “preview TTL 만료이지 build 자체의 terminal 이 아니며, phaseHistory 도 push 하지 않는다” 는 의도 명시.

### 48.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/shared-config/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p apps/build-server/tsconfig.json --noEmit`
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts tests/openapi.test.ts`
  - `cd apps/build-monitor && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk node ./node_modules/svelte-check/bin/svelte-check --tsconfig ./tsconfig.json --threshold error`
- 검증 메모:
  - TypeScript 4 packages 0 errors.
  - Go: 9 packages OK (deploy / docker / hostclient / services / worker 포함). docker 패키지는 marker guard 분기 추가로 재실행됨 (이전 cached).
  - build-server focused regression 52/52 OK.
  - build-monitor direct `tsc --noEmit` clean + `svelte-check` 0/0.
  - vitest localStorage 미구성 환경의 기존 실패는 이번 보완으로 새로 추가된 거 없음 (회귀 영향 없음).

### 48.4 결과 (Result)
- 작업 결과: PR #13 self-review 에서 발견된 race / schema 정확성 / spec 일관성 결함 7건이 amend commit 으로 닫혔다. 본 PR 의 머지 차단 이슈가 0건이 됐고, follow-up 으로 미뤄둘 항목은 없다.
- 다음 세션 시작 포인트: PR #13 squash merge 직후 TASK-060 Build Monitor status/UI refactor (M4 진입).
- 남은 리스크 및 후속 작업:
  - `runner.internal.deploy` 의 mock URL / targetRef 은 여전히 의도된 skeleton. real registry push / HTTP API / SCP/SFTP 구현은 TASK-064 (smoke / migration / visual QA baseline) 의 후속 또는 별도 adapter task 에서 다룬다.
  - `void payload.capabilities` 는 contract/server intent 정합이 필요해 별도 결정 task. 본 PR 의 contract 변경과 무관한 운영 정책 결정이므로 follow-up 으로 분리한다.

## 49. TASK-060 Build Monitor status model/UI refactor (1차 PR)

- 상태: in_progress
- 모드: Implementation | Frontend | Contract
- 우선순위: high
- 담당: Codex
- 영향 파일: `apps/build-monitor/src/routes/BuildDetail.svelte`, `apps/build-monitor/src/components/BuildRow.svelte` (status mapping), `apps/build-monitor/src/components/StatusPill.svelte` (lifecycleStatus 우선), `apps/build-monitor/src/routes/Login.test.ts` / `BuildsList.test.ts` / `AdminBuilds.test.ts` / `AdminUsers.test.ts` / `BuildRow.test.ts` (fixture), `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 49.1 계획 (Plan)
- 작업 목표: Build Monitor 가 preview-era 응답 (`previewStatus` / `previewUrl`) 에 의존하지 않고, 문서 기준의 canonical `lifecycle` / `test` / `deploy` / `resultDelivery` 4 block 을 실제 UI 에 노출한다. `BuildSummary.lifecycleStatus` (TASK-052 도입) 가 StatusPill/BuildRow 의 status 매핑 1차 기준이 되고, 기존 `status` 와 `previewStatus` 는 fallback / deprecated section 으로 분리한다.
- 완료 기준:
  - `BuildDetail.svelte` 가 canonical 4 block 을 별도 section 으로 노출하고, 기존 preview-era section 은 "Legacy preview" 라는 deprecated 안내와 함께 하단으로 분리한다.
  - `StatusPill` / `BuildRow` 의 status 매핑이 `lifecycleStatus` 우선, `status` fallback 으로 동작한다.
  - vitest fixture (`BuildRow.test.ts` / `BuildsList.test.ts` / `AdminBuilds.test.ts` / `AdminUsers.test.ts`) 가 `lifecycleStatus` + canonical block 을 채운다.
  - 회귀: `tsc --noEmit` + `svelte-check` + vitest 모두 통과.

### 49.2 실행 (Act)
- 진행 현황: `2026-07-03` 브랜치 `codex/task-060-build-monitor-ui-refactor-2026-07-03` (main HEAD `dff3f36`) 에서 1차 PR scope 진입. (변경 진행 중)
- 수정 사항: TBD

### 49.3 검증 (Validate)
- 테스트 결과: TBD

### 49.4 결과 (Result)
- TBD

## 50. PR #14 self-review follow-up 보완

- 상태: done
- 모드: Review | Hardening | Frontend
- 우선순위: high
- 담당: Codex
- 영향 파일: `apps/build-monitor/src/components/StatusPill.svelte`, `apps/build-monitor/src/components/BuildRow.svelte`, `apps/build-monitor/src/routes/BuildDetail.svelte`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 50.1 계획 (Plan)
- 작업 목표: PR #14 self-review 에서 발견된 1 MED + 3 LOW + 1 fallout(consumer 추가) 이슈를 본 PR amend commit 으로 묶어 머지 차단 이슈를 해소한다. TASK-060 2차 PR 로 분리하지 않고 같은 PR 안에서 닫는다.

### 50.2 실행 (Act)
- 진행 현황: PR #14 amend commit 으로 보완 5건 봉인.
- 수정 사항:
  - `apps/build-monitor/src/components/StatusPill.svelte` color mapping — canonical 12-state union 중 `BUILD_SUCCESS` / `TEST_SUCCESS` / `DEPLOY_SUCCESS` / `COMPLETED` 를 `--color-accent-success` 로 통일 (success 의미 색상). `TESTING` / `DEPLOYING` 만 `--color-accent-info` (in-progress 의미) 로 유지. 일관성 회복.
  - `apps/build-monitor/src/routes/BuildDetail.svelte` `.kv` grid — `@media (max-width: 640px)` 1컬럼 fallback 추가. narrow viewport 에서 horizontal scroll 차단.
  - `apps/build-monitor/src/routes/BuildDetail.svelte` canonical lifecycle section h2 — `Lifecycle` → `Build lifecycle` 로 차별화. meta row 의 짧은 `Lifecycle` 라벨과 BuildStatusResponse.lifecycle 객체의 detail snapshot 분리.
  - `apps/build-monitor/src/routes/BuildDetail.svelte` Legacy preview 안내문 — "Container test / Deployment sections above" 만 가리키던 짧은 안내를 canonical section 참조 + removal plan 명시 형태("Removal planned once admin / build-list consumers stop reading these legacy fields")로 명확화.
  - `apps/build-monitor/src/components/BuildRow.svelte` `BuildRowData` — `lifecycleStatus` optional prop 추가. StatusPill 호출에 `lifecycleStatus={build.lifecycleStatus}` 전달. BuildRow consumer (BuildsList / AdminBuilds) 가 lifecycleStatus prop 까지 전달하면 자연스럽게 fallback chain 이 BuildDetail 과 정합.

### 50.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p apps/build-server/tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk node ./node_modules/svelte-check/bin/svelte-check --tsconfig ./tsconfig.json --threshold error`
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts tests/openapi.test.ts`
- 검증 메모:
  - TypeScript 4 packages 0 errors.
  - build-monitor tsc + svelte-check 0/0.
  - Go 9 packages OK.
  - build-server focused regression 52/52.
  - vitest localStorage 미구성 환경의 기존 실패는 이번 보완으로 새로 추가된 거 없음 (회귀 영향 없음).

### 50.4 결과 (Result)
- 작업 결과: PR #14 self-review 보완 5건 (StatusPill 색상 통일 + BuildDetail .kv breakpoint + section 이름 차별화 + Legacy preview 안내문 명확화 + BuildRow lifecycleStatus prop) amend commit 으로 봉인. 본 PR 의 머지 차단 이슈가 0건. 다음: squash merge 후 TASK-060 2차 PR (fixture / BuildList / AdminBuilds / AdminUsers status 매핑) 또는 TASK-061 / TASK-062.
- 다음 세션 시작 포인트: PR #14 squash merge sync 후 TASK-060 2차 또는 TASK-061.
- 남은 리스크 및 후속 작업:
  - 본 PR 의 BuildList / AdminBuilds / AdminUsers 의 StatusPill 호출은 여전히 `status` 만 전달 (consumer 가 BuildRow 사용 시 lifecycleStatus 자동 forwarding 됨). TASK-060 2차 PR 에서 fixture 갱신 + admin 라우트 status filter 정렬.

## 51. TASK-060 2차 PR (PR #15) — fixture 갱신 + AdminUsers StatusPill 통일

- 상태: done
- 모드: Implementation | Frontend | Test
- 우선순위: high
- 담당: Codex
- 영향 파일: `apps/build-monitor/src/components/BuildRow.test.ts`, `apps/build-monitor/src/routes/BuildsList.test.ts`, `apps/build-monitor/src/routes/AdminBuilds.test.ts`, `apps/build-monitor/src/routes/AdminUsers.test.ts`, `apps/build-monitor/src/routes/AdminUsers.svelte`, `ai-workflow/memory/active/state.json`, `ai-workflow/memory/active/session_handoff.md`, `ai-workflow/memory/active/work_backlog.md`

### 51.1 계획 (Plan)
- 작업 목표: PR #14 의 follow-up 보완 5건 중 consumer 측 마무리 — vitest 4 file 의 sample data 에 `lifecycleStatus` (TASK-052) 추가 + `AdminUsers.svelte` recent builds panel 의 직접 status `<span>` 을 `StatusPill` component 로 교체. `BuildRow` lifecycleStatus prop (PR #14 amend) 가 consumer 에서 일관되게 forwarding 되는지 회귀 fixture 로 검증.
- 완료 기준:
  - 4개 vitest file 의 sample data 에 `lifecycleStatus` 추가 (optional). `BuildRow.test.ts` 에 lifecycleStatus 우선 표시 + legacy fallback 새 case 추가.
  - `AdminUsers.svelte` recent builds 의 `<span class="status mono">{b.status}</span>` 를 `<StatusPill status={b.status} lifecycleStatus={b.lifecycleStatus} />` 로 교체. 미사용 `.status` CSS 제거.
  - 회귀: `tsc -p packages/*` + `apps/build-server` 0 errors, `svelte-check` 0 errors / 0 warnings, Go 17 passed / 9 packages, build-server focused 52/52. vitest localStorage 환경 이슈는 기존 (회귀 영향 없음).

### 51.2 실행 (Act)
- 진행 현황: 브랜치 `codex/task-060-fixture-buildlist-2026-07-03` (main HEAD `651abf0` 기준), commit `71ce207` 으로 PR #15 오픈.
- 수정 사항:
  - `apps/build-monitor/src/components/BuildRow.test.ts` — sample 에 `lifecycleStatus: "PREPARING_SOURCE"` 추가, 첫 test 가 canonical label 로 StatusPill 표시 검증 (`name: /Build status: PREPARING_SOURCE/`). `sampleLegacyStatusOnly` fixture + 새 test — lifecycleStatus 부재 시 legacy `status` 로 fallback 검증.
  - `apps/build-monitor/src/routes/BuildsList.test.ts` — `sample()` helper signature 에 `lifecycleStatus?: string` 추가. status chip filter test 는 동일값 emit 으로 검증 유지. 새 test — canonical lifecycleStatus 와 legacy status 가 다른 값으로 동시 emit 될 때 StatusPill 이 canonical 우선 표시 (sample("p-canonical", "BUILDING", "DEPLOYING")).
  - `apps/build-monitor/src/routes/AdminBuilds.test.ts` — `sample()` helper signature 에 `lifecycleStatus?: string` 추가. AdminBuilds 의 `<BuildRow build={...} />` 가 forwarding 하도록 fixture 가 optional 노출.
  - `apps/build-monitor/src/routes/AdminUsers.test.ts` — inline sample 에 `lifecycleStatus: "COMPLETED"` 추가 (status 와 동일값).
  - `apps/build-monitor/src/routes/AdminUsers.svelte` — `StatusPill` import 추가, `selectedBuilds` state type 에 `lifecycleStatus?: string` 추가, `listAdminBuilds` 응답 map 에 `lifecycleStatus: b.lifecycleStatus` forwarding, recent builds markup 의 `<span class="status mono">{b.status}</span>` → `<StatusPill status={b.status} lifecycleStatus={b.lifecycleStatus} />`, 미사용 `.status` CSS (294~300) 제거.

### 51.3 검증 (Validate)
- 테스트 결과:
  - `rtk ./node_modules/.bin/tsc -p packages/shared-contract/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/shared-config/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p packages/db/tsconfig.json --noEmit`
  - `rtk ./node_modules/.bin/tsc -p apps/build-server/tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk ../../node_modules/.bin/tsc -p tsconfig.json --noEmit`
  - `cd apps/build-monitor && rtk node ./node_modules/svelte-check/bin/svelte-check --tsconfig ./tsconfig.json --threshold warning`
  - `cd apps/runner && rtk go test ./...`
  - `cd apps/build-server && rtk node --import tsx --test tests/build-routes.test.ts tests/memory-preview.test.ts tests/memory-build-repository.test.ts tests/phase-history.test.ts tests/shared-contract-response.test.ts tests/openapi.test.ts`
- 검증 메모:
  - TypeScript 4 packages 0 errors.
  - build-monitor tsc + svelte-check 0 errors / **0 warnings** (미사용 `.status` CSS 제거로 해소).
  - Go 9 packages OK.
  - build-server focused regression 52/52.
  - vitest localStorage 미구성 환경의 기존 실패는 이번 변경으로 새로 추가된 거 없음 (회귀 영향 없음).

### 51.4 결과 (Result)
- 작업 결과: PR #14 의 consumer 측 follow-up 5건 (fixture 4 files lifecycleStatus 추가 + BuildRow.test.ts 새 2 case + AdminUsers StatusPill 통일 + 미사용 CSS 제거) 이 commit `71ce207` 으로 봉인. PR #15 squash merge 로 main 합류 (`fe7f639`). 보완 필요 0건 — self-review 후 amend 없이 머지.
- 다음 세션 시작 포인트: TASK-060 3차 PR (Admin 라우트 status filter canonical 정렬) 또는 TASK-061 (Skill/MCP contract rename and payload refactor) 또는 TASK-062 (stdio transport 정리 및 generated contract 재동기화).
- 남은 리스크 및 후속 작업:
  - vitest localStorage 환경 이슈는 기존 — vitest 가 green 이 되려면 jsdom + localStorage polyfill 또는 vitest 환경 설정 보강 필요. 후속 TASK-064 (smoke / migration / visual QA baseline) 에서 함께 검토.
  - `BuildsList` / `AdminBuilds` 의 status chip filter 는 아직 legacy status 이름만 노출 (e.g. `BUILDING` / `FAILED` chip). canonical 12-state union 도 동시에 노출할지는 TASK-060 3차 PR 에서 결정.
