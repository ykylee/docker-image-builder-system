<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-08-05 작업 백로그

## Wiki anchor 구축

- 상태: done
- `AGENTS.md`가 요구하는 `ai-workflow/wiki/index.md`가 저장소에 없어, 기존 canonical 문서군으로 연결되는 wiki 탐색 허브를 신설했다.
- 제품/요구사항, 설계, 구현/계약, 운영/검증, 실서비스 진입 로드맵, AI 에이전트 작업 규칙을 링크했다.
- `README.md`의 문서 진입점에도 Wiki Index를 추가했다.
- 링크 대상은 기존 문서를 원본으로 유지하며 wiki에는 요약·탐색 정보만 둔다.

## 구현 현황 문서화 및 실서비스 진입 로드맵

- 상태: done
- `docs/operations/current-state-and-readiness-2026-08-05.md`에 현재 Build Server/React/Runner/Hosting/서비스 DB 구현, 검증 기준선, 운영 제약, self-dogfood 가능 범위와 실서비스 차단 사유를 정리했다.
- `.omx/plans/production-readiness-roadmap-2026-08-05.md`에 Phase 0 운영 경계 봉인부터 Phase 6 private beta까지 단계별 작업·파일·완료 조건·SLO·pre-mortem·unit/integration/e2e/observability 테스트 계획을 작성했다.
- `docs/PROJECT_PROFILE.md` 운영 가이드 인덱스에 두 문서를 연결했다.

## 현재 구현 실사용성 셀프 리뷰

- 상태: done
- 전체 회귀: Build Server 243/243, React 289/289, Go `go test ./...`, Skill/MCP 227건, TypeScript check PASS. 메인 `/health`, `/api/services`, `/docs` 실서비스 smoke도 PASS.
- 판정: 신뢰된 내부 환경의 self-dogfood/MVP로는 사용 가능하지만, 외부 다중 사용자 실서비스로는 아직 불가.
- 차단 이슈: `X-Admin-Id`/`X-User-Id`와 localStorage userId가 인증 수단이 아니어서 신원 위조·타 사용자 데이터 접근이 가능함; Runner claim/phase/deploy API도 인증이 없음; Docker socket과 kubeconfig를 공유하는 Runner가 비신뢰 Dockerfile을 실행함.
- 운영 차단 이슈: kind control-plane이 별도 memory backend 단일 replica라 메인 Postgres와 데이터가 분리되고 재시작 시 유실될 수 있음; build claim lease/reaper가 없어 Runner 장애 시 active build가 stuck될 수 있음; 성공 build의 workspace/image 정리 정책이 없어 디스크 누적 가능.
- DB lifecycle 결함: Secret provisioning 실패 시 `PROVISIONING` row가 남고 동일 provision 재호출은 password 없이 조기 반환해 자동 복구되지 않음. `markFailed`/재시도·보상 트랜잭션이 필요하다.
- 상세 판단과 우선순위는 이번 세션 handoff의 “현재 구현 실사용성 셀프 리뷰” 항목을 기준으로 다음 작업에서 인증/격리/복구 순서로 처리한다.

## 메인 서비스 `/api/services` 404 수정

- 상태: done
- 원인은 서버에 `GET /services` route는 있었지만 production `/api/*` rewrite 허용 목록에 `/services`가 빠져 `/api/services`가 404 JSON으로 종료되던 경로 불일치였다.
- `/services` rewrite와 OpenAPI path/schema를 추가하고 static-serve/OpenAPI 회귀 테스트 24/24를 통과했다. OpenAPI TypeScript 산출물도 갱신했다.
- 메인 이미지를 재빌드·재기동한 뒤 `GET http://100.119.181.116:3000/api/services`가 `/services`로 307 전달되고, `X-User-Id: admin` 포함 follow redirect 결과가 서비스 목록 JSON으로 반환되는 것을 확인했다. `/health`도 정상이다.

## 메인 서비스 API Docs 동일 반영

- 상태: done
- self-hosted 서비스 전용이 아니라 공통 `ApiConsole` React 번들에 적용된 API Docs context 경로와 세로 레이아웃 수정을 메인 `dibs-build-server`에도 반영했다.
- 메인 이미지를 재빌드·재기동했고 `BUILD_REPOSITORY_BACKEND=postgres`, 메인 DB URL, React dist 경로가 유지되는 것을 확인했다.
- `http://100.119.181.116:3000/api-console`, `/docs/`, `/health`가 각각 HTTP `200`, `200`, `{"status":"ok"}`로 응답했다. 실제 브라우저에서 Scalar endpoint list와 확장된 문서 영역을 확인했으며 console error는 0이다. 캡처: `output/playwright/api-console-main.png`.

## API Docs 세로 레이아웃 개선

- 상태: done
- 실제 1280x720 브라우저 캡처에서 API Console iframe이 `min-height: 480px`에 묶여 Scalar 상세 영역이 좁은 것을 확인했다.
- `.page`에 viewport 기반 최소 높이를 주고 `.frame-wrap`을 `min-height: 640px`, `height: 72dvh`로 확장했다. 모바일은 620px 문서 영역을 별도로 적용했다.
- ApiConsole Vitest 289/289, React typecheck, production Vite build PASS.
- build `9eb67357-874a-4272-a0bb-f8520c79d3c0`가 container test/K8s deploy `COMPLETED`; 최종 배포 URL에서 Scalar endpoint 목록과 browser console error 0을 재확인했다.

## API Docs context 경로 및 self-host DB 검증

- 상태: done
- API Docs의 404 원인은 `ApiConsole.tsx`와 Raw OpenAPI 링크가 root-absolute `/docs/`, `/openapi.json`을 사용해 hosted context 밖으로 이탈한 것이었다. `/<context>/docs/` 및 `/<context>/openapi.json`으로 수정했다. 실제 iframe 내부 Scalar endpoint list 로딩과 console error 0을 확인했다.
- 기존 self-host 서비스는 manifest/DB Secret/`DATABASE_URL`이 없어 memory backend 상태였다. `database.enabled=true` manifest, platform DB schema/role/Secret provision을 적용했다.
- Deployment에 Secret의 `DATABASE_URL`과 `BUILD_REPOSITORY_BACKEND=postgres`를 함께 주입하도록 Runner K8s adapter를 보완했다. 이미지 기본 backend는 사전 container-test가 Secret 없이 동작하도록 memory로 유지한다.
- 새 build `abf070a8-6030-49b7-a15b-11022c019146`가 container test와 K8s deploy까지 `COMPLETED`. Deployment env/Secret reference, pod startup migration `0001~0018`, gateway 경유 `current_user/current_schema/current_database` probe=1을 확인했다.
- 별도 `db-routing-probe-20260805` build를 서비스 `/context/builds`에 저장한 뒤 Pod 재기동 후에도 조회되어 Postgres persistence를 확인했다. `/context/api/builds`는 control-plane API라 probe가 보이지 않으며, self-host 서비스 UI API 경로와 control-plane API 경로가 분리되어 있음을 확인했다.

## Admin self-hosted deep-link/API 경로 수정

- 상태: done
- 실제 브라우저에서 `The string did not match the expected pattern.`을 재현했다. deep link의 상대 자산이 `/context/admin/assets/*`에서 SPA `index.html`로 응답되던 문제를 Build Server의 deep asset fallback 및 favicon 매핑으로 수정했다.
- `openapi-fetch`가 `baseUrl`과 leading-slash schema path를 문자열 결합하는 동작에 맞춰 hosted API base를 `/<context>/api` 절대 경로로 수정했다. 기존 `./api`는 `/context/admin/api/admin/*`를 만들었다.
- Build Server single-port 회귀 242/242, frontend Vitest 288/288, typecheck 및 `git diff --check` PASS.
- 수정본 self-host build `6a0e56dd-fbd1-42cb-beb7-dfe2c0411ca1`가 `COMPLETED`. `http://100.119.181.116:18080/docker-image-builder-system-admin-20260805-api-fixed/`에서 admin 로그인 후 Builds 화면을 확인했고, `/api/admin/builds`, `/users`, `/admins`, `/runners`, `/hosted-services` 모두 200 JSON, browser console error 0, JS asset MIME 정상이다.

## 사용자 서비스 현황 탭 및 공개 런타임 URL 정합

- 상태: done
- `Services` 사용자 탭(`/services`)과 사용자 전용 `GET /services` API를 추가했다.
- 서비스 목록은 현재 사용자의 `requestedBy`에 연결된 `HostedService`만 반환하며, Builds 이력과 분리했다.
- `AppHeader`에 Services 내비게이션을 추가하고 운영 URL/상태/현재 build/replica 정보를 카드로 표시한다.
- 컨테이너 테스트용 `container-test.local` 주소는 public `runtimeUrl`로 저장·노출하지 않도록 memory/Postgres semantics를 정렬했다. 기존 Postgres row의 내부 URL도 null로 정리했다.
- K8s hosted deployment에서는 `RUNNER_HOSTING_BASE_HOST` 기준 `http://<host>/<contextPath>/` 또는 subdomain URL을 계속 public runtime URL로 보고한다.
- 검증: build-server 전체 node:test 233/233, frontend Vitest 284/284, frontend/backend typecheck 및 production image build PASS.
- 실제 IP 확인: `http://100.119.181.116:3000/services` + `X-User-Id: admin` → `200 {"services":[]}`, `/admin/builds` 문서 요청 → SPA HTML 200, API 응답에서 `container-test.local` 미검출.
- 관리자 전체 서비스 조회: `/admin/services`를 canonical 관리자 탭/경로로 추가했다. 기존 `/admin/hosting`은 alias로 유지하고, backend `/admin/hosted-services`는 admin allow-list 전체 서비스를 반환한다. 실제 `/admin/services` HTML 200, 인증 없는 API 401, `X-Admin-Id: admin` API 200을 확인했다.

## admin 계정 self-dogfood 재실행

- 상태: done
- `requestedBy=admin`, `dockerfileMode=auto`로 `apps/build-monitor/dist-react`를 다시 제출했다.
- build ID: `35823857-d331-4ceb-ad03-2801f43ab30e`
- source upload → Runner claim → auto static-nginx Dockerfile → container test → deployment report → `COMPLETED` 전 과정을 통과했다.
- admin builds filter에서 해당 빌드와 `runtimeUrl: null`을 확인했다. 현재 compose는 skeleton/registry 경로이며 `HOSTING_BASE_HOST`가 없어 HostedService는 생성되지 않아 `/admin/hosted-services`는 `services: []`이다.
- 검증 중 public build log에 남던 내부 `container-test.local` 주소도 제거하고 기존 Postgres 로그 2건을 정리했다. 이후 build log에는 `Container test: SUCCESS`만 남는다.

## 로컬 Kubernetes hosting 환경 셋업 및 재검증

- 상태: done
- 기존 `kind-dib` 클러스터를 재사용하고 `ingress-nginx`를 설치했다. 외부 접속은 host 포트포워딩으로 `100.119.181.116:18080`에 고정했다.
- Compose의 build-server/runner에 kubeconfig named volume, `kubectl`, `HOSTING_BASE_HOST`, `RUNNER_K8S_MODE=k8s`, `dib-hosted` namespace를 연결했다. kind API 인증서 SAN 차이는 로컬 개발용 kubeconfig의 `insecure-skip-tls-verify`로 처리했다.
- Colima Docker socket의 실제 group gid `991`을 `DOCKER_SOCKET_GID`로 주입해 runner의 실제 Docker CLI build/run 권한을 복구했다.
- admin self-dogfood 재검증 build ID: `47185c31-f54c-49d9-90a3-272c721f5f1a`. 실제 Docker build → container test → K8s Deployment/Service/Ingress 생성 → `COMPLETED`를 확인했다.
- 공개 runtime URL은 `http://100.119.181.116:18080/self-dogfood-k8s-admin-retry/`이며 HTTP `200`, K8s deployment `1/1`을 확인했다.
- `/admin/hosted-services`와 `/services` 모두 `status=RUNNING`, `availableReplicas=1`인 동일 서비스를 반환한다. `/admin/services` alias도 HTTP `200`이다.

## Runtime URL 링크 대비 수정

- 상태: done
- `BuildDetail`의 runtime URL 파란 박스에서 전역 `a:not(...)` 스타일이 링크 글자색을 다시 accent blue로 덮어쓰던 specificity 충돌을 수정했다.
- `.runtime-url-box a.runtime-url-link` 선택자로 솔리드 accent 배경에 맞는 `--dib-color-on-accent` 전경색을 보장했다.
- frontend production build와 `dibs-build-server` image를 갱신했고 `/health` `200`, 관련 Vitest 12건, React typecheck, `git diff --check`를 통과했다.

## Path-hosted self-dogfood 빈 페이지 수정

- 상태: done
- 원인 1: Vite가 `/assets/...` 절대 asset URL을 생성해 Ingress context path 밖에서 JS/CSS/favicon이 404였다.
- 원인 2: React `BrowserRouter`가 `/self-dogfood-k8s-admin-retry` prefix를 모른 채 `/login`으로 이동했다.
- Vite `base: "./"`와 path prefix 자동 basename을 추가했다. production build 후 전체 frontend Vitest `286/286`, typecheck를 통과했다.
- 동일 context path로 재배포한 build ID는 `4edbeaf6-2d71-4466-909b-4d315de05428`이며 실제 browser에서 `/self-dogfood-k8s-admin-retry/login` 렌더링, console error `0`, runtime URL HTTP `200`을 확인했다.

## Path-hosted 로그인 후 API 404 수정

- 상태: done
- 로그인 후 `/builds` 화면에서 API가 runtime nginx의 `/builds`로 요청되어 404가 발생하던 문제를 확인했다.
- path-hosted runtime에서는 API client가 현재 host의 Build Server `:3000`으로 요청하도록 production API base URL 분기를 추가했다.
- 동일 URL로 재배포한 최종 build ID는 `3f58cebc-9461-4cbd-9ed7-f1268f697c7d`이며 admin 로그인 후 `/self-dogfood-k8s-admin-retry/builds`에서 build 목록과 Services/Admin 내비게이션, API 호출을 browser로 확인했다. console error `0`.

### 후속 정정: 호스트 API fallback 제거 및 cluster-local API 전환

- 상태: done
- 위 항목의 host `:3000` fallback은 컨테이너 격리 요구에 맞지 않아 제거했다.
- path-hosted bundle은 상대 경로 `./api`만 사용하고, Ingress가 `/context/api/*`를 같은 클러스터의 `dib-control-plane:3000` Service로 전달한다.
- control-plane 로그에서 Ingress pod 주소의 `/health`, `/builds` 요청을 확인했고 host IP 직접 호출은 사용하지 않는다.

## Dockerfile auto 생성 조건 보완 및 격리 self-dogfood

- 상태: done
- static auto template은 nginx `listen 8080`과 SPA fallback `try_files $uri $uri/ /index.html`을 포함한다.
- Node auto template은 `ENV PORT=8080`, `EXPOSE 8080`, `npm install --omit=dev`를 포함한다.
- `index.html`의 `/assets/...` 같은 root-absolute asset 참조는 path-hosted 서비스에서 깨지므로 actionable error로 거부한다. Vite 산출물은 `base: "./"`를 사용한다.
- 로컬 control-plane manifest를 `examples/k8s-control-plane.yaml`로 추가했다. 호스트 주소를 bundle 또는 pod 환경에 주입하지 않는다.
- 현재 runtime: `http://100.119.181.116:18080/self-dogfood-k8s-admin-isolated/`
- 검증: `/api/health` 200, `/api/builds` 200, app HTML/assets 200, runner Go 전체 테스트 PASS, frontend Vitest `286/286`, React typecheck PASS.
- 현재 control-plane은 self-dogfood용 memory backend라 host Postgres build history와 별도다. 운영 공유 이력은 cluster 내부 Postgres/control-plane 배포가 후속이다.

## 서비스별 DB 격리 설계

- 상태: in_progress
- 호스트 PostgreSQL 단일 인스턴스는 유지하되 platform DB와 service DB를 논리 분리하고, 서비스별 schema/role/credential을 발급하는 방향으로 설계했다.
- hosted pod에는 host IP를 주지 않고 cluster-local `dib-db-gateway`만 노출한다. DB gateway, NetworkPolicy, TLS, Secret reference가 격리 경계다.
- 1차 vertical slice 완료: `ServiceManifest.database` opt-in/strict contract, `service_database` Drizzle schema 및 migration `0017` 추가. credential 원문은 저장하지 않는다.
- provisioning helper 추가: stable service schema/role/Secret 이름 생성, advisory lock + transaction 기반 idempotent schema/role 생성. HTTP/Secret 연결 전까지 외부 호출에는 노출하지 않는다.
- admin provisioning API와 stdin 기반 Kubernetes Secret writer 추가. `SERVICE_DB_GATEWAY_HOST` 미설정 시 503으로 중단해 현재 클러스터에서 잘못된 DB provisioning을 만들지 않는다.
- kubectl K8s Deployment에 optional Secret-backed `DATABASE_URL` 주입과 hosted-service label을 추가했다. `examples/k8s-db-gateway.yaml`에 HAProxy gateway/NetworkPolicy를 추가했으며 upstream 확정 전이라 live apply는 보류했다.
- kind live 검증 완료: gateway `Running`, port-forward를 통한 host PostgreSQL `select 1` PASS, `db-demo` schema/role/Secret provisioning `201 READY` PASS, role의 `current_schema`가 서비스 schema이고 `public` schema table 생성은 permission denied. 기존 hosted app pod에도 Secret reference/label을 적용했으며 runtime HTTP `200`.
- Helm/ArgoCD adapter에도 `database.secretName` parameter와 hosted-service label/optional `DATABASE_URL` template을 반영했다. Helm template render와 runner Go 전체 테스트 PASS.
- 실검증 중 `service_database.id` SQL default 누락을 발견해 적용된 `0017`을 변경하지 않고 `0018_service_database_id_default.sql`로 보정했다.
- 검증: shared-contract/db/build-server typecheck PASS, build-server `236/236` PASS.
- 설계 문서: `docs/design/service-database-isolation.md`
- 구현 계획: `.omx/plans/service-database-isolation.md`

## self-dogfood 실행 모드 및 Builds 필터 보정

- 상태: in_progress
- 원인: Compose Runner가 build/run mode 기본값 `skeleton`을 사용해 이전 admin build가 약 1초 내 phase만 보고하고 실제 Docker build/run을 하지 않았다.
- Compose 기본값을 `RUNNER_DOCKER_BUILD_MODE=cli`, `RUNNER_DOCKER_RUN_MODE=cli`로 변경했다. Runner health probe는 컨테이너 내부 loopback이 아닌 `host.docker.internal`을 사용하도록 보정했다.
- 실제 재검증: 첫 cli 실행은 build `6417b465-1764-439e-82f0-4687cacfd7de`에서 실제 Docker build 후 health probe 네트워크 결함으로 33.9초 뒤 `CONTAINER_TEST_FAILED`; probe 수정 후 build `af07773b-ff84-42b1-940c-bb9ee164f865`가 4.4초에 실제 Docker build/run과 container test를 거쳐 `COMPLETED`했다.
- Builds `COMPLETED` 필터가 `lifecycleStatus=COMPLETED`를 누락하던 버그를 수정하고 회귀 테스트를 추가했다.
- 서비스 목록은 K8s HostedService만 등록하는 현재 설계와 Compose의 non-K8s skeleton deployment가 달라 빈 목록이 정상이다. 실제 서비스 목록 검증은 K8s hosting mode와 `HOSTING_BASE_HOST`를 연결하는 후속 작업이 필요하다.

## 서비스 기동 및 프로젝트 self-dogfood 실측

- 상태: done
- 범위: 기존 Docker Compose 서비스 상태 확인 및 격리된 auto Dockerfile 전체 lifecycle 검증
- 결과:
  - 기존 `dibs-build-server`/`dibs-postgres`/`dibs-runner`가 기동 중이며 Build Server `BUILD_REPOSITORY_BACKEND=postgres` 설정과 `/health`, `/openapi.json`, `/builds` 응답을 확인했다.
  - `apps/runner/scripts/e2e-dockerfile-auto-lifecycle.sh`를 별도 compose project/포트로 실행했다.
  - static source와 Node source가 generated Dockerfile → BuildKit build → container test → deploy → `COMPLETED`까지 PASS했다.
  - unsupported source는 의도대로 `FAILED` 및 `DOCKER_BUILD_FAILED`를 반환했다.
  - e2e compose는 cleanup 되었고 기존 서비스는 계속 healthy/up 상태다.
  - build IDs: `de130288-f45e-4e5e-ba0a-8c6dad1c3339`, `0e107ab5-db7d-4dfe-8127-ad3a01acdc44`, `576b16ba-125c-4c91-a15b-d38dd420d125`.
- 참고: `ai-workflow/wiki/index.md`는 저장소에 없어 누락 상태로 기록한다.
- IP 접속 보정: `compose.dev.yaml`의 `BUILD_MONITOR_REACT_DIST_PATH`를 runtime dist(`/app/apps/build-monitor/dist`)와 정렬했다. rebuild 후 `http://100.119.181.116:3000/`, `/health`, frontend asset, `/builds`가 모두 `200`이다.
- 현재 서비스 재검증: `http://100.119.181.116:3000`에 `build-monitor/dist-react`를 `dockerfileMode=auto`로 제출했다. Build ID `781ac9b6-dbb0-4c41-8841-cce2086d5273`, Runner `template=static-nginx`, 전체 lifecycle `COMPLETED`.
- SPA/API 경로 보정: `Sec-Fetch-Dest`가 제거되는 접속에서도 `Accept: text/html` 문서 요청은 `/builds`·`/admin/builds` SPA를 반환하도록 수정했다. fetch/XHR 및 일반 API 요청은 JSON을 유지한다. `static-serve` 20/20, build-server typecheck clean, 실제 IP `/builds`·`/admin/builds` 모두 HTML `200` 확인.
- persistence 보정: Compose의 self-dogfood 기본 backend를 memory에서 Postgres로 변경하고 `DATABASE_URL`, `DB_AUTO_BOOTSTRAP`, postgres health dependency를 고정했다. frontend production API도 `/api` redirect 없이 same-origin 실제 route를 직접 호출하도록 정렬했다. `build-monitor` 284/284 PASS.
- Postgres 기반 재검증: 현재 서비스에 build `3a5edbc1-519e-43fd-b590-fb62670cbe0e`를 제출해 `COMPLETED`했고, admin builds API에서 Postgres 영속 이력 3건을 확인했다.

## 프로젝트 frontend 산출물 기반 Dockerfile 생성 self-dogfood

- 상태: done
- 범위: `apps/build-monitor/dist-react` production 산출물을 source archive로 제출하고 `dockerfileMode=auto` 생성 규칙을 실제 적용
- 결과:
  - 첫 실행은 기존 장기 실행 `dibs-runner` image가 stale하여 `Dockerfile not found`로 실패했다. 해당 image는 auto generator가 반영되기 전 버전이었다.
  - 현재 소스로 Runner image를 rebuild/restart한 뒤 재실행했다.
  - Runner 로그에서 `generated Dockerfile ... template=static-nginx`를 확인했다.
  - 생성 template은 `nginx:1.27.4-alpine3.21`, port 8080 규칙이며, 실제 build/test/deploy lifecycle이 모두 `COMPLETED`였다.
  - 성공 build ID: `1f4edebf-7c08-4048-9e75-bd8968559059`.
  - 화면 확인용으로 동일 `dist-react`를 `dib-selfdogfood-preview` nginx container에 mount해 `http://127.0.0.1:38124/` HTML `200`을 확인했다. API/운영 endpoint는 `http://127.0.0.1:3000`에서 확인 가능하다.

## 원격 main 동기화 및 검증

- 상태: done
- 범위: 원격 `origin/main` 최신 커밋 동기화 및 전수 검증
- 내용:
  - 원격 최신 main 커밋(v0.9.0 포함 12개 커밋 및 service manifest / BuildKit 등 후속 6개 커밋, 총 18개 커밋)을 로컬 `main` 브랜치에 fast-forward pull 반영.
  - 의존성 설치 (`npx -y pnpm install`) 및 5개 패키지 TypeScript `--noEmit` 전수 검증 clean.
  - `build-server` 232개 node:test 및 `build-monitor` Vitest 284개 테스트전수 ALL PASS.

## 신규 기능 UI 디자인 개선 (Astryx 토큰 정합)

- 상태: done
- 범위: Admin Hosted Services 및 Build Request 신규 기능 UI 디자인 개선
- 내용:
  - **Admin Hosted Services (`AdminHostedServices.tsx` / `.css`)**:
    - **Capacity Dashboard Overview**: CPU & Memory Reserved 사용량 및 비율 게이지 바(Gradient Track) 적용 (80% 초과 시 Warning 액센트).
    - **3-Tier Capacity Visual Cards**: Sandbox, Standard, Production 수용 가능 수량을 뱃지 카드 뷰로 시각화.
    - **Service Manifest Editor**: 다크 톤 터미널 에디터 (`var(--dib-code-bg)`, `var(--dib-code-fg)`) 및 Revision 뱃지/툴바 적용.
  - **Build Request Form (`BuildRequest.tsx` / `.css`)**:
    - **Form Section Cards**: 4개 영역(App Identity, Container Policy, Tier & Resources, Routing) 카드 구분.
    - **Dockerfile Mode Visual Cards**: `Required` vs `Auto` 라디오 카드 선택 UI.
    - **3-Tier Target Selection Cards**: `Auto`, `Sandbox`, `Standard`, `Production` 클릭 선택형 카드 UI.
- 검증:
  - `AdminHostedServices.test.tsx` (6 tests PASS)
  - `BuildRequest.test.tsx` (18 tests PASS)
  - `build-monitor` Vitest 26개 파일, 284개 테스트 100% PASS
  - `build-server` 232개 테스트 100% PASS
  - TypeScript 5개 패키지 typecheck Clean
## 서비스별 DB migration gate 및 배포 adapter 정렬

- 상태: in_progress
- kubectl/Helm/ArgoCD에 서비스 DB Secret 주입을 정렬했고, migration은 별도 Job 대신 앱 Pod `initContainer` rollout gate로 구현했다.
- Helm render, Runner `go test ./...`, Build Server `236/236` 테스트를 통과했다.
- Compose runner의 `RUNNER_K8S_MIGRATION_COMMAND`는 현재 전역 설정이다. manifest의 서비스별 migration command를 claim/deploy까지 직접 전달하는 보강과 full kind migration e2e가 다음 작업이다.
- 위 전역 설정 의존성을 제거하고, Build Server가 claim 응답에 manifest의 `database.enabled`/`migrationCommand`만 전달하도록 연결했다. Runner는 해당 claim 정책으로 Secret reference와 migration `initContainer`를 구성한다. credential/host/schema 정보는 claim에 포함하지 않는다.
- full kind migration e2e에서 `initContainer`도 ResourceQuota 대상이라는 실제 결함을 발견했다. kubectl/Helm migration container에 앱과 동일한 resource profile을 추가한 뒤 migration `Completed 0`, 앱 컨테이너 Running, schema table 생성, public schema 쓰기 거부를 통과했다. 검증 Deployment와 quota는 정리해 원래 3 pods 상태로 복구했다.
- 관리자용 `GET /admin/hosted-services/:appName/database` 상태 조회 API와 OpenAPI 계약을 추가했다. 실제 Compose Build Server에서 `db-demo`의 `READY`, schema/role/Secret, migration command를 credential 없이 조회했고 `/health` 200도 확인했다. Build Server 테스트는 `238/238`이다.
- Admin 서비스 목록 UI에 서비스별 DB 상태와 migration revision을 추가했다. DB 미프로비저닝/404가 전체 목록 로딩을 막지 않고 `Not provisioned`로 표시된다. 프론트 Vitest `287/287`, React typecheck 및 최신 Build Server image rebuild를 통과했다.
- 기본 hosted service 삭제는 DB를 보존하고, 명시적 `POST /admin/hosted-services/:appName/database/purge`만 Secret/schema/role/metadata를 삭제하도록 lifecycle을 추가했다. purge는 appName exact confirmation이 필요하다. 잘못된 confirmation은 live `400`이며 `db-demo` 상태가 `READY`로 유지되는 것을 확인했다. Build Server `239/239` PASS.
- `POST /admin/hosted-services/:appName/database/rotate`를 추가해 exact confirmation으로 role password와 Kubernetes Secret을 교체하고 schema/role/data를 유지한다. live `db-demo` rotation 후 새 Secret으로 gateway 접속해 `current_user`, `current_schema`, `migration_gate_probe`를 확인했다. 응답에는 credential이 없고 Build Server `240/240` PASS.
- gateway NetworkPolicy에 DB upstream CIDR와 DNS만 허용하는 egress 제한을 추가하고 kind에 적용했다. 기존 `db-demo` gateway 접속은 계속 성공했다. 외부 HTTPS/PostgreSQL TLS는 도입하지 않고 HTTP/plain PostgreSQL 운영 범위를 유지한다.
- 환경별 upstream CIDR가 기본 gateway 매니페스트에 섞이지 않도록 `examples/k8s-db-gateway-egress.yaml`로 분리했다. kind에는 `192.168.5.2/32` 정책을 재적용했고 `0.0.0.0/0` 미사용을 확인했다.
- 기존 hosted app Pod를 사용한 실제 egress 검증에서 gateway `5432`는 허용되고 `host.docker.internal:5432` 직접 접근과 `dib-control-plane:3000` 접근은 차단됐다. ResourceQuota가 3 pods로 가득 차 임시 probe Pod 대신 기존 app Pod에서 검증했다.
- Dockerfile/skill/MCP DB 정책을 정렬했다. Runner는 기존 Dockerfile의 `DATABASE_URL`/`DB_*`/`PG*` ENV·ARG 선언을 거부하고, `build-request-intake`는 동일 입력을 `SERVICE_DATABASE_POLICY_VIOLATION`으로 거부한다. 자동 생성/추천 문서와 root Dockerfile에도 platform Secret 주입 정책을 기록했다. Go dockerfile/deploy PASS, Python skill 16/16, Build Server 240/240, diff check PASS.
- admin self-dogfood를 `requestedBy=admin`으로 실행했다. `dockerfileMode=auto` static sample이 skill 검증 → POST/source upload → Runner auto Dockerfile 생성(`static-nginx`) → real Docker build → container test → K8s deploy까지 성공했고 runtime `http://100.119.181.116:18080/self-dogfood-admin-policy-retry-20260805/` 및 `/api/health` 200, admin Builds/Services `COMPLETED`/`RUNNING`을 확인했다. 1차 시도는 sandbox quota가 control-plane+gateway를 계산하지 못해 실패해 `quotaForTierWithControlPlane(sandbox)`를 2 CPU/2Gi/3 pods로 보완했다. 검증 후 retry 서비스는 제거하고 기존 샘플과 quota를 복구했다.
- 사용자가 직전 URL `404`를 확인해 같은 context path로 admin self-dogfood을 재배포했다. 새 build `8011da1e-62a0-4b47-8f4e-925eaa4995ed`가 `COMPLETED`, runtime와 `/api/health`가 `200`, admin 서비스가 `RUNNING`이며 이번에는 서비스를 유지한다. sandbox quota 때문에 기존 샘플은 replicas=0 상태다.
- 프로젝트 자체를 `Dockerfile` 포함 archive로 `requestedBy=admin` 배포했다. archive 필수 `.generated` 누락, Build Server 앱의 `/health` readiness 경로, Ingress stale 충돌을 순차 보완했고, claim의 `runtimePort`를 container test에도 전달하도록 Runner를 수정했다. root Dockerfile에 `BUILD_MONITOR_REACT_DIST_PATH`도 추가했다. 최종 build `9cfea142-2f7a-4e7a-a163-ee1af5370aa1`가 Docker build/container test/K8s deploy `COMPLETED`; runtime `/`, `/health`, `/openapi.json` 모두 `200`, Admin Services `RUNNING`. 현재 URL은 `http://100.119.181.116:18080/docker-image-builder-system-admin-20260805/`이며 DB는 manifest opt-in 없이 memory backend다.
