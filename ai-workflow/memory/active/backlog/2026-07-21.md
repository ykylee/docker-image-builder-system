<!-- standard-ai-workflow-kit: v0.15.19-beta -->

# 2026-07-21 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **completed (TASK-124 bulk sync 회귀 복구 + TASK-125 환경 셋업 + baseline 실측 정정 + TASK-126 tar 절대경로 가드 + TASK-127 POST /builds 400 계약 + TASK-128 db-migrate.sh 실행 경로 복구 봉인 — TASK-125 발견 결함 3건 전부 해소)**
- 최종 수정일: 2026-07-21 (rev 5: TASK-128 신규. §5 `scripts/db-migrate.sh` pnpm 실행 경로 복구. TASK-125 발견 결함 3건 중 마지막 2번 해소 → **3건 전부 봉인 완료**. 스크립트가 repo root cwd 에서 `node --import tsx` 를 호출했는데 `tsx` 는 build-server devDependency 이고 pnpm 은 비-hoist 격리라 `ERR_MODULE_NOT_FOUND` 로 죽던 결함. TASK-103 의 검증이 `bash -n` / `--help` / 게이트 부재 / DATABASE_URL 미설정 4종에 그쳐 **실제 DB 연결 경로가 한 번도 실행된 적 없었음**. 수정: `run_cli()` (build-server cwd 실행) + `resolve_migrations_dir()` (cwd 변경에 따른 REPO_ROOT 기준 절대경로 정규화 — 2차 결함 예방) + `require_cli_deps()` (node/tsx 사전 점검, raw stack trace 대신 pnpm install 안내) + DATABASE_URL hint 포트 15432 → 5432 정정. **검증: 6 게이트 전부 실제 Postgres 에 실행** + 경로 override 4종 + 다른 cwd 호출 + 실패 케이스. SQL / schema / migration / version / git tag 변경 0, 애플리케이션 코드 영향 0.)
- 최종 수정일: 2026-07-21 (rev 4: TASK-127 신규. §4 POST /builds 요청 검증 400 계약 복구 + 회귀 가드 8건. TASK-125 발견 결함 3건 중 3번 (API 계약) 해소. `build-routes.ts` 상단 주석이 `safeParse` 규약을 선언하는데 13개 라우트 중 3곳 (`POST /builds` / `GET /builds/:buildId` / `GET /builds/:buildId/logs`) 만 bare `.parse` 를 써서 ZodError 가 Fastify 기본 error handler 로 새어 500 응답. client error 를 server fault 로 응답하던 계약 위반. 3곳을 기존 규약 (`safeParse` + `400 { message, issues }`) 으로 정렬. **회귀 가드 8건 신규** — 세 엔드포인트 모두 라우트 레벨 테스트가 아예 없었음이 결함 잔존의 직접 원인. **기존 테스트 1건 수정**: `static-serve.test.ts:91` 이 500 을 기대하고 있었는데 이는 명세가 아니라 결함 당시 동작을 받아적은 것 — 400 으로 갱신 + 경위 주석. build-server **164 → 172 PASS**. 실서버 확인 (400 + issues / 400 non-UUID × 2 / 202 정상). SQL / schema / migration / version / git tag 변경 0, frontend 영향 0.)
- 최종 수정일: 2026-07-21 (rev 3: TASK-126 신규. §3 runner tar 절대경로 가드 크로스플랫폼 결함 수정. TASK-125 가 발견한 결함 3건 중 1번 (보안 가드) 해소. `validateTarEntryName` 이 절대경로 판정에 호스트 OS 규약을 따르는 `filepath.IsAbs` 를 써서 Windows 빌드에서 Unix 절대경로 (`/etc/passwd`) entry 가 가드를 통과하던 결함. tar entry 는 포맷 스펙상 항상 슬래시 구분자이므로 `path.IsAbs` 병용으로 수정 (`filepath.IsAbs` 는 Windows 호스트 절대경로 추가 방어로 유지). go test **7/8 → 8/8 package PASS**. over-reject 회귀 없음 (AcceptsValidNames 6종 계속 PASS). SQL / schema / migration / version / git tag 변경 0, TS / frontend 영향 0.)
- 최종 수정일: 2026-07-21 (rev 2: TASK-125 신규. §2 로컬 개발 환경 셋업 + baseline 실측 정정. pnpm install (319 packages) + Go 1.26.5 + PostgreSQL 18.4 (localhost:5432) 셋업. migration 0001~0006 적용 + 9 테이블 생성 + Postgres 왕복 smoke PASS. 실측 결과 build-server baseline 이 165 가 아니라 **164** 임을 확정 (실행값 + 정적 카운트 일치) → CHANGELOG / RELEASE_NOTES / state.json 정정. RELEASE_NOTES 의 깨진 CHANGELOG 링크 2건 교정. CLAUDE.md 실행 기본값 TODO 5종 → 실측 명령 보정. pnpm-lock.yaml Svelte 잔재 298줄 정리. **신규 결함 3건 발견 (미수정)** — follow-up 참조. SQL / schema / migration / version / git tag 변경 0.)
- 최종 수정일: 2026-07-21 (rev 1: 신규 일일 백로그. §1 TASK-124 봉인 — `b2cbe5e` (workflow kit bulk sync) 가 `docs/PROJECT_PROFILE.md` 를 327줄 → 45줄 kit 템플릿 스켈레톤으로 덮어쓴 회귀 복구. `7e9f777` 시점 원본 복원 (302 insertions / 20 deletions) + kit 마커 v0.15.19-beta 정합 + CHANGELOG reference 경로 교정 (`../../CHANGELOG.md` → `../CHANGELOG.md`). SQL / schema / migration / version / git tag 변경 0. 회귀 영향 0 (docs only). 메타 sync 같은 commit 안에 포함.)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-20.md)

## 1. TASK-124 PROJECT_PROFILE.md bulk sync 회귀 복구 (docs only)

- 상태: completed
- 모드: Doc (docs only, 회귀 복구)
- 우선순위: high (프로젝트 고유 운영 문서 302줄 유실 — TASK-102~113 의 봉인 산출물 전체가 working tree 에서 소실된 상태였음)
- 담당: Claude Code
- 영향 파일: `docs/PROJECT_PROFILE.md` (복원)
- commit: 본 sync commit

### 1.1 계획 (Plan)

- 작업 목표: `/workflow-session-start` baseline 복원 중 `docs/PROJECT_PROFILE.md` 가 45줄 kit 템플릿 스켈레톤 (§1 목적 / §3 명령 5종 / §4 검증 / §5 정책 전부 `TODO:` · `<placeholder>`) 상태인데, TASK-102~113 의 봉인 기록은 일관되게 "§3.5 / §3.6 / §3.10 / §3.11 / §3.12 갱신" 을 주장하는 정합성 붕괴를 발견. 원인 규명 후 유실분 복원.
- 완료 기준:
  - 유실 시점 커밋 특정 + 유실 원인 규명
  - 피해 범위 확정 (동일 커밋의 다른 프로젝트 고유 문서 포함 여부)
  - 유실 직전 원본 복원 + 유실 이후의 정당한 변경 재적용
  - 복원 후 링크 정합성 + `state.json` commands SSOT 정합 검증
  - 회귀 영향 0 (docs only)

### 1.2 실행 (Act)

#### 1.2.1 원인 규명

- `git log --numstat -- docs/PROJECT_PROFILE.md` 로 `b2cbe5e` (`chore(workflow): bulk sync — workflow kit + active state rev144`, author `grok`, 2026-07-21) 가 **20 insertions / 302 deletions** 임을 특정.
- 유실 직전 `7e9f777` 시점 **327줄** → `b2cbe5e` 직후 **45줄**.
- `b2cbe5e` 가 추가한 20줄은 전부 kit 템플릿 placeholder (`TODO:` / `<...>`) 로 보존 가치 0 임을 diff 로 확인.
- 커밋 메시지에는 `docs/PROJECT_PROFILE.md: 프로젝트 메타 동기화` 로만 기재되어 302줄 유실이 드러나지 않았음 — 이것이 TASK-121 ~ TASK-123 세 TASK 동안 발견이 지연된 직접 원인.
- **판정**: TASK-102~113 의 봉인 기록은 당시 시점에 정확했으며, 이후 workflow kit bulk sync 가 프로젝트 고유 문서를 kit 템플릿으로 일괄 덮어쓴 사고. 봉인 기록 측 결함 아님.

#### 1.2.2 피해 범위 확정

- `b2cbe5e` 의 비(非) kit 경로 변경 파일 전수 확인:
  - `AGENTS.md` 9/9, `CLAUDE.md` 8/8, `MiniMax.md` 15/11, `MiniMax_config.example.json` 2/2 — 삽입/삭제 균형, 정상 sync.
  - `GROK.md` 111/0, `opencode.json` 45/0 — 신규 추가.
  - `docs/PROJECT_PROFILE.md` 20/302 — **단독 피해**.
- 피해는 `docs/PROJECT_PROFILE.md` 단독으로 확정.

#### 1.2.3 복원

- `git show 7e9f777:docs/PROJECT_PROFILE.md` 로 327줄 원본 복원.
  - 회복 범위: §3.1 활성 워크플로우 자산 (3.1.1~3.1.4) / §3.2 Admin 엔드포인트 / §3.3 Build Monitor UI 정합 / §3.4 Admin 가드 deep link UX / §3.5 Production semantic / §3.6 e2e-multi-runner BASE + heredoc / §3.7 Private registry 인증 / §3.8 htpasswd / §3.9 Credential rotation / §3.10 Insecure-registry / §3.11 Admin-initiated runner registration / §3.12 React 빌드 mount / §4 검증 포인트 / §5 예외 규칙 / 다음에 읽을 문서 (운영 가이드 reference 전체).
- 유실 이후의 정당한 변경 재적용 3건:
  1. kit 마커 `v0.11.21-beta` → `v0.15.19-beta` (TASK-120 top-level 추적 파일 마커 정합 정책 정합).
  2. 최종 수정일 `2026-07-05` → `2026-07-21`.
  3. CHANGELOG reference 재적용 (`8905cb0` / TASK-123 산출물) — 단 **경로 교정**: 원본의 `../../CHANGELOG.md` 는 `docs/` 기준 한 단계 초과 상승으로 깨진 링크였으므로 `../CHANGELOG.md` 로 정정.

### 1.3 검증 (Verify)

- `git diff --stat`: `docs/PROJECT_PROFILE.md` **302 insertions / 20 deletions** — 유실분 정확히 역전.
- 운영 가이드 링크 전수 해석: `(operations/*.md)` 참조 전부 실파일 존재, 누락 0.
- `CHANGELOG.md` 루트 존재 확인 (`../CHANGELOG.md` 해석 정상).
- §3 명령 5종이 `state.json` 의 `commands` SSOT 와 정합 확인 (install `pnpm install` / run_local memory / run_local_postgres / quick_tests tsc 5종 + go build / isolated_tests curl smoke).
- 회귀 영향 0 — docs only, 코드 / SQL / schema / migration / version / git tag 변경 0.

### 1.4 사전 결함 + 보강

1. **CHANGELOG reference 경로 결함 (TASK-123 잔재)** — `8905cb0` 이 넣은 `../../CHANGELOG.md` 가 `docs/PROJECT_PROFILE.md` 기준으로 저장소 밖을 가리키는 깨진 링크. 복원 과정에서 `../CHANGELOG.md` 로 정정 봉인.
2. **kit 마커 역행 위험** — `7e9f777` 원본의 마커가 `v0.11.21-beta` 라 그대로 복원 시 TASK-120 의 마커 정합 정책을 역행. 복원과 동시에 `v0.15.19-beta` 로 정합.
3. **bulk sync 의 구조적 재발 위험 (미봉인)** — workflow kit bulk sync 가 프로젝트 고유 문서를 kit 템플릿으로 덮어쓸 수 있는 구조가 그대로 남아 있음. 본 TASK 는 회귀 복구만 봉인하고 재발 방지는 봉인하지 않음 — follow-up 참조.

### 1.5 follow-up

- **bulk sync 덮어쓰기 재발 방지 (후속 TASK 권장)**: workflow kit bulk sync 의 대상에서 프로젝트 고유 문서 (`docs/PROJECT_PROFILE.md` 등) 를 제외하거나, sync 전후 줄수 급감 (예: -50% 초과) 시 경고 · 중단하는 가드 도입. 본 사고는 3 TASK (TASK-121~123) 동안 미발견 상태로 진행됐으므로 탐지 지연 자체가 리스크.
- 기존 결정 대기 5종 유지: 옵션 Z 외부 object storage / 신규 기능 / Nextcloud Tasks 통합 / CI migration validation / git tag 다음 version.

## 2. TASK-125 로컬 개발 환경 셋업 + baseline 실측 정정

- 상태: completed
- 모드: Env + Doc (코드 변경 0)
- 우선순위: high (본 저장소에 의존성이 한 번도 설치된 적 없어 어떤 검증도 실행 불가한 상태였음)
- 담당: Claude Code
- 영향 파일: `pnpm-lock.yaml` / `CLAUDE.md` / `CHANGELOG.md` / `docs/RELEASE_NOTES-2026-07-20.md` / `ai-workflow/memory/active/state.json`
- commit: 본 sync commit

### 2.1 계획 (Plan)

- 작업 목표: 로컬 개발 환경을 실제로 구성하고, 기록된 회귀 baseline 이 실측과 일치하는지 검증.
- 결정: Docker 미채택 (사용자 결정). Go + PostgreSQL native 설치로 진행 — runner 의
  `RUNNER_DOCKER_BUILD_MODE` 기본값이 `skeleton` 이라 단위 테스트는 Docker 없이 통과하므로
  개발·테스트 루프는 성립. 단 e2e 11종 + 실제 이미지 빌드는 검증 불가 (수용된 한계).

### 2.2 실행 (Act)

#### 2.2.1 환경 구성

- `pnpm install` — 319 packages, 5 workspace (root / build-server / build-monitor / db /
  shared-contract / shared-config). `apps/skill_mcp` 는 Python 프로젝트라 대상 아님.
- Go 1.26.5 + PostgreSQL 18.4 설치 (사용자 직접). Postgres 는 `localhost:5432`, `postgres/postgres`.
- DB `docker_image_builder` 생성 → migration `--bootstrap` 으로 0001~0006 일괄 적용 →
  9 테이블 생성 확인 (`build_request` / `build_source` / `build_source_chunk` / `build_log` /
  `build_test` / `deployment_attempt` / `runner` / `test_deployment` / `schema_migrations`).

#### 2.2.2 실측 검증

| 항목 | 실측 | 기록 baseline | 판정 |
|---|---|---|---|
| tsc `--noEmit` 5 프로젝트 | clean | clean | 일치 |
| vitest (frontend) | 130/130 PASS | 130 | 일치 |
| build-server `node:test` | **164/164 PASS, fail 0, skip 0** | **165** | **불일치** |
| `go build` / `go vet` | clean | — | OK |
| `go test ./...` | 7/8 package PASS | "7+ PASS" | 2건 FAIL (신규 발견) |
| `vite build:react` | gzip js 99.26KB / css 30.60KB | 99.01 / 30.62 | 오차 범위 |
| Postgres 왕복 smoke | `/health` ok, `POST /builds` → QUEUED, `GET /builds` 조회, DELETE 로 영속 확인 | — | PASS |

#### 2.2.3 baseline 165 → 164 정정

- 근거 2중 확인: (a) 실행 결과 `tests 164 / pass 164 / fail 0 / skipped 0`,
  (b) 정적 카운트 (파일별 `it()` 합계) 도 164 — `build-source-chunked.test.ts` 22건은
  기록의 11+4+3+4 와 일치.
- 조건부 skip (DATABASE_URL 등) 없음 확인 → Postgres 설치로도 늘지 않음. `tests/` 하위
  디렉토리 없음 확인 → glob 누락 아님. 즉 **기록된 165 가 산술 오류**.
- 정정 대상 (현재 baseline 서술만): `CHANGELOG.md` 회귀 표 / `RELEASE_NOTES` §2 + §7 + §10 /
  `state.json` current_baseline + memory_entries value_digest.
- `session_handoff.md` / `work_backlog.md` 의 historical 기록 30건은 **정정하지 않음** —
  TASK-120 의 "각 시점 기록은 historical 이므로 정합 대상 아님" 정책 정합.

#### 2.2.4 부수 정리

- `pnpm-lock.yaml` 298줄 삭제 (추가 0) — TASK-094/100/101 이 `package.json` 에서 걷어낸
  Svelte 의존성이 lockfile 에는 남아 있던 잔재. install 이 동기화.
- `RELEASE_NOTES` §10 의 깨진 CHANGELOG 링크 2건 교정 (`../../CHANGELOG.md` →
  `../CHANGELOG.md`) — TASK-124 가 PROJECT_PROFILE 에서 고친 것과 동일 결함의 잔여분.
- `CLAUDE.md` 실행 기본값 TODO 5종 → 실측 명령으로 보정 + Windows 환경 주의사항 4종 신규.

### 2.3 검증 (Verify)

- `state.json` JSON validity OK.
- 현재 baseline 문서의 `165` 잔여 0 (남은 2건은 "기존 표기 165 는 산술 오류" 정정 설명 문구 자체).
- 코드 변경 0 — SQL / schema / migration / version / git tag 변경 0.

### 2.4 신규 발견 결함 3건 (본 TASK 미수정)

1. **`apps/runner/internal/source` tar 절대경로 가드가 Windows 에서 무력화** —
   `validateTarEntryName` (`fetcher.go:262`) 이 `filepath.IsAbs` 를 쓰는데 이 함수는
   플랫폼 의존이라 Windows 빌드에서 `/etc/passwd` 를 절대경로로 인식하지 못한다
   (`TestFetcher_RejectsTarEntryAbsolute` + `TestValidateTarEntryName_RejectsUnsafeNames/absolute_unix` FAIL).
   tar entry 이름은 규약상 항상 `/` 구분자이므로 `path.IsAbs` 병용이 정답. Linux 에서는
   통과하므로 CI green — 크로스플랫폼 결함.
2. **`scripts/db-migrate.sh` 가 pnpm 환경에서 실행 불가** — `cd REPO_ROOT` 후
   `node --import tsx ...` 를 호출하는데 `tsx` 는 build-server 의 devDependency 이고
   pnpm 비-hoist 격리 때문에 루트에서 해석되지 않아 `ERR_MODULE_NOT_FOUND`.
   TASK-103 이 검증한 것은 `bash -n` / `--help` / 게이트 부재 / DATABASE_URL 미설정 뿐이라
   **실제 DB 연결 경로가 한 번도 실행된 적 없었음**.
3. **`POST /builds` 가 스키마 위반 요청에 400 이 아닌 500 반환** — ZodError 가 처리되지
   않고 Internal Server Error 로 새어 나온다 (`build-routes.js:27`). 테스트 164건이
   통과하는데도 잡히지 않았으므로 이 경로의 회귀 가드가 없음.

### 2.5 follow-up

- 위 결함 3건 수정 (각각 독립 TASK 후보. 1번은 보안 가드, 3번은 API 계약 문제로 우선순위 높음)
- TASK-124 의 bulk sync 재발 방지 가드 (여전히 미봉인)
- Docker 채택 시: e2e 11종 + 실제 이미지 빌드 검증 복구
- 기존 결정 대기 5종 유지

## 3. TASK-126 runner tar 절대경로 가드 크로스플랫폼 결함 수정

- 상태: completed
- 모드: Code (보안 가드)
- 우선순위: high (보안 가드가 특정 플랫폼에서 무력화되던 결함)
- 담당: Claude Code
- 영향 파일: `apps/runner/internal/source/fetcher.go`
- commit: 본 commit

### 3.1 결함 (TASK-125 발견)

`validateTarEntryName` 이 절대경로 판정에 `filepath.IsAbs` 를 사용했다. `filepath` 는
**호스트 OS 규약**을 따르므로 Windows 빌드에서 `filepath.IsAbs("/etc/passwd") == false` 가
되어 Unix 절대경로 entry 가 가드를 통과했다. tar entry 이름은 포맷 스펙상 **항상 슬래시
구분자**이므로 `filepath` 는 애초에 잘못된 도구였다.

- 증상: `TestFetcher_RejectsTarEntryAbsolute` + `TestValidateTarEntryName_RejectsUnsafeNames/absolute_unix` FAIL (Windows)
- Linux 에서는 통과 → **CI green 이라 그동안 드러나지 않음** (크로스플랫폼 결함의 전형)

### 3.2 수정

- `import "path"` 추가.
- `filepath.IsAbs(name)` → `path.IsAbs(name) || filepath.IsAbs(name)`.
  - `path.IsAbs` 가 슬래시 형태(tar 규약)를 플랫폼 무관하게 판정 — 본 결함의 직접 해소.
  - `filepath.IsAbs` 는 **제거하지 않고 병용** — 호스트가 Windows 일 때 `C:\foo` 같은
    호스트 절대경로 형태를 추가로 잡는 역할을 유지.
- 주석에 `filepath` 가 아니라 `path` 를 써야 하는 이유를 명시 (동일 실수 재발 방지).

### 3.3 검증

- 실패하던 2건 PASS 전환.
- `TestValidateTarEntryName_AcceptsValidNames` 6종 (`Dockerfile` / `src/lib/internal/utils.ts` /
  `.hidden` / `trailing-slash/` 등) 계속 PASS — over-reject 회귀 없음.
- `go build ./...` OK / `go vet ./...` OK.
- **`go test ./...` 8/8 package PASS** (TASK-125 baseline 7/8 → 8/8).
- SQL / schema / migration / version / git tag 변경 0. TS / frontend 영향 0.

### 3.4 남은 틈 (본 TASK 미봉인)

- `C:/foo` 처럼 **슬래시를 쓰는 Windows 드라이브 표기**는 Linux 호스트에서 여전히 통과한다
  (`path.IsAbs` false — 첫 문자가 `/` 아님 / `filepath.IsAbs` 도 Linux 에선 false).
  다만 이 경우 `filepath.Join(destDir, "C:/foo")` 가 destDir 하위로 정규화되므로
  **디렉토리 탈출은 발생하지 않는다** — destDir 밑에 `C:` 디렉토리가 생기는 기묘함에 그친다.
  드라이브 문자 패턴 (`^[A-Za-z]:`) 거부를 추가할지는 별도 결정 사항.

### 3.5 follow-up

- TASK-125 발견 결함 중 남은 2건: `POST /builds` 400 대신 500 (API 계약, 우선순위 높음) /
  `scripts/db-migrate.sh` pnpm tsx 해석 실패
- TASK-124 의 bulk sync 재발 방지 가드 (여전히 미봉인)

## 4. TASK-127 POST /builds 요청 검증 400 계약 복구 + 회귀 가드

- 상태: completed
- 모드: Code (API 계약) + Test
- 우선순위: high (client error 를 server fault 로 응답하던 계약 위반)
- 담당: Claude Code
- 영향 파일: `apps/build-server/src/routes/build-routes.ts` / `apps/build-server/tests/build-routes.test.ts` / `apps/build-server/tests/static-serve.test.ts`
- commit: 본 commit

### 4.1 결함 (TASK-125 발견)

`build-routes.ts` 상단 주석은 "Request validation is handled inline by each handler
via `schema.safeParse(...)`" 를 규약으로 선언한다. 그런데 13개 라우트 중 **3곳**이
bare `.parse(...)` 를 써서 ZodError 를 던졌고, Fastify 기본 error handler 까지 새어
나가 **500 Internal Server Error** 로 응답했다. 스키마 위반은 명백한 client error 이므로
400 이 맞다.

- `POST /builds` — `buildRequestSchema.parse(request.body)`
- `GET /builds/:buildId` — `buildIdParamsSchema.parse(request.params)`
- `GET /builds/:buildId/logs` — 동일

나머지 10개 라우트는 모두 `safeParse` + `400 { message, issues }` 로 올바르게 처리하고
있었다. 즉 **같은 파일 안에서 3곳만 규약을 벗어난 누락**이었다.

### 4.2 수정

- 3곳을 기존 규약과 동일한 형태로 정렬: `safeParse` → 실패 시
  `reply.status(400).send({ message, issues })`.
- message 문자열도 기존 관례를 따름 (`"Invalid buildId parameter"` 는 `POST /builds/:buildId/phase`
  등이 이미 쓰던 값과 동일, `POST /builds` 는 `"Invalid build request payload"`).
- `request.body ?? {}` 로 빈 body 도 스키마 경로로 흘려보내 400 을 만든다.

### 4.3 회귀 가드 신규 8건

`POST /builds` / `GET /builds/:buildId` / `GET /builds/:buildId/logs` 는 **라우트 레벨
테스트가 아예 없었다** — 이것이 결함이 오래 남은 직접 원인.

- `POST /builds`: 202 정상 / 400 필수 필드 누락 (issues 에 `requestedBy` · `sourceArchive` ·
  `entrypointPath` 포함 확인) / 400 빈 body / 400 타입 불일치
- `GET /builds/:buildId`: 400 non-UUID / 404 well-formed unknown (404 경로 보존 확인)
- `GET /builds/:buildId/logs`: 400 non-UUID / 404 well-formed unknown

### 4.4 기존 테스트 1건 수정 — 중요

`static-serve.test.ts:91` 이 `/builds/abc-123` 에 대해 **500 을 기대**하고 있었다.
이는 의도된 명세가 아니라 **결함 당시의 동작을 그대로 받아적은 것**이다 (주석에도
"UUID validation 으로 500 응답" 이라고 서술). 테스트 제목은 "UUID validation 으로 거절"
이고 본래 목적은 SPA 라우팅 우선순위 검증이므로, 단언을 400 으로 갱신하고 왜 500 이
아니었어야 하는지 주석에 남겼다.

> 이 발견으로 TASK-125 의 "이 경로에 회귀 가드가 없다" 는 진술은 부정확했음이 드러났다.
> 가드가 없던 게 아니라, **잘못된 동작을 고정한 가드가 있었다** — 더 나쁜 상태였다.

### 4.5 검증

- `tsc --noEmit` clean.
- build-server **164 → 172 PASS, fail 0** (+8 신규).
- 실서버 (Postgres backend) 확인:
  - `POST /builds {"appName":"x"}` → **400** + `{"message":"Invalid build request payload","issues":[...]}`
  - `GET /builds/abc` → **400** / `GET /builds/abc/logs` → **400**
  - `POST /builds` 정상 페이로드 → **202** (기존 동작 보존)
- smoke 데이터 정리 완료 (`build_request` count 0).
- SQL / schema / migration / version / git tag 변경 0. frontend 영향 0.

### 4.6 follow-up

- TASK-125 발견 결함 중 남은 1건: `scripts/db-migrate.sh` pnpm tsx 해석 실패
- TASK-124 의 bulk sync 재발 방지 가드 (여전히 미봉인)
- **동일 계열 점검 제안**: 다른 테스트에도 "당시 동작을 그대로 받아적은" 단언이 더 있을 수
  있다. 4xx/5xx 를 단언하는 기존 테스트를 한 번 훑어볼 가치가 있음.

## 5. TASK-128 scripts/db-migrate.sh pnpm 실행 경로 복구

- 상태: completed
- 모드: Code (운영 스크립트) + Doc
- 우선순위: medium (운영 권고 entrypoint 가 현 패키지 매니저 환경에서 실행 자체가 불가)
- 담당: Claude Code
- 영향 파일: `scripts/db-migrate.sh` / `docs/operations/migration-cli-workflow-2026-07-20.md`
- commit: 본 commit

### 5.1 결함 (TASK-125 발견)

`scripts/db-migrate.sh` 는 `cd "$REPO_ROOT"` 후 `node --import tsx apps/build-server/scripts/migrate.ts`
를 호출했다. 그런데 `tsx` 는 **build-server 의 devDependency** 이고 pnpm 은 기본적으로
비-hoist 격리라 repo root 에서 해석되지 않아 `ERR_MODULE_NOT_FOUND` 로 죽었다.
(npm 의 hoisting 환경에서는 우연히 동작했을 것이다.)

TASK-103 이 검증한 항목은 `bash -n` / `--help` / 게이트 부재 / `DATABASE_URL` 미설정
네 가지뿐 — **실제 DB 연결 경로가 한 번도 실행된 적이 없었다.** 검증 범위가 "스크립트가
죽지 않는가" 에 머물러 "스크립트가 일을 하는가" 를 보지 않은 것이 근본 원인이다.

### 5.2 수정

- `run_cli()` 신규 — CLI 를 `apps/build-server` cwd 에서 실행해 `tsx` 가 해석되게 한다.
  `$CLI` 문자열 변수(word-splitting 의존)를 함수 호출로 대체.
- `resolve_migrations_dir()` 신규 — cwd 가 바뀌므로 `MIGRATIONS_DIR` 을 **REPO_ROOT 기준
  절대경로로 정규화**한다. 상대경로를 그대로 넘기면 build-server cwd 기준으로 해석돼
  엉뚱한 곳을 가리키는 2차 결함이 생긴다. 이미 절대경로면 (`/...` 또는 `C:...`) 그대로 사용.
- `require_cli_deps()` 신규 — node 부재 / tsx 미설치를 사전 점검해 raw stack trace 대신
  `pnpm install` 안내를 낸다. `preflight()` 로 `require_database_url` 과 묶어 전 게이트 공통 적용.
- `DATABASE_URL` hint 의 포트를 `15432` → `5432` 로 정정 (TASK-125 실측 반영. 15432 는
  compose 매핑 기준이라 Docker 사용 시에만 유효).

### 5.3 검증 — 6 게이트 실제 실행

TASK-103 이 하지 않았던 실행 검증을 수행 (로컬 PostgreSQL 18.4).

- `bash -n` OK / `--help` exit 0 / 게이트 부재 exit 2 / unknown gate exit 2
- `--status` → `applied: 0001~0006, pending: (none)`
- `--plan` → `would apply: (none)` (변경 없음)
- `--apply-all` → idempotent 재적용 OK
- `--apply-up-to 0002` → 목표까지만 처리 OK / 인자 누락 시 exit 2
- `--bootstrap` → greenfield DDL + 일괄 적용 OK
- `--dr-stop` → 안내만, exit 0 (실행 없음)
- `DATABASE_URL` 미설정 → exit 2 + hint (포트 5432 로 정정 확인)
- `MIGRATIONS_DIR` 상대경로 override → REPO_ROOT 기준 정규화 확인
- `MIGRATIONS_DIR` 절대경로 override → 그대로 사용 확인
- **다른 cwd (`/tmp`) 에서 호출 → 정상 동작** (cwd 무관성 확보)
- 존재하지 않는 `MIGRATIONS_DIR` → exit 1 (실패가 조용히 묻히지 않음)

SQL / schema / migration / version / git tag 변경 0. 애플리케이션 코드 영향 0.

### 5.4 문서

- `docs/operations/migration-cli-workflow-2026-07-20.md` 에 TASK-128 정정 블록 추가
  (원래 실행되지 않았다는 사실 + 원인 + 수정) + §7 실행 검증 표 신규.

### 5.5 follow-up

- TASK-125 발견 결함 3건 **전부 해소** (TASK-126 / TASK-127 / TASK-128).
- TASK-124 의 bulk sync 재발 방지 가드 (여전히 미봉인)
- 4xx/5xx 단언 일괄 점검 (TASK-127 발 제안)
