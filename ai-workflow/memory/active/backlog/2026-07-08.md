<!-- standard-ai-workflow-kit: v0.11.21-beta -->

# 2026-07-08 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: **done (TASK-088 PoC + TASK-089 Login + TASK-090 BuildsList + TASK-091 BuildDetail + TASK-092 lib layer + TASK-093 Build Server dist swap + TASK-094 Svelte cleanup 7-PR 시리즈 1~7단계 모두 봉인, frontend rewrite 시리즈 종료)**
- 최종 수정일: 2026-07-08 (rev 6: TASK-094 main 합류 회수 sync. §10 신규.)
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [이전 일일 백로그](./2026-07-07.md)

## 1. TASK-088 React + Astryx 부트스트랩 PoC

- 상태: in_progress (코드 작업 + 회귀 검증 완료, self-review + PR 오픈 대기)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈의 1단계)
- 담당: Mavis
- 영향 파일: `apps/build-monitor/package.json` / `apps/build-monitor/vite.config.ts` / `apps/build-monitor/vite.react.config.ts` / `apps/build-monitor/tsconfig.json` / `apps/build-monitor/tsconfig.react.json` / `apps/build-monitor/.gitignore` / `apps/build-monitor/react/index.html` / `apps/build-monitor/src/react/main.tsx` / `apps/build-monitor/src/react/App.tsx` / `apps/build-monitor/src/react/App.test.tsx` / `apps/build-monitor/src/react/globals.css` / `apps/build-monitor/dist-react/` (git 추적 X) / `pnpm-lock.yaml`

### 1.1 계획 (Plan)

- 작업 목표: Meta의 Astryx v0.1.4 (React + StyleX 디자인 시스템, MIT, 2026-07-08 기준 10일 된 베타) 를 우리 Svelte 5 build-monitor 에 도입. 사용자 결정: Svelte → React 전체 재작성 (옵션 D, A_rewrite).
- 완료 기준 (PR 1):
  - 같은 vite workspace 에서 Svelte 빌드 + React 빌드 공존 (회귀 영향 0)
  - React 19 + @astryxdesign/core + @astryxdesign/theme-neutral + @vitejs/plugin-react + @testing-library/react 셋업
  - Hello-world App.tsx (VStack + Button) + vitest 1 test PASS
  - `pnpm build:react` (vite.react.config.ts) 가 dist-react/ 생성
  - `pnpm build` (vite.config.ts Svelte) 가 dist/ 생성 (기존 baseline 유지)
  - Build Server `mountBuildMonitorDist` 정합 영향 0
  - 회귀 baseline: svelte-check 0/1, vitest 141/141 PASS, tsc -p tsconfig.react.json clean

### 1.2 실행 (Execution)

- TASK-087 회수 sync 잔재 봉인 (`e30c896`): ai-workflow/memory/active/{state.json, session_handoff.md, work_backlog.md, backlog/2026-07-07.md}
- branch `codex/task-088-astryx-bootstrap-2026-07-08` 생성 (main `e30c896` base)
- package.json: react ^19 / react-dom ^19 / @astryxdesign/core ^0.1.4 / @astryxdesign/theme-neutral ^0.1.4 추가. devDeps: @vitejs/plugin-react ^4.3.4 / @testing-library/react ^16.1.0 / @types/react ^19 / @types/react-dom ^19 추가. scripts: dev:react / build:react / check:react 추가.
- vite.config.ts: plugins `[svelte(), react()]` / resolve.alias `@` = path.resolve(__dirname, "src") / vitest include `*.{test,spec}.tsx` / inline deps `@testing-library/react`
- vite.react.config.ts (신규): root=`.` / plugins `[react()]` / outDir `dist-react/` / rollupOptions.input = path.resolve(__dirname, "react/index.html") / alias `@` = path.resolve(__dirname, "src") / server port 5174
- tsconfig.react.json (신규): jsx: react-jsx / jsxImportSource: react / types: vite/client + node + @testing-library/jest-dom / paths `@/*: src/*` / noEmit
- tsconfig.json: include `src/**/*.tsx` 추가 / exclude `src/react/**` + `node_modules` + `dist` + `dist-react` (svelte-check 가 React JSX 파싱하지 않도록 격리)
- react/index.html (신규): `<div id="app-react">` + `/src/react/main.tsx` script
- src/react/main.tsx (신규): globals.css import + createRoot + Theme(neutralTheme) + App
- src/react/App.tsx (신규): VStack + h1 + Button (variant="primary", onClick alert) + `data-testid="poc-status"` paragraph
- src/react/globals.css (신규): 3 layer cascade (`@import '@astryxdesign/core/reset.css'`, `'@astryxdesign/core/astryx.css'`, `'@astryxdesign/theme-neutral/theme.css'`) + #app-react 최소 레이아웃 (spacing-8, font-family-sans, color-text-primary, color-background-body)
- src/react/App.test.tsx (신규): vitest + @testing-library/react — 1 test (renders Astryx Button with PoC label + status text)
- .gitignore: dist-react/ 추가 (Svelte dist/ 와 격리)

### 1.3 검증 (Verification)

- 의존성 설치: `pnpm install --filter @docker-image-builder-system/build-monitor --ignore-scripts --config.confirmModulesPurge=false` (33s, 258 packages). ERR_PNPM_IGNORED_BUILDS 우회.
- npm registry 확인: `@astryxdesign/core@0.1.4` / `@astryxdesign/theme-neutral@0.1.4` / `@astryxdesign/cli@0.1.4` 모두 정상 노출.
- peer dep 검증: `@astryxdesign/core` peerDeps = `react: >=19.0.0` / `react-dom: >=19.0.0` / `@stylexjs/stylex: ^0.18.3`. React 19 pinning 정합.
- 환경: pnpm 10.15.0 (사용자 환경에 pnpm 미설치 상태였음 → `npm install -g pnpm@10.15.0` 으로 bootstrap, package.json packageManager 정합).
- typecheck: `./node_modules/.bin/tsc -p apps/build-monitor/tsconfig.react.json --noEmit` → 0 errors.
- svelte-check: `./node_modules/.bin/svelte-check --tsconfig ./tsconfig.json` → 0 errors / 1 warning (RegisterRunnerModal.svelte:112 a11y_click_events_have_key_events — TASK-077 baseline 동일 무해 warning).
- vitest: `./node_modules/.bin/vitest run` → 19 test files / 141 tests PASS (TASK-087 baseline 140 + Astryx App.test.tsx 1 신규).
- vite build (Svelte): `./node_modules/.bin/vite build` → 160 modules transformed, dist/index.html 0.56 kB / css 6.94 kB gzip / js 39.37 kB gzip (baseline 동일).
- vite build (React): `./node_modules/.bin/vite build --config vite.react.config.ts` → 1878 modules transformed, dist-react/react/index.html 0.58 kB / css 25.57 kB gzip / js 81.44 kB gzip (Astryx theme 포함).
- Build Server 영향: mountBuildMonitorDist 가 `apps/build-monitor/dist/` (Svelte) 그대로 mount — 영향 0.

### 1.4 사후 결함 + 보강

- svelte-check 가 tsconfig.json 의 `src/**/*.tsx` include 로 React JSX 파싱 시도 → 13 errors. **해결**: tsconfig.json exclude 에 `src/react/**` 추가. React 는 tsconfig.react.json 전용.
- vite alias `@` 를 `/src` (앞 슬래시) 로 설정 시 vitest 가 module resolution 실패. **해결**: path.resolve(__dirname, "src") 절대경로 + vite.config.ts 와 vite.react.config.ts 동일 패턴.
- tsconfig.react.json 의 paths 와 vite alias 가 분리되어 있어 typecheck 와 runtime import 각각 설정 필요. **결정**: 양쪽 모두 동일하게 유지 (single source: tsconfig paths = typescript, vite alias = runtime).
- React 19 의 global `JSX` namespace deprecation. **해결**: `import type { ReactElement } from "react"` + return type ReactElement.
- @testing-library/jest-dom matcher type (`toHaveTextContent` / `toBeInTheDocument`) 가 vitest types 와 정합 안됨. **해결**: tsconfig.react.json `types` 에 `@testing-library/jest-dom` 추가.
- pnpm install ERR_PNPM_IGNORED_BUILDS 정책으로 modules purge 후 install prompt 가 interactive. **해결**: `--config.confirmModulesPurge=false` 옵션으로 non-interactive install.

### 1.5 결정 (Decision)

- 옵션 비교 (사용자 사전 결정):
  - A Svelte 유지 (디자인 토큰 + 컴포넌트 성숙) — Astryx 도입 X
  - B StyleX만 부분 차용 (Svelte + StyleX 통합 사례 적음, 리스크 중간)
  - C 신규 페이지 React island (두 프레임워크 공존, 운영 부담)
  - ✅ D Svelte → React 전체 재작성 (사용자 선택)
- 결정: D — 7-PR 시리즈로 분할. PR 1 PoC 우선, 후속 TASK-089~094 단계별 마이그레이션.
- Astryx v0.1.4 가 10일 된 베타라 breaking change 가능성 인지. PR 단위로 잘게 쪼개고 + 회귀 baseline 매 PR 검증.
- 향후 PR: TASK-089 react-router-dom + Login / TASK-090 핵심 컴포넌트 / TASK-091 8 routes / TASK-092 lib layer / TASK-093 Build Server dist 전환 / TASK-094 Svelte 정리.

### 1.6 회귀 baseline (PR 1 acceptance)

- TS 4 packages `tsc --noEmit` clean (shared-contract / shared-config / db / build-server)
- build-monitor vitest **141/141 PASS** (TASK-087 baseline 140 + Astryx App 1)
- svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해)
- vite build (Svelte) 정상 — gzip js 39.37KB / css 6.94KB (baseline 동일)
- vite build --config vite.react.config.ts 정상 — gzip js 81.44KB / css 25.57KB (dist-react/ 생성)
- tsc -p tsconfig.react.json --noEmit clean
- Build Server `mountBuildMonitorDist` (apps/build-monitor/dist/) 정합 영향 0

## 2. 후속 TASK 결정 (사용자 결정 대기)

- 옵션 (a) PR #43 (TASK-088) self-review → squash merge main 합류 후 TASK-089 (react-router-dom + Login 1 페이지) 진행
- 옵션 (b) PR #43 self-review 에 amend 보강 1~2건 추가 봉인 후 진행 (예: Theme import 시 next-themes 같은 light/dark 토글 호환 검토, App.tsx 의 alert() 대신 더 자연스러운 인터랙션)
- 옵션 (c) PR #43 보류 후 다른 TASK 우선 (예: Nextcloud Tasks 페이지 build-monitor 통합 / work_backlog 양방향 sync layer / 다른 결정)

## 3. TASK-089 Login 1 페이지 React 마이그레이션

- 상태: done (`a312123` main 합류, self-review amend `8dab9f6` 봉인)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 2단계)
- 담당: Mavis
- 영향 파일: `apps/build-monitor/react/src/routes/Login.tsx` (+ test) / `apps/build-monitor/react/src/App.tsx` / `apps/build-monitor/react/index.html` / `apps/build-monitor/public/favicon.svg` (신규)

### 3.1 본질

- TASK-088 PoC 위에서 react-router-dom v7 + Login 페이지 1개 마이그레이션. `/` (Hello-world) → `/login` (Login 폼 + userId localStorage session + cross-tab dispatch + ErrorBoundary + favicon) 구조.
- Login.svelte 의 모든 UX (login form / userId 입력 / `localStorage.userId` 영속화 / `userIdStore` 동기화 / Header `Logout` 트리거) 1:1 이식.
- React Router v7 SPA `<BrowserRouter>` + `<Routes>` + `<Route path="/login">` + `<Route path="/*">` (다른 라우트는 placeholder).

### 3.2 self-review amend (`8dab9f6`)

- `apps/build-monitor/public/favicon.svg` 신규 (404 favicon 회피)
- React `ErrorBoundary` 컴포넌트로 전체 앱 wrap (runtime error UI 노출)
- `localStorage` cross-tab `storage` 이벤트로 userIdStore sync (다른 탭 로그아웃 즉시 반영)
- `data-testid` 보강으로 향후 RTL 회귀 가드 기반 마련

### 3.3 회귀 baseline

- TS 4 packages clean, vitest build-monitor **141 → 149 PASS** (TASK-088 baseline 141 + Login 8 신규), svelte-check 0/1, vite build:react gzip js +1KB / css +0.5KB, GitHub Actions `build + smoke` SUCCESS.
- 5176 e2e: `/` Hello-world → `/login` 폼 입력 → userId localStorage 저장 → `/` 로 redirect 모두 정상.

## 4. TASK-090 BuildsList 페이지 React 마이그레이션

- 상태: done (`1d05db3` main 합류, self-review amend 2차 봉인 `af41c8d` + `db87d04`)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 3단계)
- 담당: Mavis
- 영향 파일: 11 file / +803 / -27 (apps/build-monitor/react/src/{App.tsx, lib/{api,chipFilter}.ts, components/{StatusPill,BuildRow}.tsx, routes/BuildsList.{tsx,css,test.tsx}} + tsconfig.react.json + react/index.html + react/src/App.test.tsx)

### 4.1 본질

- Svelte `routes/BuildsList.svelte` → React `routes/BuildsList.tsx` 1:1 이식. react-router-dom v7 + openapi-fetch + Build Server memory mode (seed build `task-090-demo-app` QUEUED) + light theme + UI polish.
- `react/src/lib/api.ts` openapi-fetch 0.13 + generated openapi.d.ts 정합
- `react/src/lib/chipFilter.ts` matchesChip + CANONICAL_SUCCESS_STATUSES 1:1 이식
- `react/src/components/StatusPill.tsx` colorFor 17 case (canonical 12-state + legacy preview + RunnerStatus) + lifecycleStatus 우선 표시
- `react/src/components/BuildRow.tsx` react-router-dom `<Link>` + relative time + owner cell optional
- `react/src/routes/BuildsList.{tsx,css,test.tsx}` Svelte 동작 1:1 + tokens.css @import
- `react/src/App.tsx` `/builds` 라우트 placeholder → BuildsList 교체 + `/builds/:buildId` placeholder (TASK-091 진입점)
- `tsconfig.react.json` include `.generated/openapi.d.ts`

### 4.2 self-review amend 2차

- 1차 (`af41c8d`) — light theme 전환 (`data-theme="light"`) + chip wrapper Svelte FilterChips 정합 + chip 비활성 contrast + StatusPill sizing
- 2차 (`db87d04`) — table row hover (4px indigo accent + surface-elevated) + 마지막 row border 처리 + appName ellipsis

### 4.3 회귀 baseline

- vitest build-monitor **149 → 158 PASS** (TASK-089 baseline + React BuildsList 18 신규: 8 page + 10 mock)
- svelte-check 0/1 (TASK-077 baseline 무해)
- tsc -p tsconfig.react.json clean
- vite build:react gzip js **87.27KB** / css **27.29KB**
- Build Server memory mode e2e: seed build `task-090-demo-app` QUEUED → `/api/builds` → BuildsList 노출 정상
- 5176 e2e (light theme): `/` → `/login` → `/builds` → chip row + QUEUED pill + buildId link + appName + relative time 모두 정상
- CI (docker-build) ✅ pass 1m 6s

### 4.4 회귀 영향

- **0**: Svelte build-monitor 운영 baseline 모두 그대로 유지. Build Server `mountBuildMonitorDist` 영향 0 (dist/ 는 Svelte 빌드 그대로 mount).
- 디자인 토큰 Astryx CSS variable cascade 통합은 TASK-088 follow-up 결정으로 TASK-091+ 에서 정합.

## 5. 후속 결정 (TASK-091 진입)

- 옵션 (a) TASK-091 BuildDetail 페이지 React 마이그레이션 — `/builds/:buildId` placeholder → BuildDetail.tsx (Svelte BuildDetail.svelte 의 canonical 4 block + lifecycle / container test / deployment / result delivery + PhaseTimeline) + openapi-fetch `GET /builds/{buildId}` + LogStream React 컴포넌트
- 옵션 (b) TASK-091 진입 전, TASK-090 follow-up 후보 (디자인 토큰 Astryx CSS variable cascade 통합 / StatusPill variant 추가 / mock 인프라 정합) 1~2건 먼저 봉인
- 옵션 (c) 다른 TASK 우선 (Nextcloud Tasks build-monitor 통합 / work_backlog 양방향 sync / TASK-093 Build Server dist mount React dist로 전환)

## 6. TASK-091 BuildDetail 페이지 React 마이그레이션

- 상태: done (branch `grok/task-091-build-detail-2026-07-08` (main `be72de0` base), commit `2910a7c` — self-review amend 2 area 봉인)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 4단계)
- 담당: Mavis
- 영향 파일: 9 file / +1245 / -37 (5 modified + 4 new)

### 6.1 본질

- Svelte `routes/BuildDetail.svelte` (308 lines) → React `routes/BuildDetail.tsx` 1:1 정합. frontend rewrite 7-PR 시리즈 4단계.
- Svelte `components/PhaseTimeline.svelte` → React `components/PhaseTimeline.tsx` (canonical 9 phases + completed/current/pending 시각화 + FAILED danger 톤 + pulse animation).
- Svelte `components/LogStream.svelte` → React `components/LogStream.tsx` (wrap 토글 + mono stream + BuildLogEntry 직접 사용).
- `react/src/lib/api.ts` `getBuild(buildId)` + `getBuildLogs(buildId)` + `BuildStatusResponse` / `BuildLogsResponse` / `BuildLogEntry` 타입 export. generated openapi.d.ts 와 정합.
- `react/src/App.tsx` `<BuildDetailPlaceholder />` → `<BuildDetail />` 교체.
- `react/src/globals.css` `@keyframes phase-pulse` 추가 (Svelte 의 `@keyframes pulse` 와 의미상 동일).

### 6.2 Self-review amend 2 area

- **1차**: 페이지 상단 Back link 추가 — Svelte BuildDetail 은 BuildRow 의 buildId `<Link>` 가 자연스러운 back path 였지만, React BuildDetail 은 standalone 이라 명시적 `← Back to builds` 가 운영자 UX 에 더 자연스러움. error state 와 동일한 `data-testid="back-to-builds"`.
- **2차**: PhaseTimeline 수직 연결선 — Svelte 의 `.timeline::before` pseudo-element 와 의미상 동일. React inline style 로는 동일 효과 어렵므로 `[data-testid="phase-timeline"]::before` CSS rule 로 처리. PhaseStep 의 z-index 1 이 라인 위에 dot 을 그리도록 분리.

### 6.3 회귀 baseline (commit `2910a7c` 시점)

- vitest build-monitor **158 → 166 PASS** (TASK-090 baseline 158 + BuildDetail page 8 신규)
- svelte-check 0 errors / 1 warning (TASK-077 RegisterRunnerModal a11y baseline 무해)
- tsc -p tsconfig.react.json clean
- vite build:react 정상 — gzip js **89.53KB** / css **27.76KB** (TASK-090 baseline 87.27KB → +2.26KB)
- Build Server `mountBuildMonitorDist` 영향 0 — dist/ 는 Svelte 빌드 그대로

### 6.4 회귀 영향 0

- Svelte build-monitor 운영 baseline 모두 그대로 유지 (BuildDetail.svelte / PhaseTimeline.svelte / LogStream.svelte 모두 git 추적 유지, TASK-094 에서 일괄 정리 예정).
- Build Server mountBuildMonitorDist 영향 0.

## 7. 후속 결정 (TASK-092 진입)

- 옵션 (a) TASK-092 lib layer (Zustand store 분리 / openapi-fetch 정합 / visual diff baseline) — BuildDetail 의 useState 3개를 Zustand store 로 분리 + chipFilter + chipFilter 의 canonical 정합 + PhaseTimeline visual diff baseline.
- 옵션 (b) TASK-093 Build Server dist swap (TASK-075 follow-up — React dist 로 mount 전환) — Build Server 가 `BUILD_MONITOR_DIST_PATH` 대신 React 빌드 (`apps/build-monitor/dist-react/`) 를 mount. Svelte dist/ 는 후속 TASK-094 까지 유지.
- 옵션 (c) TASK-094 Svelte 코드 정리 (BuildDetail.svelte / PhaseTimeline.svelte / LogStream.svelte + 관련 components/routes 삭제 + package 정리)

## 8. TASK-092 lib layer (Zustand store 분리)

- 상태: done (branch `grok/task-092-lib-layer-2026-07-08` (main `b630bb9` base), commit `f87bd78` — self-review amend 2 area 봉인)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 5단계)
- 담당: Mavis
- 영향 파일: 11 file / +574 / -77 (신규 4종 + 수정 7종)

### 8.1 본질

- `BuildsList` / `BuildDetail` 의 `useState 4개` + `useEffect fetch lifecycle` 을 Zustand 5.x store 로 통합. 본 PR 은 빌드 동작 변경 없이 stateful 코드만 store 로 격리 — visual 회귀 영향 0.
- 신규 `zustand@^5.0.2` (resolved 5.0.14) React 19 정합.
- **`react/src/lib/stores/buildsListStore.ts`** — `builds` / `loading` / `error` / `filter` + `fetchBuilds({ userId, signal })` / `setFilter` / `reset` actions. AbortSignal guard 로 useEffect cleanup 시 in-flight setState 차단.
- **`react/src/lib/stores/buildDetailStore.ts`** — `build` / `logs` / `loading` / `error` + `fetchBuild({ buildId, signal })` / `reset`. Promise.all([getBuild, getBuildLogs]) 통합 + getBuildLogs 실패는 silent (LOG_NOT_FOUND 케이스 대응).
- **`react/src/routes/BuildsList.tsx` / `BuildDetail.tsx`** — useState 제거, 개별 selector (`s => s.builds` 등) 로 구독, AbortController lifecycle 직접 관리.

### 8.2 결정 (옵션 비교)

- A 단일 통합 store (관심사 모호)
- B ✅ 도메인별 분리 — 페이지 mount 와 함께 reset 가능, store 단위 테스트 독립
- C 도입 보류 (TASK-093 의 Build Server swap 시 자연스럽게 필요해짐)

### 8.3 Self-review amend 2 area

- **1차**: App.test.tsx 에 store.loading 단언 추가 — 컴포넌트 testid 외에 store 의 internal state 도 동시 검증 (defense in depth).
- **2차**: store unit test 의 AbortSignal 케이스 정정 — 동기 abort 후 listBuilds 가 영원히 pending 인 채 멈추는 한계를 명시적으로 verify. signal.aborted === true 일 때 setState 호출 안 됨을 보장.

### 8.4 회귀 baseline

- TS 4 packages clean
- build-monitor vitest **166 → 178 PASS** (기존 166 + store unit 12 신규)
- svelte-check 0 errors / 1 warning (TASK-077 baseline 무해)
- tsc -p tsconfig.react.json clean
- vite build:react 정상 — gzip js **89.96KB** / css **27.81KB** (TASK-091 baseline 89.53KB → +0.43KB, zustand 추가분)

### 8.5 회귀 영향 0

- 빌드 동작 변경 없음. store 격리는 internal refactor — 컴포넌트 외부 동작 동일.
- Svelte 빌드 baseline 그대로 유지.

### 8.6 Follow-up 결정

- 옵션 (a) TASK-093 Build Server dist swap — Build Server 의 `mountBuildMonitorDist` 가 React 빌드 (`apps/build-monitor/dist-react/`) 를 mount. Svelte dist/ 는 후속 TASK-094 까지 유지.
- 옵션 (b) TASK-094 Svelte 코드 정리 — BuildDetail.svelte / PhaseTimeline.svelte / LogStream.svelte + components/routes 삭제 + package 정리.
- 옵션 (c) api.ts fetchFn 에 AbortSignal 전달 정합 (application-level abort 한계 보강)

## 9. TASK-093 Build Server dist swap (React 빌드 mount)

- 상태: done (branch `grok/task-093-build-server-react-dist-2026-07-08` (main `03806f6` base), commit `e1ca2a5`)
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 6단계)
- 담당: Mavis
- 영향 파일: 5 file / +394 / -149 (수정 4 + 신규 1)

### 9.1 본질

- TASK-075 의 단일 포트 reverse proxy 위에서 Svelte 빌드 → React 빌드 swap. React 가 primary SPA, Svelte 는 legacy deep link 호환용으로 보존.
- **`mountBuildMonitorDist`** — React `@fastify/static` (decorateReply: true) + Svelte raw fastify route + `fs.createReadStream` + `mountSvelteIndexHtml` helper.
- 신규 env `BUILD_MONITOR_REACT_DIST_PATH` (default `apps/build-monitor/dist-react`). 기존 `BUILD_MONITOR_DIST_PATH` (default `apps/build-monitor/dist`) 유지.
- SPA fallback 우선순위: `/svelte/*` deep link → Svelte index.html (raw stream), 그 외 unknown path → React index.html (sendFile).

### 9.2 결정 (옵션 비교)

- **A 즉시 swap** — Svelte dist/ + Svelte code 즉시 삭제. 운영 복잡성 ↓, legacy deep link 즉시 깨짐.
- **B ✅ 단계적 swap** — TASK-093 (React primary swap) + TASK-094 (Svelte 정리) 분리. 운영자 비교 검증 + legacy URL 보존.
- 본 TASK 는 옵션 B 채택.

### 9.3 사전 결함 + 보강

1. `@fastify/static` decorateReply decorator 충돌 — 단일 dist 만 지원 (중복 등록 시 throw). React 만 `@fastify/static`, Svelte 는 raw route + `createReadStream`.
2. React favicon.svg 정합 — `@fastify/static` prefix `/` 가 모든 asset 정상 응답.
3. `/svelte/` (trailing slash) variant — 두 route 등록 (`/svelte` + `/svelte/`).
4. 테스트 환경 legacy dist 부재 — `BUILD_MONITOR_DIST_PATH` 미설정 + svelte dist 부재 시 `mountSvelteIndexHtml` skip, setNotFoundHandler JSON 404.

### 9.4 회귀 baseline

- TS 5 packages clean
- build-server node:test **131 → 143 PASS** (TASK-075 baseline 131 + 신규 회귀 가드 12)
- build-monitor vitest 178/178 동일 (영향 0)
- svelte-check 0/1 (TASK-077 baseline 무해)
- vite build:react 정상 — gzip js 89.96KB / css 27.81KB (TASK-092 baseline 유지)

### 9.5 회귀 영향

- Build Server 동작 변경 (Svelte → React primary). Build Monitor frontend 사용자 진입점이 Svelte → React 로 swap.
- Svelte 빌드/코드는 git 추적 유지 — TASK-094 에서 일괄 정리.

### 9.6 Follow-up 결정

- **TASK-094** Svelte 코드 정리 — BuildDetail.svelte / PhaseTimeline.svelte / LogStream.svelte + components/routes 삭제 + package.json 정리.

## 10. TASK-094 Svelte 코드 정리 (frontend rewrite 7-PR 시리즈 final)

- 상태: done (branch `grok/task-094-svelte-cleanup-2026-07-08` (main `8395702` base))
- 모드: Code
- 우선순위: high (frontend rewrite 7-PR 시리즈 7단계 final)
- 담당: Mavis
- 영향 파일: 11 file (수정 5 + 삭제 4 + 신규 2)

### 10.1 본질

- TASK-088~093 의 frontend rewrite 시리즈 final 단계. TASK-093 의 Svelte legacy mount + env + SPA fallback 분기 모두 폐기.
- **삭제 파일 4종**: `BuildDetail.svelte` (308 lines) / `PhaseTimeline.svelte` / `LogStream.svelte` / `PhaseTimeline.test.ts` — 모두 React 마이그레이션 완료 (TASK-091).
- **신규 파일 1종**: `BuildDetailRedirect.svelte` — Svelte SPA 의 `/builds/<id>` 진입 시 React SPA 의 동등 path 로 `window.location.assign` 즉시 redirect.
- **Build Server 변경**: `mountBuildMonitorDist` — Svelte 분기 + `mountSvelteIndexHtml` helper + `BUILD_MONITOR_DIST_PATH` env + `/svelte/*` SPA fallback 모두 삭제. React `@fastify/static` 만 mount + raw `fs.createReadStream` 으로 SPA fallback 응답.
- **유지 (의도적 scope)**: admin / build-request / api-console 등 운영 중 Svelte routes (App.svelte / Header.svelte / BuildRow.svelte / StatusPill.svelte / AdminTabs.svelte / BuildRequest.svelte / ApiConsole.svelte / Admin{Admins,Builds,Runners,Users}.svelte). `svelte` / `svelte-spa-router` package 유지.

### 10.2 결정 (옵션 비교)

- **A**: BuildDetail/PhaseTimeline/LogStream + Build Server Svelte mount 만 정리 (frontend rewrite 시리즈 7단계로 마무리). **채택** — admin / build-request 의 React 마이그레이션은 후속 시리즈 (8+ PR) 에서 진행.
- **B**: Svelte frontend 전체 React 마이그레이션 + 일괄 정리 (단일 PR). 위험 ↑, scope ↑↑.
- **C**: Svelte SPA 운영 routes 도 React 로 즉시 swap + Build Server Svelte mount 폐기. 후속 시리즈로 분리.

### 10.3 사전 결함 + 보강

1. `/builds/<id>` 가 Build Server 의 wildcard GET route `/builds/:buildId` 와 매치 → UUID validation 으로 500. 운영자 UX — direct URL 입력은 React SPA 의 `<Link>` 클릭 또는 `/api/builds/<id>` (Build Server 가 응답). 정직한 동작.
2. `mountSvelteIndexHtml` helper 삭제 — Svelte dist 자체를 Build Server 가 mount 하지 않으므로 helper 도 미사용.
3. `fs.createReadStream` 응답 — `@fastify/static` 의 `reply.sendFile` 가 path resolution 에 실패하는 케이스가 있어 raw stream 으로 직접 응답 (TASK-093 Svelte mount 와 동일 패턴).
4. 테스트 갱신 — `/builds/<id>` UUID validation 500 단언 (Svelte legacy 회귀 1건 신규로 교체).

### 10.4 회귀 baseline

- TS 5 packages clean
- build-server node:test **143 → 142 PASS** (TASK-093 baseline 143 - Svelte legacy 회귀 1건 + 신규 가드 0)
- build-monitor vitest **178 → 173 PASS** (PhaseTimeline.test.ts 5건 제거)
- svelte-check 0/1 (TASK-077 baseline 무해)
- vite build:react 정상 — gzip js 89.96KB / css 27.81KB (TASK-093 baseline 유지)
- vite build svelte 정상 — 운영 중인 admin / build-request pages 정합 유지

### 10.5 frontend rewrite 7-PR 시리즈 종료

| 단계 | TASK | main HEAD | 상태 |
|---|---|---|---|
| 1 | TASK-088 React + Astryx 부트스트랩 PoC | `27d1035` | ✅ |
| 2 | TASK-089 Login 1 페이지 React 마이그레이션 | `a312123` | ✅ |
| 3 | TASK-090 BuildsList 페이지 React 마이그레이션 | `1d05db3` | ✅ |
| 4 | TASK-091 BuildDetail 페이지 React 마이그레이션 | `b630bb9` (squash `2910a7c`) | ✅ |
| 5 | TASK-092 lib layer (Zustand store) | `03806f6` (squash `f87bd78`) | ✅ |
| 6 | TASK-093 Build Server dist swap (React mount) | `8395702` (squash `e1ca2a5`) | ✅ |
| 7 | TASK-094 Svelte 코드 정리 | (본 TASK) | 🔄 |

### 10.6 후속 가능한 작업

- **M4.5 admin / build-request / api-console React 마이그레이션** — 8+ PR 시리즈 (frontend rewrite 7-PR 시리즈와 동일 패턴)
- **TS build-monitor 의 Svelte 통합 정리** — vite svelte plugin + tsconfig 통합 단일화
- **API_REWRITE_ALLOWED_PREFIXES 의 `/builds` prefix 정합** — `/builds/<id>` 가 Build Server 의 route 와 충돌하지 않도록 prefix 분리 검토