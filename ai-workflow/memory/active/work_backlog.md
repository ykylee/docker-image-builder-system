<!-- standard-ai-workflow-kit: v0.11.25-beta -->

# 작업 백로그 인덱스

- 문서 목적: 프로젝트의 모든 작업 항목과 날짜별 백로그 링크를 관리한다.
- 범위: 전체 태스크 목록, 우선순위, 진행 상태, 날짜별 기록 연결
- 대상 독자: 개발자, AI 에이전트, 프로젝트 매니저
- 상태: stable
- 최종 수정일 갱신: TASK-077 (admin-initiated runner registration) PR #42 squash merge main 합류 (`6847f1e`). 14 file 변경 (신규 2 + 수정 12). 신규 1 file: `apps/build-monitor/src/components/RegisterRunnerModal.svelte` (runnerId input + Enter/Escape 키보드 / backdrop click → close. 성공시 (201) close + onSuccess, 실패 (400/401/403/409) 시 inline error 표시 + modal 유지). amend 12 file: `packages/shared-contract/src/build/runner-registry.ts` (`adminRunnerRegisterRequestSchema` strict + `adminRunnerRegisterResponseSchema` adminRunnerSchema wrap — empty / extra field / type mismatch → 400) / `apps/build-server/src/routes/admin-routes.ts` (POST /admin/runners endpoint 신규 — 401/403/400/409/201) / `apps/build-server/src/services/build-service.ts` createAdminRunner method / `apps/build-server/src/repositories/build-repository.ts` createAdminRunner interface / `apps/build-server/src/repositories/{memory,postgres}-build-repository.ts` createAdminRunner (memory: Map.has + postgres: ON CONFLICT DO NOTHING — atomic) / `apps/build-server/src/app/openapi.ts` (OpenAPI component + path 등록) / `apps/build-server/tests/admin-runners-routes.test.ts` (8건 회귀 테스트: 401/403/400 empty/400 extra/201 created/409 duplicate/409 self-then-admin/200 list) / `apps/build-monitor/src/lib/api.ts` createAdminRunner helper / `apps/build-monitor/src/routes/AdminRunners.svelte` (`+ Register Runner` 버튼 + RegisterRunnerModal 통합) / `apps/build-monitor/src/routes/AdminRunners.test.ts` (5건 회귀 가드: button visible/modal opens/success refresh+close/409 modal open/400 modal open) / `docs/PROJECT_PROFILE.md` §3.11 신규. 사전 결함 + 보강 4건 (운영 가이드 §5): strict schema 의 extra fields 거부 (zod .strict() 가 admin 의 typing 실수를 400 으로 거부. backend 가 새 field 를 받기 전 contract 정합성 보장) / self-register 와의 race condition 방지 (postgres ON CONFLICT DO NOTHING / memory Map.has 가 atomic. 어느 한 쪽이 success, 다른 한 쪽이 409) / admin UI 의 modal close vs error 표기 policy (error 시 modal 닫지 않음 — 재시도 가능. 성공시에만 close + refresh) / runnerId 가 RUNNER_ID env 와 일치해야 함 (modal 의 modal-help 가 명시. 운영자가 mismatch 를 사전에 알 수 있도록). 회귀 baseline: TS 4 packages `tsc --noEmit` clean (변경 파일 영향 없음), build-server node:test 131 → 139 PASS (8건 신규), build-monitor vitest 135 → 140 PASS (5건 신규), Go 8 packages 모두 PASS (TASK-076 baseline 유지), svelte-check 0 errors (1 warning — modal backdrop a11y 무해), `vite build` OK (gzip js 39.46KB / css 6.93KB — RegisterRunnerModal 추가로 +0.22KB / +0.05KB). workflow meta sync (state rev 106→107, handoff 85→86, work_backlog 79→80, backlog index 50→51 / latest 27→28) 같은 sync commit 안에 포함.
- 최종 수정일: 2026-07-06 (rev 70→71: TASK-082 postgres backend multi-runner 운영 검증 PR #33 squash merge main 합류 (`94a220a`). branch `codex/task-082-postgres-multi-runner-2026-07-06` (main `dd39572` base) → squash merge → `--delete-branch` 자동 삭제 완료. 4 file / 697 insertions. 신규 3 file: `compose.dev.runner-multi-postgres.yaml` (postgres profile + 3 runner override), `apps/build-server/scripts/e2e-multi-runner-postgres.sh` (8 단계 자동 검증 — postgres cold start wait + 영속 검증 포함), `docs/operations/multi-runner-claim-postgres-2026-07-06.md` (운영 가이드). amend 1 file: `apps/build-server/src/app/create-app.ts` `MIGRATIONS_DIR` 를 `import.meta.url` 의 `../../migrations/` (dist tree depth 차이로 부재 경로 가리킴) 에서 `process.cwd()` 기준 `apps/build-server/migrations/` 로 변경 — postgres backend 부팅 시 `ENOENT` surface 됐고 memory backend 는 `applyMigrations` 호출 안 해서 잠복. TASK-075 `BUILD_MONITOR_DIST_PATH` 와 동일 `import.meta.url` 함정 봉인 패턴. self-review amend 3건 — (a) 운영 가이드 §3.1 (b) TASK-081-B 의 e2e-multi-runner.sh 같은 `BASE` + heredoc 결함 명시, (b) 운영 가이드 §3.5 분포 비결정성 명시 (TASK-081-B 1+2+2 / TASK-082 3+0+2 모두 합=5 보장), (c) e2e `BASE` env var `BUILD_SERVER_URL` override 지원. 운영 검증 결과: 3 runner 모두 ACTIVE (postgres runner table 3 row), 5 build 적재 + source upload, 5/5 build terminal phase (FAILED × 5 — size 0 source 의 의도된 dummy), buildsClaimed 분포 (3+0+2) — 총 5, 중복 0, 2+ actively claimed, postgres `build_request` 영속 검증 filtered=5 / distinct appName=5, migrations 자동 적용 0001~0005 모두. 회귀 baseline: TS 4 packages `tsc --noEmit` clean, Go 7 packages `go test ./...` 모두 PASS, e2e-multi-runner-postgres.sh ALL PASS. workflow meta sync (state rev 97→98, handoff 76→77, work_backlog 70→71, backlog index 41→42 / latest 18→19) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 95→96: TASK-102 PROJECT_PROFILE §3 baseline 양축 동기화 (docs only) 봉인 sync. 본 세션은 2026-07-20 신규 작업 흐름으로 PROJECT_PROFILE.md 의 §3 명령 박스 run_local / run_local_postgres 의 default backend 정합 + §3.5 / §3.6 / §3.10 / §3.11 / §3.12 회귀 baseline 의 memory-only 표기를 Postgres 동등 baseline 양축 동기화 + §4 검증 포인트 Postgres 운영 baseline 추가 + 다음에 읽을 문서 reference 추가 진행. branch 없음 — docs only 변경 (2 file, 123 insertions + 6 deletions) 으로 main `4ad57f2` 즉시 commit + push 후 workflow meta sync 단일 commit. 운영 가이드 신규 1 종: `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md` (의도 / 변경 / 사전 결함 + 보강 3건 / 회귀 baseline docs only / follow-up). 결정: 옵션 A (docs only + 1 운영 가이드, 사양 변경 없음). 회귀 영향 0 — TS 5 packages clean, vitest 130/130 PASS, build-server 143/143 PASS, Go 7+ packages PASS, vite build:react 정상 (gzip js 99.01KB / css 30.62KB). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 / build-server 113 → 143 / TS 5 packages clean / vite build:react 정상. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline batch 1 + batch 2 + TASK-066 follow-up batch 3 + TASK-102 docs only 까지 **20 TASK 연속 봉인** (2026-07-08 ~ 2026-07-20). follow-up: scripts/migrate.ts standalone CLI 운영 workflow / postgres migration 0004 build_source TOAST 전략 / 운영 안정화 / 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선 모두 별도 사용자 결정 대기. workflow meta sync (state rev 131→132, handoff 101→102, work_backlog 95→96, backlog index 65→66 / latest 43→44) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 96→97: TASK-103 scripts/migrate.ts standalone CLI 운영 workflow 정립 + 권고 스크립트 scripts/db-migrate.sh 봉인 sync. 본 세션의 단일 작업 흐름에서 TASK-102 (PROJECT_PROFILE §3 baseline 양축 동기화, main `4ad57f2`) + TASK-103 (권고 스크립트 + 운영 가이드 + PROJECT_PROFILE §3 migration 운영 권장 참조, main `7b74614`) 까지 **2 TASK 연속 봉인**. TASK-103 결정: 옵션 A (운영 가이드 + 권고 스크립트). branch 없음 — 신규 2 file (scripts/db-migrate.sh 6 게이트 standalone CLI 위임 wrapper + 운영 가이드) + PROJECT_PROFILE.md 1 file 수정. 3 file / 366 insertions. 회귀 영향 0 — TS 5 packages clean, vitest 130/130 PASS, build-server 143/143 PASS, Go 7+ packages PASS, vite build:react 정상, postgres migration 0001~0005 적용 정상, e2e-source-archive-postgres / e2e-multi-runner-postgres ALL PASS 유지. scripts/db-migrate.sh 자체 검증: bash -n syntax OK / --help exit 0 OK / 게이트 부재 exit 2 OK / --dr-stop 안내만 출력 OK / DATABASE_URL 미설정 hint + exit 2 OK. 결정 대기 항목 해소: scripts/migrate.ts standalone CLI 운영 workflow (TASK-103 봉인). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123) / build-server 113 → 143 (+30) / TS 5 packages clean / vite build:react 정상. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline batch 1 + batch 2 + TASK-066 follow-up batch 3 + TASK-102 docs only + TASK-103 migration CLI 운영 workflow + scripts/db-migrate.sh 권고 스크립트 까지 **21 TASK 연속 봉인** (2026-07-08 ~ 2026-07-20). follow-up: postgres migration 0004 build_source TOAST 전략 / 운영 안정화 / 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선 모두 별도 사용자 결정 대기. workflow meta sync (state rev 132→133, handoff 102→103, work_backlog 96→97, backlog index 66→67 / latest 44→45) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-20 (rev 97→98: TASK-104 build_source SLO + TOAST follow-up 운영 가이드 docs only 봉인 sync. 본 세션의 단일 작업 흐름에서 TASK-102 (PROJECT_PROFILE §3 baseline 양축 동기화, main `4ad57f2`) + TASK-103 (migration CLI 운영 workflow + scripts/db-migrate.sh, main `7b74614`) + TASK-104 (build_source SLO + TOAST follow-up 운영 가이드, main `d4fb554`) 까지 **3 TASK 연속 봉인**. TASK-104 결정: 옵션 A (현 운영 가이드 + 후속 결정 분리 절차 — SQL / schema / migration 변경 0, 결정 분리 절차 봉인). branch 없음 — 신규 1 file (운영 가이드) + PROJECT_PROFILE.md 1 file 수정 (1 line). 2 file / 122 insertions. 회귀 영향 0 — TS 5 packages clean, vitest 130/130 PASS, build-server 143/143 PASS, Go 7+ packages PASS, vite build:react 정상 (gzip js 99.01KB / css 30.62KB), postgres migration 0001~0005 적용 정상, build_source bytea column + bodyLimit: 256 MiB 동일, e2e-source-archive-postgres ALL PASS 유지. 결정 대기 항목 해소: postgres migration 0004 build_source TOAST 전략 (TASK-104 봉인, SLO 확장 봉인 안 함 — 후속 trigger 5종 발동 시 별도 TASK). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123) / build-server 113 → 143 (+30) / TS 5 packages clean / vite build:react 정상. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline batch 1 + batch 2 + TASK-066 follow-up batch 3 + TASK-102 docs only + TASK-103 migration CLI 운영 workflow + TASK-104 build_source SLO + TOAST follow-up 운영 가이드 까지 **22 TASK 연속 봉인** (2026-07-08 ~ 2026-07-20). follow-up: 운영 안정화 / 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선 / TASK-104 trigger 5종 발동 시 별도 TASK 모두 별도 사용자 결정 대기. workflow meta sync (state rev 133→134, handoff 103→104, work_backlog 97→98, backlog index 67→68 / latest 45→46) 같은 sync commit 안에 포함.)
- 관련 문서: [세션 인계](./session_handoff.md), [프로젝트 프로파일](../../docs/PROJECT_PROFILE.md)

## 1. 운영 원칙
1. 세션 시작 시 인덱스와 최신 백로그 확인
2. **세션 종료 직전(commit 직전) 인덱스 및 Handoff 갱신** — [`../core/global_workflow_standard.md` §8](../core/global_workflow_standard.md) 정합 — `memory 갱신 → commit → push` 순서
3. 모든 작업 상태는 날짜별 백로그에 기록

## 2. 날짜별 백로그
- [2026-07-20](./backlog/2026-07-20.md)
- [2026-07-18](./backlog/2026-07-18.md)
- [2026-07-08](./backlog/2026-07-08.md)
- [2026-07-07](./backlog/2026-07-07.md)
- [2026-07-06](./backlog/2026-07-06.md)
- [2026-07-05](./backlog/2026-07-05.md)
- [2026-07-04](./backlog/2026-07-04.md)
- [2026-07-03](./backlog/2026-07-03.md)
- [2026-07-02](./backlog/2026-07-02.md)

## 3. 전체 작업 상태 요약
- [x] TASK-104: build_source SLO + TOAST follow-up 운영 가이드 (docs only) — 신규 운영 가이드 `docs/operations/build-source-toast-strategy-2026-07-20.md` (현 주소 / 256 MiB 가 충분한 이유 / 트리거 5종 / 후속 후보 옵션 3종 (단순 SLO 확장 / chunked split / 외부 object storage) / 후속 결정 분리 절차 5단계). PROJECT_PROFILE.md §3.5 회귀 baseline 끝 follow-up 한 줄 + 다음에 읽을 문서 reference 추가. SQL / schema / migration 변경 0 — 본 TASK 가 SLO 확장 봉인 안 함, 후속 trigger 5종 발동 시 별도 TASK. commit `d4fb554` main 합류 (2026-07-20, 본 세션).
- [x] TASK-103: scripts/migrate.ts standalone CLI 운영 workflow 정립 (scripts/db-migrate.sh 권고 스크립트) — `scripts/db-migrate.sh` 신규 (6 게이트 단일 entrypoint --plan / --status / --apply-all / --apply-up-to / --bootstrap / --dr-stop, 모두 기존 standalone CLI 위임, SQL / schema 변경 0) + PROJECT_PROFILE.md §3 끝 migration 운영 권장 참조 + 다음에 읽을 문서 reference 추가 + 운영 가이드 신규: `docs/operations/migration-cli-workflow-2026-07-20.md` (의도 / 변경 / 자주 쓰는 5종 시나리오 / 기존 standalone CLI 와의 관계 / 사전 결함 + 보강 2건). commit `7b74614` main 합류 (2026-07-20, 본 세션).
- [x] TASK-102: PROJECT_PROFILE §3 baseline 양축 동기화 (memory/Postgres) — docs only. `docs/PROJECT_PROFILE.md` §3 명령 박스의 run_local / run_local_postgres 정합 + §3.5 / §3.6 / §3.10 / §3.11 / §3.12 회귀 baseline 양축 동기화 (memory-only 표기를 Postgres 동등 baseline 양축) + §4 검증 포인트 Postgres 운영 baseline 추가 + 다음에 읽을 문서 reference 추가. 운영 가이드 신규: `docs/operations/project-profile-baseline-postgres-sync-2026-07-20.md`. commit `4ad57f2` main 합류 (2026-07-20, 본 세션).
- [x] TASK-001: 컨셉 기반 온보딩 및 MVP 작업축 정리
- [x] TASK-002: 컨셉 고도화 및 정책 문서 정리
- [x] TASK-003: 요구사항 도출 및 정제
- [x] TASK-004: Step 04 설계 문서 구조화
- [x] TASK-005: Step 05 진입 기준 및 baseline decision 정리
- [x] TASK-006: Step 06 구현 축 및 workstream 정리
- [x] TASK-007: Step 07 구현 backlog baseline 정리
- [x] TASK-008: PKG-001 공통 계약 기준선 정리
- [x] TASK-009: Build Server 기술 스택 baseline 정리
- [x] TASK-010: 저장소 패키지 구조 초안 정리
- [x] TASK-011: `PKG-002` Build Server request intake 세부 태스크 정리
- [x] TASK-012: `PKG-003` persistence 세부 태스크 정리 또는 코드 스캐폴드 진입 판단
- [x] TASK-013: shared package 코드 스캐폴드 또는 `PKG-004` 조회 계층 세분화 판단
- [x] TASK-014: shared package 또는 API 스캐폴드 진입 판단
- [x] TASK-015: SDLC 리뷰 및 보고 패키지 작성
- [x] TASK-016: 문서 정합성 보정 및 스캐폴드 진입 준비
- [x] TASK-022: workflow 메타 정합성 보강 (commands placeholder, status assessment, report 인덱스, legacy 배너, 2026-07-02 백로그 봉인)
- [x] TASK-023: 워크플로우 skill/MCP 셋업 (워크플로우 온보딩 마무리)
- [x] TASK-024: 리뷰 반영 (08 Build Server=TS / Runner=Go baseline 정합, 09 apps/runner Go 예시·의존방향·PKG-005~007 갱신)
- [x] TASK-033: preview-readiness-checker skill 1종 구현 (Python, 42 tests OK, total 215)
- [x] TASK-034: PKG-005 Runner Claim And Build Phase Skeleton 1차 골격 구현 (claim/phase endpoint 2종 + memory/postgres repo, live smoke 7/7; 누적 회귀 TS 37 + Go 13 + Python 215 = 265/265 OK)
- [x] TASK-035: Go Runner claim loop + phase call (HTTPBuildControlClient + BuildService 4-phase 자동 보고 + Worker loop, 6 phase e2e smoke + Go 10 tests; 누적 회귀 265/265 OK)
- [x] TASK-036: PKG-006 Preview Service Queue And Readiness (4 endpoint + Runner queue/ready 자동 보고, 8 phase e2e smoke + TS 14 + Go 3 신규 tests)
- [x] TASK-032: failure-summary MCP 1종 구현 (Python, 16 tests OK, total 173)
- [x] TASK-031: failure-summary-shaper skill 1종 구현 (Python, 25 tests OK, total 157)
- [x] TASK-030: contract-drift-checker skill 1종 구현 (Python, 25 tests OK, total 132)
- [x] TASK-029: build-log-tail MCP 1종 구현 (Python, 50 tests OK)
- [x] TASK-028: latest-build-status MCP 1종 구현 (Python, 21 tests OK)
- [x] TASK-027: build-status-explainer skill 2종 구현 (Python, 22 tests OK)
- [x] TASK-026: build-request-intake skill 1종 구현 (Python, 14 tests OK)
- [x] TASK-025: 우리 시스템 skill/MCP 후보 정리 및 개발 계획 (docs/sdlc/13)
- [x] TASK-017: shared package / build-server / runner 골격 스캐폴드 및 1차 검증
- [x] TASK-018: 보고자료 재구성 및 기획안 재작성
- [x] TASK-019: 리더 소개용 HTML 보고자료 시각화 재작성
- [x] TASK-020: HTML 시각화 보강 및 오프라인 에셋 내장화
- [x] TASK-021: 발표용 카피 압축 및 승인안 톤 보정
- [x] TASK-022: 백엔드 착수용 기술스택 결정 보정
- [x] TASK-023: 백엔드 개발 계획 수립 및 문서화
- [x] TASK-038: Build Server OpenAPI/Swagger UI + CORS + DESIGN.md 1차안 부착 (PR #5). Fastify 플러그인 3종 (swagger, swagger-ui, @asteasolutions/zod-to-openapi 8.5) 부착, 16 zod schema 의 .meta({ id, description }) 통일, hand-rolled CORS (onRequest + OPTIONS wildcard, @fastify/cors 제외 — ESM/fastify-plugin fp 호환성 문제), CORS_ORIGIN ENV 추가, /openapi.json 10 paths / 17 components.schemas / 4 tags, /docs Swagger UI, docs/DESIGN.md (Stitch v1 spec) 1차안, openapi snapshot test 1종. 회귀 266/266 OK (TS 38 + Go 13 + Python 215).
- [x] TASK-039: Build Monitor frontend 부착 1차 골격 (PR #6). Svelte 5 + Vite + TypeScript 선정 (DESIGN.md §0 Stack), apps/build-monitor 골격 + DESIGN.md tokens.css CSS 변수화 + 4 컴포넌트 (StatusPill/BuildRow/PhaseTimeline/LogStream) + Header + 3 route + lib/api.ts hand-typed 1차 + Vite proxy /api → :3000 + vitest 7/7 (StatusPill 매핑) + svelte-check 0 + vite build OK (gzip 22KB). 회귀 273/273 OK (TS 38+7 + Go 13 + Python 215). openapi-typescript 자동 client / list endpoint / light mode 는 후속 PR.
- [x] TASK-040: Build Server GET /builds list endpoint + openapi-typescript 자동 client (PR #7). Memory + postgres 양쪽 listBuilds (filter status, createdAt desc, cursor pagination, limit max 200). zod BuildListQuery/BuildListResponse schema + .meta. limit 의 z.coerce.number(). build-monitor 측 openapi-typescript 7 + openapi-fetch 0.13 + tsx script (predev/prebuild hook) 로 ./.generated/openapi.d.ts 자동 생성. lib/api.ts 재작성 (SAMPLE_BUILDS 제거, openapi-fetch helper apiGet, BuildLogsResponse inline). BuildDetail nested access (.build.buildId) 수정 + LogStream LogEntry.at → createdAt. svelte-check 0, vitest 8/8 (StatusPill 7 + BuildRow 1), vite build OK (gzip 24KB). 회귀 268/268 OK (TS 42+8 + Go 13 + Python 215). live e2e: vite proxy /api/builds → 3 builds, ?limit=2 → 2 + nextCursor, /api/openapi.json 200.
- [x] TASK-043: Admin UI — `ADMIN_IDS` env (default `admin,yky.lee`) + Build Server `/admin/builds` (모든 owner) + `/admin/users` (per-user rollup). BuildListQuery 와 별도 schema (`AdminListBuildsQuery`/`AdminListBuildsResponse`/`AdminUserBuildSummary`/`AdminUserSummary`) + `Admin` OpenAPI tag. Memory + postgres 양쪽 listBuildsAcrossUsers / listBuildOwners. Header `X-Admin-Id` 가드, 401/403/200. build-monitor: AdminLogin/AdminBuilds/AdminUsers 라우트, Header adminId 분기 (`localStorage.adminId` 분리), BuildRow에 optional owner cell, owner filter 인라인, status chip. vitest 34/34 (AdminLogin 2 + AdminBuilds 4 + AdminUsers 3 + Header 6 + 기존 19), svelte-check 0/0, vite build OK. live e2e: `x-admin-id: admin` 200 / `x-admin-id: alice` 403 / no header 401, `/admin/users` → alice 2 / bob 1 + lastBuildAt, `/admin/builds?requestedBy=alice` → 2 alice builds.
- [x] TASK-044: Bug fix 2건 — (1) Header 즉시 갱신 (apps/build-monitor/src/lib/session.ts 의 userIdStore/adminIdStore writable store, login 시 store.set → 같은 탭 reactive 갱신 + storage 이벤트로 다른 탭 반영), (2) BuildRequest/BuildSummary 의 projectId/repositoryId 를 appName 단일 식별자로 collapse (BuildRequest schema, BuildSummary schema, memory/postgres repo, build-monitor BuildRow/BuildsList/BuildDetail/AdminUsers + generated/openapi.d.ts). Postgres 는 metadata->>'appName' 으로 1차 처리, projectId 컬럼은 빈 string 으로 두고 DB migration (app_name 컬럼 추가 + projectId/repositoryId drop) 은 후속 TODO. 회귀 TS build-server 54/54 + vitest 35/35 + svelte-check 0/0 + Go 13 + Python 215 = 270/270.
- [x] TASK-045: DB schema migration (v0.2 appName 단일 식별자) — packages/db/src/schema/build-request.ts 의 Drizzle 스키마에 `appName: text('app_name').notNull()` 추가 + project_id/repository_id 컬럼 제거. packages/db/src/bootstrap.ts 의 CREATE TABLE build_request 도 동일하게 app_name 만 만들고 legacy 컬럼 만들지 않음 (신규 DB greenfield). brownfield 마이그레이션 SQL `apps/build-server/migrations/0001_app_name.sql` 신규 — `ADD COLUMN IF NOT EXISTS app_name`, 기존 row 의 `metadata->>'appName'` 에서 채우기, NOT NULL 제약, btree 인덱스 `build_request_app_name_idx` + `build_request_requested_by_idx`, legacy project_id/repository_id drop. 모두 idempotent (IF EXISTS/IF NOT EXISTS) — 부분 적용된 상태 재실행 안전. `apps/build-server/src/repositories/postgres-build-repository.ts`: `mapBuildRowToSummary` 가 `row.appName` 직접 사용 (metadata JSONB fallback chain 제거), active-build dedup probe 가 `eq(buildRequestTable.appName, input.appName)` (메타 JSONB 경로 제거), insert 가 `appName: input.appName` 직접 + `metadata: input.metadata` (중복 쓰기 제거). 회귀 TS build-server 54/54 + vitest 35/35 + svelte-check 0/0 + Go 13 + Python 215 = 270/270.
- [x] TASK-046: light mode contrast QA — tokens.css light 모드의 --color-text-muted #94a3b8 → #64748b (canvas #f1f5f9 위 2.7:1 → 4.5:1 WCAG AA OK, AdminLogin/Login 의 .muted/placeholder 가 보이도록), --color-bg-surface-elevated #ffffff → #f8fafc (table row hover / ThemeToggle hover / owner pill 이 surface 와 동일해 묻히던 문제 해결), StatusPill EXPIRED+UNKNOWN fallback --color-text-muted → --color-text-secondary (15% alpha-mix 시 canvas 위 invisible), theme.css :root { color-scheme: dark } + :root[data-theme=light] { color-scheme: light } (native form control/scrollbar 가 모드 따라가기) + ThemeToggle.svelte applyTheme 이 style.colorScheme 도 js-side set (이중 안전망). BuildRow owner-cell border subtle → strong (light 모드 surface-elevated 의 더 강한 contrast). Login/AdminLogin input border-strong 명시 코멘트 (light 모드 canvas 위 1.5:1 → 진한 톤 명시). StatusPill.test.ts EXPIRED+UNKNOWN assertion secondary 로 갱신. ThemeToggle.test.ts 신규 2 케이스 (default dark / toggle light 모두 colorScheme 검증). vitest 35→37 + svelte-check 0/0 + vite build OK (gzip 28.60 KB JS + 4.28 KB CSS, 패치로 약간 증가). 회귀 TS 54 + vitest 37 + Go 13 + Python 215 = 272/272.
- [x] TASK-047: Admin allow-list backend — `createAdminAllowList` mutable Set (default `admin,yky.lee`, env `ADMIN_IDS` parse fallback) + 서버 schema + `X-Admin-Id` 가드 + `/admin/admins` GET/POST/DELETE. BuildService.addAdmin / removeAdmin / listAdmins + memory + postgres repo. seed empty/non-conforming throw. 회귀 build-server 61→68 (4 신규). PR #11 의 backend 축.
- [x] TASK-048: Admin allow-list frontend — `adminAllowListStore` (writable + persistence) + Header auto-enable (`localStorage.adminId` 가 allow-list 에 있을 때만 admin 메뉴 노출) + AdminAdmins.svelte (목록/추가/삭제 UI, regex pre-check `^[a-zA-Z0-9._-]+$`). vitest 35→44 (5 신규). PR #11 의 frontend 축.
- [x] TASK-049: Visual QA scaffolding — apps/build-monitor/scripts/visual_qa/capture.py (Playwright headless login/admin/builds 캡처, baseline/ 디렉터리 PNG, add_init_script 로 user/admin 토큰 주입) + baseline/ .gitkeep + apps/build-monitor/scripts/visual_qa/README.md 운영 메모. 회귀 영향 없음 (스크립트 + baseline 디렉터리). PR #11 의 QA 축.
- [x] TASK-050: Build phase progress timeline — BuildStatusResponse phaseHistory + currentPhase (default [] / null) 신규. in-memory repo transition (createBuild/updatePhase/queueTestDeployment → prev phase push + current 갱신 + terminal null). postgres repo fallback (단일 entry + current emit). PhaseTimeline.svelte 재구현 — canonical 9 phases 전체, completed (success dot + completedAt) / current (primary dot + pulse + startedAt) / pending (subtle dot + '— pending') 구분, FAILED=danger 톤, data-phase attribute. BuildDetail.svelte events={phase,at} → phaseHistory + currentPhase 분리. BuildStatusResponse hand-typed mirror. 회귀 build-server 73/73 (4 신규) + build-monitor 51/51 (5 신규) + Go 13/13 + Python 215/215 + svelte-check 0/0 = 352+ OK. PR #12 squash merge `1af82d0` main 합류 (commit `7c7297f` 메타 sync).
- [x] TASK-051: postgres phase history persistence 보강 (`phase_history` 저장, migration, read path 정렬). `packages/db` schema/bootstrap + `apps/build-server/migrations/0002_phase_history.sql` + postgres repo 공통 timeline helper 반영. 패키지 기준 회귀: `apps/build-server` tests 25/25 OK (`phase-history`, `build-routes`, `memory-preview`), `packages/db` direct `tsc --noEmit` OK.
- [x] TASK-052: shared-contract status/response refactor (preview-era contract → build/test/deploy/result-delivery). canonical/legacy status union 정리, `BuildStatusResponse.lifecycle/image/test/deploy/resultDelivery` 추가, `previewStatus`/`TestDeployment`는 migration shim 유지. 회귀: `packages/shared-contract` direct `tsc --noEmit` OK, `apps/build-server` shared-contract shim test 3/3 OK.
- [x] TASK-053: DB schema split (`build_test`, `deployment_attempt`, legacy preview field 축소). `packages/db`에 `build_test`/`deployment_attempt` schema + bootstrap/index export 추가, `0003_build_test_and_deployment_attempt.sql` migration 추가, `build_request.preview_*`는 legacy shim 주석으로 유지. 회귀: `packages/db` direct `tsc --noEmit` OK, `apps/build-server` direct `tsc --noEmit` OK, focused tests 28/28 OK.
- [x] TASK-054: Build Server repository/service/API refactor. canonical `BuildStatusResponse` population helper 도입, memory/postgres repo 응답에 `lifecycle/test/deploy/resultDelivery` 실제 채움, postgres 는 `build_test` 병행 write 시작. 회귀: build-server direct `tsc --noEmit` OK, focused tests 49/49 OK.
- [x] TASK-055: OpenAPI regeneration and query contract stabilization. `/openapi.json` 재생성 후 `apps/build-monitor/.generated/openapi.d.ts` 갱신, build-monitor `lib/api.ts`가 generated `BuildStatusResponse` alias 사용하도록 정리. 검증: build-monitor direct `tsc --noEmit` OK, `svelte-check` 0/0. `vitest`는 localStorage 미구성 환경으로 기존 실패 지속.
- [x] TASK-056: Runner phase model refactor. runner claim client 의 `default-runner` 하드코딩 제거, claim payload/appName/lifecycleStatus 파싱 정리, duplicate queue preview call 제거, queue/worker/build_service test 기대값 갱신. 회귀: `apps/runner` `go test ./...` 13 passed, build-server focused tests 49/49 OK.
- [x] TASK-057: docker build no-op 제거 및 source prepare/build/test skeleton 고도화. runner docker client 에 `PrepareSource` + `BuildImage` skeleton 모드 도입, workspace/source marker/Dockerfile/build-manifest 실제 생성, `cli` 모드에서 `docker build` 시도 가능, BuildService 가 source prepare 후 phase 보고하도록 정리. 회귀: `apps/runner` `go test ./...` 15 passed, build-server focused tests 49/49 OK.
- [x] TASK-058: container test result reporting
- [x] TASK-059: external deployment adapter v1
- [x] PR #13 self-review follow-up 보완 (race / schema 정확성 / spec 일관성 7건): `reportPreviewStatus` / `reportDeploymentResult` `db.transaction` wrap, `getTestDeployment` `build_test` left join 으로 `host`/`hostPort`/`internalPort`/`runtimeUrl` 노출, `0003_build_test_and_deployment_attempt.sql` 에 `DEFAULT gen_random_uuid()` + `build_id REFERENCES build_request(id) ON DELETE CASCADE` FK, `apps/runner` claim 응답 `Reason` `omitempty`, OpenAPI `POST /builds/:buildId/deployment` 200 응답 `buildStatusResponseSchema` ref, `docker.BuildImage` marker 기반 idempotent guard 로 ProcessClaim 의 중복 `PrepareSource` 제거, memory repo EXPIRED case 코멘트 보강. 회귀: TS 4 packages clean, Go 9 packages OK, build-server focused 52/52, build-monitor tsc + svelte-check 0/0. workflow meta 동기화 (state rev 65→66, handoff 44→45, work_backlog 37→38, backlog §46→47 + 새 §48) 같은 amend commit 에 포함.
- [x] TASK-060: Build Monitor status model/UI refactor (1차 PR: BuildDetail canonical 4 block + StatusPill lifecycleStatus + legacy preview 분리. 2차 PR: fixture 갱신 + AdminUsers StatusPill 통일. 3차 PR: status chip filter canonical 정렬. 후속: AdminUsers recent builds panel 을 BuildRow 로 교체)
- [x] TASK-061: Skill/MCP contract rename + payload refactor — `apps/skill_mcp/contract/canonical.py` 단일 source-of-truth (canonical 12 builds / 5 execution / 11 phases / 8 errorCodes / 8 nextActions / 7 readinessStates + 8 type-guard helpers, OPEN_PREVIEW→OPEN_DEPLOYMENT) + drift checker Python cross-check (4 sync groups, dynamic import) + build-status-explainer v2 (canonical BuildStatusResponse payload + 12 status table + legacy forward-mapper TEST_READY→TEST_SUCCESS / CLAIMED→QUEUED) + failure-summary-shaper v2 (canonical 8 ErrorCode + stage BUILD/TEST/DEPLOY/DELIVERY + OPEN_DEPLOYMENT) + preview-readiness-checker v2 (canonical test.status/executionStatuses 우선 consume + 카드 container-test 톤 정렬, 디렉터리명 보존) + latest-build-status MCP v2 (_BUILD_TOP_KEYS testDeployment→test, error→lastError + legacy forward-mapping helper) + failure-summary MCP v2 (envelope `source`→`stage`) + 5 SKILL.md/MCP.md sync + 5 test files + docs/sdlc/13-skills-and-mcp-plan §10 신설. PR #17 오픈. 회귀 221/221 PASS (15→221, +6 신규). drift-checker self-check: skill sync 4종 모두 missing=0/extra=0 (skillBuildStatuses 12 / executionStatuses 5 / skillPhases 11 / skillErrorCodes 8). 메타 sync (state rev 74→75, handoff 53→54, work_backlog 46→47) 같은 PR 안에 포함.
- [x] TASK-062: Go canonical contract mirror — ERROR_CODES 8→9 확장 (`DEPLOYMENT_FAILED` 추가, TS `packages/shared-contract/src/build/errors.ts` / Python `apps/skill_mcp/contract/canonical.py` / `apps/skill_mcp/skills/{failure_summary_shaper,build_status_explainer}/core.py` mapping 갱신) + Go `apps/runner/internal/contract/{status.go,phase.go,errors.go,contract.go,contract_test.go}` 신설 (canonicalBuildStatuses 12 + legacyBuildStatuses 2 + executionStatuses 5 + buildPhases 11 + errorCodes 9, const + slice 단일 source-of-truth) + Runner 4 file string literal 교체 (build_service.go + 테스트 3종 build_service_test.go / worker_test.go / build_control_client_test.go) + contract-drift-checker Go cross-check 확장 (`GO_CANONICAL_MAP` + `_extract_go_consts` regex parser + 5 goXxx sync group `goBuildStatuses` / `goLegacyBuildStatuses` / `goExecutionStatuses` / `goBuildPhases` / `goErrorCodes` 모두 missing=0/extra=0 검증). PR #18. 회귀 Python 226/226 (TASK-061 221 + Go 신규 5 테스트) + Go 6 packages 모두 PASS. drift-checker 3-way sync (TS↔Python↔Go) 9 sync group 모두 0 drift. 메타 sync (state rev 76→77, handoff 56→57, work_backlog 48→49) 같은 PR 안에 포함.
- [x] TASK-064: smoke / migration / visual QA baseline 정리 — `packages/db/src/migrate.ts` + `migrate.test.ts` 8 신규 (schema_migrations 테이블 위에서 0001~000N SQL 을 한 transaction 씩 idempotent 적용, --dry-run/--to/--list flag) + `apps/build-server/src/app/create-app.ts` postgres boot 시 `applyMigrations` 자동 통합 + `apps/build-server/scripts/migrate.ts` standalone CLI + `scripts/smoke.sh` 5 단계 (tsc/boot/POST+GET/duplicate 409/admin) + `apps/build-monitor/tests/visual/diff.py` Pillow 기반 per-channel histogram diff + 5 unit test + `docs/operations/smoke-and-migration.md` 운영 가이드 신설 + vitest 환경 보강 (vite.config.ts `environmentOptions.jsdom.url` + setup.ts JSDOM fallback + jsdom.d.ts) + `src/lib/chipFilter.ts` 단일 source-of-truth 추출 (BuildsList + AdminBuilds 중복 제거 + canonical success exclusion 보강). 회귀: TS 4 packages clean, packages/db migrate 8/8, apps/build-server focused 27/27, apps/runner go 17 packages, apps/skill_mcp 226/226, apps/build-monitor vitest 60/60, scripts/smoke.sh memory backend PASS. drift 54 pre-existing (TASK-064 변경 전후 동일, canonical §4~§8 baseline 잔재, follow-up). PR #19 (branch `codex/task-064-ops-baseline-2026-07-04`) 오픈. workflow meta sync (state rev 77→78, handoff 57→58, work_backlog 48→49, backlog §56) 같은 PR 안에 포함.
- [x] TASK-065: CodeWhale overlay 배포 — standard_ai_workflow v0.11.22 (cf0060d, v0.10.4) 가 추가한 CodeWhale 하네스를 본 저장소에 적용. `render_codewhale_skill()` (bootstrap_lib.harnesses.renderers) 직접 호출로 `.codewhale/skills/codewhale-workflow/SKILL.md` 단일 파일 emit. Constitution additive rule 만 포함 (Constitution Article II/III/Regulations/Statutes 와 중복 주입 금지). 기본 검증 명령 (lint/type_check/quick_test) 은 state.json 의 real commands 주입 (tsc --noEmit 4 packages + apps/runner go build). full bootstrap (`bootstrap_workflow_kit.py --harness codewhale`) 은 memory state docs (state.json / session_handoff.md / work_backlog.md / daily_backlog / assessment) 를 덮어쓰려 하므로 우회 — 기존 AGENTS.md / MiniMax.md / 메모리 8종 보존. 메모리 sync: state rev 61→62, handoff 34→35, work_backlog 35→36. 회귀 영향 없음 (신규 1 파일, 기존 entry point 무변경). 후속: 첫 CodeWhale 세션에서 SKILL.md 가 `.codewhale/skills/` 로 인식되는지 + 검증 명령이 동작하는지 확인. (TASK 번호는 merge 충돌 해소 과정에서 미점유 번호 TASK-065 로 재배치.)
- [x] TASK-066: Runner source archive fetch + real docker build (M3 follow-up, PR #20 squash merge main 합류 `ac41866`) — Build Server 측 `build_source` memory/postgres `bytea` 저장 + `POST/GET/DELETE /builds/:buildId/source` 3 endpoint + octet-stream content parser (256 MiB bodyLimit) + checksum/size 재계산 검증 + SourceArchiveUploadResponse schema + `DELETE ... RETURNING` 으로 row 부재 감지 + Runner `internal/source/fetcher.go` (BuildControlClient.DownloadSource + tar.gz extract + validateTarEntryName 으로 absolute/`..`/NUL/control/backslash reject + transport-error 1 retry + cleanup on terminal error) + `docker.BuildImage(ctx, buildID, sourceDir, dockerfileRelPath)` (scratch Dockerfile 제거, sourceDir 기준 real `docker build`) + BuildService.ProcessClaim fetcher wire-up. 보완: validateTarEntryName unit 16 case + DELETE route 5 case + memory e2e 4 case (DELETE parity) + postgres e2e script (`e2e-source-archive-postgres.sh` bytea round-trip 직접 verify) + PROJECT_PROFILE docs. self-review fix 3건 + 코드 보완 5 area 모두 봉인. 회귀: TS 4 packages clean, build-server 94/94 PASS (TASK-066 11 신규), Go 7 packages 모두 PASS, Python skill_mcp 동일, e2e memory 7/7, e2e postgres 5/5. 후속: TASK-067 (container run + healthcheck) / TASK-068 (real deploy adapter).
- [x] TASK-067: Runner container run + healthcheck (M3 follow-up) — Runner `apps/runner/internal/docker/client.go` 에 `ContainerStatus` / `ContainerRunOptions` + `RunContainer` / `WaitForHealth` / `StopContainer` + `ErrContainerHealthcheckTimeout` sentinel + RunContainer skeleton mode (mock 정합, HostPort 38124 fallback) + cli mode (real `docker run -d --name -p <host>:<internal>` + `docker inspect` host port auto-assign + OS ephemeral port fallback via `pickFreePort` + `net.Dial` TCP probe + HTTP 2xx probe + consecutive-3 stability polling) + `BuildService.ProcessClaim` 통합 (queueTestDeployment 후 RunContainer + WaitForHealth + ReportPreviewReady 에 ContainerStatus 그대로 전달, RUNNER_STOP_CONTAINER_ON_DONE=true 시 defer StopContainer) + 신규 env (RUNNER_DOCKER_RUN_MODE / RUNNER_HEALTHCHECK_PATH / RUNNER_HEALTHCHECK_TIMEOUT_SECONDS) + `BuildService.WithHostPort(int)` test seam + `apps/runner/scripts/e2e-container-run.sh` (Build Server memory backend 부팅 + busybox/scratch Dockerfile + source archive upload + Runner cli mode + docker ps lifecycle 확인 + Build Server status readback + cleanup) + 테스트 8 신규 (docker 6: RunContainer skeleton / WaitForHealth skeleton / cli mode docker args 2 case / StopContainer idempotent / probePort+probeHealth httptest; build_service 2: ContainerStatus from RunContainer / StopContainerOnDoneDefer). 회귀: TS 4 packages clean, Go 7 packages 모두 PASS, build-server 94/94 동일, Python skill_mcp 226/226 동일. 후속: TASK-068 (real deploy adapter).

**rev 53→54 (2026-07-05)**: PR #21 (TASK-067 Runner container run + healthcheck) squash merge main 합류 sync. main HEAD `574932d`. 2 commit 구조 (`fe15ebb` 본 PR + `d025b18` self-review fix 3건 — TestPickFreePortReturnsEphemeral / strconv.Atoi stdlib 교체 / inspect-fail path honest comment). 회귀 baseline: TS 4 packages clean, Go 64/64 PASS (직전 63 + pickFreePort 1), build-server 94/94 동일, Python skill_mcp 226/226 동일. 다음 TASK-068 (real deploy adapter) — M3 (Runner Realignment) 의 마지막 축. 현재 `apps/runner/internal/deploy/client.go` skeleton adapter 를 실제 외부 deploy target 으로 연결. 후보: docker registry push / compose / k8s / GitHub Pages 등. 사용자 scope 정의 필요.

**rev 54→55**: TASK-069 Runner registry v1 + admin menu main 합류 (`beb49f0`) — 신규 `runnerStatusSchema` 3-way sync + 6 repo method + 4 admin route + admin menu page + 23 신규 build-server test. 회귀 baseline: TS 4 packages clean, build-server 117/117, build-monitor 64/64 (svelte-check 0/0), Go 65/65, Python 226/226. 다음 TASK-068 real deploy adapter.

**rev 55→56 (2026-07-05)**: TASK-068 Runner real deploy adapter (DOCKER_REGISTRY push mode) 1차 PR 작성 — branch `codex/task-068-real-deploy-adapter-2026-07-05` (main `beb49f0` base). 신규 `apps/runner/internal/deploy/client.go` `DeployOptions{SourceImage}` + `Mode()`/`PushTimeout()`/`WorkspaceDir()` accessor + `DeployMode` env (default skeleton / cli) + cli mode (`docker tag <SourceImage> <targetRef>:<buildID>` → `docker push <targetRef>:<buildID>` with timeout context) + `ErrDeploySourceImageMissing` sentinel + `SetTagImageCmdForTest` / `SetPushImageCmdForTest` / `SetNowForTest` 3 test seam + cli mode 성공 후에도 `deploy-result.json` workspace emit 보존. `BuildService.ProcessClaim` 가 `containerStatus.ImageTag` 를 `deploy.DeployOptions.SourceImage` 로 전달. 신규 env 3종 — `RUNNER_DEPLOY_MODE` / `RUNNER_DEPLOY_PUSH_TIMEOUT_SECONDS` (default 120s) / `RUNNER_DOCKER_BIN` (default `docker`). `apps/runner/scripts/e2e-deploy-push.sh` (Build Server memory backend + local `registry:2` 5000 부팅 + busybox Dockerfile + Runner cli+deploy cli mode + `/v2/<repo>/tags/list` registry tag 검증 + cleanup). 테스트 10 신규 (TestDeploy_WritesDeploymentArtifact / TestDeploy_SkeletonResponsePayloadShape / TestDeploy_EmptyBuildID_ReturnsError / TestDeploy_CLIMode_TagAndPushArgs / TestDeploy_CLIMode_SourceImageMissing_ReturnsError / TestDeploy_CLIMode_TagFail_PropagatesError / TestDeploy_CLIMode_PushFail_PropagatesError / TestDeploy_CLIMode_PushTimeout_PropagatesContextDeadline / TestNewClient_DefaultsAndEnvOverrides / TestNewClient_InvalidPushTimeout_FallsBackToDefault). 회귀: TS 4 packages clean, Go 7 packages 모두 PASS (deploy 10 신규 / 65 → 75), build-server 117/117 동일, build-monitor 64/64 동일, Python skill_mcp 226/226 동일. workflow meta sync (state rev 82→83, handoff 60→61, work_backlog 55→56) 같은 commit 안에 포함. 다음: PR self-review → squash merge.

**rev 56→57 (2026-07-05)**: PR #22 (TASK-068 Runner real deploy adapter) squash merge main 합류 (`c89b1d4`). self-review amend 2건 봉인 — (1) `defaultTagImageCmd` / `defaultPushImageCmd` stderr 를 `io.MultiWriter(os.Stderr, &bytes.Buffer)` 로 부모 process + 캡쳐 동시 흘림 (docker push 진행률 운영자 가시성, TASK-067 `defaultRunContainerCmd` 패턴과 정합), (2) client.go trailing newline 복구. 회귀 baseline: TS 4 packages clean, Go 75/75 PASS (deploy 10 신규 / 65 → 75), build-server 117/117 동일, build-monitor 64/64 동일, Python skill_mcp 226/226 동일. M3 (Runner Realignment) 의 마지막 축 wire-up 완료. Build Server (source archive) → Runner (claim + fetch + build + container + healthcheck + registry state + deploy) → Test Deployment → Deploy 의 end-to-end slice 가 모두 active. 다음: 후속 TASK 결정 — 옵션 (a) compose / k8s / GitHub Pages 등 다른 deploy target, (b) M4 (Consumer Refactor), (c) M5 (Deployment Capability).

**rev 57→58 (2026-07-05)**: TASK-070 (AdminUsers recent builds → BuildRow 정렬 + AdminRunners pill 디자인 토큰 — M4 Consumer Refactor follow-up 봉인) PR #23 squash merge main 합류 (`9dc1f25`). branch `codex/task-070-admin-users-build-row-2026-07-05` (main `c879228` base) → squash merge → `--delete-branch` 자동 삭제. 신규 5 file / 204 insertions / 81 deletions. (1) `apps/build-monitor/src/routes/AdminUsers.svelte` 의 inline `<ul class="build-list">` 마크업을 canonical BuildRow 컴포넌트 + `<table>` 구조로 교체. owner cell 은 selectedUser inline expansion 이라 시각 노이즈가 되어 destructure 로 omit + BuildRow `{#if build.requestedBy}` 가드 활용. 외부 link `target="_blank"` 도 제거하여 svelte-spa-router `use:link` 와 같은 탭 라우팅 통일. 미사용 CSS (`.build-list`, `.build-list li`, `.project`, `.small`) 정리. (2) `apps/build-monitor/src/components/BuildRow.svelte` prop 타입을 local `BuildRowData` subset 에서 `BuildSummary & { requestedBy?: string }` (canonical structural super-set) 으로 통일. (3) `apps/build-monitor/src/routes/AdminRunners.svelte` `.pill.active` raw `rgb(34, 197, 94)` / `.pill.off` raw `rgb(244, 63, 94)` / `.btn-danger` raw `rgba(244, 63, 94, ...)` 를 canonical 디자인 토큰 `--color-accent-success` / `--color-accent-danger` 기반 `color-mix(in srgb, var(--color-...) N%, transparent)` 로 정렬. tokens.css light / dark 모드별 톤 자동 follow (light 성공 #059669 / danger #dc2626). (4) 신규 test 3건 (AdminUsers 2: BuildRow `data-testid="build-row"` 검증 + canonical lifecycleStatus 표시 + owner cell 미렌더 단언 + Close 버튼 unmount / AdminRunners 1: `.pill.active` / `.pill.off` disjoint 회귀 가드). 회귀 baseline: vitest 67/67 PASS (TASK-070 신규 3 추가 / 64→67), svelte-check 0 errors / 0 warnings, vite build OK (gzip js 33KB / css 5KB). PR description 에 본 변경의 의도 (Admin UX 의 svelte-spa-router 통일 / 디자인 토큰 자동 follow / M4 후속 봉인) 명시. Backward compat: BuildSummary 자체와 BuildSummary & { requestedBy: string } 모두 BuildRow prop 에 assignable. M4 (Consumer Refactor) 후속 정합 — TASK-060 의 마지막 follow-up (AdminUsers recent builds panel → BuildRow 교체) 봉인 완료. workflow meta sync (state rev 84→85, handoff 63→64, work_backlog 57→58, backlog index 32→33 / latest 5→6) 같은 sync commit 안에 포함. 다음: 후속 TASK 결정 — 옵션 (a) compose / k8s / GitHub Pages 등 다른 deploy target, (b) M5 (Deployment Capability) 진입, (c) AdminRunners status pill canonical StatusPill 승격 후속 (M4 정합).

**rev 58→59 (2026-07-06)**: TASK-071a (Runner deploy adapter robustness — tag-timeout 통합 + busybox fallback deploy-only — M5 (Deployment Capability) milestone 종료 봉인) PR #24 squash merge main 합류 (`ca2754e`). branch `codex/task-071-deploy-adapter-robustness-2026-07-05` (main `9dc1f25` base) → squash merge → `--delete-branch` 자동 삭제. 신규 3 file / 99 insertions / 23 deletions. (1) `apps/runner/internal/deploy/client.go` `runWithTimeout` helper 도입 (`context.WithTimeout(parentCtx, c.pushTimeout)` + `cancel()` + fn 호출). `runTag` 와 `runPush` 모두 helper 위임으로 통일되어 tag / push 단계 동일 pushTimeout (default 120s, `RUNNER_DEPLOY_PUSH_TIMEOUT_SECONDS` override) 안에서 강제 종료. v1 (TASK-068) 의 `runPush` 만 timeout 이었던 asymmetry 해소. 외부 interface (`PushTimeout()` / `SetNowForTest` / `RUNNER_DEPLOY_PUSH_TIMEOUT_SECONDS` env) 변경 없음. (2) `apps/runner/scripts/e2e-deploy-push.sh` busybox fallback 시 `RUNNER_DOCKER_RUN_MODE=skeleton` 으로 deploy-only 시나리오 자동 전환 — build cli + run skeleton (containerStatus.ImageTag mock) + deploy cli. 기존 full e2e 는 busybox 설치된 환경. (3) 신규 test `TestDeploy_CLIMode_TagTimeout_PropagatesContextDeadline` (deploy 10 → 11 case). 회귀 baseline: Go 8 packages 모두 PASS (76/76 / 75→76), build-monitor vitest 67/67 동일, TS 4 packages clean, build-server 117/117 동일, Python skill_mcp 226/226 동일. **M5 (Deployment Capability) milestone 종료** — TASK-059 + TASK-064 + TASK-068 + TASK-071a 가 main 모두 합류되어 docs/sdlc/15-refactoring-roadmap-and-milestones.md §5 완료 기준 5 항목 (최소 1개 external deployment adapter / polling 기본 경로 / deploy success/failure Build Server status query 반영 / 운영자 end-to-end smoke 문서+명령 / 운영 메타와 smoke 명령) 모두 충족. 다음: 후속 TASK 결정 — 옵션 (i) TASK-072 (RUNNER_REGISTRY_CONFIG_DIR private registry 인증), (ii) 옵션 (a) 다른 deploy target axis 확장, (iii) 옵션 (c) AdminRunners pill canonical StatusPill 승격. workflow meta sync (state rev 85→86, handoff 64→65, work_backlog 58→59, backlog index 33→34 / latest 6→7) 같은 sync commit 안에 포함.

**rev 59→60 (2026-07-06)**: TASK-072 (AdminRunners status pill → canonical StatusPill — M4 follow-up 옵션 (c) 봉인) PR #25 squash merge main 합류 (`f500ada`). branch `codex/task-072-admin-runner-status-pill-2026-07-06` (main `712f461` base) → squash merge → `--delete-branch` 자동 삭제. 신규 7 file / 99 insertions / 65 deletions. (1) `apps/build-monitor/src/components/StatusPill.svelte` colorFor 매핑에 RunnerStatus 2건 추가 — `ACTIVE` → `--color-accent-success` / `DISABLED` → `--color-accent-danger`. BuildSummary 의 success / failed 와 같은 색상 의미론으로 운영자가 BuildsList / AdminRunners 를 번갈아 봐도 직관. aria-label `Build status:` → `Status:` 일반화. (2) `apps/build-monitor/src/routes/AdminRunners.svelte` 의 inline `<span class="pill" class:active class:off>` 마크업을 `<StatusPill status={r.status} />` 단일 라인으로 교체 + 미사용 `.pill` / `.pill.active` / `.pill.off` CSS 21줄 제거. (3) 회귀 가드: StatusPill.test.ts 신규 2건 (ACTIVE / DISABLED 매핑) + 1건 (lifecycleStatus precedence) / AdminRunners.test.ts 기존 disjoint test 를 canonical StatusPill 기반 회귀 가드로 교체 + inline pill.raw 디자인 미사용 검증 / caller 측 기존 test 4건 (BuildRow 2 + AdminUsers 1 + BuildsList 1) 의 `Build status:` → `Status:` 일반화. 회귀 baseline: vitest 71/71 PASS (TASK-071a baseline 67 + TASK-072 신규 4 / 67→71), svelte-check 0 errors / 0 warnings, vite build OK (gzip css 5.30 → 5.23KB / 0.07KB 감소). **M4 follow-up 옵션 (c) 봉인 완료** — TASK-060 / 061 / 062 / 070 / 072 의 5 TASK 가 모두 main 합류되어 M4 의 모든 TASK 묶음 + 디자인 토큰 정렬 follow-up 닫힘. **M1 + M2 + M3 + M4 + M5 의 5 milestone 전체 TASK 묶음 종료 + follow-up robustness 정합 완료**. workflow meta sync (state rev 86→87, handoff 65→66, work_backlog 59→60, backlog index 34→35 / latest 7→8) 같은 sync commit 안에 포함.

- [x] TASK-072: AdminRunners status pill → canonical `<StatusPill>` 승격 (M4 follow-up 옵션 (c) 봉인) — `apps/build-monitor/src/components/StatusPill.svelte` colorFor 매핑에 RunnerStatus 2건 추가 (`ACTIVE` → success, `DISABLED` → danger) + aria-label `Build status:` → `Status:` 일반화. `apps/build-monitor/src/routes/AdminRunners.svelte` inline `<span class="pill" class:active class:off>` → `<StatusPill status={r.status} />` 교체 + 미사용 `.pill` / `.pill.active` / `.pill.off` CSS 21줄 제거. 회귀: vitest 71/71 PASS (TASK-071a 67 + TASK-072 신규 4), svelte-check 0/0, vite build OK (css gzip 5.30 → 5.23KB / 0.07KB 감소).

**rev 60→61 (2026-07-06)**: TASK-075 (단일 포트 reverse proxy — 사용자가 TASK-073/074 후속을 미루고 우선 요청) PR #26 squash merge main 합류 (`809f1da`). branch `codex/task-075-single-port-reverse-proxy-2026-07-06` (main `0edb5c8` base) → squash merge → `--delete-branch` 자동 삭제. 신규 6 file / 525 insertions / 2 deletions. (1) `apps/build-server/package.json` `@fastify/static` ^9.0.0 dep 추가 (lock 9.1.3). (2) `apps/build-server/src/app/create-app.ts` `mountBuildMonitorDist(app)` helper 신규 — `BUILD_MONITOR_DIST_PATH` env (runtime cwd-relative) 가 가리키는 build-monitor 의 vite build 산출물 (`apps/build-monitor/dist`) 을 `@fastify/static` 으로 mount + SPA fallback setNotFoundHandler. Build Server 자체 route (`/builds`, `/admin/*`) 가 catch 못 한 GET 은 모두 `index.html` 로 SPA fallback. Build Monitor 의 fetch 가 `/api/*` 로 보내면 registered route prefix 일 때 `/api` 떼고 307 transparent redirect. 미등록 `/api/*` 와 `/openapi|docs|health` prefix 는 JSON 404. env 미설정 시 mount skip — Build Server API 만 응답 (memory backend e2e / unit test 의도된 동작). tsc 가 workspaces 안에서 `import.meta.url` depth 가 흔들려 default path 가 위험하므로 cwd + env 로 단일화. (3) 신규 e2e script `apps/build-server/scripts/e2e-single-port.sh` — Build Server 만 띄워서 한 port 에서 4 단계 (tsc compile, boot + /health, GET / HTML, GET /admin/login SPA fallback) + API (307 → /builds) 검증. (4) 신규 test 10건 `apps/build-server/tests/static-serve.test.ts` — dist mock + SPA fallback + assets + Build Server API + /api/* 307 redirect + JSON 404 + POST JSON 404 + `/admin/*` deep link fallback. (5) `docs/PROJECT_PROFILE.md` §3 갱신 — `run_local` / `run_local_postgres` 명령 의도 명시 + `단일 포트 reverse proxy (TASK-075)` 블록 신규. 회귀 baseline: build-server tests **123/123 PASS** (기존 113 + 신규 10), ts 4 packages clean, vitest build-monitor 71/71 동일, Go 76/76 동일, build-server 117/117 동일, Python skill_mcp 226/226 동일, e2e-single-port PASS. 사용자가 `다음 테스크는 미뤄두고 다른 작업을 할거야. 우선 프록시를 우리 시스템에 포함시키고 포트 하나로 프론트, 백 모두 접근가능하도록` 라고 요청하여 TASK-073/074 (옵션 i/ii 후속) 외 작업의 최우선으로 TASK-075 진입 후 봉인. workflow meta sync (state rev 87→88, handoff 66→67, work_backlog 60→61, backlog index 35→36 / latest 8→9) 같은 sync commit 안에 포함.

- [x] TASK-075: 단일 포트 reverse proxy — Build Server 가 build-monitor 의 vite build 산출물을 정적 mount + SPA fallback 으로 함께 노출 (TASK-068/069/070/071a/072/073/074 와 직결되지 않은 사용자 최우선 요청). `apps/build-server/package.json` `@fastify/static` ^9.0.0 dep 추가. `apps/build-server/src/app/create-app.ts` `mountBuildMonitorDist(app)` helper (BUILD_MONITOR_DIST_PATH env 기반 runtime cwd-relative mount) + `@fastify/static` register + SPA fallback setNotFoundHandler (`/api/*` registered route prefix 만 307 transparent redirect, 그 외 JSON 404). 신규 e2e `apps/build-server/scripts/e2e-single-port.sh` (Build Server 만 띄워서 한 port 에서 HTML + API 둘 다 접근). 신규 test 10건. `docs/PROJECT_PROFILE.md` §3 갱신. 회귀: build-server tests 123/123 PASS, ts 4 packages clean, vitest 71/71, Go 76/76, e2e-single-port PASS.


**rev 61→62 (2026-07-06)**: TASK-076/077 (admin 진입점 + 메뉴 통합) PR #27 open (`aab8282` post-amend (AdminBuilds `$effect` 일관성 + AdminTabs `.active` class 회귀 테스트 2건), self-review 봉인 완료, squash merge 대기). branch `codex/task-077-admin-tabs-consolidation-2026-07-06` (main `809f1da` base) → open → self-review → amend 4건 (trailing newline 4건 + raw rgba → `--shadow-glow` + doc 코멘트 정합) 봉인 → squash merge 대기. 19 file / 554 insertions / 393 deletions. 사용자 요청 admin 기능을 정비 의 연속 TASK 묶음: (TASK-076) `apps/build-monitor/src/routes/AdminLogin.svelte` (183 lines) + `AdminLogin.test.ts` (2 tests) 파일 삭제 + `App.svelte` `/admin/login` 라우트 제거 + `lib/session.ts` `adminIdStore` / `ADMIN_ID_KEY` 제거 (writable store 하나로 `userId` / admin id 통합). `Header.svelte` `adminIdStore` 의존 제거 + 별도 AdminLogin 링크 / Admin Logout 버튼 제거 — 일반 Logout 한 번에 `userId` 와 admin 메뉴가 함께 사라진다. `Admin{Builds,Users,Admins,Runners}.svelte` `localStorage.getItem(adminId)` 체크 → `userIdStore` 직접 구독으로 교체. redirect target `/admin/login` → `/`. (TASK-077) 신규 `apps/build-monitor/src/components/AdminTabs.svelte` (79 lines): 4 탭 (Builds / Users / Admins / Runners) + svelte-spa-router `use:link` + `$location` store 구독으로 active tab 하이라이트. 라우트 URL 그대로 유지 (`/admin/builds` 등) — deep link / bookmark 정상 동작. 신규 `AdminTabs.test.ts` 4 tests. `Header.svelte` 의 4 nav 링크 → 단일 Admin 진입점 (`/admin/builds`). 4개 admin 페이지 상단에 `<AdminTabs />` 추가 + `<h1>` 페이지 특화 텍스트로 단축. 부수: 발견된 mock 인프라 결함 동시 봉인 — `Header.test.ts` 의 `setAdminAllowList` 가 mock `__set` 을 optional chaining 으로 호출했으나 mock 의 `__set` 이 inner writable 에만 붙어 silent no-op 이었던 것을, mock 반환 객체에 `__set` 을 직접 노출해서 helper 가 항상 inner store 까지 함께 set 하도록 수정 (회귀 안전성 회복). self-review follow-up amend 4건 — `App.svelte` / `Header.svelte` / `AdminTabs.svelte` / `AdminBuilds.svelte` trailing newline 복구 + `AdminTabs.svelte` raw `rgba(99, 102, 241, 0.4)` → `var(--shadow-glow)` 디자인 토큰 정렬 + doc 코멘트 (underline 언급) 실제 fill 스타일과 정합. 회귀 baseline: TS 4 packages clean, build-monitor vitest **80/80 PASS** (TASK-075 baseline 71 → +11: AdminTabs 신규 4 + AdminTabs self-review `.active` / `.admin-tab` class 신규 2 + Header 신규 TASK-077 회귀 가드 1 + admin 페이지 4종 tab 노출 가드 4; AdminLogin 2건 TASK-076 에서 파일 삭제; Header 9건 동일 — 신규 TASK-076 회귀 가드 3건이 기존 admin login/logout 관련 3건을 대체), svelte-check 0 errors / 0 warnings, vite build OK (gzip js 32.77KB / css 5.15KB), build-server tests **123/123 PASS** (TASK-075 baseline 유지), e2e-single-port PASS. 다음: PR #27 squash merge 대기 → 사용자 다른 지시 대기 (TASK-073 private registry auth / TASK-074 다른 deploy target 모두 보류). workflow meta sync (state rev 88→89, handoff 67→68, backlog index 36→37 / latest 9→10) 같은 sync commit 안에 포함.
- 최종 수정일: 2026-07-06 (rev 64→65: TASK-079 (skill 측 build request UI + API Console + Header nav 보완) PR #29 squash merge main 합류 (`074a80c`, 2026-07-06T07:30:00Z, by ykylee). branch `codex/task-079-build-request-ui-2026-07-06` (main `661925a` base) → squash merge → `--delete-branch` 자동 삭제 완료. 9 file / +1779 insertions / -3 deletions. 신규 5 file: BuildRequest.svelte (713 lines, POST /builds payload 직접 작성 — 3 preset + random appName + form validation + 202/409 분기 결과 패널 + Build Detail 이동), BuildRequest.test.ts (15 tests), ApiConsole.svelte (Swagger UI iframe 임베드 — `/docs/`), ApiConsole.test.ts (6 tests), lib/api.test.ts (parseApiError 7 tests). amend 4 file: BuildRequest.svelte (StatusPill + resetForm + field validation + Reset 버튼), BuildRequest.test.ts (+7 tests), ApiConsole.svelte (description 명확화), lib/api.ts (parseApiError helper). Header nav 보완: API → API Console + OpenAPI + Docs + New Build. self-review amend 두 차례 봉인 — 1차 (`110ff25`) 신규 5 file + App routing + Header nav + lib/api.ts submitBuildRequest helper. 2차 (`d7baea7`) amend 보완 7건 — (C-1) BuildSummary lifecycleStatus mock, (C-2) parseApiError helper, (I-1) StatusPill 사용, (I-2) Reset form 분리, (I-3) previewTtlMinutes/dockerfilePath validation, (D-4) default 60, (D-5) ApiConsole description. 회귀 baseline: TS 4 packages clean, build-monitor vitest **114/114 PASS** (TASK-077 baseline 102 → +12: BuildRequest 5 + parseApiError 7), build-server 123/123 PASS, Go 7 packages PASS, svelte-check 0 errors / 0 warnings, vite build OK (gzip js 37.27KB / css 6.31KB), e2e-single-port PASS. 본 TASK 봉인으로 TASK-017/035/036/043/051~079 모든 후속 TASK main 합류. 다음: 사용자 다른 지시 대기. workflow meta sync (state rev 91→92, handoff 70→71, work_backlog 64→65, backlog index 39→40 / latest 12→13) 같은 sync commit 안에 포함.)
**rev 62→63 (2026-07-06)**: TASK-076/077 (admin 진입점 + 메뉴 통합) PR #27 squash merge main 합류 (`0eb0ad7`). branch `codex/task-077-admin-tabs-consolidation-2026-07-06` (main `809f1da` base) → squash merge → `--delete-branch` 자동 삭제 완료. PR 본체 (`49dac8c`) + 1차 amend (`85a8ab5`) + 2차 amend (`aab8282`) 가 squash 단일 commit 으로 main 에 통합됨. self-review follow-up amend 두 차례 봉인 — 1차 (trailing newline 4건 + raw rgba → `--shadow-glow` + doc 코멘트 정합), 2차 (AdminBuilds 비대칭 `$effect` 일관성 + AdminTabs `.active` class 회귀 테스트 2건). 회귀 baseline: TS 4 packages clean, build-monitor vitest **80/80 PASS** (TASK-075 baseline 71 → +11: AdminTabs 신규 4 + self-review `.active` / `.admin-tab` class 신규 2 + Header 신규 TASK-077 회귀 가드 1 + admin 페이지 4종 tab 노출 가드 4; AdminLogin 2건 TASK-076 에서 파일 삭제; Header 9건 동일 — 신규 TASK-076 회귀 가드 3건이 기존 admin login/logout 관련 3건을 대체), svelte-check 0 errors / 0 warnings, vite build OK (gzip js 32.77KB / css 5.15KB), build-server tests 123/123 PASS (TASK-075 baseline 유지), e2e-single-port PASS. 본 PR 합류로 TASK-017/035/036/043/051~077 모든 후속 TASK main 합류 — 사용자 요청 'admin 기능을 정비' 의 연속 TASK 묶음 봉인. 다음: 사용자 다른 지시 대기 (TASK-073 private registry auth / TASK-074 다른 deploy target 모두 보류). workflow meta sync (state rev 89→90, handoff 68→69, backlog index 37→38 / latest 10→11) 같은 sync commit 안에 포함.

- [x] TASK-076: admin 권한 자동 부여 + Admin Login/Logout 제거 (연속 TASK 묶음 1/2) — PR #27 squash merge main 합류 (`0eb0ad7`). `apps/build-monitor/src/routes/AdminLogin.svelte` (183 lines) + `AdminLogin.test.ts` (2 tests) 파일 삭제. `App.svelte` `/admin/login` 라우트 제거. `lib/session.ts` `adminIdStore` / `ADMIN_ID_KEY` 제거 — writable store 하나로 `userId` / admin id 통합. `Header.svelte` `adminIdStore` 의존 제거 + 별도 AdminLogin 링크 / Admin Logout 버튼 제거 — 일반 Logout 한 번에 `userId` 와 admin 메뉴가 함께 사라진다. `Admin{Builds,Users,Admins,Runners}.svelte` `localStorage.getItem(adminId)` 체크 → `userIdStore` 직접 구독으로 교체. redirect target `/admin/login` → `/`. 테스트 4종 localStorage 키 `adminId` → `userId` + redirect target `/admin/login` → `/`. 회귀: build-monitor vitest 71→69 (AdminLogin 2건 TASK-076 에서 파일 삭제; Header 9건 동일 — 신규 TASK-076 회귀 가드 3건이 기존 admin login/logout 관련 3건을 대체).

- [x] TASK-077: admin 메뉴 단일 진입점 + 페이지 탭 (연속 TASK 묶음 2/2) — PR #27 squash merge main 합류 (`0eb0ad7`).
- [x] TASK-079: skill 측 build request UI + API Console + Header nav 보완 (TASK 묶음 1/1) — PR #29 squash merge main 합류 (`074a80c`). 9 file / +1779 insertions / -3 deletions. 본 PR 본질: (1) Header nav 의 API/Docs 의도 명확화 (API Console in-SPA + OpenAPI raw JSON target=_blank + Docs Swagger UI target=_blank) + New Build 진입점 추가, (2) 신규 페이지 2종 — `/build-request` BuildRequest payload 직접 작성 (3 preset + random appName + form validation + 202/409 분기 결과 패널 + Build Detail 이동) + `/api-console` Swagger UI iframe 임베드 (Refresh + Raw OpenAPI JSON + Go to Build Request 액션), (3) self-review amend 7건 봉인 (BuildSummary lifecycleStatus mock, parseApiError helper, StatusPill 사용, Reset form 분리, validation 강화, default 60, description 명확화). 회귀: TS 4 packages clean, build-monitor vitest 114/114 (was 102 → +12: BuildRequest 5 + parseApiError 7), build-server 123/123, runner 7 packages, svelte-check 0/0, vite build OK, e2e-single-port PASS.
- [x] TASK-078: container self-dogfood — Build Server + Runner Dockerfile + compose + CI workflow (TASK 묶음 1/1) — PR #28 squash merge main 합류 (`661925a`). 8 file / 927 insertions. 본 PR 본질: (1) Build Server multi-stage Dockerfile (builder + prod-deps + runtime 4-stage, USER app@1500 non-root, ADMIN_IDS default 부재, HEALTHCHECK start_period 15s), (2) Runner Dockerfile (Go multi-stage alpine 3.20 + docker-cli, USER runner@1500 + docker group supplementary), (3) compose.dev.yaml (build-server + runner + profile postgres, ${ADMIN_IDS:?...} + ${DOCKER_SOCKET_GID:?...} strict 강제), (4) .dockerignore (Dockerfile/compose 라인 제거), (5) .github/workflows/docker-build.yml (PR/main trigger, image build + size 200 MB threshold + USER non-root 검증 + /health + smoke scenario). amend 10건 봉인 — (C-1) USER directive + docker group supplementary, (C-2) ADMIN_IDS default 부재 + compose strict mode, (C-3) DOCKER_SOCKET_GID silent fallback 제거, (C-4) apk add git 제거, (C-5) HEALTHCHECK start_period 5s → 15s, (I-1) prod-deps stage 분리 (image size 309 MB → 169 MB, -45%), (I-2) .dockerignore 의 Dockerfile/compose 라인 제거, (I-3) CI workflow .github/workflows/docker-build.yml 신규, (I-4) Runner claimBackoff exponential + log throttle + TestWorker_ClaimBackoff_ExponentialWithCap 7 sub-test, (I-5) Runner EXPOSE 3000 제거. 회귀: TS 4 packages clean, build-monitor vitest 80/80, build-server 123/123, runner go test 7 packages (신규 backoff test), svelte-check 0/0, vite build OK, e2e-single-port PASS. image size: dibs/build-server:dev 169 MB / dibs/runner:dev 41 MB. 시뮬레이션 7 단계 self-dogfood 시나리오 실 검증 완료 (PHASE 0 환경 / 1 standalone 부팅 + USER + start_period / 2 OpenAPI + Admin 가드 / 3 build lifecycle / 4 compose up + Runner join + USER / 5 Runner claim + SOURCE_PREPARED + FAILED / 6 Runner backoff failure=1,2,3 → 5s,10s,20s + recovery / 7 amend 통합 검증 + 회귀 baseline + cleanup). 본 TASK 묶음 봉인으로 TASK-017/035/036/043/051~078 모든 후속 TASK main 합류. `apps/build-monitor/src/components/AdminTabs.svelte` 신규 (79 lines): 4 탭 (Builds / Users / Admins / Runners) + svelte-spa-router `use:link` + `$location` store 구독으로 active tab 하이라이트. 라우트 URL 그대로 유지 (`/admin/builds` 등) — deep link / bookmark 정상 동작. `AdminTabs.test.ts` 4 tests (4탭 렌더링 + href 매핑 + active `aria-current="page"` + 라우트 변경 시 active 전환). `Header.svelte` 의 4 nav 링크 → 단일 Admin 진입점 (`/admin/builds`). 4개 admin 페이지 상단에 `<AdminTabs />` 추가 + `<h1>` 페이지 특화 텍스트로 단축 (예: `Admin · Admins` → `Admins`). `Admin{Builds,Users,Admins,Runners}.test.ts` 에 `$location` writable store mock 추가 (`vi.hoisted` + `require` hoist-safe 패턴). 회귀: build-monitor vitest 69→78 (AdminTabs 신규 4 + AdminTabs self-review `.active` / `.admin-tab` class 신규 2 + Header 신규 TASK-077 회귀 가드 1 + admin 페이지 4종 tab 노출 가드 4).
- [x] 운영 검증 메모: `docs/operations/dogfood-e2e-2026-07-06.md` — self-dogfood 실제 build/preview/queue 검증 결과 정리. 성공 build `bab5995f-7741-4ffc-80ba-95d53684263d` + preview `:32770`, queue 3건 순차 처리 (`9562dc11` → `854fda1b` → `12ea7282`), source upload race (`/builds/:id/source` 404) 재현, admin UI consistency 메모 포함.
- [x] TASK-080: source upload race mitigation (옵션 B+C) — PR #30 squash merge main 합류 (`2e2d67a`, 2026-07-06T14:11:45Z). 14 file / +892 / -97. 옵션 B: memory `sourceArchives.has(buildId)` + postgres `build_source` INNER JOIN 으로 claim gate. 옵션 C: runner fetcher exponential retry 1s/3s/9s. 회귀 가드 신규 6건 + 기존 caller 5건 보강. 회귀: build-server 123→127 PASS, Go 8 packages 모두 PASS. 리뷰 문서 `docs/operations/dogfood-e2e-review-and-followup-2026-07-06.md` 동봉. amend 3건 (trailing newline + JSDoc 3 file).
- [x] TASK-081: multi-runner concurrent claim regression guard — PR #31 squash merge main 합류 (`16f17a0`, 2026-07-06T14:24:00Z). 1 file / +200 / -0. 본질: memory-build-repository.test.ts 의 multi-runner atomic describe 신규 4건 — (1) drains N queued builds across N sequential claim cycles with release, (2) yields exactly 1 claim + (N-1) active_build_exists when N runners race N builds in one cycle, (3) advances across multiple Promise.all cycles after each release, (4) yields 1 claim + (M-1) active_build_exists for M runners against K<M builds. 회귀: build-server 127→131 PASS, Go 8 packages 모두 PASS. 다음: TASK-081-B multi-runner 운영 검증 또는 TASK-082 postgres e2e.
- [x] TASK-081-B: multi-runner concurrent claim 운영 검증 — PR #32 squash merge main 합류 (`ddae46a`, 2026-07-06T14:44:05Z). 3 file / +495 / -0. 본질: (1) `compose.dev.runner-multi.yaml` 신규 (TASK-078 의 1 runner compose 위에 runner2 / runner3 추가), (2) `apps/build-server/scripts/e2e-multi-runner.sh` 신규 (5 build × 3 runner lifecycle 자동 검증), (3) `docs/operations/multi-runner-claim-2026-07-06.md` 신규 (운영 가이드 + 실 검증 결과). 운영 검증 결과: buildsClaimed 분산 1+2+2, 중복 claim 0, 5/5 SOURCE_PREPARED 직후 FAILED. self-review amend: 3 file trailing newline 보강. 다음: TASK-082 postgres e2e 또는 TASK-083 admin UI 일관성.
- [x] TASK-082: postgres backend multi-runner 운영 검증 — PR #33 squash merge main 합류 (`94a220a`, 2026-07-06T15:08:00Z). 4 file / 697 insertions. 본질: (1) `compose.dev.runner-multi-postgres.yaml` 신규 (postgres profile + 3 runner override — BUILD_REPOSITORY_BACKEND=postgres + DATABASE_URL + DB_AUTO_BOOTSTRAP=true + depends_on postgres healthy), (2) `apps/build-server/scripts/e2e-multi-runner-postgres.sh` 신규 (8 단계 자동 검증 — postgres healthy → build-server cold start → 3 runner registry → 5 build 적재 + source upload → lifecycle → admin 분산 검증 → postgres `build_request` 영속 검증 → compose down + cleanup), (3) `docs/operations/multi-runner-claim-postgres-2026-07-06.md` 신규 (운영 가이드 + 사전 결함 봉인 이력). amend `apps/build-server/src/app/create-app.ts` `MIGRATIONS_DIR` 를 `import.meta.url` 의 `../../migrations/` (dist tree depth 차이로 `dist/apps/build-server/migrations/` 부재 가리킴) 에서 `process.cwd()` 기준 `apps/build-server/migrations/` 로 변경 — postgres backend 부팅 시 `ENOENT` surface 됐고 memory backend 는 `applyMigrations` 호출 안 해서 잠복. TASK-075 `BUILD_MONITOR_DIST_PATH` 와 동일 import.meta.url 함정 봉인 패턴. 운영 검증 결과: 3 runner 모두 ACTIVE (postgres runner table 3 row), 5 build 적재 + source upload, 5/5 build terminal phase (FAILED × 5 — size 0 source 의 의도된 dummy), buildsClaimed 분포 (3+0+2) — 총 5, 중복 0, 2+ actively claimed, postgres `build_request` 영속 검증 filtered=5 / distinct appName=5, migrations 자동 적용 0001~0005 모두. self-review amend 3건 — (a) 운영 가이드 §3.1 (b) TASK-081-B 의 e2e-multi-runner.sh 같은 `BASE` + heredoc 결함 명시, (b) 운영 가이드 §3.5 분포 비결정성 명시 (TASK-081-B 1+2+2 / TASK-082 3+0+2 모두 합=5 보장), (c) e2e `BASE` env var `BUILD_SERVER_URL` override 지원. 회귀 baseline: TS 4 packages `tsc --noEmit` clean, Go 7 packages `go test ./...` 모두 PASS, e2e-multi-runner-postgres.sh ALL PASS. 다음: TASK-083 admin UI 일관성 또는 TASK-085 busybox/scratch Dockerfile + 실제 tar.gz source 운영 검증 보강.
- [x] TASK-083: admin UI visual + nav 정합 — `apps/build-monitor/src/components/FilterChips.svelte` 신규 (chip 디자인 단일 source, `--shadow-glow` 디자인 토큰 정렬) + 3 페이지 (AdminBuilds / AdminRunners / BuildsList) 의 chip 영역 + chip CSS 통째 제거 + AdminAdmins 페이지 구조 통일 (.page + page-head + fadeIn + h1 gradient + var(--size-xxl)) + raw rgba 잔재 9건 `var(--shadow-glow)` 정렬 (Header logo + AdminBuilds btn + ApiConsole btn × 2 + BuildRequest btn × 2 + Login btn) + `FilterChips.test.ts` 7 tests 신규 (옵션 6 + source-level 디자인 토큰 회귀 가드 1). PROJECT_PROFILE.md §3.3 신규 + `docs/operations/build-monitor-ui-visual-2026-07-06.md` 운영 문서. 보류: Header sticky bar 1px light-only shadow 디자인 토큰 신설 검토 후속. PR #34 `e28779d`.
- [x] TASK-084: admin 가드 deep link UX — 비-admin user 의 `/admin/*` deep link 진입 시 raw 403 envelope 대신 친절한 권한 없음 패널 봉인 (`docs/operations/dogfood-e2e-review-and-followup-2026-07-06.md` §3.4 G-4 gap). `apps/build-monitor/src/lib/admin-guard.ts` 신규 — `ensureAdminAccess(callerId)` helper (adminAllowListStore 캐시 + refresh + contains 체크 단일 source, 결과 `{ isAdmin, allowList, reason: NO_USER|FORBIDDEN|NOT_IN_ALLOW_LIST }`, backend 401/403 만 FORBIDDEN, 5xx fallback). `admin-guard.test.ts` 9 tests 신규. `apps/build-monitor/src/components/AdminAccessDenied.svelte` 신규 — page-head + danger accent h1 + Login.svelte .card 패턴 + 두 액션 (Back to Builds / Switch user). 4 admin 페이지 (AdminBuilds / AdminUsers / AdminAdmins / AdminRunners) onMount 첫 단계에 frontend 가드 봉인 (`accessDenied` state + ensureAdminAccess 호출 + `!isAdmin` 시 패널 노출, backend 호출은 가드 통과 후) + 4 admin 페이지 test 회귀 가드 1~2건 추가 (비-admin userId 시 accessDenied 분기 + backend API 미호출 검증). PROJECT_PROFILE.md §3.4 신규. Backend 변경 0 — Build Server 의 X-Admin-Id 401/403 envelope 그대로 유지 (defense in depth). PR #35 `32aebc7`.
- [x] TASK-085: production-semantic 운영 검증 보강 — TASK-081-B/082 의 dummy (size 0 source) 검증이 FAILED 만 다루던 한계를 dogfood `bab5995f-…-95d53684263d` 의 자동 재현 동등물로 봉인. `apps/build-server/scripts/e2e-production-semantic.sh` 신규 — 7 단계 자동 검증 (busybox pull warm-up → compose up → build-server health → runner registry → source archive POST → build COMPLETED → 10 phase + preview URL 검증 → container cleanup). `compose.dev.e2e-production.yaml` 신규 override — single runner + cli mode (RUNNER_DOCKER_BUILD_MODE=cli / RUNNER_DOCKER_RUN_MODE=cli / RUNNER_DEPLOY_MODE=skeleton) + network_mode=host + RUNNER_STOP_CONTAINER_ON_DONE=true. `apps/runner/internal/docker/client.go` 보강 — RunContainer 의 docker inspect 를 최대 5 회 × 200ms retry (docker inspect race (hostPort=0) 결함 봉인). 신규 회귀 가드 2건 (TestRunContainerCliModeInspectRetriesUntilPortAppears + AllAttemptsEmptyLeavesHostPortZero). `docs/operations/production-semantic-2026-07-07.md` 운영 가이드 (검증 결과 / 사용 절차 / 사전 결함 3건 / 한계 / 빠른 재현). PROJECT_PROFILE.md §3.5 신규. 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 121/121 동일 (frontend 변경 0), Go 7 packages 모두 PASS, svelte-check 0/0, `e2e-production-semantic.sh` ALL PASS. PR #36 `c5e3008`.
- [x] TASK-086: e2e-multi-runner.sh BASE + heredoc 결함 봉인 — TASK-081-B 가 봉인될 때 못 가져간 두 가지 결함 (1) `BASE="http://build-server:3000"` 가 host shell 에서 unreachable (compose network 내부 DNS), (2) `[5/6]` heredoc 가 stdin pipe 를 hijack (`sys.stdin.read()` 가 항상 빈 응답) — 을 봉인. amend `apps/build-server/scripts/e2e-multi-runner.sh` — `BASE="${BUILD_SERVER_URL:-http://127.0.0.1:3000}"` (TASK-082 동일), `[2/6]` runner registry 대기 30s → 90s, `[0/6] compose up` 에 `--build`, project name `$$` suffix (병렬 실행 안전), `[5/6]` heredoc 를 `RUNNERS_JSON="${RUNNERS}" python3 <<'PY'` + `os.environ["RUNNERS_JSON"]` 패턴으로 교체. follow-on `apps/build-server/scripts/e2e-production-semantic.sh` 의 `[6/7]` 10 phase 검증 — 동일 stdin hijack 결함 (TASK-085 가 우연히 동작: `${LOGS_JSON}` shell expand 으로 우회), env var 패턴 + `<<'PY'` quoted marker 로 견고화 (log JSON `'''` 포함 시 silent fail 위험 봉인). PROJECT_PROFILE.md §3.6 신규. 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 135/135 동일 (frontend 변경 0), Go 7 packages 모두 PASS, svelte-check 0/0, `e2e-multi-runner.sh` ALL PASS (~3-4 분, `2/3 runners actively claimed builds` 분산 검증 출력 회복), `e2e-production-semantic.sh` follow-on 도 ALL PASS. PR #37 `ab0be9e` (FF merge).
- [x] TASK-073: RUNNER_REGISTRY_CONFIG_DIR + cli mode registry push e2e — private Docker Hub / ECR / GCR 인증 foundation. `apps/runner/internal/config/config.go` `Config.RegistryConfigDir` 필드 + `parseString("RUNNER_REGISTRY_CONFIG_DIR", "")` (default empty → docker default 회귀 없음) + `apps/runner/cmd/runner/main.go` `os.Setenv("DOCKER_CONFIG", cfg.RegistryConfigDir)` 호출 (후속 모든 docker CLI invocation 이 그 dir 의 config.json 사용). `apps/runner/internal/config/config_test.go` 신규 6건 (default empty / env read / all-fields / PollInterval default / bare-integer seconds / Setenv observability). `compose.dev.e2e-registry.yaml` (registry:2 container + runner override cli mode + `RUNNER_DEPLOY_TARGET_REF=localhost:5000/docker-image-builder-system/cli` + `RUNNER_REGISTRY_CONFIG_DIR=/registry-config` + host 임시 config.json volume mount). `apps/build-server/scripts/e2e-registry-push.sh` 8 단계 + 보너스 (registry:2 cold start → registry healthy → build-server healthy → runner registered → source archive + cli push lifecycle → registry API catalog/tags 검증 → bonus manifest). `docs/operations/registry-push-2026-07-07.md` 운영 가이드. PROJECT_PROFILE.md §3.7 신규. 사전 결함 + 보강 4건: (1) runner binary 에 RUNNER_REGISTRY_CONFIG_DIR 가 없었음, (2) compose 검증 시 `group_add` 중복 오류 (`group_add` 제거 + Dockerfile 의 `addgroup runner docker` 의존), (3) `RUNNER_DEPLOY_TARGET_REF` 가 registry 의 slash split 에서 의도된 repo 가 안 잡힘 (localhost:5000/docker-image-builder-system/cli 정렬), (4) manifest v2 vs v1 schema (bonus, fatal 아님). 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 135/135 동일 (frontend 변경 0), Go 8 packages 모두 PASS (기존 7 + config 6/6), svelte-check 0/0, `e2e-registry-push.sh` ALL PASS (~2-3 분 — registry catalog `{"repositories":["docker-image-builder-system/cli"]}` + tags list 에 buildId 노출). PR #38 `cd01b99`.
- [x] TASK-074: htpasswd 인증 registry push e2e — TASK-073 의 insecure-registry foundation 위에서 REGISTRY_AUTH=htpasswd 가 enabled 일 때 cli mode docker push 가 base64 auths entry + bcrypt htpasswd entry 와 정합되어 통과하는지 검증. amend `compose.dev.e2e-registry.yaml` (REGISTRY_AUTH=htpasswd + REGISTRY_AUTH_HTPASSWD_REALM + REGISTRY_AUTH_HTPASSWD_PATH env 3종 + ${HOST_REGISTRY_AUTH_DIR}:/auth:ro volume mount + healthcheck `nc -z` 단순화). amend `apps/build-server/scripts/e2e-registry-push.sh` (HOST_REGISTRY_AUTH_DIR 임시 디렉터리 + chmod 0755 + `docker run --rm httpd:alpine htpasswd -nbB` 로 bcrypt hash + config.json 의 auths entry key localhost:5000 정렬 + insecure-registries 둘 다 + bonus-A 인증 부재 → 401 + bonus-B 잘못된 credential → 401 + [8/8] 인증 부착 curl + bonus-9 manifest). `docs/operations/registry-push-auth-2026-07-07.md` 운영 가이드. PROJECT_PROFILE.md §3.8 신규. 사전 결함 + 보강 4건: (1) config.json auths entry key exact match (127.0.0.1:5000 → localhost:5000 정렬), (2) host 임시 디렉터리 permission (mktemp default 0700 → chmod 0755), (3) registry:2 의 apr1 비호환 (httpd:alpine htpasswd -nbB 로 bcrypt 통일), (4) KEEP_PROJECT=1 의 bind mount dangling source (debug 보강 — trap 의 rm -rf 도 보류). 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 135/135 동일 (frontend 변경 0), Go 8 packages 모두 PASS (TASK-073 baseline 유지), svelte-check 0/0, `e2e-registry-push.sh` ALL PASS (~3-4 분 — registry catalog `{"repositories":["docker-image-builder-system/cli"]}` + tags list 에 buildId 노출 + bonus 인증 부재/잘못된 credential 모두 401). PR #39 `d87a17c`.
- [x] TASK-075: credential rotation e2e — TASK-074 의 htpasswd 인증 환경 위에서 htpasswd + config.json 의 auths entry 를 runtime 중 갱신해도 docker CLI + registry 가 새 credential 로 push 동작함을 검증. `apps/build-server/scripts/e2e-credential-rotation.sh` 신규 — 9 단계 + 보너스 2 (busybox/registry warm-up → 초기 credential v1 셋업 → compose up → registry healthy → build-server healthy → runner registered → 첫 build (v1) push 통과 → htpasswd v2 + config.json v2 갱신 → SIGHUP+restart fallback → 두 번째 build (v2) push 통과 → 옛 credential 401 → 새 credential catalog 정상 + 두 buildId tags 노출). `docs/operations/credential-rotation-2026-07-07.md` 운영 가이드. PROJECT_PROFILE.md §3.9 신규. 사전 결함 + 보강 4건: (1) htpasswd file 0444 read-only 가 갱신 시 Permission denied (silent fail 위험) → update_htpasswd() 가 chmod 0644 후 redirect + chmod 0444 재부착, (2) submit_and_wait() 함수의 stdout 이 buildId 와 progress log 가 혼합 → log() helper + >&2 redirect, (3) **registry:v2 가 htpasswd file 의 container-runtime 갱신을 즉시 반영 안 함** — SIGHUP 시도 → 안 되면 restart fallback. 운영 환경 credential rotation workflow 의 보안 결함 (옛 credential 이 cache 기간 동안 동작) 명시화, (4) v2 credential 갱신 후 옛 credential 의 즉시 무효화 → restart 후엔 즉시 401 보장. 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 135/135 동일 (frontend 변경 0), Go 8 packages 모두 PASS (TASK-074 baseline 유지), svelte-check 0/0, `e2e-registry-push.sh` (TASK-074) ALL PASS (회귀 baseline 유지), `e2e-credential-rotation.sh` ALL PASS (~3-4 분 — catalog `{"repositories":["docker-image-builder-system/cli"]}` + tags list build_v1+build_v2 둘 다 노출 + bonus 옛 credential 401 + 새 credential 정상). PR #40 `8e0b228`.
- [x] TASK-076: insecure-registry only 운영 패턴 검증 — 사용자가 "htpasswd 제외" 결정 후 TASK-074/075 의 htpasswd 모델을 supersede. `compose.dev.e2e-insecure-registry.yaml` 신규 — REGISTRY_AUTH env 미설정 (anonymous access) + REGISTRY_STORAGE_DELETE_ENABLED=true + htpasswd 인증 환경 일체 제거. `apps/build-server/scripts/e2e-insecure-registry.sh` 신규 — 8 단계 + 보너스 (busybox/registry warm-up → HOST_REGISTRY_CONFIG 셋업 / insecure-registries 만, auths 없음 → compose up → registry healthy (`/v2/` 200 — anonymous access) → build-server healthy → runner registered → **5 build 동시 push** (per-build unique source archive — build_idx 가 Dockerfile 의 RUN line content 에 반영되어 manifest digest 가 build 별 unique) → image retention 검증 (catalog + tags list 노출 → DELETE API 로 1 tag 삭제 → 다른 4 tag 영향 없음 확인) → [bonus] retention 후 새 build push). delete 6 file (htpasswd 모델 — git history 에 보존, 운영의 canonical 은 §3.10): `e2e-registry-push.sh` (TASK-074), `e2e-credential-rotation.sh` (TASK-075), `compose.dev.e2e-registry.yaml` (TASK-074), `docs/operations/registry-push-2026-07-07.md` (TASK-073), `docs/operations/registry-push-auth-2026-07-07.md` (TASK-074), `docs/operations/credential-rotation-2026-07-07.md` (TASK-075). `docs/operations/insecure-registry-only-2026-07-07.md` 운영 가이드. PROJECT_PROFILE.md §3.10 신규 + §3.9 supersede 명시. 사전 결함 + 보강 3건: (1) **동일 Dockerfile 의 5 build 가 manifest digest 가 동일 → 1 tag DELETE = 모든 tag 영향** (TASK-076 의 핵심 발견. 보존해야 할 build 들까지 사라지는 silent failure) — build_idx 를 Dockerfile 의 RUN line 에 주입해 per-build unique source archive 로 해결. 운영 권고: image 의 `LABEL build_id=$CI_COMMIT_SHA` 또는 build time 의 `RUN echo "Build: $(date +%s)"` 같은 unique content 보장, (2) curl `-I` (HEAD) 가 Docker-Content-Digest header 를 안 보냄 → `curl -sS -D - -o /dev/null` 패턴, (3) submit_and_wait 의 per-build source archive 가 mktemp cleanup 으로 source archive 까지 삭제 → `rm -rf ${per_src}` 를 upload / build lifecycle 완료 후로 이동. 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131/131 동일 (backend 변경 0), build-monitor 135/135 동일 (frontend 변경 0), Go 8 packages 모두 PASS (TASK-075 baseline 유지), svelte-check 0/0, `e2e-insecure-registry.sh` ALL PASS (~3-4 분 — catalog `{"repositories":["docker-image-builder-system/cli"]}` + tags 5 buildId 다 노출 (per-build unique manifest digest) + DELETE 202 Accepted (target tag 만 삭제, 다른 4 tag 영향 없음) + retention 후 새 build push 통과). PR #41 `8012942`.
- 최종 수정일: 2026-07-07 (rev 78→79: TASK-076 (insecure-registry only 운영 패턴 검증) PR #41 squash merge main 합류 `8012942`. workflow meta sync 같은 commit 안에 포함.)
- [x] TASK-077: admin-initiated runner registration — admin 메뉴에서 신규 runner 를 pre-registration 할 수 있도록 + Register Runner 버튼 + POST /admin/runners endpoint 신규. 기존 self-register on first claim (TASK-069) 의 한계 — 운영자가 runner 가 boot 되기 전 admin UI 에 미리 알릴 수 없었음 — 봉인. `RegisterRunnerModal.svelte` 신규 — runnerId input + Enter/Escape 키보드 / backdrop click → close. 성공시 (201) close + onSuccess, 실패 (400/401/403/409) 시 inline error 표시 + modal 유지. `adminRunnerRegisterRequestSchema` (runnerId 만, strict) + `adminRunnerRegisterResponseSchema` (adminRunnerSchema wrap) — empty / extra field / type mismatch → 400 (strict schema). POST /admin/runners endpoint — 401 (X-Admin-Id missing) / 403 (non-admin) / 400 (invalid body) / 409 (duplicate) / 201 (created) 응답. `createAdminRunner` method (memory: Map.has check + postgres: ON CONFLICT DO NOTHING — atomic). `docs/operations/admin-runner-register-2026-07-07.md` 운영 가이드. PROJECT_PROFILE.md §3.11 신규. 사전 결함 + 보강 4건: strict schema 의 extra fields 거부 (zod .strict() 가 admin 의 typing 실수를 400 으로 거부. backend 가 새 field 를 받기 전 contract 정합성 보장) / self-register 와의 race condition 방지 (postgres ON CONFLICT DO NOTHING / memory Map.has 가 atomic. 어느 한 쪽이 success, 다른 한 쪽이 409) / admin UI 의 modal close vs error 표기 policy (error 시 modal 닫지 않음 — 재시도 가능. 성공시에만 close + refresh) / runnerId 가 RUNNER_ID env 와 일치해야 함 (modal 의 modal-help 가 명시. 운영자가 mismatch 를 사전에 알 수 있도록). 회귀 baseline: TS 4 packages tsc --noEmit clean, build-server 131 → 139 PASS (8건 신규: 401/403/400 empty/400 extra/201 created/409 duplicate/409 self-then-admin/200 list), build-monitor 135 → 140 PASS (5건 신규: button visible/modal opens/success refresh+close/409 modal open/400 modal open), Go 8 packages 모두 PASS (TASK-076 baseline 유지), svelte-check 0 errors (1 warning — modal backdrop a11y 무해), `vite build` OK (gzip js 39.46KB / css 6.93KB — RegisterRunnerModal 추가로 +0.22KB / +0.05KB). PR #42 `6847f1e`.

- [x] TASK-087: Nextcloud CalDAV helper script 봉인 + e2e CRUD smoke 검증 — `https://nextcloud.ddn777.synology.me/` (Nextcloud Hub 10 / v34.0.1.2) 의 CalDAV endpoint 와 Keychain credential 연동. user-level helper 봉인: `~/.mavis/credentials/nextcloud/nextcloud-auth.sh` (Keychain credential read, perl alarm 8s 비대화형 hang 회피, base64 Basic auth, public function 4종) + `~/.mavis/credentials/nextcloud/nextcloud-caldav.sh` (CRUD wrapper, subcommand 9종, VTODO body RFC 5545 CRLF 강제, Nextcloud 34.x Sabre\DAV If-Match etag false-negative 412 known-issue 회피, last-write-wins) + `~/.mavis/credentials/nextcloud/README.md`. e2e smoke (nextcloud-caldav.sh smoke): auth sanity / list 2 existing snapshot / create UID=`2ce09256-ffaf-45cf-bf51-efbaeab823a1` HTTP 201 / update NEEDS-ACTION→IN-PROCESS HTTP 204 / update IN-PROCESS→COMPLETED HTTP 204 / delete HTTP 204 모두 PASS. workflow meta sync (state rev 107→108, handoff 77→78, work_backlog 71→72, backlog index 42→43 / latest 19→20) 같은 sync commit 안에 포함. 회귀 영향 0 (helper 자체는 user-level, git 추적 X).
- [x] TASK-088: React + Astryx 부트스트랩 PoC — Meta의 Astryx v0.1.4 (MIT, 10일 된 베타) 를 우리 Svelte 5 build-monitor 에 도입하기 위한 1단계 부트스트랩. Svelte 빌드 (apps/build-monitor/dist/, Build Server 가 mount) 와 React 빌드 (apps/build-monitor/dist-react/) 가 같은 vite workspace 에서 공존 — 회귀 영향 0. 신규 6 file: `tsconfig.react.json` (jsx: react-jsx + paths @/* 정합) / `vite.react.config.ts` (port 5174 dev + outDir dist-react + path.resolve alias) / `react/index.html` (id="app-react") / `src/react/main.tsx` (createRoot + Theme + neutralTheme) / `src/react/App.tsx` (VStack + Button PoC) / `src/react/globals.css` (3 layer cascade: reset.css → astryx.css → theme-neutral) / `src/react/App.test.tsx` (vitest + RTL 1 test). 수정 4 file: `package.json` (React 19 + Astryx + plugin-react + RTL 추가, scripts dev:react/build:react/check:react) / `vite.config.ts` (plugins [svelte(), react()] + resolve.alias @ + vitest include .tsx + /testing-library/react inline) / `tsconfig.json` (src/react/** exclude 로 svelte-check JSX 오염 차단) / `.gitignore` (dist-react/). 회귀 baseline: svelte-check 0 errors / 1 warning (TASK-077 baseline RegisterRunnerModal a11y 무해), vitest build-monitor 141/141 PASS (기존 140 + Astryx App 1 신규), vite build Svelte 정상 (gzip js 39.37KB / css 6.94KB), vite build --config vite.react.config.ts 정상 (1878 modules transformed, gzip js 81.44KB / css 25.57KB), tsc -p tsconfig.react.json clean, Build Server mountBuildMonitorDist 영향 0. 후속 TASK: TASK-089 react-router-dom + Login 페이지 1개 마이그레이션, TASK-090 핵심 컴포넌트 (StatusPill → Badge, Header → TopNav, ThemeToggle), TASK-091 8 routes, TASK-092 lib layer (openapi-fetch + Zustand + chipFilter + visual diff baseline), TASK-093 Build Server dist mount React dist로 전환 (TASK-075 follow-up), TASK-094 기존 Svelte 코드 + package 정리. workflow meta sync (state rev 108→109, handoff 77→78, work_backlog 72→73, backlog index 43→44 / latest 20→21) 같은 sync commit 안에 포함. — `https://nextcloud.ddn777.synology.me/` (Nextcloud Hub 10 / v34.0.1.2) 의 CalDAV endpoint (`/remote.php/dav/calendars/yklee/tasks/`) 와 Keychain credential 연동. 사용자 NAS 의 Nextcloud 가 살아있음을 확인 (v34 Hub 10 + Tasks 앱 enable + Calendar 앱 enable + principal `x-user-id: yklee` + DAV feature 모두 활성 — calendar-access / calendar-search / calendar-auto-schedule / nc-calendar-trashbin / nextcloud-checksum-update / nc-enable-birthday-calendar). Task REST API (`/ocs/v2.php/apps/tasks/api/v1/tasks`) 와 Calendar REST API 는 404 (앱 파일 부재 — Nextcloud 의 Synology 패키지 기반 인스톨이 bundle 한 calendar/tasks 의 REST 라우트가 없음) 이지만 CalDAV (RFC 4791) VTODO CRUD 는 정상. user-level helper 봉인: (1) `~/.mavis/credentials/nextcloud/nextcloud-auth.sh` — credential read helper (macOS Keychain entry `service=nextcloud` / `account=ddn777@hotmail.com`, perl alarm 8s 로 비대화형 GUI prompt hang 회피, base64 Basic auth header 생성, env override `NEXTCLOUD_USER` / `NEXTCLOUD_KEYCHAIN_S` / `NEXTCLOUD_TIMEOUT`). public function 4종 — `nextcloud_auth_header` / `nextcloud_auth_header_inline` / `nextcloud_basic_creds` / `nextcloud_auth_sanity`. (2) `~/.mavis/credentials/nextcloud/nextcloud-caldav.sh` — CalDAV VTODO CRUD wrapper (nextcloud-auth.sh source). subcommand 9종 — `principal` / `calendars` / `list` (JSON) / `show <uid>` / `create` (with --summary / --due / --priority / --description / --categories flags) / `update <uid>` (--status / --percent / --summary) / `delete <uid>` / `smoke` (e2e CRUD 검증) / `-h` (help). VTODO body 는 RFC 5545 CRLF line ending 강제. update 는 If-Match 없이 PUT (Nextcloud 34.x Sabre\DAV 의 If-Match etag 비교 false-negative 412 known-issue 회피, last-write-wins — 외부 client 정합). (3) `~/.mavis/credentials/nextcloud/README.md` — setup 가이드 (Keychain 등록 한 줄 명령 + 회전 절차) + subcommand reference + known-issues + example. e2e smoke 검증 (nextcloud-caldav.sh smoke): [1/6] auth sanity OK / [2/6] list 2 existing → snapshot / [3/6] create UID=`2ce09256-ffaf-45cf-bf51-efbaeab823a1` HTTP 201 / [4/6] update NEEDS-ACTION→IN-PROCESS HTTP 204 / [5/6] update IN-PROCESS→COMPLETED HTTP 204 / [6/6] delete HTTP 204. 잔여 정리 — 직전 실패 단계에서 cleanup 못 했던 test task `dd9c67aa-2ecf-42cd-861e-70b54938575f` 별도 delete (smoke 가 만든 task 와 별개). 최종 상태: 사용자 원본 task `d6fd79b5-9069-4cc1-b44f-1396bfedcce5` (SUMMARY=`minimax 사용 테스트`, STATUS=`NEEDS-ACTION`) 정합 보존. 사전 결함 + 보강 2건: (1) **macOS `security` 비대화형 환경 한계** — Mavis 의 bash tool 환경 (non-interactive zsh) 에서 `security add-generic-password` 호출 시 entry 가 등록된 듯한 rc=0 이지만 실제로는 entry 미등록. 사용자가 interactive zsh 에서 `-A` (always allow) 옵션으로 직접 추가해야 동작. 비대화형 read 도 keychain 이 unlocked 상태일 때만 동작 (perl alarm timeout 내 read). (2) **`python3 - <<HEREDOC` pipe stdin hijack** — `_caldav__report_vtodos | _caldav__todos_to_json` 호출 시 `python3 - <<'PY'` 가 stdin 을 heredoc 로 고정하여 pipe 의 XML data 가 python 에 안 들어감 (`ET.fromstring('')` ParseError). fix: `python3 - "$xml" <<'PYEOF'` (data 를 argv 로 전달) 패턴 — heredoc 는 code, argv 가 data. 회귀 영향 0 — helper 자체는 user-level (`~/.mavis/credentials/nextcloud/`, git 추적 X), 본 TASK 의 commit 대상은 workflow meta 4종 (state.json / session_handoff.md / work_backlog.md / backlog/2026-07-07.md) 만. 후속: 사용자 결정 대기 (옵션 a) TASK-088 build-monitor `Tasks` 페이지 (tsdav + App Password 입력 폼 본격 통합), 옵션 b) `ai-workflow/memory/active/work_backlog.md` ↔ Nextcloud Tasks 양방향 sync layer, 옵션 c) 다른 TASK 우선).
- 최종 수정일: 2026-07-18 (rev 88→89: 세션 종합 정리 sync (rev 118→119). 본 세션은 2026-07-08 ~ 2026-07-18 의 단일 작업 흐름에서 Svelte → React frontend rewrite 7-PR 시리즈 (TASK-088~094) 종료 + M4.5 Group A~D (TASK-095~098) main 합류까지 11 TASK 연속 봉인. main HEAD `3403c9e` (TASK-098). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 244 (+237) / build-server 113 → 143 (+30). 다음: TASK-099 Group E (BuildRequest + ApiConsole) 또는 디자인 토큰 단일화 (TASK-096.5 / M4.6) — 사용자 결정 대기. workflow meta sync (state rev 118→119, handoff 87→88, work_backlog 82→83, backlog index 52→53 / latest 30→31) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 89→90: TASK-096.5 디자인 토큰 단일화 (M4.6) 1차 commit `1cf8d0c` sync. branch `grok/task-096.5-design-tokens-unification-2026-07-18` (main `3403c9e` base). 5 file / +386 / -18. 결정: 옵션 A' (Svelte tokens.css 단일 source-of-truth + Astryx Theme 컴포넌트 보호용 layer 분리). 옵션 A (단순 단일화) 는 Astryx Theme 컴포넌트의 StyleX CSS-in-JS 가 자체 token 을 inject 하기 때문에 거부. 신규: `apps/build-monitor/react/src/tokens.css` (Svelte tokens.css 의 60+ 토큰 + light/dark cascade 사본) / `apps/build-monitor/react/src/theme.css` (Astryx Theme 보호용 3-layer @import 분리 — reset.css / astryx.css / theme-neutral/theme.css) / `docs/operations/design-tokens-unification-2026-07-18.md` (운영 가이드 — 의도 / 결정 / 변경 전후 / light/dark cascade 검증 절차 / 향후 디자인 토큰 추가 절차). 수정: `apps/build-monitor/react/src/main.tsx` (CSS import 순서 정합: tokens → theme → globals, 우리 토큰이 specificity 우선) / `apps/build-monitor/react/src/globals.css` (placeholder fallback 토큰 `var(--color-text-primary, inherit)` 등 정리, `#app` → `#app-react` 정합). 회귀 baseline: TS 5 packages clean, build-monitor vitest **244/244 PASS** (TASK-098 baseline 동일 — React 측 CSS 만 변경, 동작 영향 0), svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해), vite build:react 정상 — gzip js **95.14KB** / css **29.63KB** (TASK-098 baseline css 29.44KB → +0.19KB, tokens.css 추가분 — 의미상 Svelte baseline 토큰 그대로), vite build svelte 정상 (영향 0). 사전 결함 + 보강 2건: (1) **`#app` → `#app-react` 정합** — `react/index.html` 의 root container id 가 `app-react` 인데 globals.css 의 `#app` rule 가 dead rule 이었음. (2) **`padding: var(--spacing-8, 32px)` fallback 정리** — Astryx PoC 의 placeholder 토큰 fallback 을 우리 Svelte 토큰 `--space-xxl` 로 교체. 운영 권고: 디자인 토큰 변경 시 Svelte baseline (`apps/build-monitor/src/lib/tokens.css`) 와 React 사본 (`apps/build-monitor/react/src/tokens.css`) 양쪽을 동시 갱신 — 두 파일은 항상 동일 내용 유지. 향후 단일화 후보 (옵션 C): `shared/design-tokens.css` 추출 후 두 측이 동일 파일을 import. 본 TASK 봉인으로 frontend rewrite 7-PR 시리즈 + M4.5 Group A~D + 디자인 시스템 정합 (TASK-096.5) 까지 12 TASK 연속 봉인. workflow meta sync (state rev 119→120, handoff 88→89, work_backlog 83→84, backlog index 53→54 / latest 31→32) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 101→102: **세션 종료 종합 정리 sync (rev 130→131)**). 본 세션은 2026-07-08 ~ 2026-07-18 (10 일) 의 단일 작업 흐름에서 frontend rewrite 7-PR 시리즈 (TASK-088~094) + M4.5 8-PR 시리즈 (TASK-095~098 + TASK-099 + TASK-100 + TASK-101) + 디자인 토큰 단일화 (TASK-096.5) + PROJECT_PROFILE React baseline batch 1 (PR #57) + batch 2 (PR #58) + TASK-066 follow-up batch 3 (PR #59) 까지 **19 TASK 연속 봉인 완료**. 모든 Task 들이 main 합류. main HEAD `abb95fa` (workflow meta sync 회수). 결정: 옵션 A 일관 (단일 PR / src/ 일괄 삭제 / Svelte baseline → React baseline / Postgres default 개발 경로). 본 세션 운영 가이드 6 종 신규 (전부 2026-07-18): TASK-096.5 디자인 토큰 단일화 / TASK-099 BuildRequest + ApiConsole React / TASK-100 App.svelte router 단순화 / TASK-101 Svelte scaffold 일괄 정리 / TASK-101 follow-up batch 1+2 PROJECT_PROFILE React baseline / TASK-066 follow-up batch 3 source archive Postgres. follow-up: 별도 사용자 결정 (PROJECT_PROFILE §3 명령의 run_local / run_local_postgres 명령 + memory vs Postgres baseline 회귀 baseline 갱신 / scripts/migrate.ts 의 standalone CLI 운영 workflow / postgres migration 0004 build_source 의 TOAST 전략 / 운영 안정화 / 신규 기능 추가 / Nextcloud Tasks 통합 / 다른 TASK 우선). workflow meta sync (state rev 130→131, handoff 99→100, work_backlog 94→95, backlog index 64→65 / latest 42→43) 같은 sync commit 안에 포함. **세션 종료**.)
- 최종 수정일: 2026-07-18 (rev 100→101: PR #59 (TASK-066 follow-up batch 3 source archive Postgres 운영 가이드) main 합류 회수 sync. main HEAD `9c9bbe3` (PR #59 squash). branch `task-066-postgres-source-archive-regression-2026-07-18` (main `0a5a250` base). 2 file / +159 / -0. 결정: 옵션 A (Postgres default 개발 경로 권장). 운영 가이드 신규: `docs/operations/source-archive-postgres-2026-07-18.md` — TASK-066 의 5 단계 회귀 가드 + 사전 결함 + 보강 4건 + Postgres 운영 명령 (colima / docker 기반 부팅 + DATABASE_URL + Build Server 부팅 + e2e-source-archive-postgres.sh 회귀 검증 + 5 단계 운영 시나리오) + default 개발 경로 권장 (4가지 이유). PROJECT_PROFILE.md §3.2 + 다음에 읽을 문서 갱신. 회귀 baseline (변경 없음, docs only): TS 5 packages clean, build-monitor vitest **130/130 PASS** (TASK-101 Svelte 135 case 일괄 삭제), vite build:react 정상 — gzip js **99.01KB** / css **30.62KB**, build-server node:test **143/143 PASS**. 운영 영향 0 (docs only). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline batch 1 + batch 2 + TASK-066 follow-up batch 3 까지 19 TASK 연속 봉인 완료. workflow meta sync (state rev 129→130, handoff 98→99, work_backlog 93→94, backlog index 63→64 / latest 41→42) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 99→100: PR #58 (PROJECT_PROFILE.md React baseline batch 2 / TASK-101 follow-up) main 합류 회수 sync. main HEAD `9aa0774` (PR #58 squash). branch `docs/task-101-followup-project-profile-react-baseline-2026-07-18` (main `baa7383` base). 2 file / +93 / -8. 결정: 옵션 A (§3.5~§3.11 + §3.12 + §4 + §5 + 다음에 읽을 문서 일괄). §3.5~§3.11 (7 섹션) 의 회귀 baseline 일괄 TASK-101 React baseline 동기화. §3.12 trailing context + §4 UI 변경 + §5 TASK-101 baseline 추가 + 다음에 읽을 문서 (본 세션의 5개 운영 가이드 reference) 갱신. 운영 가이드 신규: `docs/operations/project-profile-react-baseline-full-sync-2026-07-18.md`. 회귀 baseline (변경 없음, docs only): TS 5 packages clean, build-monitor vitest **130/130 PASS**, vite build:react 정상 — gzip js **99.01KB** / css **30.62KB**, build-server node:test **143/143 PASS**. 운영 영향 0 (docs only). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline batch 1 (PR #57) + batch 2 (PR #58) 까지 18 TASK 연속 봉인 완료. workflow meta sync (state rev 128→129, handoff 97→98, work_backlog 92→93, backlog index 62→63 / latest 40→41) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 98→99: PR #57 (PROJECT_PROFILE.md React baseline 동기화 / TASK-101 follow-up) main 합류 회수 sync. main HEAD `642ac3a` (PR #57 squash). branch `docs/task-101-project-profile-react-baseline-2026-07-18` (main `2fb7c0d` base). 2 file / +92 / -30. 결정: TASK-101 follow-up (docs only) — `docs/PROJECT_PROFILE.md` 의 §3 frontend 관련 4 섹션 (§3.2 Admin / §3.3 Build Monitor UI 정합 / §3.4 Admin 가드 / §3.12 React 빌드 mount) 을 Svelte baseline → React baseline 으로 동기화. §3 명령의 TASK-075 reverse proxy 섹션 (`BUILD_MONITOR_DIST_PATH` → `BUILD_MONITOR_REACT_DIST_PATH`) 갱신. 운영 가이드 신규: `docs/operations/project-profile-react-baseline-2026-07-18.md`. 회귀 baseline (TASK-101 baseline 동일, docs only): TS 5 packages clean, build-monitor vitest **130/130 PASS** (TASK-101 baseline 동일), vite build:react 정상 — gzip js **99.01KB** / css **30.62KB** (TASK-101 baseline 동일). 운영 영향 0 (docs only). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. frontend rewrite 7-PR 시리즈 + M4.5 8-PR 시리즈 + 디자인 토큰 단일화 + PROJECT_PROFILE React baseline 동기화 까지 17 TASK 연속 봉인 완료 (2026-07-08 ~ 2026-07-18). workflow meta sync (state rev 127→128, handoff 96→97, work_backlog 91→92, backlog index 61→62 / latest 39→40) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 97→98: 세션 종합 정리 sync (rev 126→127). 본 세션은 2026-07-08 ~ 2026-07-18 (10 일) 의 단일 작업 흐름에서 frontend rewrite 7-PR 시리즈 (TASK-088~094) + M4.5 8-PR 시리즈 (TASK-095~098 + TASK-099 + TASK-100 + TASK-101) + 디자인 토큰 단일화 (TASK-096.5) 까지 16 TASK 연속 봉인 완료. 모든 Task 들이 main 합류 완료. main HEAD `7693daf` (workflow meta sync 회수). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. vite build:react 정상 (gzip js 99.01KB / css 30.62KB). 운영 영향 0 (Svelte 측 src/ 가 production 에서 unreachable — TASK-094 + TASK-100 + TASK-101 로 cross-framework dead code 일괄 폐기). follow-up: 별도 사용자 결정 (PROJECT_PROFILE.md React baseline 동기화 / 운영 안정화 / 신규 기능 추가 / Nextcloud Tasks 통합 등). workflow meta sync (state rev 126→127, handoff 95→96, work_backlog 90→91, backlog index 60→61 / latest 38→39) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 96→97: PR #56 (TASK-101 Svelte scaffold 일괄 정리) main 합류 회수 sync. main HEAD `313ae2e` (TASK-101 squash). branch `grok/task-101-svelte-scaffold-cleanup-2026-07-18` (main `be8c372` base). 51 file / +32 / -7910. 결정: 옵션 A (src/ 일괄 삭제 + 설정 파일 단순화). apps/build-monitor/src/ 전체 46 file 삭제 (App.svelte + components 8 + lib 7 + routes 16 + test 2 + main.ts) + tsconfig.json 삭제 (tsconfig.react.json 단일화) + 신규 1 file (react/src/test/setup.ts — TASK-064 의 Svelte 측 setup.ts React 측으로 이식) + 수정 4 file (vite.config.ts / package.json / 2 css @import 경로 React 측 tokens.css 로 교체). 운영 가이드 신규: `docs/operations/svelte-scaffold-cleanup-2026-07-18.md`. 사전 결함 + 보강 4건 봉인: (1) vitest localStorage 부재 → React 측 src/test/setup.ts 신규, (2) @testing-library/jest-dom matcher 부재 → setup.ts 에 import 추가, (3) React 측 component CSS 의 Svelte 측 tokens.css import 깨짐 → 두 CSS @import 경로 React 측 tokens.css 로 교체, (4) useUserId.test.ts 부재 → Login.test.tsx 가 React 측 lib/useUserId.ts 사용 — 회귀 영향 0. 운영 영향 0. 회귀 baseline (main HEAD `313ae2e`): TS 5 packages clean, build-monitor vitest **130/130 PASS** (TASK-100 baseline 265 → 130, Svelte 측 135 케이스 일괄 삭제), vite build:react 정상 — gzip js **99.01KB** / css **30.62KB** (TASK-100 baseline과 거의 동일), svelte-check script 제거. 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. frontend rewrite 7-PR 시리즈 + M4.5 Group A~G (TASK-095~101) + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 + App.svelte router 단순화 + Svelte scaffold 일괄 정리 까지 16 TASK 연속 봉인. M4.5 8-PR 시리즈 (TASK-095~102) 완료. workflow meta sync (state rev 125→126, handoff 94→95, work_backlog 89→90, backlog index 59→60 / latest 37→38) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 95→96: TASK-101 (M4.5 Group G — Svelte scaffold 일괄 정리) 1차 commit `aae7f31` sync. branch `grok/task-101-svelte-scaffold-cleanup-2026-07-18` (main `be8c372` base). 51 file / +32 / -7910. 결정: 옵션 A (src/ 일괄 삭제 + 설정 파일 단순화). apps/build-monitor/src/ 전체 46 file 삭제 (App.svelte + components 8 + lib 7 + routes 16 + test 2 + main.ts) + tsconfig.json 삭제 (tsconfig.react.json 단일화) + 신규 1 file (react/src/test/setup.ts — TASK-064 의 Svelte 측 setup.ts React 측으로 이식) + 수정 4 file (vite.config.ts / package.json / 2 css @import 경로 React 측 tokens.css 로 교체). 운영 가이드 신규: `docs/operations/svelte-scaffold-cleanup-2026-07-18.md`. 사전 결함 + 보강 4건: (1) vitest localStorage 부재 → React 측 src/test/setup.ts 신규 (Svelte 측 setup.ts 의 localStorage / sessionStorage / matchMedia fallback 이식), (2) @testing-library/jest-dom matcher 부재 → setup.ts 에 `import "@testing-library/jest-dom/vitest"` 추가, (3) React 측 component CSS 의 Svelte 측 tokens.css import 깨짐 → 두 CSS @import 경로 React 측 tokens.css 로 교체 (TASK-096.5 디자인 토큰 단일화 정합), (4) useUserId.test.ts 부재 → Login.test.tsx 가 React 측 lib/useUserId.ts 사용 — 회귀 영향 0. 운영 영향 0 (Svelte 측 src/ 가 production 에서 unreachable). 회귀 baseline: TS 5 packages clean, build-monitor vitest **130/130 PASS** (TASK-100 baseline 265 → 130, Svelte 측 135 케이스 일괄 삭제), vite build:react 정상 — gzip js **99.01KB** / css **30.62KB** (TASK-100 baseline과 거의 동일), svelte-check script 제거 (Svelte 측 일괄 삭제로 불필요). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 130 (+123, Svelte 135 케이스 일괄 삭제) / build-server 113 → 143 (+30) / TS 5 packages clean. frontend rewrite 7-PR 시리즈 + M4.5 Group A~G (TASK-095~101) + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 + App.svelte router 단순화 + Svelte scaffold 일괄 정리 까지 16 TASK 연속 봉인. M4.5 8-PR 시리즈 (TASK-095~102) 완료. workflow meta sync (state rev 124→125, handoff 93→94, work_backlog 88→89, backlog index 58→59 / latest 36→37) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 94→95: PR #55 (TASK-100 App.svelte router 단순화) main 합류 회수 sync. main HEAD `9dfbe08` (TASK-100 squash). branch `grok/task-100-app-router-simplify-2026-07-18` (main `17adad6` base). 2 file / +117 / -36. 결정: 옵션 A (Svelte routes 일괄 삭제). React 측이 primary SPA 이고 Svelte 측 routes 가 unreachable 인 상태 (TASK-094 의 Build Server 가 React dist 만 mount) 가 명확했으나 본 App.svelte 의 9 route 정의가 그대로 남아 dead code 였음. App.svelte amend — 9 route (`/`, `/builds`, `/builds/:buildId`, `/build-request`, `/api-console`, `/admin/builds`, `/admin/users`, `/admin/admins`, `/admin/runners`) + 9 component import 일괄 제거. `*` (NotFound) 만 유지하여 svelte-spa-router 가 정상 동작하는 최소 routes 정의 보존. 본 파일은 TASK-101 Group G 의 Svelte scaffold 일괄 정리 시 본 파일 자체가 삭제될 예정. 운영 가이드 신규: `docs/operations/app-router-simplify-2026-07-18.md`. PR #55 self-review amend 1건 봉인 — trailing newline 부재 (운영 가이드). 회귀 baseline (main HEAD `9dfbe08`): TS 5 packages clean, build-monitor vitest **265/265 PASS** (TASK-099 baseline 동일), svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해), vite build:react 정상 — gzip js **99.08KB** / css **30.62KB** (TASK-099 baseline 동일), vite build svelte 정상 — gzip js **21.03KB** / css **2.01KB** (TASK-099 baseline js 36.90KB → **-15.87KB** / css 6.18KB → **-4.17KB**, Svelte 측 routes 10개 → 1개 단순화 효과 — 빌드 결과물 자체도 약 44% 감소). 운영 영향 0 (Svelte 측 routes unreachable). frontend rewrite 7-PR 시리즈 + M4.5 Group A~F (TASK-095~100) + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 + App.svelte router 단순화 까지 15 TASK 연속 봉인. follow-up: TASK-101 Group G (Svelte scaffold 일괄 정리 — Svelte 측 components / routes / lib / package.json 일괄 삭제 + vite Svelte plugin / tsconfig.json 통합 단일화). workflow meta sync (state rev 123→124, handoff 92→93, work_backlog 87→88, backlog index 57→58 / latest 35→36) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 93→94: TASK-100 (M4.5 Group F — App.svelte router 단순화) 1차 commit `bce5dcd` sync. branch `grok/task-100-app-router-simplify-2026-07-18` (main `17adad6` base). 2 file / +117 / -36. 결정: 옵션 A (Svelte routes 일괄 삭제). React 측이 primary SPA 이고 Svelte 측 routes 가 unreachable 인 상태 (TASK-094 의 Build Server 가 React dist 만 mount) 가 명확했으나 본 App.svelte 의 9 route 정의가 그대로 남아 dead code 였음. App.svelte amend — 9 route (`/`, `/builds`, `/builds/:buildId`, `/build-request`, `/api-console`, `/admin/builds`, `/admin/users`, `/admin/admins`, `/admin/runners`) + 9 component import 일괄 제거. `*` (NotFound) 만 유지하여 svelte-spa-router 가 정상 동작하는 최소 routes 정의 보존. 본 파일은 TASK-101 Group G 의 Svelte scaffold 일괄 정리 시 본 파일 자체가 삭제될 예정. 운영 가이드 신규: `docs/operations/app-router-simplify-2026-07-18.md`. 사전 결함 + 보강 1건: cross-framework router 충돌 가능성 차단 — Svelte 측 routes 가 unreachable 인 dead code 였음. 운영 영향 0. 회귀 baseline: TS 5 packages clean, build-monitor vitest **265/265 PASS** (TASK-099 baseline 동일), svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해), vite build:react 정상 — gzip js **99.08KB** / css **30.62KB** (TASK-099 baseline 동일), vite build svelte 정상 — gzip js **21.03KB** / css **2.01KB** (TASK-099 baseline js 36.90KB → **-15.87KB** / css 6.18KB → **-4.17KB**, Svelte 측 routes 10개 → 1개 단순화 효과 — 빌드 결과물 자체도 약 44% 감소). frontend rewrite 7-PR 시리즈 + M4.5 Group A~F (TASK-095~100) + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 + App.svelte router 단순화 까지 15 TASK 연속 봉인. follow-up: TASK-101 Group G (Svelte scaffold 일괄 정리 — Svelte 측 components / routes / lib / package.json 일괄 삭제 + vite Svelte plugin / tsconfig.json 통합 단일화). workflow meta sync (state rev 122→123, handoff 91→92, work_backlog 86→87, backlog index 56→57 / latest 34→35) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 92→93: PR #53 (TASK-096.5 디자인 토큰 단일화) + PR #54 (TASK-099 BuildRequest + ApiConsole React 마이그레이션) main 합류 회수 sync. branch `grok/task-096.5-design-tokens-unification-2026-07-18` (main `3403c9e` base) → cherry-pick 으로 main HEAD `73c5081` (TASK-096.5 squash) 에 TASK-099 두 commit 정렬 후 별도 rebased branch. TASK-096.5 결정: 옵션 A' (Svelte tokens.css 단일 source-of-truth + Astryx Theme 컴포넌트 보호용 layer 분리). PR #53 self-review amend 5건 봉인 — trailing newline 부재 (globals.css / tokens.css / theme.css / BuildsList.css / Login.css). TASK-099 결정: 옵션 A (단일 PR). PR #54 self-review amend 3건 봉인 — (C-1) React 19 type 정합 (`useRef<HTMLInputElement>(null)` → `RefObject<HTMLInputElement | null>` 명시), (C-2) unused `act` import 제거, (I-3) trailing newline 부재 (BuildRequest.tsx / .test.tsx / ApiConsole.tsx / .test.tsx). 회귀 baseline (main HEAD `e46f2f5`): TS 5 packages clean, build-monitor vitest **265/265 PASS**, svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해), vite build:react 정상 — gzip js **99.08KB** / css **30.62KB**, vite build svelte 정상. main 합류 순서: PR #53 `73c5081` (TASK-096.5 squash) → PR #54 `e46f2f5` (TASK-099 squash). 누적 회귀 baseline (TASK-088 baseline 대비): vitest 7 → 265 (+258) / build-server 113 → 143 (+30). frontend rewrite 7-PR 시리즈 + M4.5 Group A~E (TASK-095~099) + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 까지 14 TASK 연속 봉인. workflow meta sync (state rev 121→122, handoff 90→91, work_backlog 85→86, backlog index 55→56 / latest 33→34) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-18 (rev 90→91: TASK-099 (M4.5 Group E — BuildRequest + ApiConsole React 마이그레이션) 1차 commit `8a07881` sync. branch `grok/task-099-build-api-react-2026-07-18` (main `1cf8d0c` base). 9 file / +1880 / -7. 결정: 옵션 A (단일 PR, TASK-098 Admin 4종 패턴 동일). 신규 6: `apps/build-monitor/react/src/routes/BuildRequest.tsx` (useState 8개 form state + useRef 8개 input refs + useEffect userId+hello preset + useMemo raw payload preview + useNavigate redirect; submit 함수가 input ref 의 native DOM value 를 직접 read하여 React state batching 우회) / `BuildRequest.css` (Svelte inline `<style>` 1:1 정합, 우리 Svelte tokens.css 의 디자인 토큰 직접 사용 — fallback 없음, TASK-096.5 정합) / `BuildRequest.test.tsx` (RTL + MemoryRouter 15 cases) / `ApiConsole.tsx` (useState iframeError + reloadKey + Link + iframe `key={reloadKey}` 재마운트) / `ApiConsole.css` / `ApiConsole.test.tsx` (RTL 6 cases). 수정 2: `App.tsx` routes `/build-request` + `/api-console` 추가 (Svelte 측 pages 는 그대로 유지, TASK-101 Group G 에서 일괄 정리) / `api.ts` `submitBuildRequest` + `parseApiError` + `BuildRequestPayload` / `BuildAcceptedResponse` / `BuildDuplicateResponse` / `BuildRequestResponse` 타입 export 추가. Svelte 측 `parseApiError` 의 brace-count JSON envelope 추출 + zod issue path[] dot-join 구현을 1:1 이식. 운영 가이드 신규: `docs/operations/build-request-api-console-react-2026-07-18.md`. 사전 결함 + 보강 4건: (1) **React state batching race** — fireEvent.change → setForm → setForm batch → submit 호출 사이의 race. closure 가 stale state read. **해결**: input ref 패턴으로 native DOM value 직접 read. refs 는 React render 와 무관하게 최신 DOM property 가리킴. controlled input 의 .value 가 React state 와 sync 되지 않은 시점에서도 안정적. (2) **fireEvent.click(submit-button) 이 form submit 을 trigger 하지 않음** — RTL 14.x known issue (fireEvent.click 은 native click 만 dispatch 하고 form submit 으로의 bubbling trigger 안 함). **해결**: 테스트 케이스에서 `fireEvent.submit(form)` 으로 직접 trigger. (3) **jsdom iframe onError 한계** — iframe 의 native error event 가 React onError listener 까지 dispatch 되지 않음 (cross-origin iframe 시뮬레이션). **해결**: test 케이스 단순화 (iframe 존재 + title 검증) — production 동작은 정상. Playwright visual QA 에서 별도 검증. (4) **Native form read race** — `e.currentTarget.elements.namedItem` 도 동일 race 가능. ref 패턴으로 단순화. 회귀 baseline: TS 5 packages clean, build-monitor vitest **265/265 PASS** (TASK-098 baseline 244 → +21 신규: BuildRequest 15 + ApiConsole 6), svelte-check **0 errors / 1 warning** (TASK-077 baseline 무해), vite build:react 정상 — gzip js **99.08KB** / css **30.62KB** (TASK-098 baseline js 95.14KB → +3.94KB / css 29.63KB → +0.99KB, BuildRequest + ApiConsole 추가분), vite build svelte 정상 (영향 0). 본 TASK 봉인으로 frontend rewrite 7-PR 시리즈 + M4.5 Group A~E + 디자인 토큰 단일화 + BuildRequest + ApiConsole React 마이그레이션 까지 13 TASK 연속 봉인. follow-up: TASK-100 Group F (App.svelte router 단순화) / TASK-101 Group G (Svelte scaffold 일괄 정리 — admin / build-request / api-console Svelte pages + components/routes 일괄 삭제 + svelte / svelte-spa-router package 정리). workflow meta sync (state rev 120→121, handoff 89→90, work_backlog 84→85, backlog index 54→55 / latest 32→33) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 87→88: TASK-098 (M4.5 Group D — Admin 페이지 4종 React 마이그레이션) main 합류 회수 sync. PR 본질: frontend rewrite 시리즈 M4.5 4단계. branch `grok/task-098-admin-pages-react-2026-07-08` (main `7829c92` base). Svelte 측 admin pages 4종 (AdminBuilds / AdminUsers / AdminAdmins / AdminRunners) 의 React 측 신규 추가. App.tsx 의 admin routes 4종이 React 측 pages 로 교체. Svelte 측 admin pages 는 그대로 유지 (TASK-101 Group G 에서 일괄 정리). 신규 10종 (4 admin pages + RegisterRunnerModal + 4 css) + 수정 2종 (App.tsx + api.ts). 결정: 옵션 A (React 측 admin pages 4종 신규 + App.tsx routes 교체). 회귀 baseline: TS 5 packages clean, build-monitor vitest **244/244 PASS** (page level 통합 test 는 다음 TASK 에서 일괄), svelte-check 0/1, build-server tests 동일, vite build:react 정상 (gzip js 95.14KB / css 29.44KB, TASK-097 baseline 91.04KB → +4.10KB). 다음: TASK-099 Group E (BuildRequest + ApiConsole) 진입 (사용자 결정 대기). workflow meta sync (state rev 117→118, handoff 86→87, work_backlog 81→82, backlog index 51→52 / latest 29→30) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 86→87: TASK-097 (M4.5 Group C — Admin 진입점 React 마이그레이션) main 합류 회수 sync. PR 본질: frontend rewrite 시리즈 M4.5 3단계. branch `grok/task-097-admin-entry-react-2026-07-08` (main `6f05c37` base). 신규 8종 (AdminTabs.tsx + .css / AdminAccessDenied.tsx + .css / admin-guard.ts + 3 test) + 수정 0. App.tsx 변경 없음 — admin routes 의 페이지 본체는 Svelte 측 유지. 결정: 옵션 A (cross-framework 공존). 회귀 baseline: TS 5 packages clean, build-monitor vitest **227 → 244 PASS** (신규 17: AdminTabs 6 + AdminAccessDenied 7 + admin-guard 4), svelte-check 0/1, build-server tests 동일, vite build:react 정상 (gzip js 91.04KB / css 28.10KB). 다음: TASK-098 Group D (Admin 페이지 4종) 진입 (사용자 결정 대기). workflow meta sync (state rev 116→117, handoff 85→86, work_backlog 80→81, backlog index 50→51 / latest 28→29) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 85→86: TASK-096 (M4.5 Group B — StatusPill 디자인 토큰 baseline 정합) main 합류 회수 sync. PR 본질: frontend rewrite 시리즈 M4.5 2단계. branch `grok/task-096-statuspill-design-tokens-2026-07-08` (main `4dcd2aa` base). TASK-090 self-review 의 디자인 강화분 (padding 6px / size-sm / alpha 20%) 을 Svelte baseline (padding 4px / size-xs / alpha 15%) 으로 통일. 결정: 옵션 A. 신규 2종 + 수정 1종. 디자인 토큰 시스템 단일화는 후속 TASK. 회귀 baseline: TS 5 packages clean, build-monitor vitest **204 → 227 PASS** (신규 23), svelte-check 0/1, build-server tests 동일, vite build:react 정상 (gzip js 91.04KB / css 28.10KB). 다음: TASK-097 Group C 또는 디자인 토큰 단일화 TASK-096.5 (사용자 결정 대기). workflow meta sync (state rev 115→116, handoff 84→85, work_backlog 79→80, backlog index 49→50 / latest 27→28) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 84→85: TASK-095 (M4.5 Group A — Header / ThemeToggle / FilterChips React 마이그레이션) main 합류 회수 sync. PR 본질: frontend rewrite 7-PR 시리즈 후속 M4.5 의 1단계. branch `grok/task-095-header-theme-react-2026-07-08` (main `1ab566d` base). 신규 8종 (Header.tsx / Header.css / ThemeToggle.tsx / FilterChips.tsx / adminAllowListStore.ts + 4 test) + 수정 2종 (App.tsx 모든 라우트가 Header 공유 / api.ts adminAllowList helper). Svelte 측 컴포넌트는 cross-framework 공존 유지 — TASK-096+ 에서 일괄 정리. 결정: 옵션 A 채택. 사전 결함 + 보강: ThemeToggle matchMedia jsdom 부재 try/catch fallback; adminAllowListStore mock 호환성 → Zustand setState 직접 set; admin /admin/admins path generated 부재 → apiGet/apiSend 캐스팅. 회귀 baseline: TS 5 packages clean, build-monitor vitest **173 → 204 PASS** (신규 31), svelte-check 0/1, build-server tests 동일 (영향 0), vite build:react 정상 (gzip js 91.03KB / css 28.10KB, TASK-094 baseline 89.96KB → +1.07KB), vite build svelte 정상. 다음: TASK-096 Group B (StatusPill) 또는 TASK-097 Group C (Admin 진입점) 진입 (사용자 결정 대기). workflow meta sync (state rev 114→115, handoff 83→84, work_backlog 78→79, backlog index 48→49 / latest 26→27) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 83→84: TASK-094 (Svelte 코드 정리 — frontend rewrite 7-PR 시리즈 final) main 합류 회수 sync. PR 본질: TASK-088~093 의 frontend rewrite 시리즈 final 단계. branch `grok/task-094-svelte-cleanup-2026-07-08` (main `8395702` base). TASK-093 의 Svelte legacy mount + env + SPA fallback 분기 모두 폐기. 삭제 4종 (BuildDetail.svelte 308 lines / PhaseTimeline.svelte / LogStream.svelte / PhaseTimeline.test.ts) — 모두 React 마이그레이션 완료. 신규 1종 (BuildDetailRedirect.svelte). Build Server `mountBuildMonitorDist` — Svelte 분기 + helper + env + SPA fallback 모두 삭제, React `@fastify/static` 만 mount + raw `fs.createReadStream` SPA fallback. 유지 (의도적 scope): admin / build-request / api-console Svelte routes — 후속 시리즈 React 마이그레이션. 결정: 옵션 A 채택. 사전 결함 + 보강: `/builds/<id>` 가 Build Server wildcard GET route 매치 → UUID validation 500 (정직한 동작, 운영자 UX 는 React SPA 의 `<Link>` 클릭 사용). 회귀 baseline: TS 5 packages clean, build-server node:test **143 → 142 PASS** (Svelte legacy 5건 폐기 + 신규 1건), build-monitor vitest **178 → 173 PASS** (PhaseTimeline.test.ts 5건 제거), svelte-check 0/1 (TASK-077 baseline 무해), vite build:react 정상 (gzip js 89.96KB / css 27.81KB), vite build svelte 정상 (운영 중 admin / build-request pages 정합 유지). frontend rewrite 7-PR 시리즈 종료. 다음: M4.5 admin / build-request / api-console React 마이그레이션 또는 다른 TASK (사용자 결정 대기). workflow meta sync (state rev 113→114, handoff 82→83, work_backlog 77→78, backlog index 47→48 / latest 25→26) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 82→83: TASK-093 (Build Server dist swap — React 빌드 mount) main 합류 회수 sync. PR 본질: Svelte → React frontend rewrite 7-PR 시리즈 6단계. branch `grok/task-093-build-server-react-dist-2026-07-08` (main `03806f6` base), commit `e1ca2a5`. TASK-075 의 단일 포트 reverse proxy 위에서 Svelte 빌드 → React 빌드 swap. React 가 primary SPA, Svelte 는 legacy deep link 호환용으로 보존. 신규 `BUILD_MONITOR_REACT_DIST_PATH` env (default `apps/build-monitor/dist-react`). `mountBuildMonitorDist` — React `@fastify/static` (decorateReply: true) + Svelte raw fastify route + `fs.createReadStream` + `mountSvelteIndexHtml` helper. SPA fallback 우선순위: `/svelte/*` deep link → Svelte index.html, 그 외 → React index.html. 결정: 옵션 A 즉시 swap / B ✅ 단계적 swap (TASK-094 에서 일괄 정리). 신규 회귀 가드 12건. 회귀 baseline: TS 5 packages clean, build-server node:test **131 → 143 PASS**, build-monitor vitest 178/178 동일, svelte-check 0/1, vite build:react 정상. 다음: TASK-094 Svelte 코드 정리 진입 (사용자 결정 대기). workflow meta sync (state rev 112→113, handoff 81→82, work_backlog 76→77, backlog index 46→47 / latest 24→25) 같은 sync commit 안에 포함.)
- 최종 수정일: 2026-07-08 (rev 81→82: TASK-092 (lib layer — Zustand store 분리) main 합류 회수 sync. PR 본질: Svelte → React frontend rewrite 7-PR 시리즈 5단계. branch `grok/task-092-lib-layer-2026-07-08` (main `b630bb9` base), commit `f87bd78`. BuildsList / BuildDetail 의 useState 4개 + useEffect fetch lifecycle 을 Zustand 5.x store 로 통합. 본 PR 은 빌드 동작 변경 없이 stateful 코드만 store 로 격리 — visual 회귀 영향 0. 신규 `zustand@^5.0.2` (resolved 5.0.14) React 19 정합. 신규 store 2종 (buildsListStore / buildDetailStore) + store unit test 12 신규. self-review amend 2 area 봉인 (App.test.tsx store.loading 단언 + AbortSignal 케이스 정정). 회귀 baseline: TS 4 packages clean, build-monitor vitest **166 → 178 PASS** (기존 166 + store unit 12 신규), svelte-check 0/1 (TASK-077 baseline 무해), tsc -p tsconfig.react.json clean, vite build:react gzip js **89.96KB** / css **27.81KB** (TASK-091 baseline 89.53KB → +0.43KB). workflow meta sync (state rev 111→112, handoff 80→81, work_backlog 75→76, backlog index 45→46 / latest 23→24) 같은 sync commit 안에 포함. 다음: TASK-093 Build Server dist swap (Build Server 의 mountBuildMonitorDist 가 React 빌드를 mount) 또는 TASK-094 Svelte 코드 정리 진입 (사용자 결정 대기). 회귀 영향 0 — 빌드 동작 변경 없음.)
- 최종 수정일: 2026-07-08 (rev 80→81: TASK-091 (BuildDetail 페이지 React 마이그레이션) main 합류 회수 sync. PR 본질: Svelte → React frontend rewrite 7-PR 시리즈 4단계. branch `grok/task-091-build-detail-2026-07-08` (main `be72de0` base), commit `2910a7c`. Svelte `routes/BuildDetail.svelte` (308 lines) → React `routes/BuildDetail.tsx` 1:1 정합 + PhaseTimeline + LogStream React 컴포넌트 신규 + api.ts getBuild/getBuildLogs 추가 + App.tsx placeholder → BuildDetail 교체. self-review amend 2 area 봉인 (페이지 상단 Back link + PhaseTimeline 수직 연결선 CSS rule). 회귀 baseline: TS 4 packages clean, build-monitor vitest **158 → 166 PASS** (BuildDetail page 8 신규), svelte-check 0/1 (TASK-077 baseline 무해), tsc -p tsconfig.react.json clean, vite build:react gzip js **89.53KB** / css **27.76KB** (TASK-090 baseline 87.27KB → +2.26KB), Build Server `mountBuildMonitorDist` 영향 0. workflow meta sync (state rev 110→111, handoff 79→80, work_backlog 74→75, backlog index 44→45 / latest 22→23) 같은 sync commit 안에 포함. 다음: TASK-092 lib layer (Zustand store 분리 / openapi-fetch 정합 / visual diff baseline) 또는 TASK-093 Build Server dist swap 진입 (사용자 결정 대기). 회귀 영향 0 — Svelte build-monitor 운영 baseline 모두 그대로 유지.)
- 최종 수정일: 2026-07-08 (rev 79→80: TASK-089 (Login 1 페이지 React 마이그레이션) + TASK-090 (BuildsList 페이지 React 마이그레이션) main 합류 회수 sync. PR 본질: frontend rewrite 7-PR 시리즈의 2~3단계 — TASK-088 PoC 위에서 react-router-dom v7 + Login 페이지 1개 + BuildsList 페이지 1개 마이그레이션. commit 5개 (`a312123` Login 본체 → `8dab9f6` Login self-review amend → `8c5eb92` BuildsList 본체 → `af41c8d` BuildsList self-review UI polish → `db87d04` BuildsList self-review follow-up). TASK-089 amend 4 area 봉인: `apps/build-monitor/public/favicon.svg` 신규 (404 favicon 회피) + React ErrorBoundary 컴포넌트로 전체 앱 wrap (runtime error UI 노출) + `localStorage` cross-tab `storage` 이벤트로 userIdStore sync (다른 탭 로그아웃 즉시 반영) + `data-testid` 보강. TASK-090 amend 6 area 봉인: 1차 light theme 전환 (`data-theme="light"`) + chip wrapper Svelte FilterChips 정합 + chip 비활성 contrast + StatusPill sizing, 2차 table row hover (4px indigo accent + surface-elevated) + 마지막 row border 처리 + appName ellipsis. 회귀 baseline: TS 4 packages clean, build-monitor vitest **141 → 158 PASS** (Login 8 + BuildsList 18 신규; Svelte 140 baseline 유지), svelte-check 0/1 (TASK-077 baseline 무해), tsc -p tsconfig.react.json clean, vite build:react gzip js **87.27KB** / css **27.29KB**, Build Server memory mode e2e: seed build `task-090-demo-app` QUEUED → `/api/builds` → BuildsList 노출 정상, 5176 e2e light theme PASS (`/` → `/login` → `/builds` → chip + QUEUED pill + buildId link + appName + relative time), CI (docker-build) ✅ pass 1m 6s, Build Server `mountBuildMonitorDist` (dist/ Svelte) 영향 0. workflow meta sync (state rev 109→110, handoff 78→79, work_backlog 73→74, backlog index 43→44 / latest 21→22) 같은 sync commit 안에 포함. 다음: TASK-091 BuildDetail 페이지 React 마이그레이션 진입. 회귀 영향 0 — Svelte build-monitor 운영 baseline 모두 그대로 유지.)
- 최종 수정일: 2026-07-08 (rev 78→79: TASK-088 (React + Astryx 부트스트랩 PoC) — branch `codex/task-088-astryx-bootstrap-2026-07-08` (main `e30c896` base). PR 본질: Meta의 Astryx v0.1.4 부트스트랩. Svelte 빌드와 React 빌드 공존 (dist/ vs dist-react/) — 회귀 영향 0. 신규 6 file + 수정 4 file. 회귀 baseline: svelte-check 0/1 (TASK-077 baseline), vitest 141/141 PASS, vite build Svelte 정상 (gzip js 39.37KB / css 6.94KB), vite build --config vite.react.config.ts 정상 (gzip js 81.44KB / css 25.57KB), tsc -p tsconfig.react.json clean. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-07 (rev 77→78: TASK-075 (credential rotation e2e) PR #40 squash merge main 합류 `8e0b228`. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-07 (rev 76→77: TASK-074 (htpasswd 인증 registry push e2e) PR #39 squash merge main 합류 `d87a17c`. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-07 (rev 75→76: TASK-073 (RUNNER_REGISTRY_CONFIG_DIR + cli mode registry push e2e) PR #38 squash merge main 합류 `cd01b99`. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-07 (rev 74→75: TASK-086 (e2e-multi-runner.sh BASE + heredoc 결함 봉인) PR #37 main 합류 `ab0be9e`. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-07 (rev 73→74: TASK-085 (production-semantic 운영 검증 보강) PR #36 squash merge main 합류 `c5e3008`. workflow meta sync 같은 commit 안에 포함.)
- 최종 수정일: 2026-07-06 (rev 69→70: TASK-081-B multi-runner 운영 검증 PR #32 squash merge main 합류 `ddae46a`. workflow meta sync 같은 commit 안에 포함.)
